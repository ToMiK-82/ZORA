import {
  require_react_dom
} from "./chunk-LPPTHLN7.js";
import {
  require_jsx_runtime
} from "./chunk-S77I6LSE.js";
import {
  require_react
} from "./chunk-3TFVT2CW.js";
import {
  __commonJS,
  __toESM
} from "./chunk-4MBMRILA.js";

// node_modules/@nivo/core/node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js
var require_react_is_development = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var hasSymbol = typeof Symbol === "function" && Symbol.for;
        var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for("react.element") : 60103;
        var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for("react.portal") : 60106;
        var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for("react.fragment") : 60107;
        var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for("react.strict_mode") : 60108;
        var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for("react.profiler") : 60114;
        var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for("react.provider") : 60109;
        var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for("react.context") : 60110;
        var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for("react.async_mode") : 60111;
        var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for("react.concurrent_mode") : 60111;
        var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for("react.forward_ref") : 60112;
        var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for("react.suspense") : 60113;
        var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for("react.suspense_list") : 60120;
        var REACT_MEMO_TYPE = hasSymbol ? Symbol.for("react.memo") : 60115;
        var REACT_LAZY_TYPE = hasSymbol ? Symbol.for("react.lazy") : 60116;
        var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for("react.block") : 60121;
        var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for("react.fundamental") : 60117;
        var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for("react.responder") : 60118;
        var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for("react.scope") : 60119;
        function isValidElementType(type) {
          return typeof type === "string" || typeof type === "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
          type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
        }
        function typeOf(object) {
          if (typeof object === "object" && object !== null) {
            var $$typeof = object.$$typeof;
            switch ($$typeof) {
              case REACT_ELEMENT_TYPE:
                var type = object.type;
                switch (type) {
                  case REACT_ASYNC_MODE_TYPE:
                  case REACT_CONCURRENT_MODE_TYPE:
                  case REACT_FRAGMENT_TYPE:
                  case REACT_PROFILER_TYPE:
                  case REACT_STRICT_MODE_TYPE:
                  case REACT_SUSPENSE_TYPE:
                    return type;
                  default:
                    var $$typeofType = type && type.$$typeof;
                    switch ($$typeofType) {
                      case REACT_CONTEXT_TYPE:
                      case REACT_FORWARD_REF_TYPE:
                      case REACT_LAZY_TYPE:
                      case REACT_MEMO_TYPE:
                      case REACT_PROVIDER_TYPE:
                        return $$typeofType;
                      default:
                        return $$typeof;
                    }
                }
              case REACT_PORTAL_TYPE:
                return $$typeof;
            }
          }
          return void 0;
        }
        var AsyncMode = REACT_ASYNC_MODE_TYPE;
        var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
        var ContextConsumer = REACT_CONTEXT_TYPE;
        var ContextProvider = REACT_PROVIDER_TYPE;
        var Element = REACT_ELEMENT_TYPE;
        var ForwardRef = REACT_FORWARD_REF_TYPE;
        var Fragment3 = REACT_FRAGMENT_TYPE;
        var Lazy = REACT_LAZY_TYPE;
        var Memo = REACT_MEMO_TYPE;
        var Portal = REACT_PORTAL_TYPE;
        var Profiler = REACT_PROFILER_TYPE;
        var StrictMode = REACT_STRICT_MODE_TYPE;
        var Suspense = REACT_SUSPENSE_TYPE;
        var hasWarnedAboutDeprecatedIsAsyncMode = false;
        function isAsyncMode(object) {
          {
            if (!hasWarnedAboutDeprecatedIsAsyncMode) {
              hasWarnedAboutDeprecatedIsAsyncMode = true;
              console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
            }
          }
          return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
        }
        function isConcurrentMode(object) {
          return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
        }
        function isContextConsumer(object) {
          return typeOf(object) === REACT_CONTEXT_TYPE;
        }
        function isContextProvider(object) {
          return typeOf(object) === REACT_PROVIDER_TYPE;
        }
        function isElement(object) {
          return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
        }
        function isForwardRef(object) {
          return typeOf(object) === REACT_FORWARD_REF_TYPE;
        }
        function isFragment(object) {
          return typeOf(object) === REACT_FRAGMENT_TYPE;
        }
        function isLazy(object) {
          return typeOf(object) === REACT_LAZY_TYPE;
        }
        function isMemo(object) {
          return typeOf(object) === REACT_MEMO_TYPE;
        }
        function isPortal(object) {
          return typeOf(object) === REACT_PORTAL_TYPE;
        }
        function isProfiler(object) {
          return typeOf(object) === REACT_PROFILER_TYPE;
        }
        function isStrictMode(object) {
          return typeOf(object) === REACT_STRICT_MODE_TYPE;
        }
        function isSuspense(object) {
          return typeOf(object) === REACT_SUSPENSE_TYPE;
        }
        exports.AsyncMode = AsyncMode;
        exports.ConcurrentMode = ConcurrentMode;
        exports.ContextConsumer = ContextConsumer;
        exports.ContextProvider = ContextProvider;
        exports.Element = Element;
        exports.ForwardRef = ForwardRef;
        exports.Fragment = Fragment3;
        exports.Lazy = Lazy;
        exports.Memo = Memo;
        exports.Portal = Portal;
        exports.Profiler = Profiler;
        exports.StrictMode = StrictMode;
        exports.Suspense = Suspense;
        exports.isAsyncMode = isAsyncMode;
        exports.isConcurrentMode = isConcurrentMode;
        exports.isContextConsumer = isContextConsumer;
        exports.isContextProvider = isContextProvider;
        exports.isElement = isElement;
        exports.isForwardRef = isForwardRef;
        exports.isFragment = isFragment;
        exports.isLazy = isLazy;
        exports.isMemo = isMemo;
        exports.isPortal = isPortal;
        exports.isProfiler = isProfiler;
        exports.isStrictMode = isStrictMode;
        exports.isSuspense = isSuspense;
        exports.isValidElementType = isValidElementType;
        exports.typeOf = typeOf;
      })();
    }
  }
});

// node_modules/@nivo/core/node_modules/prop-types/node_modules/react-is/index.js
var require_react_is = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/node_modules/react-is/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_is_development();
    }
  }
});

// node_modules/@nivo/core/node_modules/prop-types/node_modules/object-assign/index.js
var require_object_assign = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/node_modules/object-assign/index.js"(exports, module) {
    "use strict";
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var propIsEnumerable = Object.prototype.propertyIsEnumerable;
    function toObject(val) {
      if (val === null || val === void 0) {
        throw new TypeError("Object.assign cannot be called with null or undefined");
      }
      return Object(val);
    }
    function shouldUseNative() {
      try {
        if (!Object.assign) {
          return false;
        }
        var test1 = new String("abc");
        test1[5] = "de";
        if (Object.getOwnPropertyNames(test1)[0] === "5") {
          return false;
        }
        var test2 = {};
        for (var i5 = 0; i5 < 10; i5++) {
          test2["_" + String.fromCharCode(i5)] = i5;
        }
        var order2 = Object.getOwnPropertyNames(test2).map(function(n5) {
          return test2[n5];
        });
        if (order2.join("") !== "0123456789") {
          return false;
        }
        var test3 = {};
        "abcdefghijklmnopqrst".split("").forEach(function(letter) {
          test3[letter] = letter;
        });
        if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") {
          return false;
        }
        return true;
      } catch (err) {
        return false;
      }
    }
    module.exports = shouldUseNative() ? Object.assign : function(target, source) {
      var from;
      var to5 = toObject(target);
      var symbols;
      for (var s3 = 1; s3 < arguments.length; s3++) {
        from = Object(arguments[s3]);
        for (var key in from) {
          if (hasOwnProperty.call(from, key)) {
            to5[key] = from[key];
          }
        }
        if (getOwnPropertySymbols) {
          symbols = getOwnPropertySymbols(from);
          for (var i5 = 0; i5 < symbols.length; i5++) {
            if (propIsEnumerable.call(from, symbols[i5])) {
              to5[symbols[i5]] = from[symbols[i5]];
            }
          }
        }
      }
      return to5;
    };
  }
});

// node_modules/@nivo/core/node_modules/prop-types/lib/ReactPropTypesSecret.js
var require_ReactPropTypesSecret = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/lib/ReactPropTypesSecret.js"(exports, module) {
    "use strict";
    var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
    module.exports = ReactPropTypesSecret;
  }
});

// node_modules/@nivo/core/node_modules/prop-types/lib/has.js
var require_has = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/lib/has.js"(exports, module) {
    module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
  }
});

// node_modules/@nivo/core/node_modules/prop-types/checkPropTypes.js
var require_checkPropTypes = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/checkPropTypes.js"(exports, module) {
    "use strict";
    var printWarning = function() {
    };
    if (true) {
      ReactPropTypesSecret = require_ReactPropTypesSecret();
      loggedTypeFailures = {};
      has = require_has();
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x4) {
        }
      };
    }
    var ReactPropTypesSecret;
    var loggedTypeFailures;
    var has;
    function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
      if (true) {
        for (var typeSpecName in typeSpecs) {
          if (has(typeSpecs, typeSpecName)) {
            var error;
            try {
              if (typeof typeSpecs[typeSpecName] !== "function") {
                var err = Error(
                  (componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
                );
                err.name = "Invariant Violation";
                throw err;
              }
              error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
            } catch (ex) {
              error = ex;
            }
            if (error && !(error instanceof Error)) {
              printWarning(
                (componentName || "React class") + ": type specification of " + location + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
              );
            }
            if (error instanceof Error && !(error.message in loggedTypeFailures)) {
              loggedTypeFailures[error.message] = true;
              var stack = getStack ? getStack() : "";
              printWarning(
                "Failed " + location + " type: " + error.message + (stack != null ? stack : "")
              );
            }
          }
        }
      }
    }
    checkPropTypes.resetWarningCache = function() {
      if (true) {
        loggedTypeFailures = {};
      }
    };
    module.exports = checkPropTypes;
  }
});

// node_modules/@nivo/core/node_modules/prop-types/factoryWithTypeCheckers.js
var require_factoryWithTypeCheckers = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/factoryWithTypeCheckers.js"(exports, module) {
    "use strict";
    var ReactIs = require_react_is();
    var assign3 = require_object_assign();
    var ReactPropTypesSecret = require_ReactPropTypesSecret();
    var has = require_has();
    var checkPropTypes = require_checkPropTypes();
    var printWarning = function() {
    };
    if (true) {
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x4) {
        }
      };
    }
    function emptyFunctionThatReturnsNull() {
      return null;
    }
    module.exports = function(isValidElement, throwOnDirectAccess) {
      var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
      var FAUX_ITERATOR_SYMBOL = "@@iterator";
      function getIteratorFn(maybeIterable) {
        var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
        if (typeof iteratorFn === "function") {
          return iteratorFn;
        }
      }
      var ANONYMOUS = "<<anonymous>>";
      var ReactPropTypes = {
        array: createPrimitiveTypeChecker("array"),
        bigint: createPrimitiveTypeChecker("bigint"),
        bool: createPrimitiveTypeChecker("boolean"),
        func: createPrimitiveTypeChecker("function"),
        number: createPrimitiveTypeChecker("number"),
        object: createPrimitiveTypeChecker("object"),
        string: createPrimitiveTypeChecker("string"),
        symbol: createPrimitiveTypeChecker("symbol"),
        any: createAnyTypeChecker(),
        arrayOf: createArrayOfTypeChecker,
        element: createElementTypeChecker(),
        elementType: createElementTypeTypeChecker(),
        instanceOf: createInstanceTypeChecker,
        node: createNodeChecker(),
        objectOf: createObjectOfTypeChecker,
        oneOf: createEnumTypeChecker,
        oneOfType: createUnionTypeChecker,
        shape: createShapeTypeChecker,
        exact: createStrictShapeTypeChecker
      };
      function is3(x4, y6) {
        if (x4 === y6) {
          return x4 !== 0 || 1 / x4 === 1 / y6;
        } else {
          return x4 !== x4 && y6 !== y6;
        }
      }
      function PropTypeError(message, data) {
        this.message = message;
        this.data = data && typeof data === "object" ? data : {};
        this.stack = "";
      }
      PropTypeError.prototype = Error.prototype;
      function createChainableTypeChecker(validate) {
        if (true) {
          var manualPropTypeCallCache = {};
          var manualPropTypeWarningCount = 0;
        }
        function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
          componentName = componentName || ANONYMOUS;
          propFullName = propFullName || propName;
          if (secret !== ReactPropTypesSecret) {
            if (throwOnDirectAccess) {
              var err = new Error(
                "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
              );
              err.name = "Invariant Violation";
              throw err;
            } else if (typeof console !== "undefined") {
              var cacheKey3 = componentName + ":" + propName;
              if (!manualPropTypeCallCache[cacheKey3] && // Avoid spamming the console because they are often not actionable except for lib authors
              manualPropTypeWarningCount < 3) {
                printWarning(
                  "You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
                );
                manualPropTypeCallCache[cacheKey3] = true;
                manualPropTypeWarningCount++;
              }
            }
          }
          if (props[propName] == null) {
            if (isRequired) {
              if (props[propName] === null) {
                return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
              }
              return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
            }
            return null;
          } else {
            return validate(props, propName, componentName, location, propFullName);
          }
        }
        var chainedCheckType = checkType.bind(null, false);
        chainedCheckType.isRequired = checkType.bind(null, true);
        return chainedCheckType;
      }
      function createPrimitiveTypeChecker(expectedType) {
        function validate(props, propName, componentName, location, propFullName, secret) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== expectedType) {
            var preciseType = getPreciseType(propValue);
            return new PropTypeError(
              "Invalid " + location + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."),
              { expectedType }
            );
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createAnyTypeChecker() {
        return createChainableTypeChecker(emptyFunctionThatReturnsNull);
      }
      function createArrayOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
          }
          var propValue = props[propName];
          if (!Array.isArray(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
          }
          for (var i5 = 0; i5 < propValue.length; i5++) {
            var error = typeChecker(propValue, i5, componentName, location, propFullName + "[" + i5 + "]", ReactPropTypesSecret);
            if (error instanceof Error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!isValidElement(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!ReactIs.isValidElementType(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createInstanceTypeChecker(expectedClass) {
        function validate(props, propName, componentName, location, propFullName) {
          if (!(props[propName] instanceof expectedClass)) {
            var expectedClassName = expectedClass.name || ANONYMOUS;
            var actualClassName = getClassName(props[propName]);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createEnumTypeChecker(expectedValues) {
        if (!Array.isArray(expectedValues)) {
          if (true) {
            if (arguments.length > 1) {
              printWarning(
                "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
              );
            } else {
              printWarning("Invalid argument supplied to oneOf, expected an array.");
            }
          }
          return emptyFunctionThatReturnsNull;
        }
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          for (var i5 = 0; i5 < expectedValues.length; i5++) {
            if (is3(propValue, expectedValues[i5])) {
              return null;
            }
          }
          var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
            var type = getPreciseType(value);
            if (type === "symbol") {
              return String(value);
            }
            return value;
          });
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createObjectOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
          }
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
          }
          for (var key in propValue) {
            if (has(propValue, key)) {
              var error = typeChecker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
              if (error instanceof Error) {
                return error;
              }
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createUnionTypeChecker(arrayOfTypeCheckers) {
        if (!Array.isArray(arrayOfTypeCheckers)) {
          true ? printWarning("Invalid argument supplied to oneOfType, expected an instance of array.") : void 0;
          return emptyFunctionThatReturnsNull;
        }
        for (var i5 = 0; i5 < arrayOfTypeCheckers.length; i5++) {
          var checker = arrayOfTypeCheckers[i5];
          if (typeof checker !== "function") {
            printWarning(
              "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i5 + "."
            );
            return emptyFunctionThatReturnsNull;
          }
        }
        function validate(props, propName, componentName, location, propFullName) {
          var expectedTypes = [];
          for (var i6 = 0; i6 < arrayOfTypeCheckers.length; i6++) {
            var checker2 = arrayOfTypeCheckers[i6];
            var checkerResult = checker2(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
            if (checkerResult == null) {
              return null;
            }
            if (checkerResult.data && has(checkerResult.data, "expectedType")) {
              expectedTypes.push(checkerResult.data.expectedType);
            }
          }
          var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createNodeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          if (!isNode(props[propName])) {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function invalidValidatorError(componentName, location, propFullName, key, type) {
        return new PropTypeError(
          (componentName || "React class") + ": " + location + " type `" + propFullName + "." + key + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`."
        );
      }
      function createShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          for (var key in shapeTypes) {
            var checker = shapeTypes[key];
            if (typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
            }
            var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createStrictShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          var allKeys = assign3({}, props[propName], shapeTypes);
          for (var key in allKeys) {
            var checker = shapeTypes[key];
            if (has(shapeTypes, key) && typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
            }
            if (!checker) {
              return new PropTypeError(
                "Invalid " + location + " `" + propFullName + "` key `" + key + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  ")
              );
            }
            var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function isNode(propValue) {
        switch (typeof propValue) {
          case "number":
          case "string":
          case "undefined":
            return true;
          case "boolean":
            return !propValue;
          case "object":
            if (Array.isArray(propValue)) {
              return propValue.every(isNode);
            }
            if (propValue === null || isValidElement(propValue)) {
              return true;
            }
            var iteratorFn = getIteratorFn(propValue);
            if (iteratorFn) {
              var iterator = iteratorFn.call(propValue);
              var step;
              if (iteratorFn !== propValue.entries) {
                while (!(step = iterator.next()).done) {
                  if (!isNode(step.value)) {
                    return false;
                  }
                }
              } else {
                while (!(step = iterator.next()).done) {
                  var entry = step.value;
                  if (entry) {
                    if (!isNode(entry[1])) {
                      return false;
                    }
                  }
                }
              }
            } else {
              return false;
            }
            return true;
          default:
            return false;
        }
      }
      function isSymbol(propType, propValue) {
        if (propType === "symbol") {
          return true;
        }
        if (!propValue) {
          return false;
        }
        if (propValue["@@toStringTag"] === "Symbol") {
          return true;
        }
        if (typeof Symbol === "function" && propValue instanceof Symbol) {
          return true;
        }
        return false;
      }
      function getPropType(propValue) {
        var propType = typeof propValue;
        if (Array.isArray(propValue)) {
          return "array";
        }
        if (propValue instanceof RegExp) {
          return "object";
        }
        if (isSymbol(propType, propValue)) {
          return "symbol";
        }
        return propType;
      }
      function getPreciseType(propValue) {
        if (typeof propValue === "undefined" || propValue === null) {
          return "" + propValue;
        }
        var propType = getPropType(propValue);
        if (propType === "object") {
          if (propValue instanceof Date) {
            return "date";
          } else if (propValue instanceof RegExp) {
            return "regexp";
          }
        }
        return propType;
      }
      function getPostfixForTypeWarning(value) {
        var type = getPreciseType(value);
        switch (type) {
          case "array":
          case "object":
            return "an " + type;
          case "boolean":
          case "date":
          case "regexp":
            return "a " + type;
          default:
            return type;
        }
      }
      function getClassName(propValue) {
        if (!propValue.constructor || !propValue.constructor.name) {
          return ANONYMOUS;
        }
        return propValue.constructor.name;
      }
      ReactPropTypes.checkPropTypes = checkPropTypes;
      ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
      ReactPropTypes.PropTypes = ReactPropTypes;
      return ReactPropTypes;
    };
  }
});

// node_modules/@nivo/core/node_modules/prop-types/index.js
var require_prop_types = __commonJS({
  "node_modules/@nivo/core/node_modules/prop-types/index.js"(exports, module) {
    if (true) {
      ReactIs = require_react_is();
      throwOnDirectAccess = true;
      module.exports = require_factoryWithTypeCheckers()(ReactIs.isElement, throwOnDirectAccess);
    } else {
      module.exports = null();
    }
    var ReactIs;
    var throwOnDirectAccess;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_listCacheClear.js
var require_listCacheClear = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_listCacheClear.js"(exports, module) {
    function listCacheClear() {
      this.__data__ = [];
      this.size = 0;
    }
    module.exports = listCacheClear;
  }
});

// node_modules/@nivo/core/node_modules/lodash/eq.js
var require_eq = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/eq.js"(exports, module) {
    function eq(value, other) {
      return value === other || value !== value && other !== other;
    }
    module.exports = eq;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_assocIndexOf.js
var require_assocIndexOf = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_assocIndexOf.js"(exports, module) {
    var eq = require_eq();
    function assocIndexOf(array4, key) {
      var length = array4.length;
      while (length--) {
        if (eq(array4[length][0], key)) {
          return length;
        }
      }
      return -1;
    }
    module.exports = assocIndexOf;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_listCacheDelete.js
var require_listCacheDelete = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_listCacheDelete.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    var arrayProto = Array.prototype;
    var splice = arrayProto.splice;
    function listCacheDelete(key) {
      var data = this.__data__, index4 = assocIndexOf(data, key);
      if (index4 < 0) {
        return false;
      }
      var lastIndex = data.length - 1;
      if (index4 == lastIndex) {
        data.pop();
      } else {
        splice.call(data, index4, 1);
      }
      --this.size;
      return true;
    }
    module.exports = listCacheDelete;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_listCacheGet.js
var require_listCacheGet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_listCacheGet.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    function listCacheGet(key) {
      var data = this.__data__, index4 = assocIndexOf(data, key);
      return index4 < 0 ? void 0 : data[index4][1];
    }
    module.exports = listCacheGet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_listCacheHas.js
var require_listCacheHas = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_listCacheHas.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    function listCacheHas(key) {
      return assocIndexOf(this.__data__, key) > -1;
    }
    module.exports = listCacheHas;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_listCacheSet.js
var require_listCacheSet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_listCacheSet.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf();
    function listCacheSet(key, value) {
      var data = this.__data__, index4 = assocIndexOf(data, key);
      if (index4 < 0) {
        ++this.size;
        data.push([key, value]);
      } else {
        data[index4][1] = value;
      }
      return this;
    }
    module.exports = listCacheSet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_ListCache.js
var require_ListCache = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_ListCache.js"(exports, module) {
    var listCacheClear = require_listCacheClear();
    var listCacheDelete = require_listCacheDelete();
    var listCacheGet = require_listCacheGet();
    var listCacheHas = require_listCacheHas();
    var listCacheSet = require_listCacheSet();
    function ListCache(entries) {
      var index4 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index4 < length) {
        var entry = entries[index4];
        this.set(entry[0], entry[1]);
      }
    }
    ListCache.prototype.clear = listCacheClear;
    ListCache.prototype["delete"] = listCacheDelete;
    ListCache.prototype.get = listCacheGet;
    ListCache.prototype.has = listCacheHas;
    ListCache.prototype.set = listCacheSet;
    module.exports = ListCache;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_stackClear.js
var require_stackClear = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_stackClear.js"(exports, module) {
    var ListCache = require_ListCache();
    function stackClear() {
      this.__data__ = new ListCache();
      this.size = 0;
    }
    module.exports = stackClear;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_stackDelete.js
var require_stackDelete = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_stackDelete.js"(exports, module) {
    function stackDelete(key) {
      var data = this.__data__, result = data["delete"](key);
      this.size = data.size;
      return result;
    }
    module.exports = stackDelete;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_stackGet.js
var require_stackGet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_stackGet.js"(exports, module) {
    function stackGet(key) {
      return this.__data__.get(key);
    }
    module.exports = stackGet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_stackHas.js
var require_stackHas = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_stackHas.js"(exports, module) {
    function stackHas(key) {
      return this.__data__.has(key);
    }
    module.exports = stackHas;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_freeGlobal.js
var require_freeGlobal = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_freeGlobal.js"(exports, module) {
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    module.exports = freeGlobal;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_root.js
var require_root = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_root.js"(exports, module) {
    var freeGlobal = require_freeGlobal();
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root = freeGlobal || freeSelf || Function("return this")();
    module.exports = root;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_Symbol.js
var require_Symbol = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_Symbol.js"(exports, module) {
    var root = require_root();
    var Symbol3 = root.Symbol;
    module.exports = Symbol3;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getRawTag.js
var require_getRawTag = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getRawTag.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var nativeObjectToString = objectProto.toString;
    var symToStringTag = Symbol3 ? Symbol3.toStringTag : void 0;
    function getRawTag(value) {
      var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
      try {
        value[symToStringTag] = void 0;
        var unmasked = true;
      } catch (e7) {
      }
      var result = nativeObjectToString.call(value);
      if (unmasked) {
        if (isOwn) {
          value[symToStringTag] = tag;
        } else {
          delete value[symToStringTag];
        }
      }
      return result;
    }
    module.exports = getRawTag;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_objectToString.js
var require_objectToString = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_objectToString.js"(exports, module) {
    var objectProto = Object.prototype;
    var nativeObjectToString = objectProto.toString;
    function objectToString(value) {
      return nativeObjectToString.call(value);
    }
    module.exports = objectToString;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseGetTag.js
var require_baseGetTag = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseGetTag.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var getRawTag = require_getRawTag();
    var objectToString = require_objectToString();
    var nullTag = "[object Null]";
    var undefinedTag = "[object Undefined]";
    var symToStringTag = Symbol3 ? Symbol3.toStringTag : void 0;
    function baseGetTag(value) {
      if (value == null) {
        return value === void 0 ? undefinedTag : nullTag;
      }
      return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
    }
    module.exports = baseGetTag;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isObject.js
var require_isObject = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isObject.js"(exports, module) {
    function isObject(value) {
      var type = typeof value;
      return value != null && (type == "object" || type == "function");
    }
    module.exports = isObject;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isFunction.js
var require_isFunction = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isFunction.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObject = require_isObject();
    var asyncTag = "[object AsyncFunction]";
    var funcTag = "[object Function]";
    var genTag = "[object GeneratorFunction]";
    var proxyTag = "[object Proxy]";
    function isFunction(value) {
      if (!isObject(value)) {
        return false;
      }
      var tag = baseGetTag(value);
      return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
    }
    module.exports = isFunction;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_coreJsData.js
var require_coreJsData = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_coreJsData.js"(exports, module) {
    var root = require_root();
    var coreJsData = root["__core-js_shared__"];
    module.exports = coreJsData;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_isMasked.js
var require_isMasked = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_isMasked.js"(exports, module) {
    var coreJsData = require_coreJsData();
    var maskSrcKey = function() {
      var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
      return uid ? "Symbol(src)_1." + uid : "";
    }();
    function isMasked(func) {
      return !!maskSrcKey && maskSrcKey in func;
    }
    module.exports = isMasked;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_toSource.js
var require_toSource = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_toSource.js"(exports, module) {
    var funcProto = Function.prototype;
    var funcToString = funcProto.toString;
    function toSource(func) {
      if (func != null) {
        try {
          return funcToString.call(func);
        } catch (e7) {
        }
        try {
          return func + "";
        } catch (e7) {
        }
      }
      return "";
    }
    module.exports = toSource;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseIsNative.js
var require_baseIsNative = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseIsNative.js"(exports, module) {
    var isFunction = require_isFunction();
    var isMasked = require_isMasked();
    var isObject = require_isObject();
    var toSource = require_toSource();
    var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
    var reIsHostCtor = /^\[object .+?Constructor\]$/;
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var reIsNative = RegExp(
      "^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
    );
    function baseIsNative(value) {
      if (!isObject(value) || isMasked(value)) {
        return false;
      }
      var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
      return pattern.test(toSource(value));
    }
    module.exports = baseIsNative;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getValue.js
var require_getValue = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getValue.js"(exports, module) {
    function getValue(object, key) {
      return object == null ? void 0 : object[key];
    }
    module.exports = getValue;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getNative.js
var require_getNative = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getNative.js"(exports, module) {
    var baseIsNative = require_baseIsNative();
    var getValue = require_getValue();
    function getNative(object, key) {
      var value = getValue(object, key);
      return baseIsNative(value) ? value : void 0;
    }
    module.exports = getNative;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_Map.js
var require_Map = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_Map.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var Map2 = getNative(root, "Map");
    module.exports = Map2;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_nativeCreate.js
var require_nativeCreate = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_nativeCreate.js"(exports, module) {
    var getNative = require_getNative();
    var nativeCreate = getNative(Object, "create");
    module.exports = nativeCreate;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_hashClear.js
var require_hashClear = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_hashClear.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    function hashClear() {
      this.__data__ = nativeCreate ? nativeCreate(null) : {};
      this.size = 0;
    }
    module.exports = hashClear;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_hashDelete.js
var require_hashDelete = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_hashDelete.js"(exports, module) {
    function hashDelete(key) {
      var result = this.has(key) && delete this.__data__[key];
      this.size -= result ? 1 : 0;
      return result;
    }
    module.exports = hashDelete;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_hashGet.js
var require_hashGet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_hashGet.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function hashGet(key) {
      var data = this.__data__;
      if (nativeCreate) {
        var result = data[key];
        return result === HASH_UNDEFINED ? void 0 : result;
      }
      return hasOwnProperty.call(data, key) ? data[key] : void 0;
    }
    module.exports = hashGet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_hashHas.js
var require_hashHas = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_hashHas.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function hashHas(key) {
      var data = this.__data__;
      return nativeCreate ? data[key] !== void 0 : hasOwnProperty.call(data, key);
    }
    module.exports = hashHas;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_hashSet.js
var require_hashSet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_hashSet.js"(exports, module) {
    var nativeCreate = require_nativeCreate();
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    function hashSet(key, value) {
      var data = this.__data__;
      this.size += this.has(key) ? 0 : 1;
      data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
      return this;
    }
    module.exports = hashSet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_Hash.js
var require_Hash = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_Hash.js"(exports, module) {
    var hashClear = require_hashClear();
    var hashDelete = require_hashDelete();
    var hashGet = require_hashGet();
    var hashHas = require_hashHas();
    var hashSet = require_hashSet();
    function Hash(entries) {
      var index4 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index4 < length) {
        var entry = entries[index4];
        this.set(entry[0], entry[1]);
      }
    }
    Hash.prototype.clear = hashClear;
    Hash.prototype["delete"] = hashDelete;
    Hash.prototype.get = hashGet;
    Hash.prototype.has = hashHas;
    Hash.prototype.set = hashSet;
    module.exports = Hash;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_mapCacheClear.js
var require_mapCacheClear = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_mapCacheClear.js"(exports, module) {
    var Hash = require_Hash();
    var ListCache = require_ListCache();
    var Map2 = require_Map();
    function mapCacheClear() {
      this.size = 0;
      this.__data__ = {
        "hash": new Hash(),
        "map": new (Map2 || ListCache)(),
        "string": new Hash()
      };
    }
    module.exports = mapCacheClear;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_isKeyable.js
var require_isKeyable = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_isKeyable.js"(exports, module) {
    function isKeyable(value) {
      var type = typeof value;
      return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
    }
    module.exports = isKeyable;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getMapData.js
var require_getMapData = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getMapData.js"(exports, module) {
    var isKeyable = require_isKeyable();
    function getMapData(map9, key) {
      var data = map9.__data__;
      return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
    }
    module.exports = getMapData;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_mapCacheDelete.js
var require_mapCacheDelete = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_mapCacheDelete.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheDelete(key) {
      var result = getMapData(this, key)["delete"](key);
      this.size -= result ? 1 : 0;
      return result;
    }
    module.exports = mapCacheDelete;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_mapCacheGet.js
var require_mapCacheGet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_mapCacheGet.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheGet(key) {
      return getMapData(this, key).get(key);
    }
    module.exports = mapCacheGet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_mapCacheHas.js
var require_mapCacheHas = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_mapCacheHas.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheHas(key) {
      return getMapData(this, key).has(key);
    }
    module.exports = mapCacheHas;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_mapCacheSet.js
var require_mapCacheSet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_mapCacheSet.js"(exports, module) {
    var getMapData = require_getMapData();
    function mapCacheSet(key, value) {
      var data = getMapData(this, key), size = data.size;
      data.set(key, value);
      this.size += data.size == size ? 0 : 1;
      return this;
    }
    module.exports = mapCacheSet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_MapCache.js
var require_MapCache = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_MapCache.js"(exports, module) {
    var mapCacheClear = require_mapCacheClear();
    var mapCacheDelete = require_mapCacheDelete();
    var mapCacheGet = require_mapCacheGet();
    var mapCacheHas = require_mapCacheHas();
    var mapCacheSet = require_mapCacheSet();
    function MapCache(entries) {
      var index4 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index4 < length) {
        var entry = entries[index4];
        this.set(entry[0], entry[1]);
      }
    }
    MapCache.prototype.clear = mapCacheClear;
    MapCache.prototype["delete"] = mapCacheDelete;
    MapCache.prototype.get = mapCacheGet;
    MapCache.prototype.has = mapCacheHas;
    MapCache.prototype.set = mapCacheSet;
    module.exports = MapCache;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_stackSet.js
var require_stackSet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_stackSet.js"(exports, module) {
    var ListCache = require_ListCache();
    var Map2 = require_Map();
    var MapCache = require_MapCache();
    var LARGE_ARRAY_SIZE = 200;
    function stackSet(key, value) {
      var data = this.__data__;
      if (data instanceof ListCache) {
        var pairs4 = data.__data__;
        if (!Map2 || pairs4.length < LARGE_ARRAY_SIZE - 1) {
          pairs4.push([key, value]);
          this.size = ++data.size;
          return this;
        }
        data = this.__data__ = new MapCache(pairs4);
      }
      data.set(key, value);
      this.size = data.size;
      return this;
    }
    module.exports = stackSet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_Stack.js
var require_Stack = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_Stack.js"(exports, module) {
    var ListCache = require_ListCache();
    var stackClear = require_stackClear();
    var stackDelete = require_stackDelete();
    var stackGet = require_stackGet();
    var stackHas = require_stackHas();
    var stackSet = require_stackSet();
    function Stack(entries) {
      var data = this.__data__ = new ListCache(entries);
      this.size = data.size;
    }
    Stack.prototype.clear = stackClear;
    Stack.prototype["delete"] = stackDelete;
    Stack.prototype.get = stackGet;
    Stack.prototype.has = stackHas;
    Stack.prototype.set = stackSet;
    module.exports = Stack;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_defineProperty.js
var require_defineProperty = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_defineProperty.js"(exports, module) {
    var getNative = require_getNative();
    var defineProperty = function() {
      try {
        var func = getNative(Object, "defineProperty");
        func({}, "", {});
        return func;
      } catch (e7) {
      }
    }();
    module.exports = defineProperty;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseAssignValue.js
var require_baseAssignValue = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseAssignValue.js"(exports, module) {
    var defineProperty = require_defineProperty();
    function baseAssignValue(object, key, value) {
      if (key == "__proto__" && defineProperty) {
        defineProperty(object, key, {
          "configurable": true,
          "enumerable": true,
          "value": value,
          "writable": true
        });
      } else {
        object[key] = value;
      }
    }
    module.exports = baseAssignValue;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_assignMergeValue.js
var require_assignMergeValue = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_assignMergeValue.js"(exports, module) {
    var baseAssignValue = require_baseAssignValue();
    var eq = require_eq();
    function assignMergeValue(object, key, value) {
      if (value !== void 0 && !eq(object[key], value) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    module.exports = assignMergeValue;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_createBaseFor.js
var require_createBaseFor = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_createBaseFor.js"(exports, module) {
    function createBaseFor(fromRight) {
      return function(object, iteratee, keysFunc) {
        var index4 = -1, iterable = Object(object), props = keysFunc(object), length = props.length;
        while (length--) {
          var key = props[fromRight ? length : ++index4];
          if (iteratee(iterable[key], key, iterable) === false) {
            break;
          }
        }
        return object;
      };
    }
    module.exports = createBaseFor;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseFor.js
var require_baseFor = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseFor.js"(exports, module) {
    var createBaseFor = require_createBaseFor();
    var baseFor = createBaseFor();
    module.exports = baseFor;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_cloneBuffer.js
var require_cloneBuffer = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_cloneBuffer.js"(exports, module) {
    var root = require_root();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0;
    var allocUnsafe = Buffer ? Buffer.allocUnsafe : void 0;
    function cloneBuffer(buffer, isDeep) {
      if (isDeep) {
        return buffer.slice();
      }
      var length = buffer.length, result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);
      buffer.copy(result);
      return result;
    }
    module.exports = cloneBuffer;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_Uint8Array.js
var require_Uint8Array = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_Uint8Array.js"(exports, module) {
    var root = require_root();
    var Uint8Array = root.Uint8Array;
    module.exports = Uint8Array;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_cloneArrayBuffer.js
var require_cloneArrayBuffer = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_cloneArrayBuffer.js"(exports, module) {
    var Uint8Array = require_Uint8Array();
    function cloneArrayBuffer(arrayBuffer) {
      var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
      new Uint8Array(result).set(new Uint8Array(arrayBuffer));
      return result;
    }
    module.exports = cloneArrayBuffer;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_cloneTypedArray.js
var require_cloneTypedArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_cloneTypedArray.js"(exports, module) {
    var cloneArrayBuffer = require_cloneArrayBuffer();
    function cloneTypedArray(typedArray, isDeep) {
      var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
      return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
    }
    module.exports = cloneTypedArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_copyArray.js
var require_copyArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_copyArray.js"(exports, module) {
    function copyArray(source, array4) {
      var index4 = -1, length = source.length;
      array4 || (array4 = Array(length));
      while (++index4 < length) {
        array4[index4] = source[index4];
      }
      return array4;
    }
    module.exports = copyArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseCreate.js
var require_baseCreate = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseCreate.js"(exports, module) {
    var isObject = require_isObject();
    var objectCreate = Object.create;
    var baseCreate = /* @__PURE__ */ function() {
      function object() {
      }
      return function(proto) {
        if (!isObject(proto)) {
          return {};
        }
        if (objectCreate) {
          return objectCreate(proto);
        }
        object.prototype = proto;
        var result = new object();
        object.prototype = void 0;
        return result;
      };
    }();
    module.exports = baseCreate;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_overArg.js
var require_overArg = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_overArg.js"(exports, module) {
    function overArg(func, transform) {
      return function(arg) {
        return func(transform(arg));
      };
    }
    module.exports = overArg;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getPrototype.js
var require_getPrototype = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getPrototype.js"(exports, module) {
    var overArg = require_overArg();
    var getPrototype = overArg(Object.getPrototypeOf, Object);
    module.exports = getPrototype;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_isPrototype.js
var require_isPrototype = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_isPrototype.js"(exports, module) {
    var objectProto = Object.prototype;
    function isPrototype(value) {
      var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
      return value === proto;
    }
    module.exports = isPrototype;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_initCloneObject.js
var require_initCloneObject = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_initCloneObject.js"(exports, module) {
    var baseCreate = require_baseCreate();
    var getPrototype = require_getPrototype();
    var isPrototype = require_isPrototype();
    function initCloneObject(object) {
      return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
    }
    module.exports = initCloneObject;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isObjectLike.js
var require_isObjectLike = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isObjectLike.js"(exports, module) {
    function isObjectLike(value) {
      return value != null && typeof value == "object";
    }
    module.exports = isObjectLike;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseIsArguments.js
var require_baseIsArguments = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseIsArguments.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var argsTag = "[object Arguments]";
    function baseIsArguments(value) {
      return isObjectLike(value) && baseGetTag(value) == argsTag;
    }
    module.exports = baseIsArguments;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isArguments.js
var require_isArguments = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isArguments.js"(exports, module) {
    var baseIsArguments = require_baseIsArguments();
    var isObjectLike = require_isObjectLike();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var isArguments = baseIsArguments(/* @__PURE__ */ function() {
      return arguments;
    }()) ? baseIsArguments : function(value) {
      return isObjectLike(value) && hasOwnProperty.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
    };
    module.exports = isArguments;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isArray.js
var require_isArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isArray.js"(exports, module) {
    var isArray = Array.isArray;
    module.exports = isArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isLength.js
var require_isLength = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isLength.js"(exports, module) {
    var MAX_SAFE_INTEGER = 9007199254740991;
    function isLength(value) {
      return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
    }
    module.exports = isLength;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isArrayLike.js
var require_isArrayLike = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isArrayLike.js"(exports, module) {
    var isFunction = require_isFunction();
    var isLength = require_isLength();
    function isArrayLike(value) {
      return value != null && isLength(value.length) && !isFunction(value);
    }
    module.exports = isArrayLike;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isArrayLikeObject.js
var require_isArrayLikeObject = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isArrayLikeObject.js"(exports, module) {
    var isArrayLike = require_isArrayLike();
    var isObjectLike = require_isObjectLike();
    function isArrayLikeObject(value) {
      return isObjectLike(value) && isArrayLike(value);
    }
    module.exports = isArrayLikeObject;
  }
});

// node_modules/@nivo/core/node_modules/lodash/stubFalse.js
var require_stubFalse = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/stubFalse.js"(exports, module) {
    function stubFalse() {
      return false;
    }
    module.exports = stubFalse;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isBuffer.js
var require_isBuffer = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isBuffer.js"(exports, module) {
    var root = require_root();
    var stubFalse = require_stubFalse();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0;
    var nativeIsBuffer = Buffer ? Buffer.isBuffer : void 0;
    var isBuffer = nativeIsBuffer || stubFalse;
    module.exports = isBuffer;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isPlainObject.js
var require_isPlainObject = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isPlainObject.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var getPrototype = require_getPrototype();
    var isObjectLike = require_isObjectLike();
    var objectTag = "[object Object]";
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var objectCtorString = funcToString.call(Object);
    function isPlainObject(value) {
      if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
        return false;
      }
      var proto = getPrototype(value);
      if (proto === null) {
        return true;
      }
      var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
      return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
    }
    module.exports = isPlainObject;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseIsTypedArray.js
var require_baseIsTypedArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseIsTypedArray.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isLength = require_isLength();
    var isObjectLike = require_isObjectLike();
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var funcTag = "[object Function]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var objectTag = "[object Object]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var weakMapTag = "[object WeakMap]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    var typedArrayTags = {};
    typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
    typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
    function baseIsTypedArray(value) {
      return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
    }
    module.exports = baseIsTypedArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseUnary.js
var require_baseUnary = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseUnary.js"(exports, module) {
    function baseUnary(func) {
      return function(value) {
        return func(value);
      };
    }
    module.exports = baseUnary;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_nodeUtil.js
var require_nodeUtil = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_nodeUtil.js"(exports, module) {
    var freeGlobal = require_freeGlobal();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var freeProcess = moduleExports && freeGlobal.process;
    var nodeUtil = function() {
      try {
        var types = freeModule && freeModule.require && freeModule.require("util").types;
        if (types) {
          return types;
        }
        return freeProcess && freeProcess.binding && freeProcess.binding("util");
      } catch (e7) {
      }
    }();
    module.exports = nodeUtil;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isTypedArray.js
var require_isTypedArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isTypedArray.js"(exports, module) {
    var baseIsTypedArray = require_baseIsTypedArray();
    var baseUnary = require_baseUnary();
    var nodeUtil = require_nodeUtil();
    var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
    var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
    module.exports = isTypedArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_safeGet.js
var require_safeGet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_safeGet.js"(exports, module) {
    function safeGet(object, key) {
      if (key === "constructor" && typeof object[key] === "function") {
        return;
      }
      if (key == "__proto__") {
        return;
      }
      return object[key];
    }
    module.exports = safeGet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_assignValue.js
var require_assignValue = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_assignValue.js"(exports, module) {
    var baseAssignValue = require_baseAssignValue();
    var eq = require_eq();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function assignValue(object, key, value) {
      var objValue = object[key];
      if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    module.exports = assignValue;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_copyObject.js
var require_copyObject = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_copyObject.js"(exports, module) {
    var assignValue = require_assignValue();
    var baseAssignValue = require_baseAssignValue();
    function copyObject(source, props, object, customizer) {
      var isNew = !object;
      object || (object = {});
      var index4 = -1, length = props.length;
      while (++index4 < length) {
        var key = props[index4];
        var newValue = customizer ? customizer(object[key], source[key], key, object, source) : void 0;
        if (newValue === void 0) {
          newValue = source[key];
        }
        if (isNew) {
          baseAssignValue(object, key, newValue);
        } else {
          assignValue(object, key, newValue);
        }
      }
      return object;
    }
    module.exports = copyObject;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseTimes.js
var require_baseTimes = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseTimes.js"(exports, module) {
    function baseTimes(n5, iteratee) {
      var index4 = -1, result = Array(n5);
      while (++index4 < n5) {
        result[index4] = iteratee(index4);
      }
      return result;
    }
    module.exports = baseTimes;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_isIndex.js
var require_isIndex = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_isIndex.js"(exports, module) {
    var MAX_SAFE_INTEGER = 9007199254740991;
    var reIsUint = /^(?:0|[1-9]\d*)$/;
    function isIndex(value, length) {
      var type = typeof value;
      length = length == null ? MAX_SAFE_INTEGER : length;
      return !!length && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
    }
    module.exports = isIndex;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_arrayLikeKeys.js
var require_arrayLikeKeys = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_arrayLikeKeys.js"(exports, module) {
    var baseTimes = require_baseTimes();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var isBuffer = require_isBuffer();
    var isIndex = require_isIndex();
    var isTypedArray = require_isTypedArray();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function arrayLikeKeys(value, inherited) {
      var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result = skipIndexes ? baseTimes(value.length, String) : [], length = result.length;
      for (var key in value) {
        if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
        (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
        isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
        isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
        isIndex(key, length)))) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = arrayLikeKeys;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_nativeKeysIn.js
var require_nativeKeysIn = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_nativeKeysIn.js"(exports, module) {
    function nativeKeysIn(object) {
      var result = [];
      if (object != null) {
        for (var key in Object(object)) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = nativeKeysIn;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseKeysIn.js
var require_baseKeysIn = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseKeysIn.js"(exports, module) {
    var isObject = require_isObject();
    var isPrototype = require_isPrototype();
    var nativeKeysIn = require_nativeKeysIn();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseKeysIn(object) {
      if (!isObject(object)) {
        return nativeKeysIn(object);
      }
      var isProto = isPrototype(object), result = [];
      for (var key in object) {
        if (!(key == "constructor" && (isProto || !hasOwnProperty.call(object, key)))) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = baseKeysIn;
  }
});

// node_modules/@nivo/core/node_modules/lodash/keysIn.js
var require_keysIn = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/keysIn.js"(exports, module) {
    var arrayLikeKeys = require_arrayLikeKeys();
    var baseKeysIn = require_baseKeysIn();
    var isArrayLike = require_isArrayLike();
    function keysIn(object) {
      return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
    }
    module.exports = keysIn;
  }
});

// node_modules/@nivo/core/node_modules/lodash/toPlainObject.js
var require_toPlainObject = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/toPlainObject.js"(exports, module) {
    var copyObject = require_copyObject();
    var keysIn = require_keysIn();
    function toPlainObject(value) {
      return copyObject(value, keysIn(value));
    }
    module.exports = toPlainObject;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseMergeDeep.js
var require_baseMergeDeep = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseMergeDeep.js"(exports, module) {
    var assignMergeValue = require_assignMergeValue();
    var cloneBuffer = require_cloneBuffer();
    var cloneTypedArray = require_cloneTypedArray();
    var copyArray = require_copyArray();
    var initCloneObject = require_initCloneObject();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var isArrayLikeObject = require_isArrayLikeObject();
    var isBuffer = require_isBuffer();
    var isFunction = require_isFunction();
    var isObject = require_isObject();
    var isPlainObject = require_isPlainObject();
    var isTypedArray = require_isTypedArray();
    var safeGet = require_safeGet();
    var toPlainObject = require_toPlainObject();
    function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
      var objValue = safeGet(object, key), srcValue = safeGet(source, key), stacked = stack.get(srcValue);
      if (stacked) {
        assignMergeValue(object, key, stacked);
        return;
      }
      var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source, stack) : void 0;
      var isCommon = newValue === void 0;
      if (isCommon) {
        var isArr = isArray(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
        newValue = srcValue;
        if (isArr || isBuff || isTyped) {
          if (isArray(objValue)) {
            newValue = objValue;
          } else if (isArrayLikeObject(objValue)) {
            newValue = copyArray(objValue);
          } else if (isBuff) {
            isCommon = false;
            newValue = cloneBuffer(srcValue, true);
          } else if (isTyped) {
            isCommon = false;
            newValue = cloneTypedArray(srcValue, true);
          } else {
            newValue = [];
          }
        } else if (isPlainObject(srcValue) || isArguments(srcValue)) {
          newValue = objValue;
          if (isArguments(objValue)) {
            newValue = toPlainObject(objValue);
          } else if (!isObject(objValue) || isFunction(objValue)) {
            newValue = initCloneObject(srcValue);
          }
        } else {
          isCommon = false;
        }
      }
      if (isCommon) {
        stack.set(srcValue, newValue);
        mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
        stack["delete"](srcValue);
      }
      assignMergeValue(object, key, newValue);
    }
    module.exports = baseMergeDeep;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseMerge.js
var require_baseMerge = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseMerge.js"(exports, module) {
    var Stack = require_Stack();
    var assignMergeValue = require_assignMergeValue();
    var baseFor = require_baseFor();
    var baseMergeDeep = require_baseMergeDeep();
    var isObject = require_isObject();
    var keysIn = require_keysIn();
    var safeGet = require_safeGet();
    function baseMerge(object, source, srcIndex, customizer, stack) {
      if (object === source) {
        return;
      }
      baseFor(source, function(srcValue, key) {
        stack || (stack = new Stack());
        if (isObject(srcValue)) {
          baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
        } else {
          var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source, stack) : void 0;
          if (newValue === void 0) {
            newValue = srcValue;
          }
          assignMergeValue(object, key, newValue);
        }
      }, keysIn);
    }
    module.exports = baseMerge;
  }
});

// node_modules/@nivo/core/node_modules/lodash/identity.js
var require_identity = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/identity.js"(exports, module) {
    function identity10(value) {
      return value;
    }
    module.exports = identity10;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_apply.js
var require_apply = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_apply.js"(exports, module) {
    function apply(func, thisArg, args) {
      switch (args.length) {
        case 0:
          return func.call(thisArg);
        case 1:
          return func.call(thisArg, args[0]);
        case 2:
          return func.call(thisArg, args[0], args[1]);
        case 3:
          return func.call(thisArg, args[0], args[1], args[2]);
      }
      return func.apply(thisArg, args);
    }
    module.exports = apply;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_overRest.js
var require_overRest = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_overRest.js"(exports, module) {
    var apply = require_apply();
    var nativeMax = Math.max;
    function overRest(func, start3, transform) {
      start3 = nativeMax(start3 === void 0 ? func.length - 1 : start3, 0);
      return function() {
        var args = arguments, index4 = -1, length = nativeMax(args.length - start3, 0), array4 = Array(length);
        while (++index4 < length) {
          array4[index4] = args[start3 + index4];
        }
        index4 = -1;
        var otherArgs = Array(start3 + 1);
        while (++index4 < start3) {
          otherArgs[index4] = args[index4];
        }
        otherArgs[start3] = transform(array4);
        return apply(func, this, otherArgs);
      };
    }
    module.exports = overRest;
  }
});

// node_modules/@nivo/core/node_modules/lodash/constant.js
var require_constant = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/constant.js"(exports, module) {
    function constant3(value) {
      return function() {
        return value;
      };
    }
    module.exports = constant3;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseSetToString.js
var require_baseSetToString = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseSetToString.js"(exports, module) {
    var constant3 = require_constant();
    var defineProperty = require_defineProperty();
    var identity10 = require_identity();
    var baseSetToString = !defineProperty ? identity10 : function(func, string) {
      return defineProperty(func, "toString", {
        "configurable": true,
        "enumerable": false,
        "value": constant3(string),
        "writable": true
      });
    };
    module.exports = baseSetToString;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_shortOut.js
var require_shortOut = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_shortOut.js"(exports, module) {
    var HOT_COUNT = 800;
    var HOT_SPAN = 16;
    var nativeNow = Date.now;
    function shortOut(func) {
      var count5 = 0, lastCalled = 0;
      return function() {
        var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
        lastCalled = stamp;
        if (remaining > 0) {
          if (++count5 >= HOT_COUNT) {
            return arguments[0];
          }
        } else {
          count5 = 0;
        }
        return func.apply(void 0, arguments);
      };
    }
    module.exports = shortOut;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_setToString.js
var require_setToString = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_setToString.js"(exports, module) {
    var baseSetToString = require_baseSetToString();
    var shortOut = require_shortOut();
    var setToString = shortOut(baseSetToString);
    module.exports = setToString;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseRest.js
var require_baseRest = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseRest.js"(exports, module) {
    var identity10 = require_identity();
    var overRest = require_overRest();
    var setToString = require_setToString();
    function baseRest(func, start3) {
      return setToString(overRest(func, start3, identity10), func + "");
    }
    module.exports = baseRest;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_isIterateeCall.js
var require_isIterateeCall = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_isIterateeCall.js"(exports, module) {
    var eq = require_eq();
    var isArrayLike = require_isArrayLike();
    var isIndex = require_isIndex();
    var isObject = require_isObject();
    function isIterateeCall(value, index4, object) {
      if (!isObject(object)) {
        return false;
      }
      var type = typeof index4;
      if (type == "number" ? isArrayLike(object) && isIndex(index4, object.length) : type == "string" && index4 in object) {
        return eq(object[index4], value);
      }
      return false;
    }
    module.exports = isIterateeCall;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_createAssigner.js
var require_createAssigner = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_createAssigner.js"(exports, module) {
    var baseRest = require_baseRest();
    var isIterateeCall = require_isIterateeCall();
    function createAssigner(assigner) {
      return baseRest(function(object, sources) {
        var index4 = -1, length = sources.length, customizer = length > 1 ? sources[length - 1] : void 0, guard = length > 2 ? sources[2] : void 0;
        customizer = assigner.length > 3 && typeof customizer == "function" ? (length--, customizer) : void 0;
        if (guard && isIterateeCall(sources[0], sources[1], guard)) {
          customizer = length < 3 ? void 0 : customizer;
          length = 1;
        }
        object = Object(object);
        while (++index4 < length) {
          var source = sources[index4];
          if (source) {
            assigner(object, source, index4, customizer);
          }
        }
        return object;
      });
    }
    module.exports = createAssigner;
  }
});

// node_modules/@nivo/core/node_modules/lodash/merge.js
var require_merge = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/merge.js"(exports, module) {
    var baseMerge = require_baseMerge();
    var createAssigner = require_createAssigner();
    var merge4 = createAssigner(function(object, source, srcIndex) {
      baseMerge(object, source, srcIndex);
    });
    module.exports = merge4;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isSymbol.js
var require_isSymbol = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isSymbol.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var symbolTag = "[object Symbol]";
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
    }
    module.exports = isSymbol;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_isKey.js
var require_isKey = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_isKey.js"(exports, module) {
    var isArray = require_isArray();
    var isSymbol = require_isSymbol();
    var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
    var reIsPlainProp = /^\w*$/;
    function isKey(value, object) {
      if (isArray(value)) {
        return false;
      }
      var type = typeof value;
      if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
        return true;
      }
      return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
    }
    module.exports = isKey;
  }
});

// node_modules/@nivo/core/node_modules/lodash/memoize.js
var require_memoize = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/memoize.js"(exports, module) {
    var MapCache = require_MapCache();
    var FUNC_ERROR_TEXT = "Expected a function";
    function memoize(func, resolver) {
      if (typeof func != "function" || resolver != null && typeof resolver != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      var memoized = function() {
        var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
        if (cache.has(key)) {
          return cache.get(key);
        }
        var result = func.apply(this, args);
        memoized.cache = cache.set(key, result) || cache;
        return result;
      };
      memoized.cache = new (memoize.Cache || MapCache)();
      return memoized;
    }
    memoize.Cache = MapCache;
    module.exports = memoize;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_memoizeCapped.js
var require_memoizeCapped = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_memoizeCapped.js"(exports, module) {
    var memoize = require_memoize();
    var MAX_MEMOIZE_SIZE = 500;
    function memoizeCapped(func) {
      var result = memoize(func, function(key) {
        if (cache.size === MAX_MEMOIZE_SIZE) {
          cache.clear();
        }
        return key;
      });
      var cache = result.cache;
      return result;
    }
    module.exports = memoizeCapped;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_stringToPath.js
var require_stringToPath = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_stringToPath.js"(exports, module) {
    var memoizeCapped = require_memoizeCapped();
    var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
    var reEscapeChar = /\\(\\)?/g;
    var stringToPath = memoizeCapped(function(string) {
      var result = [];
      if (string.charCodeAt(0) === 46) {
        result.push("");
      }
      string.replace(rePropName, function(match, number5, quote, subString) {
        result.push(quote ? subString.replace(reEscapeChar, "$1") : number5 || match);
      });
      return result;
    });
    module.exports = stringToPath;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_arrayMap.js
var require_arrayMap = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_arrayMap.js"(exports, module) {
    function arrayMap(array4, iteratee) {
      var index4 = -1, length = array4 == null ? 0 : array4.length, result = Array(length);
      while (++index4 < length) {
        result[index4] = iteratee(array4[index4], index4, array4);
      }
      return result;
    }
    module.exports = arrayMap;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseToString.js
var require_baseToString = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseToString.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var arrayMap = require_arrayMap();
    var isArray = require_isArray();
    var isSymbol = require_isSymbol();
    var INFINITY = 1 / 0;
    var symbolProto = Symbol3 ? Symbol3.prototype : void 0;
    var symbolToString = symbolProto ? symbolProto.toString : void 0;
    function baseToString(value) {
      if (typeof value == "string") {
        return value;
      }
      if (isArray(value)) {
        return arrayMap(value, baseToString) + "";
      }
      if (isSymbol(value)) {
        return symbolToString ? symbolToString.call(value) : "";
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    module.exports = baseToString;
  }
});

// node_modules/@nivo/core/node_modules/lodash/toString.js
var require_toString = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/toString.js"(exports, module) {
    var baseToString = require_baseToString();
    function toString(value) {
      return value == null ? "" : baseToString(value);
    }
    module.exports = toString;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_castPath.js
var require_castPath = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_castPath.js"(exports, module) {
    var isArray = require_isArray();
    var isKey = require_isKey();
    var stringToPath = require_stringToPath();
    var toString = require_toString();
    function castPath(value, object) {
      if (isArray(value)) {
        return value;
      }
      return isKey(value, object) ? [value] : stringToPath(toString(value));
    }
    module.exports = castPath;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_toKey.js
var require_toKey = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_toKey.js"(exports, module) {
    var isSymbol = require_isSymbol();
    var INFINITY = 1 / 0;
    function toKey(value) {
      if (typeof value == "string" || isSymbol(value)) {
        return value;
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    module.exports = toKey;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseGet.js
var require_baseGet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseGet.js"(exports, module) {
    var castPath = require_castPath();
    var toKey = require_toKey();
    function baseGet(object, path2) {
      path2 = castPath(path2, object);
      var index4 = 0, length = path2.length;
      while (object != null && index4 < length) {
        object = object[toKey(path2[index4++])];
      }
      return index4 && index4 == length ? object : void 0;
    }
    module.exports = baseGet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/get.js
var require_get = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/get.js"(exports, module) {
    var baseGet = require_baseGet();
    function get(object, path2, defaultValue) {
      var result = object == null ? void 0 : baseGet(object, path2);
      return result === void 0 ? defaultValue : result;
    }
    module.exports = get;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseSet.js
var require_baseSet = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseSet.js"(exports, module) {
    var assignValue = require_assignValue();
    var castPath = require_castPath();
    var isIndex = require_isIndex();
    var isObject = require_isObject();
    var toKey = require_toKey();
    function baseSet(object, path2, value, customizer) {
      if (!isObject(object)) {
        return object;
      }
      path2 = castPath(path2, object);
      var index4 = -1, length = path2.length, lastIndex = length - 1, nested = object;
      while (nested != null && ++index4 < length) {
        var key = toKey(path2[index4]), newValue = value;
        if (key === "__proto__" || key === "constructor" || key === "prototype") {
          return object;
        }
        if (index4 != lastIndex) {
          var objValue = nested[key];
          newValue = customizer ? customizer(objValue, key, nested) : void 0;
          if (newValue === void 0) {
            newValue = isObject(objValue) ? objValue : isIndex(path2[index4 + 1]) ? [] : {};
          }
        }
        assignValue(nested, key, newValue);
        nested = nested[key];
      }
      return object;
    }
    module.exports = baseSet;
  }
});

// node_modules/@nivo/core/node_modules/lodash/set.js
var require_set = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/set.js"(exports, module) {
    var baseSet = require_baseSet();
    function set2(object, path2, value) {
      return object == null ? object : baseSet(object, path2, value);
    }
    module.exports = set2;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isString.js
var require_isString = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isString.js"(exports, module) {
    var baseGetTag = require_baseGetTag();
    var isArray = require_isArray();
    var isObjectLike = require_isObjectLike();
    var stringTag = "[object String]";
    function isString(value) {
      return typeof value == "string" || !isArray(value) && isObjectLike(value) && baseGetTag(value) == stringTag;
    }
    module.exports = isString;
  }
});

// node_modules/@nivo/core/node_modules/lodash/last.js
var require_last = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/last.js"(exports, module) {
    function last(array4) {
      var length = array4 == null ? 0 : array4.length;
      return length ? array4[length - 1] : void 0;
    }
    module.exports = last;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_setCacheAdd.js
var require_setCacheAdd = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_setCacheAdd.js"(exports, module) {
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    function setCacheAdd(value) {
      this.__data__.set(value, HASH_UNDEFINED);
      return this;
    }
    module.exports = setCacheAdd;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_setCacheHas.js
var require_setCacheHas = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_setCacheHas.js"(exports, module) {
    function setCacheHas(value) {
      return this.__data__.has(value);
    }
    module.exports = setCacheHas;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_SetCache.js
var require_SetCache = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_SetCache.js"(exports, module) {
    var MapCache = require_MapCache();
    var setCacheAdd = require_setCacheAdd();
    var setCacheHas = require_setCacheHas();
    function SetCache(values) {
      var index4 = -1, length = values == null ? 0 : values.length;
      this.__data__ = new MapCache();
      while (++index4 < length) {
        this.add(values[index4]);
      }
    }
    SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
    SetCache.prototype.has = setCacheHas;
    module.exports = SetCache;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseFindIndex.js
var require_baseFindIndex = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseFindIndex.js"(exports, module) {
    function baseFindIndex(array4, predicate, fromIndex, fromRight) {
      var length = array4.length, index4 = fromIndex + (fromRight ? 1 : -1);
      while (fromRight ? index4-- : ++index4 < length) {
        if (predicate(array4[index4], index4, array4)) {
          return index4;
        }
      }
      return -1;
    }
    module.exports = baseFindIndex;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseIsNaN.js
var require_baseIsNaN = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseIsNaN.js"(exports, module) {
    function baseIsNaN(value) {
      return value !== value;
    }
    module.exports = baseIsNaN;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_strictIndexOf.js
var require_strictIndexOf = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_strictIndexOf.js"(exports, module) {
    function strictIndexOf(array4, value, fromIndex) {
      var index4 = fromIndex - 1, length = array4.length;
      while (++index4 < length) {
        if (array4[index4] === value) {
          return index4;
        }
      }
      return -1;
    }
    module.exports = strictIndexOf;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseIndexOf.js
var require_baseIndexOf = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseIndexOf.js"(exports, module) {
    var baseFindIndex = require_baseFindIndex();
    var baseIsNaN = require_baseIsNaN();
    var strictIndexOf = require_strictIndexOf();
    function baseIndexOf(array4, value, fromIndex) {
      return value === value ? strictIndexOf(array4, value, fromIndex) : baseFindIndex(array4, baseIsNaN, fromIndex);
    }
    module.exports = baseIndexOf;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_arrayIncludes.js
var require_arrayIncludes = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_arrayIncludes.js"(exports, module) {
    var baseIndexOf = require_baseIndexOf();
    function arrayIncludes(array4, value) {
      var length = array4 == null ? 0 : array4.length;
      return !!length && baseIndexOf(array4, value, 0) > -1;
    }
    module.exports = arrayIncludes;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_arrayIncludesWith.js
var require_arrayIncludesWith = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_arrayIncludesWith.js"(exports, module) {
    function arrayIncludesWith(array4, value, comparator) {
      var index4 = -1, length = array4 == null ? 0 : array4.length;
      while (++index4 < length) {
        if (comparator(value, array4[index4])) {
          return true;
        }
      }
      return false;
    }
    module.exports = arrayIncludesWith;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_cacheHas.js
var require_cacheHas = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_cacheHas.js"(exports, module) {
    function cacheHas(cache, key) {
      return cache.has(key);
    }
    module.exports = cacheHas;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseDifference.js
var require_baseDifference = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseDifference.js"(exports, module) {
    var SetCache = require_SetCache();
    var arrayIncludes = require_arrayIncludes();
    var arrayIncludesWith = require_arrayIncludesWith();
    var arrayMap = require_arrayMap();
    var baseUnary = require_baseUnary();
    var cacheHas = require_cacheHas();
    var LARGE_ARRAY_SIZE = 200;
    function baseDifference(array4, values, iteratee, comparator) {
      var index4 = -1, includes = arrayIncludes, isCommon = true, length = array4.length, result = [], valuesLength = values.length;
      if (!length) {
        return result;
      }
      if (iteratee) {
        values = arrayMap(values, baseUnary(iteratee));
      }
      if (comparator) {
        includes = arrayIncludesWith;
        isCommon = false;
      } else if (values.length >= LARGE_ARRAY_SIZE) {
        includes = cacheHas;
        isCommon = false;
        values = new SetCache(values);
      }
      outer:
        while (++index4 < length) {
          var value = array4[index4], computed = iteratee == null ? value : iteratee(value);
          value = comparator || value !== 0 ? value : 0;
          if (isCommon && computed === computed) {
            var valuesIndex = valuesLength;
            while (valuesIndex--) {
              if (values[valuesIndex] === computed) {
                continue outer;
              }
            }
            result.push(value);
          } else if (!includes(values, computed, comparator)) {
            result.push(value);
          }
        }
      return result;
    }
    module.exports = baseDifference;
  }
});

// node_modules/@nivo/core/node_modules/lodash/without.js
var require_without = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/without.js"(exports, module) {
    var baseDifference = require_baseDifference();
    var baseRest = require_baseRest();
    var isArrayLikeObject = require_isArrayLikeObject();
    var without = baseRest(function(array4, values) {
      return isArrayLikeObject(array4) ? baseDifference(array4, values) : [];
    });
    module.exports = without;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_basePickBy.js
var require_basePickBy = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_basePickBy.js"(exports, module) {
    var baseGet = require_baseGet();
    var baseSet = require_baseSet();
    var castPath = require_castPath();
    function basePickBy(object, paths, predicate) {
      var index4 = -1, length = paths.length, result = {};
      while (++index4 < length) {
        var path2 = paths[index4], value = baseGet(object, path2);
        if (predicate(value, path2)) {
          baseSet(result, castPath(path2, object), value);
        }
      }
      return result;
    }
    module.exports = basePickBy;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseHasIn.js
var require_baseHasIn = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseHasIn.js"(exports, module) {
    function baseHasIn(object, key) {
      return object != null && key in Object(object);
    }
    module.exports = baseHasIn;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_hasPath.js
var require_hasPath = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_hasPath.js"(exports, module) {
    var castPath = require_castPath();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var isIndex = require_isIndex();
    var isLength = require_isLength();
    var toKey = require_toKey();
    function hasPath(object, path2, hasFunc) {
      path2 = castPath(path2, object);
      var index4 = -1, length = path2.length, result = false;
      while (++index4 < length) {
        var key = toKey(path2[index4]);
        if (!(result = object != null && hasFunc(object, key))) {
          break;
        }
        object = object[key];
      }
      if (result || ++index4 != length) {
        return result;
      }
      length = object == null ? 0 : object.length;
      return !!length && isLength(length) && isIndex(key, length) && (isArray(object) || isArguments(object));
    }
    module.exports = hasPath;
  }
});

// node_modules/@nivo/core/node_modules/lodash/hasIn.js
var require_hasIn = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/hasIn.js"(exports, module) {
    var baseHasIn = require_baseHasIn();
    var hasPath = require_hasPath();
    function hasIn(object, path2) {
      return object != null && hasPath(object, path2, baseHasIn);
    }
    module.exports = hasIn;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_basePick.js
var require_basePick = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_basePick.js"(exports, module) {
    var basePickBy = require_basePickBy();
    var hasIn = require_hasIn();
    function basePick(object, paths) {
      return basePickBy(object, paths, function(value, path2) {
        return hasIn(object, path2);
      });
    }
    module.exports = basePick;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_arrayPush.js
var require_arrayPush = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_arrayPush.js"(exports, module) {
    function arrayPush(array4, values) {
      var index4 = -1, length = values.length, offset = array4.length;
      while (++index4 < length) {
        array4[offset + index4] = values[index4];
      }
      return array4;
    }
    module.exports = arrayPush;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_isFlattenable.js
var require_isFlattenable = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_isFlattenable.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var isArguments = require_isArguments();
    var isArray = require_isArray();
    var spreadableSymbol = Symbol3 ? Symbol3.isConcatSpreadable : void 0;
    function isFlattenable(value) {
      return isArray(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
    }
    module.exports = isFlattenable;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseFlatten.js
var require_baseFlatten = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseFlatten.js"(exports, module) {
    var arrayPush = require_arrayPush();
    var isFlattenable = require_isFlattenable();
    function baseFlatten(array4, depth, predicate, isStrict, result) {
      var index4 = -1, length = array4.length;
      predicate || (predicate = isFlattenable);
      result || (result = []);
      while (++index4 < length) {
        var value = array4[index4];
        if (depth > 0 && predicate(value)) {
          if (depth > 1) {
            baseFlatten(value, depth - 1, predicate, isStrict, result);
          } else {
            arrayPush(result, value);
          }
        } else if (!isStrict) {
          result[result.length] = value;
        }
      }
      return result;
    }
    module.exports = baseFlatten;
  }
});

// node_modules/@nivo/core/node_modules/lodash/flatten.js
var require_flatten = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/flatten.js"(exports, module) {
    var baseFlatten = require_baseFlatten();
    function flatten(array4) {
      var length = array4 == null ? 0 : array4.length;
      return length ? baseFlatten(array4, 1) : [];
    }
    module.exports = flatten;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_flatRest.js
var require_flatRest = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_flatRest.js"(exports, module) {
    var flatten = require_flatten();
    var overRest = require_overRest();
    var setToString = require_setToString();
    function flatRest(func) {
      return setToString(overRest(func, void 0, flatten), func + "");
    }
    module.exports = flatRest;
  }
});

// node_modules/@nivo/core/node_modules/lodash/pick.js
var require_pick = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/pick.js"(exports, module) {
    var basePick = require_basePick();
    var flatRest = require_flatRest();
    var pick = flatRest(function(object, paths) {
      return object == null ? {} : basePick(object, paths);
    });
    module.exports = pick;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_arraySome.js
var require_arraySome = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_arraySome.js"(exports, module) {
    function arraySome(array4, predicate) {
      var index4 = -1, length = array4 == null ? 0 : array4.length;
      while (++index4 < length) {
        if (predicate(array4[index4], index4, array4)) {
          return true;
        }
      }
      return false;
    }
    module.exports = arraySome;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_equalArrays.js
var require_equalArrays = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_equalArrays.js"(exports, module) {
    var SetCache = require_SetCache();
    var arraySome = require_arraySome();
    var cacheHas = require_cacheHas();
    var COMPARE_PARTIAL_FLAG = 1;
    var COMPARE_UNORDERED_FLAG = 2;
    function equalArrays(array4, other, bitmask, customizer, equalFunc, stack) {
      var isPartial = bitmask & COMPARE_PARTIAL_FLAG, arrLength = array4.length, othLength = other.length;
      if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
        return false;
      }
      var arrStacked = stack.get(array4);
      var othStacked = stack.get(other);
      if (arrStacked && othStacked) {
        return arrStacked == other && othStacked == array4;
      }
      var index4 = -1, result = true, seen = bitmask & COMPARE_UNORDERED_FLAG ? new SetCache() : void 0;
      stack.set(array4, other);
      stack.set(other, array4);
      while (++index4 < arrLength) {
        var arrValue = array4[index4], othValue = other[index4];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, arrValue, index4, other, array4, stack) : customizer(arrValue, othValue, index4, array4, other, stack);
        }
        if (compared !== void 0) {
          if (compared) {
            continue;
          }
          result = false;
          break;
        }
        if (seen) {
          if (!arraySome(other, function(othValue2, othIndex) {
            if (!cacheHas(seen, othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, bitmask, customizer, stack))) {
              return seen.push(othIndex);
            }
          })) {
            result = false;
            break;
          }
        } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
          result = false;
          break;
        }
      }
      stack["delete"](array4);
      stack["delete"](other);
      return result;
    }
    module.exports = equalArrays;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_mapToArray.js
var require_mapToArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_mapToArray.js"(exports, module) {
    function mapToArray(map9) {
      var index4 = -1, result = Array(map9.size);
      map9.forEach(function(value, key) {
        result[++index4] = [key, value];
      });
      return result;
    }
    module.exports = mapToArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_setToArray.js
var require_setToArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_setToArray.js"(exports, module) {
    function setToArray(set2) {
      var index4 = -1, result = Array(set2.size);
      set2.forEach(function(value) {
        result[++index4] = value;
      });
      return result;
    }
    module.exports = setToArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_equalByTag.js
var require_equalByTag = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_equalByTag.js"(exports, module) {
    var Symbol3 = require_Symbol();
    var Uint8Array = require_Uint8Array();
    var eq = require_eq();
    var equalArrays = require_equalArrays();
    var mapToArray = require_mapToArray();
    var setToArray = require_setToArray();
    var COMPARE_PARTIAL_FLAG = 1;
    var COMPARE_UNORDERED_FLAG = 2;
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var symbolTag = "[object Symbol]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var symbolProto = Symbol3 ? Symbol3.prototype : void 0;
    var symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
    function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
      switch (tag) {
        case dataViewTag:
          if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
            return false;
          }
          object = object.buffer;
          other = other.buffer;
        case arrayBufferTag:
          if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array(object), new Uint8Array(other))) {
            return false;
          }
          return true;
        case boolTag:
        case dateTag:
        case numberTag:
          return eq(+object, +other);
        case errorTag:
          return object.name == other.name && object.message == other.message;
        case regexpTag:
        case stringTag:
          return object == other + "";
        case mapTag:
          var convert = mapToArray;
        case setTag:
          var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
          convert || (convert = setToArray);
          if (object.size != other.size && !isPartial) {
            return false;
          }
          var stacked = stack.get(object);
          if (stacked) {
            return stacked == other;
          }
          bitmask |= COMPARE_UNORDERED_FLAG;
          stack.set(object, other);
          var result = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
          stack["delete"](object);
          return result;
        case symbolTag:
          if (symbolValueOf) {
            return symbolValueOf.call(object) == symbolValueOf.call(other);
          }
      }
      return false;
    }
    module.exports = equalByTag;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseGetAllKeys.js
var require_baseGetAllKeys = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseGetAllKeys.js"(exports, module) {
    var arrayPush = require_arrayPush();
    var isArray = require_isArray();
    function baseGetAllKeys(object, keysFunc, symbolsFunc) {
      var result = keysFunc(object);
      return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
    }
    module.exports = baseGetAllKeys;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_arrayFilter.js
var require_arrayFilter = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_arrayFilter.js"(exports, module) {
    function arrayFilter(array4, predicate) {
      var index4 = -1, length = array4 == null ? 0 : array4.length, resIndex = 0, result = [];
      while (++index4 < length) {
        var value = array4[index4];
        if (predicate(value, index4, array4)) {
          result[resIndex++] = value;
        }
      }
      return result;
    }
    module.exports = arrayFilter;
  }
});

// node_modules/@nivo/core/node_modules/lodash/stubArray.js
var require_stubArray = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/stubArray.js"(exports, module) {
    function stubArray() {
      return [];
    }
    module.exports = stubArray;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getSymbols.js
var require_getSymbols = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getSymbols.js"(exports, module) {
    var arrayFilter = require_arrayFilter();
    var stubArray = require_stubArray();
    var objectProto = Object.prototype;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var nativeGetSymbols = Object.getOwnPropertySymbols;
    var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
      if (object == null) {
        return [];
      }
      object = Object(object);
      return arrayFilter(nativeGetSymbols(object), function(symbol) {
        return propertyIsEnumerable.call(object, symbol);
      });
    };
    module.exports = getSymbols;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_nativeKeys.js
var require_nativeKeys = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_nativeKeys.js"(exports, module) {
    var overArg = require_overArg();
    var nativeKeys = overArg(Object.keys, Object);
    module.exports = nativeKeys;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseKeys.js
var require_baseKeys = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseKeys.js"(exports, module) {
    var isPrototype = require_isPrototype();
    var nativeKeys = require_nativeKeys();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseKeys(object) {
      if (!isPrototype(object)) {
        return nativeKeys(object);
      }
      var result = [];
      for (var key in Object(object)) {
        if (hasOwnProperty.call(object, key) && key != "constructor") {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = baseKeys;
  }
});

// node_modules/@nivo/core/node_modules/lodash/keys.js
var require_keys = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/keys.js"(exports, module) {
    var arrayLikeKeys = require_arrayLikeKeys();
    var baseKeys = require_baseKeys();
    var isArrayLike = require_isArrayLike();
    function keys(object) {
      return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
    }
    module.exports = keys;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getAllKeys.js
var require_getAllKeys = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getAllKeys.js"(exports, module) {
    var baseGetAllKeys = require_baseGetAllKeys();
    var getSymbols = require_getSymbols();
    var keys = require_keys();
    function getAllKeys(object) {
      return baseGetAllKeys(object, keys, getSymbols);
    }
    module.exports = getAllKeys;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_equalObjects.js
var require_equalObjects = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_equalObjects.js"(exports, module) {
    var getAllKeys = require_getAllKeys();
    var COMPARE_PARTIAL_FLAG = 1;
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
      var isPartial = bitmask & COMPARE_PARTIAL_FLAG, objProps = getAllKeys(object), objLength = objProps.length, othProps = getAllKeys(other), othLength = othProps.length;
      if (objLength != othLength && !isPartial) {
        return false;
      }
      var index4 = objLength;
      while (index4--) {
        var key = objProps[index4];
        if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
          return false;
        }
      }
      var objStacked = stack.get(object);
      var othStacked = stack.get(other);
      if (objStacked && othStacked) {
        return objStacked == other && othStacked == object;
      }
      var result = true;
      stack.set(object, other);
      stack.set(other, object);
      var skipCtor = isPartial;
      while (++index4 < objLength) {
        key = objProps[index4];
        var objValue = object[key], othValue = other[key];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
        }
        if (!(compared === void 0 ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
          result = false;
          break;
        }
        skipCtor || (skipCtor = key == "constructor");
      }
      if (result && !skipCtor) {
        var objCtor = object.constructor, othCtor = other.constructor;
        if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
          result = false;
        }
      }
      stack["delete"](object);
      stack["delete"](other);
      return result;
    }
    module.exports = equalObjects;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_DataView.js
var require_DataView = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_DataView.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var DataView2 = getNative(root, "DataView");
    module.exports = DataView2;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_Promise.js
var require_Promise = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_Promise.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var Promise2 = getNative(root, "Promise");
    module.exports = Promise2;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_Set.js
var require_Set = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_Set.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var Set2 = getNative(root, "Set");
    module.exports = Set2;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_WeakMap.js
var require_WeakMap = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_WeakMap.js"(exports, module) {
    var getNative = require_getNative();
    var root = require_root();
    var WeakMap2 = getNative(root, "WeakMap");
    module.exports = WeakMap2;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_getTag.js
var require_getTag = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_getTag.js"(exports, module) {
    var DataView2 = require_DataView();
    var Map2 = require_Map();
    var Promise2 = require_Promise();
    var Set2 = require_Set();
    var WeakMap2 = require_WeakMap();
    var baseGetTag = require_baseGetTag();
    var toSource = require_toSource();
    var mapTag = "[object Map]";
    var objectTag = "[object Object]";
    var promiseTag = "[object Promise]";
    var setTag = "[object Set]";
    var weakMapTag = "[object WeakMap]";
    var dataViewTag = "[object DataView]";
    var dataViewCtorString = toSource(DataView2);
    var mapCtorString = toSource(Map2);
    var promiseCtorString = toSource(Promise2);
    var setCtorString = toSource(Set2);
    var weakMapCtorString = toSource(WeakMap2);
    var getTag = baseGetTag;
    if (DataView2 && getTag(new DataView2(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap2 && getTag(new WeakMap2()) != weakMapTag) {
      getTag = function(value) {
        var result = baseGetTag(value), Ctor = result == objectTag ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : "";
        if (ctorString) {
          switch (ctorString) {
            case dataViewCtorString:
              return dataViewTag;
            case mapCtorString:
              return mapTag;
            case promiseCtorString:
              return promiseTag;
            case setCtorString:
              return setTag;
            case weakMapCtorString:
              return weakMapTag;
          }
        }
        return result;
      };
    }
    module.exports = getTag;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseIsEqualDeep.js
var require_baseIsEqualDeep = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseIsEqualDeep.js"(exports, module) {
    var Stack = require_Stack();
    var equalArrays = require_equalArrays();
    var equalByTag = require_equalByTag();
    var equalObjects = require_equalObjects();
    var getTag = require_getTag();
    var isArray = require_isArray();
    var isBuffer = require_isBuffer();
    var isTypedArray = require_isTypedArray();
    var COMPARE_PARTIAL_FLAG = 1;
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var objectTag = "[object Object]";
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
      var objIsArr = isArray(object), othIsArr = isArray(other), objTag = objIsArr ? arrayTag : getTag(object), othTag = othIsArr ? arrayTag : getTag(other);
      objTag = objTag == argsTag ? objectTag : objTag;
      othTag = othTag == argsTag ? objectTag : othTag;
      var objIsObj = objTag == objectTag, othIsObj = othTag == objectTag, isSameTag = objTag == othTag;
      if (isSameTag && isBuffer(object)) {
        if (!isBuffer(other)) {
          return false;
        }
        objIsArr = true;
        objIsObj = false;
      }
      if (isSameTag && !objIsObj) {
        stack || (stack = new Stack());
        return objIsArr || isTypedArray(object) ? equalArrays(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
      }
      if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
        var objIsWrapped = objIsObj && hasOwnProperty.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty.call(other, "__wrapped__");
        if (objIsWrapped || othIsWrapped) {
          var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
          stack || (stack = new Stack());
          return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
        }
      }
      if (!isSameTag) {
        return false;
      }
      stack || (stack = new Stack());
      return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
    }
    module.exports = baseIsEqualDeep;
  }
});

// node_modules/@nivo/core/node_modules/lodash/_baseIsEqual.js
var require_baseIsEqual = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/_baseIsEqual.js"(exports, module) {
    var baseIsEqualDeep = require_baseIsEqualDeep();
    var isObjectLike = require_isObjectLike();
    function baseIsEqual(value, other, bitmask, customizer, stack) {
      if (value === other) {
        return true;
      }
      if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) {
        return value !== value && other !== other;
      }
      return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
    }
    module.exports = baseIsEqual;
  }
});

// node_modules/@nivo/core/node_modules/lodash/isEqual.js
var require_isEqual = __commonJS({
  "node_modules/@nivo/core/node_modules/lodash/isEqual.js"(exports, module) {
    var baseIsEqual = require_baseIsEqual();
    function isEqual3(value, other) {
      return baseIsEqual(value, other);
    }
    module.exports = isEqual3;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_arrayMap.js
var require_arrayMap2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_arrayMap.js"(exports, module) {
    function arrayMap(array4, iteratee) {
      var index4 = -1, length = array4 == null ? 0 : array4.length, result = Array(length);
      while (++index4 < length) {
        result[index4] = iteratee(array4[index4], index4, array4);
      }
      return result;
    }
    module.exports = arrayMap;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_listCacheClear.js
var require_listCacheClear2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_listCacheClear.js"(exports, module) {
    function listCacheClear() {
      this.__data__ = [];
      this.size = 0;
    }
    module.exports = listCacheClear;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/eq.js
var require_eq2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/eq.js"(exports, module) {
    function eq(value, other) {
      return value === other || value !== value && other !== other;
    }
    module.exports = eq;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_assocIndexOf.js
var require_assocIndexOf2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_assocIndexOf.js"(exports, module) {
    var eq = require_eq2();
    function assocIndexOf(array4, key) {
      var length = array4.length;
      while (length--) {
        if (eq(array4[length][0], key)) {
          return length;
        }
      }
      return -1;
    }
    module.exports = assocIndexOf;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_listCacheDelete.js
var require_listCacheDelete2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_listCacheDelete.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf2();
    var arrayProto = Array.prototype;
    var splice = arrayProto.splice;
    function listCacheDelete(key) {
      var data = this.__data__, index4 = assocIndexOf(data, key);
      if (index4 < 0) {
        return false;
      }
      var lastIndex = data.length - 1;
      if (index4 == lastIndex) {
        data.pop();
      } else {
        splice.call(data, index4, 1);
      }
      --this.size;
      return true;
    }
    module.exports = listCacheDelete;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_listCacheGet.js
var require_listCacheGet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_listCacheGet.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf2();
    function listCacheGet(key) {
      var data = this.__data__, index4 = assocIndexOf(data, key);
      return index4 < 0 ? void 0 : data[index4][1];
    }
    module.exports = listCacheGet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_listCacheHas.js
var require_listCacheHas2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_listCacheHas.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf2();
    function listCacheHas(key) {
      return assocIndexOf(this.__data__, key) > -1;
    }
    module.exports = listCacheHas;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_listCacheSet.js
var require_listCacheSet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_listCacheSet.js"(exports, module) {
    var assocIndexOf = require_assocIndexOf2();
    function listCacheSet(key, value) {
      var data = this.__data__, index4 = assocIndexOf(data, key);
      if (index4 < 0) {
        ++this.size;
        data.push([key, value]);
      } else {
        data[index4][1] = value;
      }
      return this;
    }
    module.exports = listCacheSet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_ListCache.js
var require_ListCache2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_ListCache.js"(exports, module) {
    var listCacheClear = require_listCacheClear2();
    var listCacheDelete = require_listCacheDelete2();
    var listCacheGet = require_listCacheGet2();
    var listCacheHas = require_listCacheHas2();
    var listCacheSet = require_listCacheSet2();
    function ListCache(entries) {
      var index4 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index4 < length) {
        var entry = entries[index4];
        this.set(entry[0], entry[1]);
      }
    }
    ListCache.prototype.clear = listCacheClear;
    ListCache.prototype["delete"] = listCacheDelete;
    ListCache.prototype.get = listCacheGet;
    ListCache.prototype.has = listCacheHas;
    ListCache.prototype.set = listCacheSet;
    module.exports = ListCache;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_stackClear.js
var require_stackClear2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_stackClear.js"(exports, module) {
    var ListCache = require_ListCache2();
    function stackClear() {
      this.__data__ = new ListCache();
      this.size = 0;
    }
    module.exports = stackClear;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_stackDelete.js
var require_stackDelete2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_stackDelete.js"(exports, module) {
    function stackDelete(key) {
      var data = this.__data__, result = data["delete"](key);
      this.size = data.size;
      return result;
    }
    module.exports = stackDelete;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_stackGet.js
var require_stackGet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_stackGet.js"(exports, module) {
    function stackGet(key) {
      return this.__data__.get(key);
    }
    module.exports = stackGet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_stackHas.js
var require_stackHas2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_stackHas.js"(exports, module) {
    function stackHas(key) {
      return this.__data__.has(key);
    }
    module.exports = stackHas;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_freeGlobal.js
var require_freeGlobal2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_freeGlobal.js"(exports, module) {
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    module.exports = freeGlobal;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_root.js
var require_root2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_root.js"(exports, module) {
    var freeGlobal = require_freeGlobal2();
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root = freeGlobal || freeSelf || Function("return this")();
    module.exports = root;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_Symbol.js
var require_Symbol2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_Symbol.js"(exports, module) {
    var root = require_root2();
    var Symbol3 = root.Symbol;
    module.exports = Symbol3;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getRawTag.js
var require_getRawTag2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getRawTag.js"(exports, module) {
    var Symbol3 = require_Symbol2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var nativeObjectToString = objectProto.toString;
    var symToStringTag = Symbol3 ? Symbol3.toStringTag : void 0;
    function getRawTag(value) {
      var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
      try {
        value[symToStringTag] = void 0;
        var unmasked = true;
      } catch (e7) {
      }
      var result = nativeObjectToString.call(value);
      if (unmasked) {
        if (isOwn) {
          value[symToStringTag] = tag;
        } else {
          delete value[symToStringTag];
        }
      }
      return result;
    }
    module.exports = getRawTag;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_objectToString.js
var require_objectToString2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_objectToString.js"(exports, module) {
    var objectProto = Object.prototype;
    var nativeObjectToString = objectProto.toString;
    function objectToString(value) {
      return nativeObjectToString.call(value);
    }
    module.exports = objectToString;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseGetTag.js
var require_baseGetTag2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseGetTag.js"(exports, module) {
    var Symbol3 = require_Symbol2();
    var getRawTag = require_getRawTag2();
    var objectToString = require_objectToString2();
    var nullTag = "[object Null]";
    var undefinedTag = "[object Undefined]";
    var symToStringTag = Symbol3 ? Symbol3.toStringTag : void 0;
    function baseGetTag(value) {
      if (value == null) {
        return value === void 0 ? undefinedTag : nullTag;
      }
      return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
    }
    module.exports = baseGetTag;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isObject.js
var require_isObject2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isObject.js"(exports, module) {
    function isObject(value) {
      var type = typeof value;
      return value != null && (type == "object" || type == "function");
    }
    module.exports = isObject;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isFunction.js
var require_isFunction2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isFunction.js"(exports, module) {
    var baseGetTag = require_baseGetTag2();
    var isObject = require_isObject2();
    var asyncTag = "[object AsyncFunction]";
    var funcTag = "[object Function]";
    var genTag = "[object GeneratorFunction]";
    var proxyTag = "[object Proxy]";
    function isFunction(value) {
      if (!isObject(value)) {
        return false;
      }
      var tag = baseGetTag(value);
      return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
    }
    module.exports = isFunction;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_coreJsData.js
var require_coreJsData2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_coreJsData.js"(exports, module) {
    var root = require_root2();
    var coreJsData = root["__core-js_shared__"];
    module.exports = coreJsData;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_isMasked.js
var require_isMasked2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_isMasked.js"(exports, module) {
    var coreJsData = require_coreJsData2();
    var maskSrcKey = function() {
      var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
      return uid ? "Symbol(src)_1." + uid : "";
    }();
    function isMasked(func) {
      return !!maskSrcKey && maskSrcKey in func;
    }
    module.exports = isMasked;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_toSource.js
var require_toSource2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_toSource.js"(exports, module) {
    var funcProto = Function.prototype;
    var funcToString = funcProto.toString;
    function toSource(func) {
      if (func != null) {
        try {
          return funcToString.call(func);
        } catch (e7) {
        }
        try {
          return func + "";
        } catch (e7) {
        }
      }
      return "";
    }
    module.exports = toSource;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseIsNative.js
var require_baseIsNative2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseIsNative.js"(exports, module) {
    var isFunction = require_isFunction2();
    var isMasked = require_isMasked2();
    var isObject = require_isObject2();
    var toSource = require_toSource2();
    var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
    var reIsHostCtor = /^\[object .+?Constructor\]$/;
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var reIsNative = RegExp(
      "^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
    );
    function baseIsNative(value) {
      if (!isObject(value) || isMasked(value)) {
        return false;
      }
      var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
      return pattern.test(toSource(value));
    }
    module.exports = baseIsNative;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getValue.js
var require_getValue2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getValue.js"(exports, module) {
    function getValue(object, key) {
      return object == null ? void 0 : object[key];
    }
    module.exports = getValue;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getNative.js
var require_getNative2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getNative.js"(exports, module) {
    var baseIsNative = require_baseIsNative2();
    var getValue = require_getValue2();
    function getNative(object, key) {
      var value = getValue(object, key);
      return baseIsNative(value) ? value : void 0;
    }
    module.exports = getNative;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_Map.js
var require_Map2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_Map.js"(exports, module) {
    var getNative = require_getNative2();
    var root = require_root2();
    var Map2 = getNative(root, "Map");
    module.exports = Map2;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_nativeCreate.js
var require_nativeCreate2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_nativeCreate.js"(exports, module) {
    var getNative = require_getNative2();
    var nativeCreate = getNative(Object, "create");
    module.exports = nativeCreate;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_hashClear.js
var require_hashClear2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_hashClear.js"(exports, module) {
    var nativeCreate = require_nativeCreate2();
    function hashClear() {
      this.__data__ = nativeCreate ? nativeCreate(null) : {};
      this.size = 0;
    }
    module.exports = hashClear;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_hashDelete.js
var require_hashDelete2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_hashDelete.js"(exports, module) {
    function hashDelete(key) {
      var result = this.has(key) && delete this.__data__[key];
      this.size -= result ? 1 : 0;
      return result;
    }
    module.exports = hashDelete;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_hashGet.js
var require_hashGet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_hashGet.js"(exports, module) {
    var nativeCreate = require_nativeCreate2();
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function hashGet(key) {
      var data = this.__data__;
      if (nativeCreate) {
        var result = data[key];
        return result === HASH_UNDEFINED ? void 0 : result;
      }
      return hasOwnProperty.call(data, key) ? data[key] : void 0;
    }
    module.exports = hashGet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_hashHas.js
var require_hashHas2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_hashHas.js"(exports, module) {
    var nativeCreate = require_nativeCreate2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function hashHas(key) {
      var data = this.__data__;
      return nativeCreate ? data[key] !== void 0 : hasOwnProperty.call(data, key);
    }
    module.exports = hashHas;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_hashSet.js
var require_hashSet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_hashSet.js"(exports, module) {
    var nativeCreate = require_nativeCreate2();
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    function hashSet(key, value) {
      var data = this.__data__;
      this.size += this.has(key) ? 0 : 1;
      data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
      return this;
    }
    module.exports = hashSet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_Hash.js
var require_Hash2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_Hash.js"(exports, module) {
    var hashClear = require_hashClear2();
    var hashDelete = require_hashDelete2();
    var hashGet = require_hashGet2();
    var hashHas = require_hashHas2();
    var hashSet = require_hashSet2();
    function Hash(entries) {
      var index4 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index4 < length) {
        var entry = entries[index4];
        this.set(entry[0], entry[1]);
      }
    }
    Hash.prototype.clear = hashClear;
    Hash.prototype["delete"] = hashDelete;
    Hash.prototype.get = hashGet;
    Hash.prototype.has = hashHas;
    Hash.prototype.set = hashSet;
    module.exports = Hash;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_mapCacheClear.js
var require_mapCacheClear2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_mapCacheClear.js"(exports, module) {
    var Hash = require_Hash2();
    var ListCache = require_ListCache2();
    var Map2 = require_Map2();
    function mapCacheClear() {
      this.size = 0;
      this.__data__ = {
        "hash": new Hash(),
        "map": new (Map2 || ListCache)(),
        "string": new Hash()
      };
    }
    module.exports = mapCacheClear;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_isKeyable.js
var require_isKeyable2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_isKeyable.js"(exports, module) {
    function isKeyable(value) {
      var type = typeof value;
      return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
    }
    module.exports = isKeyable;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getMapData.js
var require_getMapData2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getMapData.js"(exports, module) {
    var isKeyable = require_isKeyable2();
    function getMapData(map9, key) {
      var data = map9.__data__;
      return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
    }
    module.exports = getMapData;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_mapCacheDelete.js
var require_mapCacheDelete2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_mapCacheDelete.js"(exports, module) {
    var getMapData = require_getMapData2();
    function mapCacheDelete(key) {
      var result = getMapData(this, key)["delete"](key);
      this.size -= result ? 1 : 0;
      return result;
    }
    module.exports = mapCacheDelete;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_mapCacheGet.js
var require_mapCacheGet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_mapCacheGet.js"(exports, module) {
    var getMapData = require_getMapData2();
    function mapCacheGet(key) {
      return getMapData(this, key).get(key);
    }
    module.exports = mapCacheGet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_mapCacheHas.js
var require_mapCacheHas2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_mapCacheHas.js"(exports, module) {
    var getMapData = require_getMapData2();
    function mapCacheHas(key) {
      return getMapData(this, key).has(key);
    }
    module.exports = mapCacheHas;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_mapCacheSet.js
var require_mapCacheSet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_mapCacheSet.js"(exports, module) {
    var getMapData = require_getMapData2();
    function mapCacheSet(key, value) {
      var data = getMapData(this, key), size = data.size;
      data.set(key, value);
      this.size += data.size == size ? 0 : 1;
      return this;
    }
    module.exports = mapCacheSet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_MapCache.js
var require_MapCache2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_MapCache.js"(exports, module) {
    var mapCacheClear = require_mapCacheClear2();
    var mapCacheDelete = require_mapCacheDelete2();
    var mapCacheGet = require_mapCacheGet2();
    var mapCacheHas = require_mapCacheHas2();
    var mapCacheSet = require_mapCacheSet2();
    function MapCache(entries) {
      var index4 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index4 < length) {
        var entry = entries[index4];
        this.set(entry[0], entry[1]);
      }
    }
    MapCache.prototype.clear = mapCacheClear;
    MapCache.prototype["delete"] = mapCacheDelete;
    MapCache.prototype.get = mapCacheGet;
    MapCache.prototype.has = mapCacheHas;
    MapCache.prototype.set = mapCacheSet;
    module.exports = MapCache;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_stackSet.js
var require_stackSet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_stackSet.js"(exports, module) {
    var ListCache = require_ListCache2();
    var Map2 = require_Map2();
    var MapCache = require_MapCache2();
    var LARGE_ARRAY_SIZE = 200;
    function stackSet(key, value) {
      var data = this.__data__;
      if (data instanceof ListCache) {
        var pairs4 = data.__data__;
        if (!Map2 || pairs4.length < LARGE_ARRAY_SIZE - 1) {
          pairs4.push([key, value]);
          this.size = ++data.size;
          return this;
        }
        data = this.__data__ = new MapCache(pairs4);
      }
      data.set(key, value);
      this.size = data.size;
      return this;
    }
    module.exports = stackSet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_Stack.js
var require_Stack2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_Stack.js"(exports, module) {
    var ListCache = require_ListCache2();
    var stackClear = require_stackClear2();
    var stackDelete = require_stackDelete2();
    var stackGet = require_stackGet2();
    var stackHas = require_stackHas2();
    var stackSet = require_stackSet2();
    function Stack(entries) {
      var data = this.__data__ = new ListCache(entries);
      this.size = data.size;
    }
    Stack.prototype.clear = stackClear;
    Stack.prototype["delete"] = stackDelete;
    Stack.prototype.get = stackGet;
    Stack.prototype.has = stackHas;
    Stack.prototype.set = stackSet;
    module.exports = Stack;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_arrayEach.js
var require_arrayEach = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_arrayEach.js"(exports, module) {
    function arrayEach(array4, iteratee) {
      var index4 = -1, length = array4 == null ? 0 : array4.length;
      while (++index4 < length) {
        if (iteratee(array4[index4], index4, array4) === false) {
          break;
        }
      }
      return array4;
    }
    module.exports = arrayEach;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_defineProperty.js
var require_defineProperty2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_defineProperty.js"(exports, module) {
    var getNative = require_getNative2();
    var defineProperty = function() {
      try {
        var func = getNative(Object, "defineProperty");
        func({}, "", {});
        return func;
      } catch (e7) {
      }
    }();
    module.exports = defineProperty;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseAssignValue.js
var require_baseAssignValue2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseAssignValue.js"(exports, module) {
    var defineProperty = require_defineProperty2();
    function baseAssignValue(object, key, value) {
      if (key == "__proto__" && defineProperty) {
        defineProperty(object, key, {
          "configurable": true,
          "enumerable": true,
          "value": value,
          "writable": true
        });
      } else {
        object[key] = value;
      }
    }
    module.exports = baseAssignValue;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_assignValue.js
var require_assignValue2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_assignValue.js"(exports, module) {
    var baseAssignValue = require_baseAssignValue2();
    var eq = require_eq2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function assignValue(object, key, value) {
      var objValue = object[key];
      if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    module.exports = assignValue;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_copyObject.js
var require_copyObject2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_copyObject.js"(exports, module) {
    var assignValue = require_assignValue2();
    var baseAssignValue = require_baseAssignValue2();
    function copyObject(source, props, object, customizer) {
      var isNew = !object;
      object || (object = {});
      var index4 = -1, length = props.length;
      while (++index4 < length) {
        var key = props[index4];
        var newValue = customizer ? customizer(object[key], source[key], key, object, source) : void 0;
        if (newValue === void 0) {
          newValue = source[key];
        }
        if (isNew) {
          baseAssignValue(object, key, newValue);
        } else {
          assignValue(object, key, newValue);
        }
      }
      return object;
    }
    module.exports = copyObject;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseTimes.js
var require_baseTimes2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseTimes.js"(exports, module) {
    function baseTimes(n5, iteratee) {
      var index4 = -1, result = Array(n5);
      while (++index4 < n5) {
        result[index4] = iteratee(index4);
      }
      return result;
    }
    module.exports = baseTimes;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isObjectLike.js
var require_isObjectLike2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isObjectLike.js"(exports, module) {
    function isObjectLike(value) {
      return value != null && typeof value == "object";
    }
    module.exports = isObjectLike;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseIsArguments.js
var require_baseIsArguments2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseIsArguments.js"(exports, module) {
    var baseGetTag = require_baseGetTag2();
    var isObjectLike = require_isObjectLike2();
    var argsTag = "[object Arguments]";
    function baseIsArguments(value) {
      return isObjectLike(value) && baseGetTag(value) == argsTag;
    }
    module.exports = baseIsArguments;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isArguments.js
var require_isArguments2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isArguments.js"(exports, module) {
    var baseIsArguments = require_baseIsArguments2();
    var isObjectLike = require_isObjectLike2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var isArguments = baseIsArguments(/* @__PURE__ */ function() {
      return arguments;
    }()) ? baseIsArguments : function(value) {
      return isObjectLike(value) && hasOwnProperty.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
    };
    module.exports = isArguments;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isArray.js
var require_isArray2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isArray.js"(exports, module) {
    var isArray = Array.isArray;
    module.exports = isArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/stubFalse.js
var require_stubFalse2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/stubFalse.js"(exports, module) {
    function stubFalse() {
      return false;
    }
    module.exports = stubFalse;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isBuffer.js
var require_isBuffer2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isBuffer.js"(exports, module) {
    var root = require_root2();
    var stubFalse = require_stubFalse2();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0;
    var nativeIsBuffer = Buffer ? Buffer.isBuffer : void 0;
    var isBuffer = nativeIsBuffer || stubFalse;
    module.exports = isBuffer;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_isIndex.js
var require_isIndex2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_isIndex.js"(exports, module) {
    var MAX_SAFE_INTEGER = 9007199254740991;
    var reIsUint = /^(?:0|[1-9]\d*)$/;
    function isIndex(value, length) {
      var type = typeof value;
      length = length == null ? MAX_SAFE_INTEGER : length;
      return !!length && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
    }
    module.exports = isIndex;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isLength.js
var require_isLength2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isLength.js"(exports, module) {
    var MAX_SAFE_INTEGER = 9007199254740991;
    function isLength(value) {
      return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
    }
    module.exports = isLength;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseIsTypedArray.js
var require_baseIsTypedArray2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseIsTypedArray.js"(exports, module) {
    var baseGetTag = require_baseGetTag2();
    var isLength = require_isLength2();
    var isObjectLike = require_isObjectLike2();
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var funcTag = "[object Function]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var objectTag = "[object Object]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var weakMapTag = "[object WeakMap]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    var typedArrayTags = {};
    typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
    typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
    function baseIsTypedArray(value) {
      return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
    }
    module.exports = baseIsTypedArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseUnary.js
var require_baseUnary2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseUnary.js"(exports, module) {
    function baseUnary(func) {
      return function(value) {
        return func(value);
      };
    }
    module.exports = baseUnary;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_nodeUtil.js
var require_nodeUtil2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_nodeUtil.js"(exports, module) {
    var freeGlobal = require_freeGlobal2();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var freeProcess = moduleExports && freeGlobal.process;
    var nodeUtil = function() {
      try {
        var types = freeModule && freeModule.require && freeModule.require("util").types;
        if (types) {
          return types;
        }
        return freeProcess && freeProcess.binding && freeProcess.binding("util");
      } catch (e7) {
      }
    }();
    module.exports = nodeUtil;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isTypedArray.js
var require_isTypedArray2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isTypedArray.js"(exports, module) {
    var baseIsTypedArray = require_baseIsTypedArray2();
    var baseUnary = require_baseUnary2();
    var nodeUtil = require_nodeUtil2();
    var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
    var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
    module.exports = isTypedArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_arrayLikeKeys.js
var require_arrayLikeKeys2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_arrayLikeKeys.js"(exports, module) {
    var baseTimes = require_baseTimes2();
    var isArguments = require_isArguments2();
    var isArray = require_isArray2();
    var isBuffer = require_isBuffer2();
    var isIndex = require_isIndex2();
    var isTypedArray = require_isTypedArray2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function arrayLikeKeys(value, inherited) {
      var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result = skipIndexes ? baseTimes(value.length, String) : [], length = result.length;
      for (var key in value) {
        if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
        (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
        isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
        isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
        isIndex(key, length)))) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = arrayLikeKeys;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_isPrototype.js
var require_isPrototype2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_isPrototype.js"(exports, module) {
    var objectProto = Object.prototype;
    function isPrototype(value) {
      var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
      return value === proto;
    }
    module.exports = isPrototype;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_overArg.js
var require_overArg2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_overArg.js"(exports, module) {
    function overArg(func, transform) {
      return function(arg) {
        return func(transform(arg));
      };
    }
    module.exports = overArg;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_nativeKeys.js
var require_nativeKeys2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_nativeKeys.js"(exports, module) {
    var overArg = require_overArg2();
    var nativeKeys = overArg(Object.keys, Object);
    module.exports = nativeKeys;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseKeys.js
var require_baseKeys2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseKeys.js"(exports, module) {
    var isPrototype = require_isPrototype2();
    var nativeKeys = require_nativeKeys2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseKeys(object) {
      if (!isPrototype(object)) {
        return nativeKeys(object);
      }
      var result = [];
      for (var key in Object(object)) {
        if (hasOwnProperty.call(object, key) && key != "constructor") {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = baseKeys;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isArrayLike.js
var require_isArrayLike2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isArrayLike.js"(exports, module) {
    var isFunction = require_isFunction2();
    var isLength = require_isLength2();
    function isArrayLike(value) {
      return value != null && isLength(value.length) && !isFunction(value);
    }
    module.exports = isArrayLike;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/keys.js
var require_keys2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/keys.js"(exports, module) {
    var arrayLikeKeys = require_arrayLikeKeys2();
    var baseKeys = require_baseKeys2();
    var isArrayLike = require_isArrayLike2();
    function keys(object) {
      return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
    }
    module.exports = keys;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseAssign.js
var require_baseAssign = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseAssign.js"(exports, module) {
    var copyObject = require_copyObject2();
    var keys = require_keys2();
    function baseAssign(object, source) {
      return object && copyObject(source, keys(source), object);
    }
    module.exports = baseAssign;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_nativeKeysIn.js
var require_nativeKeysIn2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_nativeKeysIn.js"(exports, module) {
    function nativeKeysIn(object) {
      var result = [];
      if (object != null) {
        for (var key in Object(object)) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = nativeKeysIn;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseKeysIn.js
var require_baseKeysIn2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseKeysIn.js"(exports, module) {
    var isObject = require_isObject2();
    var isPrototype = require_isPrototype2();
    var nativeKeysIn = require_nativeKeysIn2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseKeysIn(object) {
      if (!isObject(object)) {
        return nativeKeysIn(object);
      }
      var isProto = isPrototype(object), result = [];
      for (var key in object) {
        if (!(key == "constructor" && (isProto || !hasOwnProperty.call(object, key)))) {
          result.push(key);
        }
      }
      return result;
    }
    module.exports = baseKeysIn;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/keysIn.js
var require_keysIn2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/keysIn.js"(exports, module) {
    var arrayLikeKeys = require_arrayLikeKeys2();
    var baseKeysIn = require_baseKeysIn2();
    var isArrayLike = require_isArrayLike2();
    function keysIn(object) {
      return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
    }
    module.exports = keysIn;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseAssignIn.js
var require_baseAssignIn = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseAssignIn.js"(exports, module) {
    var copyObject = require_copyObject2();
    var keysIn = require_keysIn2();
    function baseAssignIn(object, source) {
      return object && copyObject(source, keysIn(source), object);
    }
    module.exports = baseAssignIn;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_cloneBuffer.js
var require_cloneBuffer2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_cloneBuffer.js"(exports, module) {
    var root = require_root2();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0;
    var allocUnsafe = Buffer ? Buffer.allocUnsafe : void 0;
    function cloneBuffer(buffer, isDeep) {
      if (isDeep) {
        return buffer.slice();
      }
      var length = buffer.length, result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);
      buffer.copy(result);
      return result;
    }
    module.exports = cloneBuffer;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_copyArray.js
var require_copyArray2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_copyArray.js"(exports, module) {
    function copyArray(source, array4) {
      var index4 = -1, length = source.length;
      array4 || (array4 = Array(length));
      while (++index4 < length) {
        array4[index4] = source[index4];
      }
      return array4;
    }
    module.exports = copyArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_arrayFilter.js
var require_arrayFilter2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_arrayFilter.js"(exports, module) {
    function arrayFilter(array4, predicate) {
      var index4 = -1, length = array4 == null ? 0 : array4.length, resIndex = 0, result = [];
      while (++index4 < length) {
        var value = array4[index4];
        if (predicate(value, index4, array4)) {
          result[resIndex++] = value;
        }
      }
      return result;
    }
    module.exports = arrayFilter;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/stubArray.js
var require_stubArray2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/stubArray.js"(exports, module) {
    function stubArray() {
      return [];
    }
    module.exports = stubArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getSymbols.js
var require_getSymbols2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getSymbols.js"(exports, module) {
    var arrayFilter = require_arrayFilter2();
    var stubArray = require_stubArray2();
    var objectProto = Object.prototype;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var nativeGetSymbols = Object.getOwnPropertySymbols;
    var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
      if (object == null) {
        return [];
      }
      object = Object(object);
      return arrayFilter(nativeGetSymbols(object), function(symbol) {
        return propertyIsEnumerable.call(object, symbol);
      });
    };
    module.exports = getSymbols;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_copySymbols.js
var require_copySymbols = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_copySymbols.js"(exports, module) {
    var copyObject = require_copyObject2();
    var getSymbols = require_getSymbols2();
    function copySymbols(source, object) {
      return copyObject(source, getSymbols(source), object);
    }
    module.exports = copySymbols;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_arrayPush.js
var require_arrayPush2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_arrayPush.js"(exports, module) {
    function arrayPush(array4, values) {
      var index4 = -1, length = values.length, offset = array4.length;
      while (++index4 < length) {
        array4[offset + index4] = values[index4];
      }
      return array4;
    }
    module.exports = arrayPush;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getPrototype.js
var require_getPrototype2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getPrototype.js"(exports, module) {
    var overArg = require_overArg2();
    var getPrototype = overArg(Object.getPrototypeOf, Object);
    module.exports = getPrototype;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getSymbolsIn.js
var require_getSymbolsIn = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getSymbolsIn.js"(exports, module) {
    var arrayPush = require_arrayPush2();
    var getPrototype = require_getPrototype2();
    var getSymbols = require_getSymbols2();
    var stubArray = require_stubArray2();
    var nativeGetSymbols = Object.getOwnPropertySymbols;
    var getSymbolsIn = !nativeGetSymbols ? stubArray : function(object) {
      var result = [];
      while (object) {
        arrayPush(result, getSymbols(object));
        object = getPrototype(object);
      }
      return result;
    };
    module.exports = getSymbolsIn;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_copySymbolsIn.js
var require_copySymbolsIn = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_copySymbolsIn.js"(exports, module) {
    var copyObject = require_copyObject2();
    var getSymbolsIn = require_getSymbolsIn();
    function copySymbolsIn(source, object) {
      return copyObject(source, getSymbolsIn(source), object);
    }
    module.exports = copySymbolsIn;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseGetAllKeys.js
var require_baseGetAllKeys2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseGetAllKeys.js"(exports, module) {
    var arrayPush = require_arrayPush2();
    var isArray = require_isArray2();
    function baseGetAllKeys(object, keysFunc, symbolsFunc) {
      var result = keysFunc(object);
      return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
    }
    module.exports = baseGetAllKeys;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getAllKeys.js
var require_getAllKeys2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getAllKeys.js"(exports, module) {
    var baseGetAllKeys = require_baseGetAllKeys2();
    var getSymbols = require_getSymbols2();
    var keys = require_keys2();
    function getAllKeys(object) {
      return baseGetAllKeys(object, keys, getSymbols);
    }
    module.exports = getAllKeys;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getAllKeysIn.js
var require_getAllKeysIn = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getAllKeysIn.js"(exports, module) {
    var baseGetAllKeys = require_baseGetAllKeys2();
    var getSymbolsIn = require_getSymbolsIn();
    var keysIn = require_keysIn2();
    function getAllKeysIn(object) {
      return baseGetAllKeys(object, keysIn, getSymbolsIn);
    }
    module.exports = getAllKeysIn;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_DataView.js
var require_DataView2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_DataView.js"(exports, module) {
    var getNative = require_getNative2();
    var root = require_root2();
    var DataView2 = getNative(root, "DataView");
    module.exports = DataView2;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_Promise.js
var require_Promise2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_Promise.js"(exports, module) {
    var getNative = require_getNative2();
    var root = require_root2();
    var Promise2 = getNative(root, "Promise");
    module.exports = Promise2;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_Set.js
var require_Set2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_Set.js"(exports, module) {
    var getNative = require_getNative2();
    var root = require_root2();
    var Set2 = getNative(root, "Set");
    module.exports = Set2;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_WeakMap.js
var require_WeakMap2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_WeakMap.js"(exports, module) {
    var getNative = require_getNative2();
    var root = require_root2();
    var WeakMap2 = getNative(root, "WeakMap");
    module.exports = WeakMap2;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_getTag.js
var require_getTag2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_getTag.js"(exports, module) {
    var DataView2 = require_DataView2();
    var Map2 = require_Map2();
    var Promise2 = require_Promise2();
    var Set2 = require_Set2();
    var WeakMap2 = require_WeakMap2();
    var baseGetTag = require_baseGetTag2();
    var toSource = require_toSource2();
    var mapTag = "[object Map]";
    var objectTag = "[object Object]";
    var promiseTag = "[object Promise]";
    var setTag = "[object Set]";
    var weakMapTag = "[object WeakMap]";
    var dataViewTag = "[object DataView]";
    var dataViewCtorString = toSource(DataView2);
    var mapCtorString = toSource(Map2);
    var promiseCtorString = toSource(Promise2);
    var setCtorString = toSource(Set2);
    var weakMapCtorString = toSource(WeakMap2);
    var getTag = baseGetTag;
    if (DataView2 && getTag(new DataView2(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap2 && getTag(new WeakMap2()) != weakMapTag) {
      getTag = function(value) {
        var result = baseGetTag(value), Ctor = result == objectTag ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : "";
        if (ctorString) {
          switch (ctorString) {
            case dataViewCtorString:
              return dataViewTag;
            case mapCtorString:
              return mapTag;
            case promiseCtorString:
              return promiseTag;
            case setCtorString:
              return setTag;
            case weakMapCtorString:
              return weakMapTag;
          }
        }
        return result;
      };
    }
    module.exports = getTag;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_initCloneArray.js
var require_initCloneArray = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_initCloneArray.js"(exports, module) {
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function initCloneArray(array4) {
      var length = array4.length, result = new array4.constructor(length);
      if (length && typeof array4[0] == "string" && hasOwnProperty.call(array4, "index")) {
        result.index = array4.index;
        result.input = array4.input;
      }
      return result;
    }
    module.exports = initCloneArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_Uint8Array.js
var require_Uint8Array2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_Uint8Array.js"(exports, module) {
    var root = require_root2();
    var Uint8Array = root.Uint8Array;
    module.exports = Uint8Array;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_cloneArrayBuffer.js
var require_cloneArrayBuffer2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_cloneArrayBuffer.js"(exports, module) {
    var Uint8Array = require_Uint8Array2();
    function cloneArrayBuffer(arrayBuffer) {
      var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
      new Uint8Array(result).set(new Uint8Array(arrayBuffer));
      return result;
    }
    module.exports = cloneArrayBuffer;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_cloneDataView.js
var require_cloneDataView = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_cloneDataView.js"(exports, module) {
    var cloneArrayBuffer = require_cloneArrayBuffer2();
    function cloneDataView(dataView, isDeep) {
      var buffer = isDeep ? cloneArrayBuffer(dataView.buffer) : dataView.buffer;
      return new dataView.constructor(buffer, dataView.byteOffset, dataView.byteLength);
    }
    module.exports = cloneDataView;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_cloneRegExp.js
var require_cloneRegExp = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_cloneRegExp.js"(exports, module) {
    var reFlags = /\w*$/;
    function cloneRegExp(regexp) {
      var result = new regexp.constructor(regexp.source, reFlags.exec(regexp));
      result.lastIndex = regexp.lastIndex;
      return result;
    }
    module.exports = cloneRegExp;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_cloneSymbol.js
var require_cloneSymbol = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_cloneSymbol.js"(exports, module) {
    var Symbol3 = require_Symbol2();
    var symbolProto = Symbol3 ? Symbol3.prototype : void 0;
    var symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
    function cloneSymbol(symbol) {
      return symbolValueOf ? Object(symbolValueOf.call(symbol)) : {};
    }
    module.exports = cloneSymbol;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_cloneTypedArray.js
var require_cloneTypedArray2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_cloneTypedArray.js"(exports, module) {
    var cloneArrayBuffer = require_cloneArrayBuffer2();
    function cloneTypedArray(typedArray, isDeep) {
      var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
      return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
    }
    module.exports = cloneTypedArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_initCloneByTag.js
var require_initCloneByTag = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_initCloneByTag.js"(exports, module) {
    var cloneArrayBuffer = require_cloneArrayBuffer2();
    var cloneDataView = require_cloneDataView();
    var cloneRegExp = require_cloneRegExp();
    var cloneSymbol = require_cloneSymbol();
    var cloneTypedArray = require_cloneTypedArray2();
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var symbolTag = "[object Symbol]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    function initCloneByTag(object, tag, isDeep) {
      var Ctor = object.constructor;
      switch (tag) {
        case arrayBufferTag:
          return cloneArrayBuffer(object);
        case boolTag:
        case dateTag:
          return new Ctor(+object);
        case dataViewTag:
          return cloneDataView(object, isDeep);
        case float32Tag:
        case float64Tag:
        case int8Tag:
        case int16Tag:
        case int32Tag:
        case uint8Tag:
        case uint8ClampedTag:
        case uint16Tag:
        case uint32Tag:
          return cloneTypedArray(object, isDeep);
        case mapTag:
          return new Ctor();
        case numberTag:
        case stringTag:
          return new Ctor(object);
        case regexpTag:
          return cloneRegExp(object);
        case setTag:
          return new Ctor();
        case symbolTag:
          return cloneSymbol(object);
      }
    }
    module.exports = initCloneByTag;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseCreate.js
var require_baseCreate2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseCreate.js"(exports, module) {
    var isObject = require_isObject2();
    var objectCreate = Object.create;
    var baseCreate = /* @__PURE__ */ function() {
      function object() {
      }
      return function(proto) {
        if (!isObject(proto)) {
          return {};
        }
        if (objectCreate) {
          return objectCreate(proto);
        }
        object.prototype = proto;
        var result = new object();
        object.prototype = void 0;
        return result;
      };
    }();
    module.exports = baseCreate;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_initCloneObject.js
var require_initCloneObject2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_initCloneObject.js"(exports, module) {
    var baseCreate = require_baseCreate2();
    var getPrototype = require_getPrototype2();
    var isPrototype = require_isPrototype2();
    function initCloneObject(object) {
      return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
    }
    module.exports = initCloneObject;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseIsMap.js
var require_baseIsMap = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseIsMap.js"(exports, module) {
    var getTag = require_getTag2();
    var isObjectLike = require_isObjectLike2();
    var mapTag = "[object Map]";
    function baseIsMap(value) {
      return isObjectLike(value) && getTag(value) == mapTag;
    }
    module.exports = baseIsMap;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isMap.js
var require_isMap = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isMap.js"(exports, module) {
    var baseIsMap = require_baseIsMap();
    var baseUnary = require_baseUnary2();
    var nodeUtil = require_nodeUtil2();
    var nodeIsMap = nodeUtil && nodeUtil.isMap;
    var isMap = nodeIsMap ? baseUnary(nodeIsMap) : baseIsMap;
    module.exports = isMap;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseIsSet.js
var require_baseIsSet = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseIsSet.js"(exports, module) {
    var getTag = require_getTag2();
    var isObjectLike = require_isObjectLike2();
    var setTag = "[object Set]";
    function baseIsSet(value) {
      return isObjectLike(value) && getTag(value) == setTag;
    }
    module.exports = baseIsSet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isSet.js
var require_isSet = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isSet.js"(exports, module) {
    var baseIsSet = require_baseIsSet();
    var baseUnary = require_baseUnary2();
    var nodeUtil = require_nodeUtil2();
    var nodeIsSet = nodeUtil && nodeUtil.isSet;
    var isSet = nodeIsSet ? baseUnary(nodeIsSet) : baseIsSet;
    module.exports = isSet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseClone.js
var require_baseClone = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseClone.js"(exports, module) {
    var Stack = require_Stack2();
    var arrayEach = require_arrayEach();
    var assignValue = require_assignValue2();
    var baseAssign = require_baseAssign();
    var baseAssignIn = require_baseAssignIn();
    var cloneBuffer = require_cloneBuffer2();
    var copyArray = require_copyArray2();
    var copySymbols = require_copySymbols();
    var copySymbolsIn = require_copySymbolsIn();
    var getAllKeys = require_getAllKeys2();
    var getAllKeysIn = require_getAllKeysIn();
    var getTag = require_getTag2();
    var initCloneArray = require_initCloneArray();
    var initCloneByTag = require_initCloneByTag();
    var initCloneObject = require_initCloneObject2();
    var isArray = require_isArray2();
    var isBuffer = require_isBuffer2();
    var isMap = require_isMap();
    var isObject = require_isObject2();
    var isSet = require_isSet();
    var keys = require_keys2();
    var keysIn = require_keysIn2();
    var CLONE_DEEP_FLAG = 1;
    var CLONE_FLAT_FLAG = 2;
    var CLONE_SYMBOLS_FLAG = 4;
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var funcTag = "[object Function]";
    var genTag = "[object GeneratorFunction]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var objectTag = "[object Object]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var symbolTag = "[object Symbol]";
    var weakMapTag = "[object WeakMap]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    var cloneableTags = {};
    cloneableTags[argsTag] = cloneableTags[arrayTag] = cloneableTags[arrayBufferTag] = cloneableTags[dataViewTag] = cloneableTags[boolTag] = cloneableTags[dateTag] = cloneableTags[float32Tag] = cloneableTags[float64Tag] = cloneableTags[int8Tag] = cloneableTags[int16Tag] = cloneableTags[int32Tag] = cloneableTags[mapTag] = cloneableTags[numberTag] = cloneableTags[objectTag] = cloneableTags[regexpTag] = cloneableTags[setTag] = cloneableTags[stringTag] = cloneableTags[symbolTag] = cloneableTags[uint8Tag] = cloneableTags[uint8ClampedTag] = cloneableTags[uint16Tag] = cloneableTags[uint32Tag] = true;
    cloneableTags[errorTag] = cloneableTags[funcTag] = cloneableTags[weakMapTag] = false;
    function baseClone(value, bitmask, customizer, key, object, stack) {
      var result, isDeep = bitmask & CLONE_DEEP_FLAG, isFlat = bitmask & CLONE_FLAT_FLAG, isFull = bitmask & CLONE_SYMBOLS_FLAG;
      if (customizer) {
        result = object ? customizer(value, key, object, stack) : customizer(value);
      }
      if (result !== void 0) {
        return result;
      }
      if (!isObject(value)) {
        return value;
      }
      var isArr = isArray(value);
      if (isArr) {
        result = initCloneArray(value);
        if (!isDeep) {
          return copyArray(value, result);
        }
      } else {
        var tag = getTag(value), isFunc = tag == funcTag || tag == genTag;
        if (isBuffer(value)) {
          return cloneBuffer(value, isDeep);
        }
        if (tag == objectTag || tag == argsTag || isFunc && !object) {
          result = isFlat || isFunc ? {} : initCloneObject(value);
          if (!isDeep) {
            return isFlat ? copySymbolsIn(value, baseAssignIn(result, value)) : copySymbols(value, baseAssign(result, value));
          }
        } else {
          if (!cloneableTags[tag]) {
            return object ? value : {};
          }
          result = initCloneByTag(value, tag, isDeep);
        }
      }
      stack || (stack = new Stack());
      var stacked = stack.get(value);
      if (stacked) {
        return stacked;
      }
      stack.set(value, result);
      if (isSet(value)) {
        value.forEach(function(subValue) {
          result.add(baseClone(subValue, bitmask, customizer, subValue, value, stack));
        });
      } else if (isMap(value)) {
        value.forEach(function(subValue, key2) {
          result.set(key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
        });
      }
      var keysFunc = isFull ? isFlat ? getAllKeysIn : getAllKeys : isFlat ? keysIn : keys;
      var props = isArr ? void 0 : keysFunc(value);
      arrayEach(props || value, function(subValue, key2) {
        if (props) {
          key2 = subValue;
          subValue = value[key2];
        }
        assignValue(result, key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
      });
      return result;
    }
    module.exports = baseClone;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isSymbol.js
var require_isSymbol2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isSymbol.js"(exports, module) {
    var baseGetTag = require_baseGetTag2();
    var isObjectLike = require_isObjectLike2();
    var symbolTag = "[object Symbol]";
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
    }
    module.exports = isSymbol;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_isKey.js
var require_isKey2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_isKey.js"(exports, module) {
    var isArray = require_isArray2();
    var isSymbol = require_isSymbol2();
    var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
    var reIsPlainProp = /^\w*$/;
    function isKey(value, object) {
      if (isArray(value)) {
        return false;
      }
      var type = typeof value;
      if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
        return true;
      }
      return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
    }
    module.exports = isKey;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/memoize.js
var require_memoize2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/memoize.js"(exports, module) {
    var MapCache = require_MapCache2();
    var FUNC_ERROR_TEXT = "Expected a function";
    function memoize(func, resolver) {
      if (typeof func != "function" || resolver != null && typeof resolver != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      var memoized = function() {
        var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
        if (cache.has(key)) {
          return cache.get(key);
        }
        var result = func.apply(this, args);
        memoized.cache = cache.set(key, result) || cache;
        return result;
      };
      memoized.cache = new (memoize.Cache || MapCache)();
      return memoized;
    }
    memoize.Cache = MapCache;
    module.exports = memoize;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_memoizeCapped.js
var require_memoizeCapped2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_memoizeCapped.js"(exports, module) {
    var memoize = require_memoize2();
    var MAX_MEMOIZE_SIZE = 500;
    function memoizeCapped(func) {
      var result = memoize(func, function(key) {
        if (cache.size === MAX_MEMOIZE_SIZE) {
          cache.clear();
        }
        return key;
      });
      var cache = result.cache;
      return result;
    }
    module.exports = memoizeCapped;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_stringToPath.js
var require_stringToPath2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_stringToPath.js"(exports, module) {
    var memoizeCapped = require_memoizeCapped2();
    var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
    var reEscapeChar = /\\(\\)?/g;
    var stringToPath = memoizeCapped(function(string) {
      var result = [];
      if (string.charCodeAt(0) === 46) {
        result.push("");
      }
      string.replace(rePropName, function(match, number5, quote, subString) {
        result.push(quote ? subString.replace(reEscapeChar, "$1") : number5 || match);
      });
      return result;
    });
    module.exports = stringToPath;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseToString.js
var require_baseToString2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseToString.js"(exports, module) {
    var Symbol3 = require_Symbol2();
    var arrayMap = require_arrayMap2();
    var isArray = require_isArray2();
    var isSymbol = require_isSymbol2();
    var INFINITY = 1 / 0;
    var symbolProto = Symbol3 ? Symbol3.prototype : void 0;
    var symbolToString = symbolProto ? symbolProto.toString : void 0;
    function baseToString(value) {
      if (typeof value == "string") {
        return value;
      }
      if (isArray(value)) {
        return arrayMap(value, baseToString) + "";
      }
      if (isSymbol(value)) {
        return symbolToString ? symbolToString.call(value) : "";
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    module.exports = baseToString;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/toString.js
var require_toString2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/toString.js"(exports, module) {
    var baseToString = require_baseToString2();
    function toString(value) {
      return value == null ? "" : baseToString(value);
    }
    module.exports = toString;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_castPath.js
var require_castPath2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_castPath.js"(exports, module) {
    var isArray = require_isArray2();
    var isKey = require_isKey2();
    var stringToPath = require_stringToPath2();
    var toString = require_toString2();
    function castPath(value, object) {
      if (isArray(value)) {
        return value;
      }
      return isKey(value, object) ? [value] : stringToPath(toString(value));
    }
    module.exports = castPath;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/last.js
var require_last2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/last.js"(exports, module) {
    function last(array4) {
      var length = array4 == null ? 0 : array4.length;
      return length ? array4[length - 1] : void 0;
    }
    module.exports = last;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_toKey.js
var require_toKey2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_toKey.js"(exports, module) {
    var isSymbol = require_isSymbol2();
    var INFINITY = 1 / 0;
    function toKey(value) {
      if (typeof value == "string" || isSymbol(value)) {
        return value;
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    module.exports = toKey;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseGet.js
var require_baseGet2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseGet.js"(exports, module) {
    var castPath = require_castPath2();
    var toKey = require_toKey2();
    function baseGet(object, path2) {
      path2 = castPath(path2, object);
      var index4 = 0, length = path2.length;
      while (object != null && index4 < length) {
        object = object[toKey(path2[index4++])];
      }
      return index4 && index4 == length ? object : void 0;
    }
    module.exports = baseGet;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseSlice.js
var require_baseSlice = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseSlice.js"(exports, module) {
    function baseSlice(array4, start3, end) {
      var index4 = -1, length = array4.length;
      if (start3 < 0) {
        start3 = -start3 > length ? 0 : length + start3;
      }
      end = end > length ? length : end;
      if (end < 0) {
        end += length;
      }
      length = start3 > end ? 0 : end - start3 >>> 0;
      start3 >>>= 0;
      var result = Array(length);
      while (++index4 < length) {
        result[index4] = array4[index4 + start3];
      }
      return result;
    }
    module.exports = baseSlice;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_parent.js
var require_parent = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_parent.js"(exports, module) {
    var baseGet = require_baseGet2();
    var baseSlice = require_baseSlice();
    function parent(object, path2) {
      return path2.length < 2 ? object : baseGet(object, baseSlice(path2, 0, -1));
    }
    module.exports = parent;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseUnset.js
var require_baseUnset = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseUnset.js"(exports, module) {
    var castPath = require_castPath2();
    var last = require_last2();
    var parent = require_parent();
    var toKey = require_toKey2();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    function baseUnset(object, path2) {
      path2 = castPath(path2, object);
      var index4 = -1, length = path2.length;
      if (!length) {
        return true;
      }
      while (++index4 < length) {
        var key = toKey(path2[index4]);
        if (key === "__proto__" && !hasOwnProperty.call(object, "__proto__")) {
          return false;
        }
        if ((key === "constructor" || key === "prototype") && index4 < length - 1) {
          return false;
        }
      }
      var obj = parent(object, path2);
      return obj == null || delete obj[toKey(last(path2))];
    }
    module.exports = baseUnset;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/isPlainObject.js
var require_isPlainObject2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/isPlainObject.js"(exports, module) {
    var baseGetTag = require_baseGetTag2();
    var getPrototype = require_getPrototype2();
    var isObjectLike = require_isObjectLike2();
    var objectTag = "[object Object]";
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var objectCtorString = funcToString.call(Object);
    function isPlainObject(value) {
      if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
        return false;
      }
      var proto = getPrototype(value);
      if (proto === null) {
        return true;
      }
      var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
      return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
    }
    module.exports = isPlainObject;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_customOmitClone.js
var require_customOmitClone = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_customOmitClone.js"(exports, module) {
    var isPlainObject = require_isPlainObject2();
    function customOmitClone(value) {
      return isPlainObject(value) ? void 0 : value;
    }
    module.exports = customOmitClone;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_isFlattenable.js
var require_isFlattenable2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_isFlattenable.js"(exports, module) {
    var Symbol3 = require_Symbol2();
    var isArguments = require_isArguments2();
    var isArray = require_isArray2();
    var spreadableSymbol = Symbol3 ? Symbol3.isConcatSpreadable : void 0;
    function isFlattenable(value) {
      return isArray(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
    }
    module.exports = isFlattenable;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseFlatten.js
var require_baseFlatten2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseFlatten.js"(exports, module) {
    var arrayPush = require_arrayPush2();
    var isFlattenable = require_isFlattenable2();
    function baseFlatten(array4, depth, predicate, isStrict, result) {
      var index4 = -1, length = array4.length;
      predicate || (predicate = isFlattenable);
      result || (result = []);
      while (++index4 < length) {
        var value = array4[index4];
        if (depth > 0 && predicate(value)) {
          if (depth > 1) {
            baseFlatten(value, depth - 1, predicate, isStrict, result);
          } else {
            arrayPush(result, value);
          }
        } else if (!isStrict) {
          result[result.length] = value;
        }
      }
      return result;
    }
    module.exports = baseFlatten;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/flatten.js
var require_flatten2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/flatten.js"(exports, module) {
    var baseFlatten = require_baseFlatten2();
    function flatten(array4) {
      var length = array4 == null ? 0 : array4.length;
      return length ? baseFlatten(array4, 1) : [];
    }
    module.exports = flatten;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_apply.js
var require_apply2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_apply.js"(exports, module) {
    function apply(func, thisArg, args) {
      switch (args.length) {
        case 0:
          return func.call(thisArg);
        case 1:
          return func.call(thisArg, args[0]);
        case 2:
          return func.call(thisArg, args[0], args[1]);
        case 3:
          return func.call(thisArg, args[0], args[1], args[2]);
      }
      return func.apply(thisArg, args);
    }
    module.exports = apply;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_overRest.js
var require_overRest2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_overRest.js"(exports, module) {
    var apply = require_apply2();
    var nativeMax = Math.max;
    function overRest(func, start3, transform) {
      start3 = nativeMax(start3 === void 0 ? func.length - 1 : start3, 0);
      return function() {
        var args = arguments, index4 = -1, length = nativeMax(args.length - start3, 0), array4 = Array(length);
        while (++index4 < length) {
          array4[index4] = args[start3 + index4];
        }
        index4 = -1;
        var otherArgs = Array(start3 + 1);
        while (++index4 < start3) {
          otherArgs[index4] = args[index4];
        }
        otherArgs[start3] = transform(array4);
        return apply(func, this, otherArgs);
      };
    }
    module.exports = overRest;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/constant.js
var require_constant2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/constant.js"(exports, module) {
    function constant3(value) {
      return function() {
        return value;
      };
    }
    module.exports = constant3;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/identity.js
var require_identity2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/identity.js"(exports, module) {
    function identity10(value) {
      return value;
    }
    module.exports = identity10;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_baseSetToString.js
var require_baseSetToString2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_baseSetToString.js"(exports, module) {
    var constant3 = require_constant2();
    var defineProperty = require_defineProperty2();
    var identity10 = require_identity2();
    var baseSetToString = !defineProperty ? identity10 : function(func, string) {
      return defineProperty(func, "toString", {
        "configurable": true,
        "enumerable": false,
        "value": constant3(string),
        "writable": true
      });
    };
    module.exports = baseSetToString;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_shortOut.js
var require_shortOut2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_shortOut.js"(exports, module) {
    var HOT_COUNT = 800;
    var HOT_SPAN = 16;
    var nativeNow = Date.now;
    function shortOut(func) {
      var count5 = 0, lastCalled = 0;
      return function() {
        var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
        lastCalled = stamp;
        if (remaining > 0) {
          if (++count5 >= HOT_COUNT) {
            return arguments[0];
          }
        } else {
          count5 = 0;
        }
        return func.apply(void 0, arguments);
      };
    }
    module.exports = shortOut;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_setToString.js
var require_setToString2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_setToString.js"(exports, module) {
    var baseSetToString = require_baseSetToString2();
    var shortOut = require_shortOut2();
    var setToString = shortOut(baseSetToString);
    module.exports = setToString;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_flatRest.js
var require_flatRest2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_flatRest.js"(exports, module) {
    var flatten = require_flatten2();
    var overRest = require_overRest2();
    var setToString = require_setToString2();
    function flatRest(func) {
      return setToString(overRest(func, void 0, flatten), func + "");
    }
    module.exports = flatRest;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/omit.js
var require_omit = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/omit.js"(exports, module) {
    var arrayMap = require_arrayMap2();
    var baseClone = require_baseClone();
    var baseUnset = require_baseUnset();
    var castPath = require_castPath2();
    var copyObject = require_copyObject2();
    var customOmitClone = require_customOmitClone();
    var flatRest = require_flatRest2();
    var getAllKeysIn = require_getAllKeysIn();
    var CLONE_DEEP_FLAG = 1;
    var CLONE_FLAT_FLAG = 2;
    var CLONE_SYMBOLS_FLAG = 4;
    var omit = flatRest(function(object, paths) {
      var result = {};
      if (object == null) {
        return result;
      }
      var isDeep = false;
      paths = arrayMap(paths, function(path2) {
        path2 = castPath(path2, object);
        isDeep || (isDeep = path2.length > 1);
        return path2;
      });
      copyObject(object, getAllKeysIn(object), result);
      if (isDeep) {
        result = baseClone(result, CLONE_DEEP_FLAG | CLONE_FLAT_FLAG | CLONE_SYMBOLS_FLAG, customOmitClone);
      }
      var length = paths.length;
      while (length--) {
        baseUnset(result, paths[length]);
      }
      return result;
    });
    module.exports = omit;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/cloneDeep.js
var require_cloneDeep = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/cloneDeep.js"(exports, module) {
    var baseClone = require_baseClone();
    var CLONE_DEEP_FLAG = 1;
    var CLONE_SYMBOLS_FLAG = 4;
    function cloneDeep(value) {
      return baseClone(value, CLONE_DEEP_FLAG | CLONE_SYMBOLS_FLAG);
    }
    module.exports = cloneDeep;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_arrayReduce.js
var require_arrayReduce = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_arrayReduce.js"(exports, module) {
    function arrayReduce(array4, iteratee, accumulator, initAccum) {
      var index4 = -1, length = array4 == null ? 0 : array4.length;
      if (initAccum && length) {
        accumulator = array4[++index4];
      }
      while (++index4 < length) {
        accumulator = iteratee(accumulator, array4[index4], index4, array4);
      }
      return accumulator;
    }
    module.exports = arrayReduce;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_basePropertyOf.js
var require_basePropertyOf = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_basePropertyOf.js"(exports, module) {
    function basePropertyOf(object) {
      return function(key) {
        return object == null ? void 0 : object[key];
      };
    }
    module.exports = basePropertyOf;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_deburrLetter.js
var require_deburrLetter = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_deburrLetter.js"(exports, module) {
    var basePropertyOf = require_basePropertyOf();
    var deburredLetters = {
      // Latin-1 Supplement block.
      "À": "A",
      "Á": "A",
      "Â": "A",
      "Ã": "A",
      "Ä": "A",
      "Å": "A",
      "à": "a",
      "á": "a",
      "â": "a",
      "ã": "a",
      "ä": "a",
      "å": "a",
      "Ç": "C",
      "ç": "c",
      "Ð": "D",
      "ð": "d",
      "È": "E",
      "É": "E",
      "Ê": "E",
      "Ë": "E",
      "è": "e",
      "é": "e",
      "ê": "e",
      "ë": "e",
      "Ì": "I",
      "Í": "I",
      "Î": "I",
      "Ï": "I",
      "ì": "i",
      "í": "i",
      "î": "i",
      "ï": "i",
      "Ñ": "N",
      "ñ": "n",
      "Ò": "O",
      "Ó": "O",
      "Ô": "O",
      "Õ": "O",
      "Ö": "O",
      "Ø": "O",
      "ò": "o",
      "ó": "o",
      "ô": "o",
      "õ": "o",
      "ö": "o",
      "ø": "o",
      "Ù": "U",
      "Ú": "U",
      "Û": "U",
      "Ü": "U",
      "ù": "u",
      "ú": "u",
      "û": "u",
      "ü": "u",
      "Ý": "Y",
      "ý": "y",
      "ÿ": "y",
      "Æ": "Ae",
      "æ": "ae",
      "Þ": "Th",
      "þ": "th",
      "ß": "ss",
      // Latin Extended-A block.
      "Ā": "A",
      "Ă": "A",
      "Ą": "A",
      "ā": "a",
      "ă": "a",
      "ą": "a",
      "Ć": "C",
      "Ĉ": "C",
      "Ċ": "C",
      "Č": "C",
      "ć": "c",
      "ĉ": "c",
      "ċ": "c",
      "č": "c",
      "Ď": "D",
      "Đ": "D",
      "ď": "d",
      "đ": "d",
      "Ē": "E",
      "Ĕ": "E",
      "Ė": "E",
      "Ę": "E",
      "Ě": "E",
      "ē": "e",
      "ĕ": "e",
      "ė": "e",
      "ę": "e",
      "ě": "e",
      "Ĝ": "G",
      "Ğ": "G",
      "Ġ": "G",
      "Ģ": "G",
      "ĝ": "g",
      "ğ": "g",
      "ġ": "g",
      "ģ": "g",
      "Ĥ": "H",
      "Ħ": "H",
      "ĥ": "h",
      "ħ": "h",
      "Ĩ": "I",
      "Ī": "I",
      "Ĭ": "I",
      "Į": "I",
      "İ": "I",
      "ĩ": "i",
      "ī": "i",
      "ĭ": "i",
      "į": "i",
      "ı": "i",
      "Ĵ": "J",
      "ĵ": "j",
      "Ķ": "K",
      "ķ": "k",
      "ĸ": "k",
      "Ĺ": "L",
      "Ļ": "L",
      "Ľ": "L",
      "Ŀ": "L",
      "Ł": "L",
      "ĺ": "l",
      "ļ": "l",
      "ľ": "l",
      "ŀ": "l",
      "ł": "l",
      "Ń": "N",
      "Ņ": "N",
      "Ň": "N",
      "Ŋ": "N",
      "ń": "n",
      "ņ": "n",
      "ň": "n",
      "ŋ": "n",
      "Ō": "O",
      "Ŏ": "O",
      "Ő": "O",
      "ō": "o",
      "ŏ": "o",
      "ő": "o",
      "Ŕ": "R",
      "Ŗ": "R",
      "Ř": "R",
      "ŕ": "r",
      "ŗ": "r",
      "ř": "r",
      "Ś": "S",
      "Ŝ": "S",
      "Ş": "S",
      "Š": "S",
      "ś": "s",
      "ŝ": "s",
      "ş": "s",
      "š": "s",
      "Ţ": "T",
      "Ť": "T",
      "Ŧ": "T",
      "ţ": "t",
      "ť": "t",
      "ŧ": "t",
      "Ũ": "U",
      "Ū": "U",
      "Ŭ": "U",
      "Ů": "U",
      "Ű": "U",
      "Ų": "U",
      "ũ": "u",
      "ū": "u",
      "ŭ": "u",
      "ů": "u",
      "ű": "u",
      "ų": "u",
      "Ŵ": "W",
      "ŵ": "w",
      "Ŷ": "Y",
      "ŷ": "y",
      "Ÿ": "Y",
      "Ź": "Z",
      "Ż": "Z",
      "Ž": "Z",
      "ź": "z",
      "ż": "z",
      "ž": "z",
      "Ĳ": "IJ",
      "ĳ": "ij",
      "Œ": "Oe",
      "œ": "oe",
      "ŉ": "'n",
      "ſ": "s"
    };
    var deburrLetter = basePropertyOf(deburredLetters);
    module.exports = deburrLetter;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/deburr.js
var require_deburr = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/deburr.js"(exports, module) {
    var deburrLetter = require_deburrLetter();
    var toString = require_toString2();
    var reLatin = /[\xc0-\xd6\xd8-\xf6\xf8-\xff\u0100-\u017f]/g;
    var rsComboMarksRange = "\\u0300-\\u036f";
    var reComboHalfMarksRange = "\\ufe20-\\ufe2f";
    var rsComboSymbolsRange = "\\u20d0-\\u20ff";
    var rsComboRange = rsComboMarksRange + reComboHalfMarksRange + rsComboSymbolsRange;
    var rsCombo = "[" + rsComboRange + "]";
    var reComboMark = RegExp(rsCombo, "g");
    function deburr(string) {
      string = toString(string);
      return string && string.replace(reLatin, deburrLetter).replace(reComboMark, "");
    }
    module.exports = deburr;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_asciiWords.js
var require_asciiWords = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_asciiWords.js"(exports, module) {
    var reAsciiWord = /[^\x00-\x2f\x3a-\x40\x5b-\x60\x7b-\x7f]+/g;
    function asciiWords(string) {
      return string.match(reAsciiWord) || [];
    }
    module.exports = asciiWords;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_hasUnicodeWord.js
var require_hasUnicodeWord = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_hasUnicodeWord.js"(exports, module) {
    var reHasUnicodeWord = /[a-z][A-Z]|[A-Z]{2}[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/;
    function hasUnicodeWord(string) {
      return reHasUnicodeWord.test(string);
    }
    module.exports = hasUnicodeWord;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_unicodeWords.js
var require_unicodeWords = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_unicodeWords.js"(exports, module) {
    var rsAstralRange = "\\ud800-\\udfff";
    var rsComboMarksRange = "\\u0300-\\u036f";
    var reComboHalfMarksRange = "\\ufe20-\\ufe2f";
    var rsComboSymbolsRange = "\\u20d0-\\u20ff";
    var rsComboRange = rsComboMarksRange + reComboHalfMarksRange + rsComboSymbolsRange;
    var rsDingbatRange = "\\u2700-\\u27bf";
    var rsLowerRange = "a-z\\xdf-\\xf6\\xf8-\\xff";
    var rsMathOpRange = "\\xac\\xb1\\xd7\\xf7";
    var rsNonCharRange = "\\x00-\\x2f\\x3a-\\x40\\x5b-\\x60\\x7b-\\xbf";
    var rsPunctuationRange = "\\u2000-\\u206f";
    var rsSpaceRange = " \\t\\x0b\\f\\xa0\\ufeff\\n\\r\\u2028\\u2029\\u1680\\u180e\\u2000\\u2001\\u2002\\u2003\\u2004\\u2005\\u2006\\u2007\\u2008\\u2009\\u200a\\u202f\\u205f\\u3000";
    var rsUpperRange = "A-Z\\xc0-\\xd6\\xd8-\\xde";
    var rsVarRange = "\\ufe0e\\ufe0f";
    var rsBreakRange = rsMathOpRange + rsNonCharRange + rsPunctuationRange + rsSpaceRange;
    var rsApos = "['’]";
    var rsBreak = "[" + rsBreakRange + "]";
    var rsCombo = "[" + rsComboRange + "]";
    var rsDigits = "\\d+";
    var rsDingbat = "[" + rsDingbatRange + "]";
    var rsLower = "[" + rsLowerRange + "]";
    var rsMisc = "[^" + rsAstralRange + rsBreakRange + rsDigits + rsDingbatRange + rsLowerRange + rsUpperRange + "]";
    var rsFitz = "\\ud83c[\\udffb-\\udfff]";
    var rsModifier = "(?:" + rsCombo + "|" + rsFitz + ")";
    var rsNonAstral = "[^" + rsAstralRange + "]";
    var rsRegional = "(?:\\ud83c[\\udde6-\\uddff]){2}";
    var rsSurrPair = "[\\ud800-\\udbff][\\udc00-\\udfff]";
    var rsUpper = "[" + rsUpperRange + "]";
    var rsZWJ = "\\u200d";
    var rsMiscLower = "(?:" + rsLower + "|" + rsMisc + ")";
    var rsMiscUpper = "(?:" + rsUpper + "|" + rsMisc + ")";
    var rsOptContrLower = "(?:" + rsApos + "(?:d|ll|m|re|s|t|ve))?";
    var rsOptContrUpper = "(?:" + rsApos + "(?:D|LL|M|RE|S|T|VE))?";
    var reOptMod = rsModifier + "?";
    var rsOptVar = "[" + rsVarRange + "]?";
    var rsOptJoin = "(?:" + rsZWJ + "(?:" + [rsNonAstral, rsRegional, rsSurrPair].join("|") + ")" + rsOptVar + reOptMod + ")*";
    var rsOrdLower = "\\d*(?:1st|2nd|3rd|(?![123])\\dth)(?=\\b|[A-Z_])";
    var rsOrdUpper = "\\d*(?:1ST|2ND|3RD|(?![123])\\dTH)(?=\\b|[a-z_])";
    var rsSeq = rsOptVar + reOptMod + rsOptJoin;
    var rsEmoji = "(?:" + [rsDingbat, rsRegional, rsSurrPair].join("|") + ")" + rsSeq;
    var reUnicodeWord = RegExp([
      rsUpper + "?" + rsLower + "+" + rsOptContrLower + "(?=" + [rsBreak, rsUpper, "$"].join("|") + ")",
      rsMiscUpper + "+" + rsOptContrUpper + "(?=" + [rsBreak, rsUpper + rsMiscLower, "$"].join("|") + ")",
      rsUpper + "?" + rsMiscLower + "+" + rsOptContrLower,
      rsUpper + "+" + rsOptContrUpper,
      rsOrdUpper,
      rsOrdLower,
      rsDigits,
      rsEmoji
    ].join("|"), "g");
    function unicodeWords(string) {
      return string.match(reUnicodeWord) || [];
    }
    module.exports = unicodeWords;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/words.js
var require_words = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/words.js"(exports, module) {
    var asciiWords = require_asciiWords();
    var hasUnicodeWord = require_hasUnicodeWord();
    var toString = require_toString2();
    var unicodeWords = require_unicodeWords();
    function words(string, pattern, guard) {
      string = toString(string);
      pattern = guard ? void 0 : pattern;
      if (pattern === void 0) {
        return hasUnicodeWord(string) ? unicodeWords(string) : asciiWords(string);
      }
      return string.match(pattern) || [];
    }
    module.exports = words;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_createCompounder.js
var require_createCompounder = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_createCompounder.js"(exports, module) {
    var arrayReduce = require_arrayReduce();
    var deburr = require_deburr();
    var words = require_words();
    var rsApos = "['’]";
    var reApos = RegExp(rsApos, "g");
    function createCompounder(callback) {
      return function(string) {
        return arrayReduce(words(deburr(string).replace(reApos, "")), callback, "");
      };
    }
    module.exports = createCompounder;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_castSlice.js
var require_castSlice = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_castSlice.js"(exports, module) {
    var baseSlice = require_baseSlice();
    function castSlice(array4, start3, end) {
      var length = array4.length;
      end = end === void 0 ? length : end;
      return !start3 && end >= length ? array4 : baseSlice(array4, start3, end);
    }
    module.exports = castSlice;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_hasUnicode.js
var require_hasUnicode = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_hasUnicode.js"(exports, module) {
    var rsAstralRange = "\\ud800-\\udfff";
    var rsComboMarksRange = "\\u0300-\\u036f";
    var reComboHalfMarksRange = "\\ufe20-\\ufe2f";
    var rsComboSymbolsRange = "\\u20d0-\\u20ff";
    var rsComboRange = rsComboMarksRange + reComboHalfMarksRange + rsComboSymbolsRange;
    var rsVarRange = "\\ufe0e\\ufe0f";
    var rsZWJ = "\\u200d";
    var reHasUnicode = RegExp("[" + rsZWJ + rsAstralRange + rsComboRange + rsVarRange + "]");
    function hasUnicode(string) {
      return reHasUnicode.test(string);
    }
    module.exports = hasUnicode;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_asciiToArray.js
var require_asciiToArray = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_asciiToArray.js"(exports, module) {
    function asciiToArray(string) {
      return string.split("");
    }
    module.exports = asciiToArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_unicodeToArray.js
var require_unicodeToArray = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_unicodeToArray.js"(exports, module) {
    var rsAstralRange = "\\ud800-\\udfff";
    var rsComboMarksRange = "\\u0300-\\u036f";
    var reComboHalfMarksRange = "\\ufe20-\\ufe2f";
    var rsComboSymbolsRange = "\\u20d0-\\u20ff";
    var rsComboRange = rsComboMarksRange + reComboHalfMarksRange + rsComboSymbolsRange;
    var rsVarRange = "\\ufe0e\\ufe0f";
    var rsAstral = "[" + rsAstralRange + "]";
    var rsCombo = "[" + rsComboRange + "]";
    var rsFitz = "\\ud83c[\\udffb-\\udfff]";
    var rsModifier = "(?:" + rsCombo + "|" + rsFitz + ")";
    var rsNonAstral = "[^" + rsAstralRange + "]";
    var rsRegional = "(?:\\ud83c[\\udde6-\\uddff]){2}";
    var rsSurrPair = "[\\ud800-\\udbff][\\udc00-\\udfff]";
    var rsZWJ = "\\u200d";
    var reOptMod = rsModifier + "?";
    var rsOptVar = "[" + rsVarRange + "]?";
    var rsOptJoin = "(?:" + rsZWJ + "(?:" + [rsNonAstral, rsRegional, rsSurrPair].join("|") + ")" + rsOptVar + reOptMod + ")*";
    var rsSeq = rsOptVar + reOptMod + rsOptJoin;
    var rsSymbol = "(?:" + [rsNonAstral + rsCombo + "?", rsCombo, rsRegional, rsSurrPair, rsAstral].join("|") + ")";
    var reUnicode = RegExp(rsFitz + "(?=" + rsFitz + ")|" + rsSymbol + rsSeq, "g");
    function unicodeToArray(string) {
      return string.match(reUnicode) || [];
    }
    module.exports = unicodeToArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_stringToArray.js
var require_stringToArray = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_stringToArray.js"(exports, module) {
    var asciiToArray = require_asciiToArray();
    var hasUnicode = require_hasUnicode();
    var unicodeToArray = require_unicodeToArray();
    function stringToArray(string) {
      return hasUnicode(string) ? unicodeToArray(string) : asciiToArray(string);
    }
    module.exports = stringToArray;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/_createCaseFirst.js
var require_createCaseFirst = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/_createCaseFirst.js"(exports, module) {
    var castSlice = require_castSlice();
    var hasUnicode = require_hasUnicode();
    var stringToArray = require_stringToArray();
    var toString = require_toString2();
    function createCaseFirst(methodName) {
      return function(string) {
        string = toString(string);
        var strSymbols = hasUnicode(string) ? stringToArray(string) : void 0;
        var chr = strSymbols ? strSymbols[0] : string.charAt(0);
        var trailing = strSymbols ? castSlice(strSymbols, 1).join("") : string.slice(1);
        return chr[methodName]() + trailing;
      };
    }
    module.exports = createCaseFirst;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/upperFirst.js
var require_upperFirst = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/upperFirst.js"(exports, module) {
    var createCaseFirst = require_createCaseFirst();
    var upperFirst = createCaseFirst("toUpperCase");
    module.exports = upperFirst;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/startCase.js
var require_startCase = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/startCase.js"(exports, module) {
    var createCompounder = require_createCompounder();
    var upperFirst = require_upperFirst();
    var startCase = createCompounder(function(result, word, index4) {
      return result + (index4 ? " " : "") + upperFirst(word);
    });
    module.exports = startCase;
  }
});

// node_modules/@nivo/treemap/node_modules/lodash/get.js
var require_get2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/lodash/get.js"(exports, module) {
    var baseGet = require_baseGet2();
    function get(object, path2, defaultValue) {
      var result = object == null ? void 0 : baseGet(object, path2);
      return result === void 0 ? defaultValue : result;
    }
    module.exports = get;
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js
var require_react_is_development2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var hasSymbol = typeof Symbol === "function" && Symbol.for;
        var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for("react.element") : 60103;
        var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for("react.portal") : 60106;
        var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for("react.fragment") : 60107;
        var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for("react.strict_mode") : 60108;
        var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for("react.profiler") : 60114;
        var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for("react.provider") : 60109;
        var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for("react.context") : 60110;
        var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for("react.async_mode") : 60111;
        var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for("react.concurrent_mode") : 60111;
        var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for("react.forward_ref") : 60112;
        var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for("react.suspense") : 60113;
        var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for("react.suspense_list") : 60120;
        var REACT_MEMO_TYPE = hasSymbol ? Symbol.for("react.memo") : 60115;
        var REACT_LAZY_TYPE = hasSymbol ? Symbol.for("react.lazy") : 60116;
        var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for("react.block") : 60121;
        var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for("react.fundamental") : 60117;
        var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for("react.responder") : 60118;
        var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for("react.scope") : 60119;
        function isValidElementType(type) {
          return typeof type === "string" || typeof type === "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
          type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
        }
        function typeOf(object) {
          if (typeof object === "object" && object !== null) {
            var $$typeof = object.$$typeof;
            switch ($$typeof) {
              case REACT_ELEMENT_TYPE:
                var type = object.type;
                switch (type) {
                  case REACT_ASYNC_MODE_TYPE:
                  case REACT_CONCURRENT_MODE_TYPE:
                  case REACT_FRAGMENT_TYPE:
                  case REACT_PROFILER_TYPE:
                  case REACT_STRICT_MODE_TYPE:
                  case REACT_SUSPENSE_TYPE:
                    return type;
                  default:
                    var $$typeofType = type && type.$$typeof;
                    switch ($$typeofType) {
                      case REACT_CONTEXT_TYPE:
                      case REACT_FORWARD_REF_TYPE:
                      case REACT_LAZY_TYPE:
                      case REACT_MEMO_TYPE:
                      case REACT_PROVIDER_TYPE:
                        return $$typeofType;
                      default:
                        return $$typeof;
                    }
                }
              case REACT_PORTAL_TYPE:
                return $$typeof;
            }
          }
          return void 0;
        }
        var AsyncMode = REACT_ASYNC_MODE_TYPE;
        var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
        var ContextConsumer = REACT_CONTEXT_TYPE;
        var ContextProvider = REACT_PROVIDER_TYPE;
        var Element = REACT_ELEMENT_TYPE;
        var ForwardRef = REACT_FORWARD_REF_TYPE;
        var Fragment3 = REACT_FRAGMENT_TYPE;
        var Lazy = REACT_LAZY_TYPE;
        var Memo = REACT_MEMO_TYPE;
        var Portal = REACT_PORTAL_TYPE;
        var Profiler = REACT_PROFILER_TYPE;
        var StrictMode = REACT_STRICT_MODE_TYPE;
        var Suspense = REACT_SUSPENSE_TYPE;
        var hasWarnedAboutDeprecatedIsAsyncMode = false;
        function isAsyncMode(object) {
          {
            if (!hasWarnedAboutDeprecatedIsAsyncMode) {
              hasWarnedAboutDeprecatedIsAsyncMode = true;
              console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
            }
          }
          return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
        }
        function isConcurrentMode(object) {
          return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
        }
        function isContextConsumer(object) {
          return typeOf(object) === REACT_CONTEXT_TYPE;
        }
        function isContextProvider(object) {
          return typeOf(object) === REACT_PROVIDER_TYPE;
        }
        function isElement(object) {
          return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
        }
        function isForwardRef(object) {
          return typeOf(object) === REACT_FORWARD_REF_TYPE;
        }
        function isFragment(object) {
          return typeOf(object) === REACT_FRAGMENT_TYPE;
        }
        function isLazy(object) {
          return typeOf(object) === REACT_LAZY_TYPE;
        }
        function isMemo(object) {
          return typeOf(object) === REACT_MEMO_TYPE;
        }
        function isPortal(object) {
          return typeOf(object) === REACT_PORTAL_TYPE;
        }
        function isProfiler(object) {
          return typeOf(object) === REACT_PROFILER_TYPE;
        }
        function isStrictMode(object) {
          return typeOf(object) === REACT_STRICT_MODE_TYPE;
        }
        function isSuspense(object) {
          return typeOf(object) === REACT_SUSPENSE_TYPE;
        }
        exports.AsyncMode = AsyncMode;
        exports.ConcurrentMode = ConcurrentMode;
        exports.ContextConsumer = ContextConsumer;
        exports.ContextProvider = ContextProvider;
        exports.Element = Element;
        exports.ForwardRef = ForwardRef;
        exports.Fragment = Fragment3;
        exports.Lazy = Lazy;
        exports.Memo = Memo;
        exports.Portal = Portal;
        exports.Profiler = Profiler;
        exports.StrictMode = StrictMode;
        exports.Suspense = Suspense;
        exports.isAsyncMode = isAsyncMode;
        exports.isConcurrentMode = isConcurrentMode;
        exports.isContextConsumer = isContextConsumer;
        exports.isContextProvider = isContextProvider;
        exports.isElement = isElement;
        exports.isForwardRef = isForwardRef;
        exports.isFragment = isFragment;
        exports.isLazy = isLazy;
        exports.isMemo = isMemo;
        exports.isPortal = isPortal;
        exports.isProfiler = isProfiler;
        exports.isStrictMode = isStrictMode;
        exports.isSuspense = isSuspense;
        exports.isValidElementType = isValidElementType;
        exports.typeOf = typeOf;
      })();
    }
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/node_modules/react-is/index.js
var require_react_is2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/node_modules/react-is/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_is_development2();
    }
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/node_modules/object-assign/index.js
var require_object_assign2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/node_modules/object-assign/index.js"(exports, module) {
    "use strict";
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var propIsEnumerable = Object.prototype.propertyIsEnumerable;
    function toObject(val) {
      if (val === null || val === void 0) {
        throw new TypeError("Object.assign cannot be called with null or undefined");
      }
      return Object(val);
    }
    function shouldUseNative() {
      try {
        if (!Object.assign) {
          return false;
        }
        var test1 = new String("abc");
        test1[5] = "de";
        if (Object.getOwnPropertyNames(test1)[0] === "5") {
          return false;
        }
        var test2 = {};
        for (var i5 = 0; i5 < 10; i5++) {
          test2["_" + String.fromCharCode(i5)] = i5;
        }
        var order2 = Object.getOwnPropertyNames(test2).map(function(n5) {
          return test2[n5];
        });
        if (order2.join("") !== "0123456789") {
          return false;
        }
        var test3 = {};
        "abcdefghijklmnopqrst".split("").forEach(function(letter) {
          test3[letter] = letter;
        });
        if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") {
          return false;
        }
        return true;
      } catch (err) {
        return false;
      }
    }
    module.exports = shouldUseNative() ? Object.assign : function(target, source) {
      var from;
      var to5 = toObject(target);
      var symbols;
      for (var s3 = 1; s3 < arguments.length; s3++) {
        from = Object(arguments[s3]);
        for (var key in from) {
          if (hasOwnProperty.call(from, key)) {
            to5[key] = from[key];
          }
        }
        if (getOwnPropertySymbols) {
          symbols = getOwnPropertySymbols(from);
          for (var i5 = 0; i5 < symbols.length; i5++) {
            if (propIsEnumerable.call(from, symbols[i5])) {
              to5[symbols[i5]] = from[symbols[i5]];
            }
          }
        }
      }
      return to5;
    };
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/lib/ReactPropTypesSecret.js
var require_ReactPropTypesSecret2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/lib/ReactPropTypesSecret.js"(exports, module) {
    "use strict";
    var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
    module.exports = ReactPropTypesSecret;
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/lib/has.js
var require_has2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/lib/has.js"(exports, module) {
    module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/checkPropTypes.js
var require_checkPropTypes2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/checkPropTypes.js"(exports, module) {
    "use strict";
    var printWarning = function() {
    };
    if (true) {
      ReactPropTypesSecret = require_ReactPropTypesSecret2();
      loggedTypeFailures = {};
      has = require_has2();
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x4) {
        }
      };
    }
    var ReactPropTypesSecret;
    var loggedTypeFailures;
    var has;
    function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
      if (true) {
        for (var typeSpecName in typeSpecs) {
          if (has(typeSpecs, typeSpecName)) {
            var error;
            try {
              if (typeof typeSpecs[typeSpecName] !== "function") {
                var err = Error(
                  (componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
                );
                err.name = "Invariant Violation";
                throw err;
              }
              error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
            } catch (ex) {
              error = ex;
            }
            if (error && !(error instanceof Error)) {
              printWarning(
                (componentName || "React class") + ": type specification of " + location + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
              );
            }
            if (error instanceof Error && !(error.message in loggedTypeFailures)) {
              loggedTypeFailures[error.message] = true;
              var stack = getStack ? getStack() : "";
              printWarning(
                "Failed " + location + " type: " + error.message + (stack != null ? stack : "")
              );
            }
          }
        }
      }
    }
    checkPropTypes.resetWarningCache = function() {
      if (true) {
        loggedTypeFailures = {};
      }
    };
    module.exports = checkPropTypes;
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/factoryWithTypeCheckers.js
var require_factoryWithTypeCheckers2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/factoryWithTypeCheckers.js"(exports, module) {
    "use strict";
    var ReactIs = require_react_is2();
    var assign3 = require_object_assign2();
    var ReactPropTypesSecret = require_ReactPropTypesSecret2();
    var has = require_has2();
    var checkPropTypes = require_checkPropTypes2();
    var printWarning = function() {
    };
    if (true) {
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x4) {
        }
      };
    }
    function emptyFunctionThatReturnsNull() {
      return null;
    }
    module.exports = function(isValidElement, throwOnDirectAccess) {
      var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
      var FAUX_ITERATOR_SYMBOL = "@@iterator";
      function getIteratorFn(maybeIterable) {
        var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
        if (typeof iteratorFn === "function") {
          return iteratorFn;
        }
      }
      var ANONYMOUS = "<<anonymous>>";
      var ReactPropTypes = {
        array: createPrimitiveTypeChecker("array"),
        bigint: createPrimitiveTypeChecker("bigint"),
        bool: createPrimitiveTypeChecker("boolean"),
        func: createPrimitiveTypeChecker("function"),
        number: createPrimitiveTypeChecker("number"),
        object: createPrimitiveTypeChecker("object"),
        string: createPrimitiveTypeChecker("string"),
        symbol: createPrimitiveTypeChecker("symbol"),
        any: createAnyTypeChecker(),
        arrayOf: createArrayOfTypeChecker,
        element: createElementTypeChecker(),
        elementType: createElementTypeTypeChecker(),
        instanceOf: createInstanceTypeChecker,
        node: createNodeChecker(),
        objectOf: createObjectOfTypeChecker,
        oneOf: createEnumTypeChecker,
        oneOfType: createUnionTypeChecker,
        shape: createShapeTypeChecker,
        exact: createStrictShapeTypeChecker
      };
      function is3(x4, y6) {
        if (x4 === y6) {
          return x4 !== 0 || 1 / x4 === 1 / y6;
        } else {
          return x4 !== x4 && y6 !== y6;
        }
      }
      function PropTypeError(message, data) {
        this.message = message;
        this.data = data && typeof data === "object" ? data : {};
        this.stack = "";
      }
      PropTypeError.prototype = Error.prototype;
      function createChainableTypeChecker(validate) {
        if (true) {
          var manualPropTypeCallCache = {};
          var manualPropTypeWarningCount = 0;
        }
        function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
          componentName = componentName || ANONYMOUS;
          propFullName = propFullName || propName;
          if (secret !== ReactPropTypesSecret) {
            if (throwOnDirectAccess) {
              var err = new Error(
                "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
              );
              err.name = "Invariant Violation";
              throw err;
            } else if (typeof console !== "undefined") {
              var cacheKey3 = componentName + ":" + propName;
              if (!manualPropTypeCallCache[cacheKey3] && // Avoid spamming the console because they are often not actionable except for lib authors
              manualPropTypeWarningCount < 3) {
                printWarning(
                  "You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
                );
                manualPropTypeCallCache[cacheKey3] = true;
                manualPropTypeWarningCount++;
              }
            }
          }
          if (props[propName] == null) {
            if (isRequired) {
              if (props[propName] === null) {
                return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
              }
              return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
            }
            return null;
          } else {
            return validate(props, propName, componentName, location, propFullName);
          }
        }
        var chainedCheckType = checkType.bind(null, false);
        chainedCheckType.isRequired = checkType.bind(null, true);
        return chainedCheckType;
      }
      function createPrimitiveTypeChecker(expectedType) {
        function validate(props, propName, componentName, location, propFullName, secret) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== expectedType) {
            var preciseType = getPreciseType(propValue);
            return new PropTypeError(
              "Invalid " + location + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."),
              { expectedType }
            );
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createAnyTypeChecker() {
        return createChainableTypeChecker(emptyFunctionThatReturnsNull);
      }
      function createArrayOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
          }
          var propValue = props[propName];
          if (!Array.isArray(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
          }
          for (var i5 = 0; i5 < propValue.length; i5++) {
            var error = typeChecker(propValue, i5, componentName, location, propFullName + "[" + i5 + "]", ReactPropTypesSecret);
            if (error instanceof Error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!isValidElement(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!ReactIs.isValidElementType(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createInstanceTypeChecker(expectedClass) {
        function validate(props, propName, componentName, location, propFullName) {
          if (!(props[propName] instanceof expectedClass)) {
            var expectedClassName = expectedClass.name || ANONYMOUS;
            var actualClassName = getClassName(props[propName]);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createEnumTypeChecker(expectedValues) {
        if (!Array.isArray(expectedValues)) {
          if (true) {
            if (arguments.length > 1) {
              printWarning(
                "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
              );
            } else {
              printWarning("Invalid argument supplied to oneOf, expected an array.");
            }
          }
          return emptyFunctionThatReturnsNull;
        }
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          for (var i5 = 0; i5 < expectedValues.length; i5++) {
            if (is3(propValue, expectedValues[i5])) {
              return null;
            }
          }
          var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
            var type = getPreciseType(value);
            if (type === "symbol") {
              return String(value);
            }
            return value;
          });
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createObjectOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
          }
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
          }
          for (var key in propValue) {
            if (has(propValue, key)) {
              var error = typeChecker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
              if (error instanceof Error) {
                return error;
              }
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createUnionTypeChecker(arrayOfTypeCheckers) {
        if (!Array.isArray(arrayOfTypeCheckers)) {
          true ? printWarning("Invalid argument supplied to oneOfType, expected an instance of array.") : void 0;
          return emptyFunctionThatReturnsNull;
        }
        for (var i5 = 0; i5 < arrayOfTypeCheckers.length; i5++) {
          var checker = arrayOfTypeCheckers[i5];
          if (typeof checker !== "function") {
            printWarning(
              "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i5 + "."
            );
            return emptyFunctionThatReturnsNull;
          }
        }
        function validate(props, propName, componentName, location, propFullName) {
          var expectedTypes = [];
          for (var i6 = 0; i6 < arrayOfTypeCheckers.length; i6++) {
            var checker2 = arrayOfTypeCheckers[i6];
            var checkerResult = checker2(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
            if (checkerResult == null) {
              return null;
            }
            if (checkerResult.data && has(checkerResult.data, "expectedType")) {
              expectedTypes.push(checkerResult.data.expectedType);
            }
          }
          var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createNodeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          if (!isNode(props[propName])) {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function invalidValidatorError(componentName, location, propFullName, key, type) {
        return new PropTypeError(
          (componentName || "React class") + ": " + location + " type `" + propFullName + "." + key + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`."
        );
      }
      function createShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          for (var key in shapeTypes) {
            var checker = shapeTypes[key];
            if (typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
            }
            var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createStrictShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          var allKeys = assign3({}, props[propName], shapeTypes);
          for (var key in allKeys) {
            var checker = shapeTypes[key];
            if (has(shapeTypes, key) && typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
            }
            if (!checker) {
              return new PropTypeError(
                "Invalid " + location + " `" + propFullName + "` key `" + key + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  ")
              );
            }
            var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function isNode(propValue) {
        switch (typeof propValue) {
          case "number":
          case "string":
          case "undefined":
            return true;
          case "boolean":
            return !propValue;
          case "object":
            if (Array.isArray(propValue)) {
              return propValue.every(isNode);
            }
            if (propValue === null || isValidElement(propValue)) {
              return true;
            }
            var iteratorFn = getIteratorFn(propValue);
            if (iteratorFn) {
              var iterator = iteratorFn.call(propValue);
              var step;
              if (iteratorFn !== propValue.entries) {
                while (!(step = iterator.next()).done) {
                  if (!isNode(step.value)) {
                    return false;
                  }
                }
              } else {
                while (!(step = iterator.next()).done) {
                  var entry = step.value;
                  if (entry) {
                    if (!isNode(entry[1])) {
                      return false;
                    }
                  }
                }
              }
            } else {
              return false;
            }
            return true;
          default:
            return false;
        }
      }
      function isSymbol(propType, propValue) {
        if (propType === "symbol") {
          return true;
        }
        if (!propValue) {
          return false;
        }
        if (propValue["@@toStringTag"] === "Symbol") {
          return true;
        }
        if (typeof Symbol === "function" && propValue instanceof Symbol) {
          return true;
        }
        return false;
      }
      function getPropType(propValue) {
        var propType = typeof propValue;
        if (Array.isArray(propValue)) {
          return "array";
        }
        if (propValue instanceof RegExp) {
          return "object";
        }
        if (isSymbol(propType, propValue)) {
          return "symbol";
        }
        return propType;
      }
      function getPreciseType(propValue) {
        if (typeof propValue === "undefined" || propValue === null) {
          return "" + propValue;
        }
        var propType = getPropType(propValue);
        if (propType === "object") {
          if (propValue instanceof Date) {
            return "date";
          } else if (propValue instanceof RegExp) {
            return "regexp";
          }
        }
        return propType;
      }
      function getPostfixForTypeWarning(value) {
        var type = getPreciseType(value);
        switch (type) {
          case "array":
          case "object":
            return "an " + type;
          case "boolean":
          case "date":
          case "regexp":
            return "a " + type;
          default:
            return type;
        }
      }
      function getClassName(propValue) {
        if (!propValue.constructor || !propValue.constructor.name) {
          return ANONYMOUS;
        }
        return propValue.constructor.name;
      }
      ReactPropTypes.checkPropTypes = checkPropTypes;
      ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
      ReactPropTypes.PropTypes = ReactPropTypes;
      return ReactPropTypes;
    };
  }
});

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/index.js
var require_prop_types2 = __commonJS({
  "node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/prop-types/index.js"(exports, module) {
    if (true) {
      ReactIs = require_react_is2();
      throwOnDirectAccess = true;
      module.exports = require_factoryWithTypeCheckers2()(ReactIs.isElement, throwOnDirectAccess);
    } else {
      module.exports = null();
    }
    var ReactIs;
    var throwOnDirectAccess;
  }
});

// node_modules/@nivo/treemap/dist/nivo-treemap.es.js
var import_react33 = __toESM(require_react());

// node_modules/@nivo/core/dist/nivo-core.es.js
var import_react16 = __toESM(require_react());
var import_prop_types = __toESM(require_prop_types());

// node_modules/@nivo/core/node_modules/@nivo/tooltip/dist/nivo-tooltip.es.js
var import_react15 = __toESM(require_react());

// node_modules/@nivo/core/node_modules/@react-spring/web/node_modules/@react-spring/shared/node_modules/@react-spring/rafz/dist/react-spring_rafz.modern.mjs
var updateQueue = makeQueue();
var raf = (fn2) => schedule(fn2, updateQueue);
var writeQueue = makeQueue();
raf.write = (fn2) => schedule(fn2, writeQueue);
var onStartQueue = makeQueue();
raf.onStart = (fn2) => schedule(fn2, onStartQueue);
var onFrameQueue = makeQueue();
raf.onFrame = (fn2) => schedule(fn2, onFrameQueue);
var onFinishQueue = makeQueue();
raf.onFinish = (fn2) => schedule(fn2, onFinishQueue);
var timeouts = [];
raf.setTimeout = (handler, ms) => {
  const time3 = raf.now() + ms;
  const cancel = () => {
    const i5 = timeouts.findIndex((t7) => t7.cancel == cancel);
    if (~i5)
      timeouts.splice(i5, 1);
    pendingCount -= ~i5 ? 1 : 0;
  };
  const timeout = { time: time3, handler, cancel };
  timeouts.splice(findTimeout(time3), 0, timeout);
  pendingCount += 1;
  start();
  return timeout;
};
var findTimeout = (time3) => ~(~timeouts.findIndex((t7) => t7.time > time3) || ~timeouts.length);
raf.cancel = (fn2) => {
  onStartQueue.delete(fn2);
  onFrameQueue.delete(fn2);
  onFinishQueue.delete(fn2);
  updateQueue.delete(fn2);
  writeQueue.delete(fn2);
};
raf.sync = (fn2) => {
  sync = true;
  raf.batchedUpdates(fn2);
  sync = false;
};
raf.throttle = (fn2) => {
  let lastArgs;
  function queuedFn() {
    try {
      fn2(...lastArgs);
    } finally {
      lastArgs = null;
    }
  }
  function throttled(...args) {
    lastArgs = args;
    raf.onStart(queuedFn);
  }
  throttled.handler = fn2;
  throttled.cancel = () => {
    onStartQueue.delete(queuedFn);
    lastArgs = null;
  };
  return throttled;
};
var nativeRaf = typeof window != "undefined" ? window.requestAnimationFrame : (
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  () => {
  }
);
raf.use = (impl) => nativeRaf = impl;
raf.now = typeof performance != "undefined" ? () => performance.now() : Date.now;
raf.batchedUpdates = (fn2) => fn2();
raf.catch = console.error;
raf.frameLoop = "always";
raf.advance = () => {
  if (raf.frameLoop !== "demand") {
    console.warn(
      "Cannot call the manual advancement of rafz whilst frameLoop is not set as demand"
    );
  } else {
    update();
  }
};
var ts = -1;
var pendingCount = 0;
var sync = false;
function schedule(fn2, queue) {
  if (sync) {
    queue.delete(fn2);
    fn2(0);
  } else {
    queue.add(fn2);
    start();
  }
}
function start() {
  if (ts < 0) {
    ts = 0;
    if (raf.frameLoop !== "demand") {
      nativeRaf(loop);
    }
  }
}
function stop() {
  ts = -1;
}
function loop() {
  if (~ts) {
    nativeRaf(loop);
    raf.batchedUpdates(update);
  }
}
function update() {
  const prevTs = ts;
  ts = raf.now();
  const count5 = findTimeout(ts);
  if (count5) {
    eachSafely(timeouts.splice(0, count5), (t7) => t7.handler());
    pendingCount -= count5;
  }
  if (!pendingCount) {
    stop();
    return;
  }
  onStartQueue.flush();
  updateQueue.flush(prevTs ? Math.min(64, ts - prevTs) : 16.667);
  onFrameQueue.flush();
  writeQueue.flush();
  onFinishQueue.flush();
}
function makeQueue() {
  let next = /* @__PURE__ */ new Set();
  let current = next;
  return {
    add(fn2) {
      pendingCount += current == next && !next.has(fn2) ? 1 : 0;
      next.add(fn2);
    },
    delete(fn2) {
      pendingCount -= current == next && next.has(fn2) ? 1 : 0;
      return next.delete(fn2);
    },
    flush(arg) {
      if (current.size) {
        next = /* @__PURE__ */ new Set();
        pendingCount -= current.size;
        eachSafely(current, (fn2) => fn2(arg) && next.add(fn2));
        pendingCount += next.size;
        current = next;
      }
    }
  };
}
function eachSafely(values, each3) {
  values.forEach((value) => {
    try {
      each3(value);
    } catch (e7) {
      raf.catch(e7);
    }
  });
}

// node_modules/@nivo/core/node_modules/@react-spring/web/node_modules/@react-spring/shared/dist/react-spring_shared.modern.mjs
var import_react = __toESM(require_react(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_react3 = __toESM(require_react(), 1);
var import_react4 = __toESM(require_react(), 1);
var import_react5 = __toESM(require_react(), 1);
var import_react6 = __toESM(require_react(), 1);
var import_react7 = __toESM(require_react(), 1);
var import_react8 = __toESM(require_react(), 1);
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var globals_exports = {};
__export(globals_exports, {
  assign: () => assign,
  colors: () => colors,
  createStringInterpolator: () => createStringInterpolator,
  skipAnimation: () => skipAnimation,
  to: () => to,
  willAdvance: () => willAdvance
});
function noop() {
}
var defineHidden = (obj, key, value) => Object.defineProperty(obj, key, { value, writable: true, configurable: true });
var is = {
  arr: Array.isArray,
  obj: (a4) => !!a4 && a4.constructor.name === "Object",
  fun: (a4) => typeof a4 === "function",
  str: (a4) => typeof a4 === "string",
  num: (a4) => typeof a4 === "number",
  und: (a4) => a4 === void 0
};
function isEqual(a4, b3) {
  if (is.arr(a4)) {
    if (!is.arr(b3) || a4.length !== b3.length)
      return false;
    for (let i5 = 0; i5 < a4.length; i5++) {
      if (a4[i5] !== b3[i5])
        return false;
    }
    return true;
  }
  return a4 === b3;
}
var each = (obj, fn2) => obj.forEach(fn2);
function eachProp(obj, fn2, ctx3) {
  if (is.arr(obj)) {
    for (let i5 = 0; i5 < obj.length; i5++) {
      fn2.call(ctx3, obj[i5], `${i5}`);
    }
    return;
  }
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      fn2.call(ctx3, obj[key], key);
    }
  }
}
var toArray = (a4) => is.und(a4) ? [] : is.arr(a4) ? a4 : [a4];
function flush(queue, iterator) {
  if (queue.size) {
    const items = Array.from(queue);
    queue.clear();
    each(items, iterator);
  }
}
var flushCalls = (queue, ...args) => flush(queue, (fn2) => fn2(...args));
var isSSR = () => typeof window === "undefined" || !window.navigator || /ServerSideRendering|^Deno\//.test(window.navigator.userAgent);
var createStringInterpolator;
var to;
var colors = null;
var skipAnimation = false;
var willAdvance = noop;
var assign = (globals) => {
  if (globals.to)
    to = globals.to;
  if (globals.now)
    raf.now = globals.now;
  if (globals.colors !== void 0)
    colors = globals.colors;
  if (globals.skipAnimation != null)
    skipAnimation = globals.skipAnimation;
  if (globals.createStringInterpolator)
    createStringInterpolator = globals.createStringInterpolator;
  if (globals.requestAnimationFrame)
    raf.use(globals.requestAnimationFrame);
  if (globals.batchedUpdates)
    raf.batchedUpdates = globals.batchedUpdates;
  if (globals.willAdvance)
    willAdvance = globals.willAdvance;
  if (globals.frameLoop)
    raf.frameLoop = globals.frameLoop;
};
var startQueue = /* @__PURE__ */ new Set();
var currentFrame = [];
var prevFrame = [];
var priority = 0;
var frameLoop = {
  get idle() {
    return !startQueue.size && !currentFrame.length;
  },
  /** Advance the given animation on every frame until idle. */
  start(animation) {
    if (priority > animation.priority) {
      startQueue.add(animation);
      raf.onStart(flushStartQueue);
    } else {
      startSafely(animation);
      raf(advance);
    }
  },
  /** Advance all animations by the given time. */
  advance,
  /** Call this when an animation's priority changes. */
  sort(animation) {
    if (priority) {
      raf.onFrame(() => frameLoop.sort(animation));
    } else {
      const prevIndex = currentFrame.indexOf(animation);
      if (~prevIndex) {
        currentFrame.splice(prevIndex, 1);
        startUnsafely(animation);
      }
    }
  },
  /**
   * Clear all animations. For testing purposes.
   *
   * ☠️ Never call this from within the frameloop.
   */
  clear() {
    currentFrame = [];
    startQueue.clear();
  }
};
function flushStartQueue() {
  startQueue.forEach(startSafely);
  startQueue.clear();
  raf(advance);
}
function startSafely(animation) {
  if (!currentFrame.includes(animation))
    startUnsafely(animation);
}
function startUnsafely(animation) {
  currentFrame.splice(
    findIndex(currentFrame, (other) => other.priority > animation.priority),
    0,
    animation
  );
}
function advance(dt2) {
  const nextFrame = prevFrame;
  for (let i5 = 0; i5 < currentFrame.length; i5++) {
    const animation = currentFrame[i5];
    priority = animation.priority;
    if (!animation.idle) {
      willAdvance(animation);
      animation.advance(dt2);
      if (!animation.idle) {
        nextFrame.push(animation);
      }
    }
  }
  priority = 0;
  prevFrame = currentFrame;
  prevFrame.length = 0;
  currentFrame = nextFrame;
  return currentFrame.length > 0;
}
function findIndex(arr, test) {
  const index4 = arr.findIndex(test);
  return index4 < 0 ? arr.length : index4;
}
var clamp = (min5, max5, v4) => Math.min(Math.max(v4, min5), max5);
var colors2 = {
  transparent: 0,
  aliceblue: 4042850303,
  antiquewhite: 4209760255,
  aqua: 16777215,
  aquamarine: 2147472639,
  azure: 4043309055,
  beige: 4126530815,
  bisque: 4293182719,
  black: 255,
  blanchedalmond: 4293643775,
  blue: 65535,
  blueviolet: 2318131967,
  brown: 2771004159,
  burlywood: 3736635391,
  burntsienna: 3934150143,
  cadetblue: 1604231423,
  chartreuse: 2147418367,
  chocolate: 3530104575,
  coral: 4286533887,
  cornflowerblue: 1687547391,
  cornsilk: 4294499583,
  crimson: 3692313855,
  cyan: 16777215,
  darkblue: 35839,
  darkcyan: 9145343,
  darkgoldenrod: 3095792639,
  darkgray: 2846468607,
  darkgreen: 6553855,
  darkgrey: 2846468607,
  darkkhaki: 3182914559,
  darkmagenta: 2332068863,
  darkolivegreen: 1433087999,
  darkorange: 4287365375,
  darkorchid: 2570243327,
  darkred: 2332033279,
  darksalmon: 3918953215,
  darkseagreen: 2411499519,
  darkslateblue: 1211993087,
  darkslategray: 793726975,
  darkslategrey: 793726975,
  darkturquoise: 13554175,
  darkviolet: 2483082239,
  deeppink: 4279538687,
  deepskyblue: 12582911,
  dimgray: 1768516095,
  dimgrey: 1768516095,
  dodgerblue: 512819199,
  firebrick: 2988581631,
  floralwhite: 4294635775,
  forestgreen: 579543807,
  fuchsia: 4278255615,
  gainsboro: 3705462015,
  ghostwhite: 4177068031,
  gold: 4292280575,
  goldenrod: 3668254975,
  gray: 2155905279,
  green: 8388863,
  greenyellow: 2919182335,
  grey: 2155905279,
  honeydew: 4043305215,
  hotpink: 4285117695,
  indianred: 3445382399,
  indigo: 1258324735,
  ivory: 4294963455,
  khaki: 4041641215,
  lavender: 3873897215,
  lavenderblush: 4293981695,
  lawngreen: 2096890111,
  lemonchiffon: 4294626815,
  lightblue: 2916673279,
  lightcoral: 4034953471,
  lightcyan: 3774873599,
  lightgoldenrodyellow: 4210742015,
  lightgray: 3553874943,
  lightgreen: 2431553791,
  lightgrey: 3553874943,
  lightpink: 4290167295,
  lightsalmon: 4288707327,
  lightseagreen: 548580095,
  lightskyblue: 2278488831,
  lightslategray: 2005441023,
  lightslategrey: 2005441023,
  lightsteelblue: 2965692159,
  lightyellow: 4294959359,
  lime: 16711935,
  limegreen: 852308735,
  linen: 4210091775,
  magenta: 4278255615,
  maroon: 2147483903,
  mediumaquamarine: 1724754687,
  mediumblue: 52735,
  mediumorchid: 3126187007,
  mediumpurple: 2473647103,
  mediumseagreen: 1018393087,
  mediumslateblue: 2070474495,
  mediumspringgreen: 16423679,
  mediumturquoise: 1221709055,
  mediumvioletred: 3340076543,
  midnightblue: 421097727,
  mintcream: 4127193855,
  mistyrose: 4293190143,
  moccasin: 4293178879,
  navajowhite: 4292783615,
  navy: 33023,
  oldlace: 4260751103,
  olive: 2155872511,
  olivedrab: 1804477439,
  orange: 4289003775,
  orangered: 4282712319,
  orchid: 3664828159,
  palegoldenrod: 4008225535,
  palegreen: 2566625535,
  paleturquoise: 2951671551,
  palevioletred: 3681588223,
  papayawhip: 4293907967,
  peachpuff: 4292524543,
  peru: 3448061951,
  pink: 4290825215,
  plum: 3718307327,
  powderblue: 2967529215,
  purple: 2147516671,
  rebeccapurple: 1714657791,
  red: 4278190335,
  rosybrown: 3163525119,
  royalblue: 1097458175,
  saddlebrown: 2336560127,
  salmon: 4202722047,
  sandybrown: 4104413439,
  seagreen: 780883967,
  seashell: 4294307583,
  sienna: 2689740287,
  silver: 3233857791,
  skyblue: 2278484991,
  slateblue: 1784335871,
  slategray: 1887473919,
  slategrey: 1887473919,
  snow: 4294638335,
  springgreen: 16744447,
  steelblue: 1182971135,
  tan: 3535047935,
  teal: 8421631,
  thistle: 3636451583,
  tomato: 4284696575,
  turquoise: 1088475391,
  violet: 4001558271,
  wheat: 4125012991,
  white: 4294967295,
  whitesmoke: 4126537215,
  yellow: 4294902015,
  yellowgreen: 2597139199
};
var NUMBER = "[-+]?\\d*\\.?\\d+";
var PERCENTAGE = NUMBER + "%";
function call(...parts) {
  return "\\(\\s*(" + parts.join(")\\s*,\\s*(") + ")\\s*\\)";
}
var rgb = new RegExp("rgb" + call(NUMBER, NUMBER, NUMBER));
var rgba = new RegExp("rgba" + call(NUMBER, NUMBER, NUMBER, NUMBER));
var hsl = new RegExp("hsl" + call(NUMBER, PERCENTAGE, PERCENTAGE));
var hsla = new RegExp(
  "hsla" + call(NUMBER, PERCENTAGE, PERCENTAGE, NUMBER)
);
var hex3 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex4 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex6 = /^#([0-9a-fA-F]{6})$/;
var hex8 = /^#([0-9a-fA-F]{8})$/;
function normalizeColor(color3) {
  let match;
  if (typeof color3 === "number") {
    return color3 >>> 0 === color3 && color3 >= 0 && color3 <= 4294967295 ? color3 : null;
  }
  if (match = hex6.exec(color3))
    return parseInt(match[1] + "ff", 16) >>> 0;
  if (colors && colors[color3] !== void 0) {
    return colors[color3];
  }
  if (match = rgb.exec(color3)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    255) >>> // a
    0;
  }
  if (match = rgba.exec(color3)) {
    return (parse255(match[1]) << 24 | // r
    parse255(match[2]) << 16 | // g
    parse255(match[3]) << 8 | // b
    parse1(match[4])) >>> // a
    0;
  }
  if (match = hex3.exec(color3)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      "ff",
      // a
      16
    ) >>> 0;
  }
  if (match = hex8.exec(color3))
    return parseInt(match[1], 16) >>> 0;
  if (match = hex4.exec(color3)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      match[4] + match[4],
      // a
      16
    ) >>> 0;
  }
  if (match = hsl.exec(color3)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | 255) >>> // a
    0;
  }
  if (match = hsla.exec(color3)) {
    return (hslToRgb(
      parse360(match[1]),
      // h
      parsePercentage(match[2]),
      // s
      parsePercentage(match[3])
      // l
    ) | parse1(match[4])) >>> // a
    0;
  }
  return null;
}
function hue2rgb(p3, q3, t7) {
  if (t7 < 0)
    t7 += 1;
  if (t7 > 1)
    t7 -= 1;
  if (t7 < 1 / 6)
    return p3 + (q3 - p3) * 6 * t7;
  if (t7 < 1 / 2)
    return q3;
  if (t7 < 2 / 3)
    return p3 + (q3 - p3) * (2 / 3 - t7) * 6;
  return p3;
}
function hslToRgb(h, s3, l4) {
  const q3 = l4 < 0.5 ? l4 * (1 + s3) : l4 + s3 - l4 * s3;
  const p3 = 2 * l4 - q3;
  const r5 = hue2rgb(p3, q3, h + 1 / 3);
  const g3 = hue2rgb(p3, q3, h);
  const b3 = hue2rgb(p3, q3, h - 1 / 3);
  return Math.round(r5 * 255) << 24 | Math.round(g3 * 255) << 16 | Math.round(b3 * 255) << 8;
}
function parse255(str) {
  const int = parseInt(str, 10);
  if (int < 0)
    return 0;
  if (int > 255)
    return 255;
  return int;
}
function parse360(str) {
  const int = parseFloat(str);
  return (int % 360 + 360) % 360 / 360;
}
function parse1(str) {
  const num = parseFloat(str);
  if (num < 0)
    return 0;
  if (num > 1)
    return 255;
  return Math.round(num * 255);
}
function parsePercentage(str) {
  const int = parseFloat(str);
  if (int < 0)
    return 0;
  if (int > 100)
    return 1;
  return int / 100;
}
function colorToRgba(input) {
  let int32Color = normalizeColor(input);
  if (int32Color === null)
    return input;
  int32Color = int32Color || 0;
  const r5 = (int32Color & 4278190080) >>> 24;
  const g3 = (int32Color & 16711680) >>> 16;
  const b3 = (int32Color & 65280) >>> 8;
  const a4 = (int32Color & 255) / 255;
  return `rgba(${r5}, ${g3}, ${b3}, ${a4})`;
}
var createInterpolator = (range3, output, extrapolate) => {
  if (is.fun(range3)) {
    return range3;
  }
  if (is.arr(range3)) {
    return createInterpolator({
      range: range3,
      output,
      extrapolate
    });
  }
  if (is.str(range3.output[0])) {
    return createStringInterpolator(range3);
  }
  const config3 = range3;
  const outputRange = config3.output;
  const inputRange = config3.range || [0, 1];
  const extrapolateLeft = config3.extrapolateLeft || config3.extrapolate || "extend";
  const extrapolateRight = config3.extrapolateRight || config3.extrapolate || "extend";
  const easing = config3.easing || ((t7) => t7);
  return (input) => {
    const range22 = findRange(input, inputRange);
    return interpolate(
      input,
      inputRange[range22],
      inputRange[range22 + 1],
      outputRange[range22],
      outputRange[range22 + 1],
      easing,
      extrapolateLeft,
      extrapolateRight,
      config3.map
    );
  };
};
function interpolate(input, inputMin, inputMax, outputMin, outputMax, easing, extrapolateLeft, extrapolateRight, map9) {
  let result = map9 ? map9(input) : input;
  if (result < inputMin) {
    if (extrapolateLeft === "identity")
      return result;
    else if (extrapolateLeft === "clamp")
      result = inputMin;
  }
  if (result > inputMax) {
    if (extrapolateRight === "identity")
      return result;
    else if (extrapolateRight === "clamp")
      result = inputMax;
  }
  if (outputMin === outputMax)
    return outputMin;
  if (inputMin === inputMax)
    return input <= inputMin ? outputMin : outputMax;
  if (inputMin === -Infinity)
    result = -result;
  else if (inputMax === Infinity)
    result = result - inputMin;
  else
    result = (result - inputMin) / (inputMax - inputMin);
  result = easing(result);
  if (outputMin === -Infinity)
    result = -result;
  else if (outputMax === Infinity)
    result = result + outputMin;
  else
    result = result * (outputMax - outputMin) + outputMin;
  return result;
}
function findRange(input, inputRange) {
  for (var i5 = 1; i5 < inputRange.length - 1; ++i5)
    if (inputRange[i5] >= input)
      break;
  return i5 - 1;
}
var steps = (steps22, direction = "end") => (progress2) => {
  progress2 = direction === "end" ? Math.min(progress2, 0.999) : Math.max(progress2, 1e-3);
  const expanded = progress2 * steps22;
  const rounded = direction === "end" ? Math.floor(expanded) : Math.ceil(expanded);
  return clamp(0, 1, rounded / steps22);
};
var c1 = 1.70158;
var c2 = c1 * 1.525;
var c3 = c1 + 1;
var c4 = 2 * Math.PI / 3;
var c5 = 2 * Math.PI / 4.5;
var bounceOut = (x4) => {
  const n1 = 7.5625;
  const d1 = 2.75;
  if (x4 < 1 / d1) {
    return n1 * x4 * x4;
  } else if (x4 < 2 / d1) {
    return n1 * (x4 -= 1.5 / d1) * x4 + 0.75;
  } else if (x4 < 2.5 / d1) {
    return n1 * (x4 -= 2.25 / d1) * x4 + 0.9375;
  } else {
    return n1 * (x4 -= 2.625 / d1) * x4 + 0.984375;
  }
};
var easings = {
  linear: (x4) => x4,
  easeInQuad: (x4) => x4 * x4,
  easeOutQuad: (x4) => 1 - (1 - x4) * (1 - x4),
  easeInOutQuad: (x4) => x4 < 0.5 ? 2 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 2) / 2,
  easeInCubic: (x4) => x4 * x4 * x4,
  easeOutCubic: (x4) => 1 - Math.pow(1 - x4, 3),
  easeInOutCubic: (x4) => x4 < 0.5 ? 4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 3) / 2,
  easeInQuart: (x4) => x4 * x4 * x4 * x4,
  easeOutQuart: (x4) => 1 - Math.pow(1 - x4, 4),
  easeInOutQuart: (x4) => x4 < 0.5 ? 8 * x4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 4) / 2,
  easeInQuint: (x4) => x4 * x4 * x4 * x4 * x4,
  easeOutQuint: (x4) => 1 - Math.pow(1 - x4, 5),
  easeInOutQuint: (x4) => x4 < 0.5 ? 16 * x4 * x4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 5) / 2,
  easeInSine: (x4) => 1 - Math.cos(x4 * Math.PI / 2),
  easeOutSine: (x4) => Math.sin(x4 * Math.PI / 2),
  easeInOutSine: (x4) => -(Math.cos(Math.PI * x4) - 1) / 2,
  easeInExpo: (x4) => x4 === 0 ? 0 : Math.pow(2, 10 * x4 - 10),
  easeOutExpo: (x4) => x4 === 1 ? 1 : 1 - Math.pow(2, -10 * x4),
  easeInOutExpo: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : x4 < 0.5 ? Math.pow(2, 20 * x4 - 10) / 2 : (2 - Math.pow(2, -20 * x4 + 10)) / 2,
  easeInCirc: (x4) => 1 - Math.sqrt(1 - Math.pow(x4, 2)),
  easeOutCirc: (x4) => Math.sqrt(1 - Math.pow(x4 - 1, 2)),
  easeInOutCirc: (x4) => x4 < 0.5 ? (1 - Math.sqrt(1 - Math.pow(2 * x4, 2))) / 2 : (Math.sqrt(1 - Math.pow(-2 * x4 + 2, 2)) + 1) / 2,
  easeInBack: (x4) => c3 * x4 * x4 * x4 - c1 * x4 * x4,
  easeOutBack: (x4) => 1 + c3 * Math.pow(x4 - 1, 3) + c1 * Math.pow(x4 - 1, 2),
  easeInOutBack: (x4) => x4 < 0.5 ? Math.pow(2 * x4, 2) * ((c2 + 1) * 2 * x4 - c2) / 2 : (Math.pow(2 * x4 - 2, 2) * ((c2 + 1) * (x4 * 2 - 2) + c2) + 2) / 2,
  easeInElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : -Math.pow(2, 10 * x4 - 10) * Math.sin((x4 * 10 - 10.75) * c4),
  easeOutElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : Math.pow(2, -10 * x4) * Math.sin((x4 * 10 - 0.75) * c4) + 1,
  easeInOutElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : x4 < 0.5 ? -(Math.pow(2, 20 * x4 - 10) * Math.sin((20 * x4 - 11.125) * c5)) / 2 : Math.pow(2, -20 * x4 + 10) * Math.sin((20 * x4 - 11.125) * c5) / 2 + 1,
  easeInBounce: (x4) => 1 - bounceOut(1 - x4),
  easeOutBounce: bounceOut,
  easeInOutBounce: (x4) => x4 < 0.5 ? (1 - bounceOut(1 - 2 * x4)) / 2 : (1 + bounceOut(2 * x4 - 1)) / 2,
  steps
};
var $get = Symbol.for("FluidValue.get");
var $observers = Symbol.for("FluidValue.observers");
var hasFluidValue = (arg) => Boolean(arg && arg[$get]);
var getFluidValue = (arg) => arg && arg[$get] ? arg[$get]() : arg;
var getFluidObservers = (target) => target[$observers] || null;
function callFluidObserver(observer2, event) {
  if (observer2.eventObserved) {
    observer2.eventObserved(event);
  } else {
    observer2(event);
  }
}
function callFluidObservers(target, event) {
  const observers = target[$observers];
  if (observers) {
    observers.forEach((observer2) => {
      callFluidObserver(observer2, event);
    });
  }
}
var FluidValue = class {
  constructor(get) {
    if (!get && !(get = this.get)) {
      throw Error("Unknown getter");
    }
    setFluidGetter(this, get);
  }
};
var setFluidGetter = (target, get) => setHidden(target, $get, get);
function addFluidObserver(target, observer2) {
  if (target[$get]) {
    let observers = target[$observers];
    if (!observers) {
      setHidden(target, $observers, observers = /* @__PURE__ */ new Set());
    }
    if (!observers.has(observer2)) {
      observers.add(observer2);
      if (target.observerAdded) {
        target.observerAdded(observers.size, observer2);
      }
    }
  }
  return observer2;
}
function removeFluidObserver(target, observer2) {
  const observers = target[$observers];
  if (observers && observers.has(observer2)) {
    const count5 = observers.size - 1;
    if (count5) {
      observers.delete(observer2);
    } else {
      target[$observers] = null;
    }
    if (target.observerRemoved) {
      target.observerRemoved(count5, observer2);
    }
  }
}
var setHidden = (target, key, value) => Object.defineProperty(target, key, {
  value,
  writable: true,
  configurable: true
});
var numberRegex = /[+\-]?(?:0|[1-9]\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?/g;
var colorRegex = /(#(?:[0-9a-f]{2}){2,4}|(#[0-9a-f]{3})|(rgb|hsl)a?\((-?\d+%?[,\s]+){2,3}\s*[\d\.]+%?\))/gi;
var unitRegex = new RegExp(`(${numberRegex.source})(%|[a-z]+)`, "i");
var rgbaRegex = /rgba\(([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+)\)/gi;
var cssVariableRegex = /var\((--[a-zA-Z0-9-_]+),? ?([a-zA-Z0-9 ()%#.,-]+)?\)/;
var variableToRgba = (input) => {
  const [token, fallback] = parseCSSVariable(input);
  if (!token || isSSR()) {
    return input;
  }
  const value = window.getComputedStyle(document.documentElement).getPropertyValue(token);
  if (value) {
    return value.trim();
  } else if (fallback && fallback.startsWith("--")) {
    const value2 = window.getComputedStyle(document.documentElement).getPropertyValue(fallback);
    if (value2) {
      return value2;
    } else {
      return input;
    }
  } else if (fallback && cssVariableRegex.test(fallback)) {
    return variableToRgba(fallback);
  } else if (fallback) {
    return fallback;
  }
  return input;
};
var parseCSSVariable = (current) => {
  const match = cssVariableRegex.exec(current);
  if (!match)
    return [,];
  const [, token, fallback] = match;
  return [token, fallback];
};
var namedColorRegex;
var rgbaRound = (_2, p1, p22, p3, p4) => `rgba(${Math.round(p1)}, ${Math.round(p22)}, ${Math.round(p3)}, ${p4})`;
var createStringInterpolator2 = (config3) => {
  if (!namedColorRegex)
    namedColorRegex = colors ? (
      // match color names, ignore partial matches
      new RegExp(`(${Object.keys(colors).join("|")})(?!\\w)`, "g")
    ) : (
      // never match
      /^\b$/
    );
  const output = config3.output.map((value) => {
    return getFluidValue(value).replace(cssVariableRegex, variableToRgba).replace(colorRegex, colorToRgba).replace(namedColorRegex, colorToRgba);
  });
  const keyframes = output.map((value) => value.match(numberRegex).map(Number));
  const outputRanges = keyframes[0].map(
    (_2, i5) => keyframes.map((values) => {
      if (!(i5 in values)) {
        throw Error('The arity of each "output" value must be equal');
      }
      return values[i5];
    })
  );
  const interpolators = outputRanges.map(
    (output2) => createInterpolator({ ...config3, output: output2 })
  );
  return (input) => {
    var _a;
    const missingUnit = !unitRegex.test(output[0]) && ((_a = output.find((value) => unitRegex.test(value))) == null ? void 0 : _a.replace(numberRegex, ""));
    let i5 = 0;
    return output[0].replace(
      numberRegex,
      () => `${interpolators[i5++](input)}${missingUnit || ""}`
    ).replace(rgbaRegex, rgbaRound);
  };
};
var prefix = "react-spring: ";
var once = (fn2) => {
  const func = fn2;
  let called = false;
  if (typeof func != "function") {
    throw new TypeError(`${prefix}once requires a function parameter`);
  }
  return (...args) => {
    if (!called) {
      func(...args);
      called = true;
    }
  };
};
var warnInterpolate = once(console.warn);
function deprecateInterpolate() {
  warnInterpolate(
    `${prefix}The "interpolate" function is deprecated in v9 (use "to" instead)`
  );
}
var warnDirectCall = once(console.warn);
function deprecateDirectCall() {
  warnDirectCall(
    `${prefix}Directly calling start instead of using the api object is deprecated in v9 (use ".start" instead), this will be removed in later 0.X.0 versions`
  );
}
function isAnimatedString(value) {
  return is.str(value) && (value[0] == "#" || /\d/.test(value) || // Do not identify a CSS variable as an AnimatedString if its SSR
  !isSSR() && cssVariableRegex.test(value) || value in (colors || {}));
}
var useIsomorphicLayoutEffect = isSSR() ? import_react4.useEffect : import_react4.useLayoutEffect;
var useIsMounted = () => {
  const isMounted = (0, import_react3.useRef)(false);
  useIsomorphicLayoutEffect(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};
function useForceUpdate() {
  const update5 = (0, import_react2.useState)()[1];
  const isMounted = useIsMounted();
  return () => {
    if (isMounted.current) {
      update5(Math.random());
    }
  };
}
function useMemoOne(getResult, inputs) {
  const [initial] = (0, import_react5.useState)(
    () => ({
      inputs,
      result: getResult()
    })
  );
  const committed = (0, import_react5.useRef)();
  const prevCache = committed.current;
  let cache = prevCache;
  if (cache) {
    const useCache = Boolean(
      inputs && cache.inputs && areInputsEqual(inputs, cache.inputs)
    );
    if (!useCache) {
      cache = {
        inputs,
        result: getResult()
      };
    }
  } else {
    cache = initial;
  }
  (0, import_react5.useEffect)(() => {
    committed.current = cache;
    if (prevCache == initial) {
      initial.inputs = initial.result = void 0;
    }
  }, [cache]);
  return cache.result;
}
function areInputsEqual(next, prev) {
  if (next.length !== prev.length) {
    return false;
  }
  for (let i5 = 0; i5 < next.length; i5++) {
    if (next[i5] !== prev[i5]) {
      return false;
    }
  }
  return true;
}
var useOnce = (effect) => (0, import_react6.useEffect)(effect, emptyDeps);
var emptyDeps = [];
function usePrev(value) {
  const prevRef = (0, import_react7.useRef)();
  (0, import_react7.useEffect)(() => {
    prevRef.current = value;
  });
  return prevRef.current;
}

// node_modules/@nivo/core/node_modules/@react-spring/web/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var import_react10 = __toESM(require_react(), 1);

// node_modules/@nivo/core/node_modules/@react-spring/web/node_modules/@react-spring/animated/dist/react-spring_animated.modern.mjs
var React = __toESM(require_react(), 1);
var import_react9 = __toESM(require_react(), 1);
var $node = Symbol.for("Animated:node");
var isAnimated = (value) => !!value && value[$node] === value;
var getAnimated = (owner) => owner && owner[$node];
var setAnimated = (owner, node) => defineHidden(owner, $node, node);
var getPayload = (owner) => owner && owner[$node] && owner[$node].getPayload();
var Animated = class {
  constructor() {
    setAnimated(this, this);
  }
  /** Get every `AnimatedValue` used by this node. */
  getPayload() {
    return this.payload || [];
  }
};
var AnimatedValue = class extends Animated {
  constructor(_value) {
    super();
    this._value = _value;
    this.done = true;
    this.durationProgress = 0;
    if (is.num(this._value)) {
      this.lastPosition = this._value;
    }
  }
  /** @internal */
  static create(value) {
    return new AnimatedValue(value);
  }
  getPayload() {
    return [this];
  }
  getValue() {
    return this._value;
  }
  setValue(value, step) {
    if (is.num(value)) {
      this.lastPosition = value;
      if (step) {
        value = Math.round(value / step) * step;
        if (this.done) {
          this.lastPosition = value;
        }
      }
    }
    if (this._value === value) {
      return false;
    }
    this._value = value;
    return true;
  }
  reset() {
    const { done } = this;
    this.done = false;
    if (is.num(this._value)) {
      this.elapsedTime = 0;
      this.durationProgress = 0;
      this.lastPosition = this._value;
      if (done)
        this.lastVelocity = null;
      this.v0 = null;
    }
  }
};
var AnimatedString = class extends AnimatedValue {
  constructor(value) {
    super(0);
    this._string = null;
    this._toString = createInterpolator({
      output: [value, value]
    });
  }
  /** @internal */
  static create(value) {
    return new AnimatedString(value);
  }
  getValue() {
    const value = this._string;
    return value == null ? this._string = this._toString(this._value) : value;
  }
  setValue(value) {
    if (is.str(value)) {
      if (value == this._string) {
        return false;
      }
      this._string = value;
      this._value = 1;
    } else if (super.setValue(value)) {
      this._string = null;
    } else {
      return false;
    }
    return true;
  }
  reset(goal) {
    if (goal) {
      this._toString = createInterpolator({
        output: [this.getValue(), goal]
      });
    }
    this._value = 0;
    super.reset();
  }
};
var TreeContext = { dependencies: null };
var AnimatedObject = class extends Animated {
  constructor(source) {
    super();
    this.source = source;
    this.setValue(source);
  }
  getValue(animated3) {
    const values = {};
    eachProp(this.source, (source, key) => {
      if (isAnimated(source)) {
        values[key] = source.getValue(animated3);
      } else if (hasFluidValue(source)) {
        values[key] = getFluidValue(source);
      } else if (!animated3) {
        values[key] = source;
      }
    });
    return values;
  }
  /** Replace the raw object data */
  setValue(source) {
    this.source = source;
    this.payload = this._makePayload(source);
  }
  reset() {
    if (this.payload) {
      each(this.payload, (node) => node.reset());
    }
  }
  /** Create a payload set. */
  _makePayload(source) {
    if (source) {
      const payload = /* @__PURE__ */ new Set();
      eachProp(source, this._addToPayload, payload);
      return Array.from(payload);
    }
  }
  /** Add to a payload set. */
  _addToPayload(source) {
    if (TreeContext.dependencies && hasFluidValue(source)) {
      TreeContext.dependencies.add(source);
    }
    const payload = getPayload(source);
    if (payload) {
      each(payload, (node) => this.add(node));
    }
  }
};
var AnimatedArray = class extends AnimatedObject {
  constructor(source) {
    super(source);
  }
  /** @internal */
  static create(source) {
    return new AnimatedArray(source);
  }
  getValue() {
    return this.source.map((node) => node.getValue());
  }
  setValue(source) {
    const payload = this.getPayload();
    if (source.length == payload.length) {
      return payload.map((node, i5) => node.setValue(source[i5])).some(Boolean);
    }
    super.setValue(source.map(makeAnimated));
    return true;
  }
};
function makeAnimated(value) {
  const nodeType = isAnimatedString(value) ? AnimatedString : AnimatedValue;
  return nodeType.create(value);
}
function getAnimatedType(value) {
  const parentNode = getAnimated(value);
  return parentNode ? parentNode.constructor : is.arr(value) ? AnimatedArray : isAnimatedString(value) ? AnimatedString : AnimatedValue;
}
var withAnimated = (Component, host3) => {
  const hasInstance = (
    // Function components must use "forwardRef" to avoid being
    // re-rendered on every animation frame.
    !is.fun(Component) || Component.prototype && Component.prototype.isReactComponent
  );
  return (0, import_react9.forwardRef)((givenProps, givenRef) => {
    const instanceRef = (0, import_react9.useRef)(null);
    const ref = hasInstance && // eslint-disable-next-line react-hooks/rules-of-hooks
    (0, import_react9.useCallback)(
      (value) => {
        instanceRef.current = updateRef(givenRef, value);
      },
      [givenRef]
    );
    const [props, deps] = getAnimatedState(givenProps, host3);
    const forceUpdate = useForceUpdate();
    const callback = () => {
      const instance = instanceRef.current;
      if (hasInstance && !instance) {
        return;
      }
      const didUpdate = instance ? host3.applyAnimatedValues(instance, props.getValue(true)) : false;
      if (didUpdate === false) {
        forceUpdate();
      }
    };
    const observer = new PropsObserver(callback, deps);
    const observerRef = (0, import_react9.useRef)();
    useIsomorphicLayoutEffect(() => {
      observerRef.current = observer;
      each(deps, (dep) => addFluidObserver(dep, observer));
      return () => {
        if (observerRef.current) {
          each(
            observerRef.current.deps,
            (dep) => removeFluidObserver(dep, observerRef.current)
          );
          raf.cancel(observerRef.current.update);
        }
      };
    });
    (0, import_react9.useEffect)(callback, []);
    useOnce(() => () => {
      const observer2 = observerRef.current;
      each(observer2.deps, (dep) => removeFluidObserver(dep, observer2));
    });
    const usedProps = host3.getComponentProps(props.getValue());
    return React.createElement(Component, { ...usedProps, ref });
  });
};
var PropsObserver = class {
  constructor(update5, deps) {
    this.update = update5;
    this.deps = deps;
  }
  eventObserved(event) {
    if (event.type == "change") {
      raf.write(this.update);
    }
  }
};
function getAnimatedState(props, host3) {
  const dependencies = /* @__PURE__ */ new Set();
  TreeContext.dependencies = dependencies;
  if (props.style)
    props = {
      ...props,
      style: host3.createAnimatedStyle(props.style)
    };
  props = new AnimatedObject(props);
  TreeContext.dependencies = null;
  return [props, dependencies];
}
function updateRef(ref, value) {
  if (ref) {
    if (is.fun(ref))
      ref(value);
    else
      ref.current = value;
  }
  return value;
}
var cacheKey = Symbol.for("AnimatedComponent");
var createHost = (components, {
  applyAnimatedValues: applyAnimatedValues3 = () => false,
  createAnimatedStyle = (style) => new AnimatedObject(style),
  getComponentProps = (props) => props
} = {}) => {
  const hostConfig = {
    applyAnimatedValues: applyAnimatedValues3,
    createAnimatedStyle,
    getComponentProps
  };
  const animated3 = (Component) => {
    const displayName = getDisplayName(Component) || "Anonymous";
    if (is.str(Component)) {
      Component = animated3[Component] || (animated3[Component] = withAnimated(Component, hostConfig));
    } else {
      Component = Component[cacheKey] || (Component[cacheKey] = withAnimated(Component, hostConfig));
    }
    Component.displayName = `Animated(${displayName})`;
    return Component;
  };
  eachProp(components, (Component, key) => {
    if (is.arr(components)) {
      key = getDisplayName(Component);
    }
    animated3[key] = animated3(Component);
  });
  return {
    animated: animated3
  };
};
var getDisplayName = (arg) => is.str(arg) ? arg : arg && is.str(arg.displayName) ? arg.displayName : is.fun(arg) && arg.name || null;

// node_modules/@nivo/core/node_modules/@react-spring/web/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var React2 = __toESM(require_react(), 1);
var import_react11 = __toESM(require_react(), 1);
var import_react12 = __toESM(require_react(), 1);
var React22 = __toESM(require_react(), 1);
var import_react13 = __toESM(require_react(), 1);
var import_react14 = __toESM(require_react(), 1);
function callProp(value, ...args) {
  return is.fun(value) ? value(...args) : value;
}
var matchProp = (value, key) => value === true || !!(key && value && (is.fun(value) ? value(key) : toArray(value).includes(key)));
var resolveProp = (prop, key) => is.obj(prop) ? key && prop[key] : prop;
var getDefaultProp = (props, key) => props.default === true ? props[key] : props.default ? props.default[key] : void 0;
var noopTransform = (value) => value;
var getDefaultProps = (props, transform = noopTransform) => {
  let keys = DEFAULT_PROPS;
  if (props.default && props.default !== true) {
    props = props.default;
    keys = Object.keys(props);
  }
  const defaults22 = {};
  for (const key of keys) {
    const value = transform(props[key], key);
    if (!is.und(value)) {
      defaults22[key] = value;
    }
  }
  return defaults22;
};
var DEFAULT_PROPS = [
  "config",
  "onProps",
  "onStart",
  "onChange",
  "onPause",
  "onResume",
  "onRest"
];
var RESERVED_PROPS = {
  config: 1,
  from: 1,
  to: 1,
  ref: 1,
  loop: 1,
  reset: 1,
  pause: 1,
  cancel: 1,
  reverse: 1,
  immediate: 1,
  default: 1,
  delay: 1,
  onProps: 1,
  onStart: 1,
  onChange: 1,
  onPause: 1,
  onResume: 1,
  onRest: 1,
  onResolve: 1,
  // Transition props
  items: 1,
  trail: 1,
  sort: 1,
  expires: 1,
  initial: 1,
  enter: 1,
  update: 1,
  leave: 1,
  children: 1,
  onDestroyed: 1,
  // Internal props
  keys: 1,
  callId: 1,
  parentId: 1
};
function getForwardProps(props) {
  const forward = {};
  let count5 = 0;
  eachProp(props, (value, prop) => {
    if (!RESERVED_PROPS[prop]) {
      forward[prop] = value;
      count5++;
    }
  });
  if (count5) {
    return forward;
  }
}
function inferTo(props) {
  const to22 = getForwardProps(props);
  if (to22) {
    const out = { to: to22 };
    eachProp(props, (val, key) => key in to22 || (out[key] = val));
    return out;
  }
  return { ...props };
}
function computeGoal(value) {
  value = getFluidValue(value);
  return is.arr(value) ? value.map(computeGoal) : isAnimatedString(value) ? globals_exports.createStringInterpolator({
    range: [0, 1],
    output: [value, value]
  })(1) : value;
}
function hasProps(props) {
  for (const _2 in props)
    return true;
  return false;
}
function isAsyncTo(to22) {
  return is.fun(to22) || is.arr(to22) && is.obj(to22[0]);
}
function detachRefs(ctrl, ref) {
  var _a;
  (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
  ref == null ? void 0 : ref.delete(ctrl);
}
function replaceRef(ctrl, ref) {
  var _a;
  if (ref && ctrl.ref !== ref) {
    (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
    ref.add(ctrl);
    ctrl.ref = ref;
  }
}
var config = {
  default: { tension: 170, friction: 26 },
  gentle: { tension: 120, friction: 14 },
  wobbly: { tension: 180, friction: 12 },
  stiff: { tension: 210, friction: 20 },
  slow: { tension: 280, friction: 60 },
  molasses: { tension: 280, friction: 120 }
};
var defaults = {
  ...config.default,
  mass: 1,
  damping: 1,
  easing: easings.linear,
  clamp: false
};
var AnimationConfig = class {
  constructor() {
    this.velocity = 0;
    Object.assign(this, defaults);
  }
};
function mergeConfig(config22, newConfig, defaultConfig) {
  if (defaultConfig) {
    defaultConfig = { ...defaultConfig };
    sanitizeConfig(defaultConfig, newConfig);
    newConfig = { ...defaultConfig, ...newConfig };
  }
  sanitizeConfig(config22, newConfig);
  Object.assign(config22, newConfig);
  for (const key in defaults) {
    if (config22[key] == null) {
      config22[key] = defaults[key];
    }
  }
  let { frequency, damping } = config22;
  const { mass } = config22;
  if (!is.und(frequency)) {
    if (frequency < 0.01)
      frequency = 0.01;
    if (damping < 0)
      damping = 0;
    config22.tension = Math.pow(2 * Math.PI / frequency, 2) * mass;
    config22.friction = 4 * Math.PI * damping * mass / frequency;
  }
  return config22;
}
function sanitizeConfig(config22, props) {
  if (!is.und(props.decay)) {
    config22.duration = void 0;
  } else {
    const isTensionConfig = !is.und(props.tension) || !is.und(props.friction);
    if (isTensionConfig || !is.und(props.frequency) || !is.und(props.damping) || !is.und(props.mass)) {
      config22.duration = void 0;
      config22.decay = void 0;
    }
    if (isTensionConfig) {
      config22.frequency = void 0;
    }
  }
}
var emptyArray = [];
var Animation = class {
  constructor() {
    this.changed = false;
    this.values = emptyArray;
    this.toValues = null;
    this.fromValues = emptyArray;
    this.config = new AnimationConfig();
    this.immediate = false;
  }
};
function scheduleProps(callId, { key, props, defaultProps, state, actions }) {
  return new Promise((resolve, reject) => {
    let delay;
    let timeout;
    let cancel = matchProp(props.cancel ?? (defaultProps == null ? void 0 : defaultProps.cancel), key);
    if (cancel) {
      onStart();
    } else {
      if (!is.und(props.pause)) {
        state.paused = matchProp(props.pause, key);
      }
      let pause = defaultProps == null ? void 0 : defaultProps.pause;
      if (pause !== true) {
        pause = state.paused || matchProp(pause, key);
      }
      delay = callProp(props.delay || 0, key);
      if (pause) {
        state.resumeQueue.add(onResume);
        actions.pause();
      } else {
        actions.resume();
        onResume();
      }
    }
    function onPause() {
      state.resumeQueue.add(onResume);
      state.timeouts.delete(timeout);
      timeout.cancel();
      delay = timeout.time - raf.now();
    }
    function onResume() {
      if (delay > 0 && !globals_exports.skipAnimation) {
        state.delayed = true;
        timeout = raf.setTimeout(onStart, delay);
        state.pauseQueue.add(onPause);
        state.timeouts.add(timeout);
      } else {
        onStart();
      }
    }
    function onStart() {
      if (state.delayed) {
        state.delayed = false;
      }
      state.pauseQueue.delete(onPause);
      state.timeouts.delete(timeout);
      if (callId <= (state.cancelId || 0)) {
        cancel = true;
      }
      try {
        actions.start({ ...props, callId, cancel }, resolve);
      } catch (err) {
        reject(err);
      }
    }
  });
}
var getCombinedResult = (target, results) => results.length == 1 ? results[0] : results.some((result) => result.cancelled) ? getCancelledResult(target.get()) : results.every((result) => result.noop) ? getNoopResult(target.get()) : getFinishedResult(
  target.get(),
  results.every((result) => result.finished)
);
var getNoopResult = (value) => ({
  value,
  noop: true,
  finished: true,
  cancelled: false
});
var getFinishedResult = (value, finished, cancelled = false) => ({
  value,
  finished,
  cancelled
});
var getCancelledResult = (value) => ({
  value,
  cancelled: true,
  finished: false
});
function runAsync(to22, props, state, target) {
  const { callId, parentId, onRest } = props;
  const { asyncTo: prevTo, promise: prevPromise } = state;
  if (!parentId && to22 === prevTo && !props.reset) {
    return prevPromise;
  }
  return state.promise = (async () => {
    state.asyncId = callId;
    state.asyncTo = to22;
    const defaultProps = getDefaultProps(
      props,
      (value, key) => (
        // The `onRest` prop is only called when the `runAsync` promise is resolved.
        key === "onRest" ? void 0 : value
      )
    );
    let preventBail;
    let bail;
    const bailPromise = new Promise(
      (resolve, reject) => (preventBail = resolve, bail = reject)
    );
    const bailIfEnded = (bailSignal) => {
      const bailResult = (
        // The `cancel` prop or `stop` method was used.
        callId <= (state.cancelId || 0) && getCancelledResult(target) || // The async `to` prop was replaced.
        callId !== state.asyncId && getFinishedResult(target, false)
      );
      if (bailResult) {
        bailSignal.result = bailResult;
        bail(bailSignal);
        throw bailSignal;
      }
    };
    const animate = (arg1, arg2) => {
      const bailSignal = new BailSignal();
      const skipAnimationSignal = new SkipAnimationSignal();
      return (async () => {
        if (globals_exports.skipAnimation) {
          stopAsync(state);
          skipAnimationSignal.result = getFinishedResult(target, false);
          bail(skipAnimationSignal);
          throw skipAnimationSignal;
        }
        bailIfEnded(bailSignal);
        const props2 = is.obj(arg1) ? { ...arg1 } : { ...arg2, to: arg1 };
        props2.parentId = callId;
        eachProp(defaultProps, (value, key) => {
          if (is.und(props2[key])) {
            props2[key] = value;
          }
        });
        const result2 = await target.start(props2);
        bailIfEnded(bailSignal);
        if (state.paused) {
          await new Promise((resume) => {
            state.resumeQueue.add(resume);
          });
        }
        return result2;
      })();
    };
    let result;
    if (globals_exports.skipAnimation) {
      stopAsync(state);
      return getFinishedResult(target, false);
    }
    try {
      let animating;
      if (is.arr(to22)) {
        animating = (async (queue) => {
          for (const props2 of queue) {
            await animate(props2);
          }
        })(to22);
      } else {
        animating = Promise.resolve(to22(animate, target.stop.bind(target)));
      }
      await Promise.all([animating.then(preventBail), bailPromise]);
      result = getFinishedResult(target.get(), true, false);
    } catch (err) {
      if (err instanceof BailSignal) {
        result = err.result;
      } else if (err instanceof SkipAnimationSignal) {
        result = err.result;
      } else {
        throw err;
      }
    } finally {
      if (callId == state.asyncId) {
        state.asyncId = parentId;
        state.asyncTo = parentId ? prevTo : void 0;
        state.promise = parentId ? prevPromise : void 0;
      }
    }
    if (is.fun(onRest)) {
      raf.batchedUpdates(() => {
        onRest(result, target, target.item);
      });
    }
    return result;
  })();
}
function stopAsync(state, cancelId) {
  flush(state.timeouts, (t7) => t7.cancel());
  state.pauseQueue.clear();
  state.resumeQueue.clear();
  state.asyncId = state.asyncTo = state.promise = void 0;
  if (cancelId)
    state.cancelId = cancelId;
}
var BailSignal = class extends Error {
  constructor() {
    super(
      "An async animation has been interrupted. You see this error because you forgot to use `await` or `.catch(...)` on its returned promise."
    );
  }
};
var SkipAnimationSignal = class extends Error {
  constructor() {
    super("SkipAnimationSignal");
  }
};
var isFrameValue = (value) => value instanceof FrameValue;
var nextId = 1;
var FrameValue = class extends FluidValue {
  constructor() {
    super(...arguments);
    this.id = nextId++;
    this._priority = 0;
  }
  get priority() {
    return this._priority;
  }
  set priority(priority3) {
    if (this._priority != priority3) {
      this._priority = priority3;
      this._onPriorityChange(priority3);
    }
  }
  /** Get the current value */
  get() {
    const node = getAnimated(this);
    return node && node.getValue();
  }
  /** Create a spring that maps our value to another value */
  to(...args) {
    return globals_exports.to(this, args);
  }
  /** @deprecated Use the `to` method instead. */
  interpolate(...args) {
    deprecateInterpolate();
    return globals_exports.to(this, args);
  }
  toJSON() {
    return this.get();
  }
  observerAdded(count5) {
    if (count5 == 1)
      this._attach();
  }
  observerRemoved(count5) {
    if (count5 == 0)
      this._detach();
  }
  /** Called when the first child is added. */
  _attach() {
  }
  /** Called when the last child is removed. */
  _detach() {
  }
  /** Tell our children about our new value */
  _onChange(value, idle = false) {
    callFluidObservers(this, {
      type: "change",
      parent: this,
      value,
      idle
    });
  }
  /** Tell our children about our new priority */
  _onPriorityChange(priority3) {
    if (!this.idle) {
      frameLoop.sort(this);
    }
    callFluidObservers(this, {
      type: "priority",
      parent: this,
      priority: priority3
    });
  }
};
var $P = Symbol.for("SpringPhase");
var HAS_ANIMATED = 1;
var IS_ANIMATING = 2;
var IS_PAUSED = 4;
var hasAnimated = (target) => (target[$P] & HAS_ANIMATED) > 0;
var isAnimating = (target) => (target[$P] & IS_ANIMATING) > 0;
var isPaused = (target) => (target[$P] & IS_PAUSED) > 0;
var setActiveBit = (target, active) => active ? target[$P] |= IS_ANIMATING | HAS_ANIMATED : target[$P] &= ~IS_ANIMATING;
var setPausedBit = (target, paused) => paused ? target[$P] |= IS_PAUSED : target[$P] &= ~IS_PAUSED;
var SpringValue = class extends FrameValue {
  constructor(arg1, arg2) {
    super();
    this.animation = new Animation();
    this.defaultProps = {};
    this._state = {
      paused: false,
      delayed: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._pendingCalls = /* @__PURE__ */ new Set();
    this._lastCallId = 0;
    this._lastToId = 0;
    this._memoizedDuration = 0;
    if (!is.und(arg1) || !is.und(arg2)) {
      const props = is.obj(arg1) ? { ...arg1 } : { ...arg2, from: arg1 };
      if (is.und(props.default)) {
        props.default = true;
      }
      this.start(props);
    }
  }
  /** Equals true when not advancing on each frame. */
  get idle() {
    return !(isAnimating(this) || this._state.asyncTo) || isPaused(this);
  }
  get goal() {
    return getFluidValue(this.animation.to);
  }
  get velocity() {
    const node = getAnimated(this);
    return node instanceof AnimatedValue ? node.lastVelocity || 0 : node.getPayload().map((node2) => node2.lastVelocity || 0);
  }
  /**
   * When true, this value has been animated at least once.
   */
  get hasAnimated() {
    return hasAnimated(this);
  }
  /**
   * When true, this value has an unfinished animation,
   * which is either active or paused.
   */
  get isAnimating() {
    return isAnimating(this);
  }
  /**
   * When true, all current and future animations are paused.
   */
  get isPaused() {
    return isPaused(this);
  }
  /**
   *
   *
   */
  get isDelayed() {
    return this._state.delayed;
  }
  /** Advance the current animation by a number of milliseconds */
  advance(dt2) {
    let idle = true;
    let changed = false;
    const anim = this.animation;
    let { toValues } = anim;
    const { config: config22 } = anim;
    const payload = getPayload(anim.to);
    if (!payload && hasFluidValue(anim.to)) {
      toValues = toArray(getFluidValue(anim.to));
    }
    anim.values.forEach((node2, i5) => {
      if (node2.done)
        return;
      const to22 = (
        // Animated strings always go from 0 to 1.
        node2.constructor == AnimatedString ? 1 : payload ? payload[i5].lastPosition : toValues[i5]
      );
      let finished = anim.immediate;
      let position = to22;
      if (!finished) {
        position = node2.lastPosition;
        if (config22.tension <= 0) {
          node2.done = true;
          return;
        }
        let elapsed = node2.elapsedTime += dt2;
        const from = anim.fromValues[i5];
        const v0 = node2.v0 != null ? node2.v0 : node2.v0 = is.arr(config22.velocity) ? config22.velocity[i5] : config22.velocity;
        let velocity;
        const precision = config22.precision || (from == to22 ? 5e-3 : Math.min(1, Math.abs(to22 - from) * 1e-3));
        if (!is.und(config22.duration)) {
          let p3 = 1;
          if (config22.duration > 0) {
            if (this._memoizedDuration !== config22.duration) {
              this._memoizedDuration = config22.duration;
              if (node2.durationProgress > 0) {
                node2.elapsedTime = config22.duration * node2.durationProgress;
                elapsed = node2.elapsedTime += dt2;
              }
            }
            p3 = (config22.progress || 0) + elapsed / this._memoizedDuration;
            p3 = p3 > 1 ? 1 : p3 < 0 ? 0 : p3;
            node2.durationProgress = p3;
          }
          position = from + config22.easing(p3) * (to22 - from);
          velocity = (position - node2.lastPosition) / dt2;
          finished = p3 == 1;
        } else if (config22.decay) {
          const decay = config22.decay === true ? 0.998 : config22.decay;
          const e7 = Math.exp(-(1 - decay) * elapsed);
          position = from + v0 / (1 - decay) * (1 - e7);
          finished = Math.abs(node2.lastPosition - position) <= precision;
          velocity = v0 * e7;
        } else {
          velocity = node2.lastVelocity == null ? v0 : node2.lastVelocity;
          const restVelocity = config22.restVelocity || precision / 10;
          const bounceFactor = config22.clamp ? 0 : config22.bounce;
          const canBounce = !is.und(bounceFactor);
          const isGrowing = from == to22 ? node2.v0 > 0 : from < to22;
          let isMoving;
          let isBouncing = false;
          const step = 1;
          const numSteps = Math.ceil(dt2 / step);
          for (let n5 = 0; n5 < numSteps; ++n5) {
            isMoving = Math.abs(velocity) > restVelocity;
            if (!isMoving) {
              finished = Math.abs(to22 - position) <= precision;
              if (finished) {
                break;
              }
            }
            if (canBounce) {
              isBouncing = position == to22 || position > to22 == isGrowing;
              if (isBouncing) {
                velocity = -velocity * bounceFactor;
                position = to22;
              }
            }
            const springForce = -config22.tension * 1e-6 * (position - to22);
            const dampingForce = -config22.friction * 1e-3 * velocity;
            const acceleration = (springForce + dampingForce) / config22.mass;
            velocity = velocity + acceleration * step;
            position = position + velocity * step;
          }
        }
        node2.lastVelocity = velocity;
        if (Number.isNaN(position)) {
          console.warn(`Got NaN while animating:`, this);
          finished = true;
        }
      }
      if (payload && !payload[i5].done) {
        finished = false;
      }
      if (finished) {
        node2.done = true;
      } else {
        idle = false;
      }
      if (node2.setValue(position, config22.round)) {
        changed = true;
      }
    });
    const node = getAnimated(this);
    const currVal = node.getValue();
    if (idle) {
      const finalVal = getFluidValue(anim.to);
      if ((currVal !== finalVal || changed) && !config22.decay) {
        node.setValue(finalVal);
        this._onChange(finalVal);
      } else if (changed && config22.decay) {
        this._onChange(currVal);
      }
      this._stop();
    } else if (changed) {
      this._onChange(currVal);
    }
  }
  /** Set the current value, while stopping the current animation */
  set(value) {
    raf.batchedUpdates(() => {
      this._stop();
      this._focus(value);
      this._set(value);
    });
    return this;
  }
  /**
   * Freeze the active animation in time, as well as any updates merged
   * before `resume` is called.
   */
  pause() {
    this._update({ pause: true });
  }
  /** Resume the animation if paused. */
  resume() {
    this._update({ pause: false });
  }
  /** Skip to the end of the current animation. */
  finish() {
    if (isAnimating(this)) {
      const { to: to22, config: config22 } = this.animation;
      raf.batchedUpdates(() => {
        this._onStart();
        if (!config22.decay) {
          this._set(to22, false);
        }
        this._stop();
      });
    }
    return this;
  }
  /** Push props into the pending queue. */
  update(props) {
    const queue = this.queue || (this.queue = []);
    queue.push(props);
    return this;
  }
  start(to22, arg2) {
    let queue;
    if (!is.und(to22)) {
      queue = [is.obj(to22) ? to22 : { ...arg2, to: to22 }];
    } else {
      queue = this.queue || [];
      this.queue = [];
    }
    return Promise.all(
      queue.map((props) => {
        const up = this._update(props);
        return up;
      })
    ).then((results) => getCombinedResult(this, results));
  }
  /**
   * Stop the current animation, and cancel any delayed updates.
   *
   * Pass `true` to call `onRest` with `cancelled: true`.
   */
  stop(cancel) {
    const { to: to22 } = this.animation;
    this._focus(this.get());
    stopAsync(this._state, cancel && this._lastCallId);
    raf.batchedUpdates(() => this._stop(to22, cancel));
    return this;
  }
  /** Restart the animation. */
  reset() {
    this._update({ reset: true });
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._start();
    } else if (event.type == "priority") {
      this.priority = event.priority + 1;
    }
  }
  /**
   * Parse the `to` and `from` range from the given `props` object.
   *
   * This also ensures the initial value is available to animated components
   * during the render phase.
   */
  _prepareNode(props) {
    const key = this.key || "";
    let { to: to22, from } = props;
    to22 = is.obj(to22) ? to22[key] : to22;
    if (to22 == null || isAsyncTo(to22)) {
      to22 = void 0;
    }
    from = is.obj(from) ? from[key] : from;
    if (from == null) {
      from = void 0;
    }
    const range3 = { to: to22, from };
    if (!hasAnimated(this)) {
      if (props.reverse)
        [to22, from] = [from, to22];
      from = getFluidValue(from);
      if (!is.und(from)) {
        this._set(from);
      } else if (!getAnimated(this)) {
        this._set(to22);
      }
    }
    return range3;
  }
  /** Every update is processed by this method before merging. */
  _update({ ...props }, isLoop) {
    const { key, defaultProps } = this;
    if (props.default)
      Object.assign(
        defaultProps,
        getDefaultProps(
          props,
          (value, prop) => /^on/.test(prop) ? resolveProp(value, key) : value
        )
      );
    mergeActiveFn(this, props, "onProps");
    sendEvent(this, "onProps", props, this);
    const range3 = this._prepareNode(props);
    if (Object.isFrozen(this)) {
      throw Error(
        "Cannot animate a `SpringValue` object that is frozen. Did you forget to pass your component to `animated(...)` before animating its props?"
      );
    }
    const state = this._state;
    return scheduleProps(++this._lastCallId, {
      key,
      props,
      defaultProps,
      state,
      actions: {
        pause: () => {
          if (!isPaused(this)) {
            setPausedBit(this, true);
            flushCalls(state.pauseQueue);
            sendEvent(
              this,
              "onPause",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        resume: () => {
          if (isPaused(this)) {
            setPausedBit(this, false);
            if (isAnimating(this)) {
              this._resume();
            }
            flushCalls(state.resumeQueue);
            sendEvent(
              this,
              "onResume",
              getFinishedResult(this, checkFinished(this, this.animation.to)),
              this
            );
          }
        },
        start: this._merge.bind(this, range3)
      }
    }).then((result) => {
      if (props.loop && result.finished && !(isLoop && result.noop)) {
        const nextProps = createLoopUpdate(props);
        if (nextProps) {
          return this._update(nextProps, true);
        }
      }
      return result;
    });
  }
  /** Merge props into the current animation */
  _merge(range3, props, resolve) {
    if (props.cancel) {
      this.stop(true);
      return resolve(getCancelledResult(this));
    }
    const hasToProp = !is.und(range3.to);
    const hasFromProp = !is.und(range3.from);
    if (hasToProp || hasFromProp) {
      if (props.callId > this._lastToId) {
        this._lastToId = props.callId;
      } else {
        return resolve(getCancelledResult(this));
      }
    }
    const { key, defaultProps, animation: anim } = this;
    const { to: prevTo, from: prevFrom } = anim;
    let { to: to22 = prevTo, from = prevFrom } = range3;
    if (hasFromProp && !hasToProp && (!props.default || is.und(to22))) {
      to22 = from;
    }
    if (props.reverse)
      [to22, from] = [from, to22];
    const hasFromChanged = !isEqual(from, prevFrom);
    if (hasFromChanged) {
      anim.from = from;
    }
    from = getFluidValue(from);
    const hasToChanged = !isEqual(to22, prevTo);
    if (hasToChanged) {
      this._focus(to22);
    }
    const hasAsyncTo = isAsyncTo(props.to);
    const { config: config22 } = anim;
    const { decay, velocity } = config22;
    if (hasToProp || hasFromProp) {
      config22.velocity = 0;
    }
    if (props.config && !hasAsyncTo) {
      mergeConfig(
        config22,
        callProp(props.config, key),
        // Avoid calling the same "config" prop twice.
        props.config !== defaultProps.config ? callProp(defaultProps.config, key) : void 0
      );
    }
    let node = getAnimated(this);
    if (!node || is.und(to22)) {
      return resolve(getFinishedResult(this, true));
    }
    const reset = (
      // When `reset` is undefined, the `from` prop implies `reset: true`,
      // except for declarative updates. When `reset` is defined, there
      // must exist a value to animate from.
      is.und(props.reset) ? hasFromProp && !props.default : !is.und(from) && matchProp(props.reset, key)
    );
    const value = reset ? from : this.get();
    const goal = computeGoal(to22);
    const isAnimatable = is.num(goal) || is.arr(goal) || isAnimatedString(goal);
    const immediate = !hasAsyncTo && (!isAnimatable || matchProp(defaultProps.immediate || props.immediate, key));
    if (hasToChanged) {
      const nodeType = getAnimatedType(to22);
      if (nodeType !== node.constructor) {
        if (immediate) {
          node = this._set(goal);
        } else
          throw Error(
            `Cannot animate between ${node.constructor.name} and ${nodeType.name}, as the "to" prop suggests`
          );
      }
    }
    const goalType = node.constructor;
    let started = hasFluidValue(to22);
    let finished = false;
    if (!started) {
      const hasValueChanged = reset || !hasAnimated(this) && hasFromChanged;
      if (hasToChanged || hasValueChanged) {
        finished = isEqual(computeGoal(value), goal);
        started = !finished;
      }
      if (!isEqual(anim.immediate, immediate) && !immediate || !isEqual(config22.decay, decay) || !isEqual(config22.velocity, velocity)) {
        started = true;
      }
    }
    if (finished && isAnimating(this)) {
      if (anim.changed && !reset) {
        started = true;
      } else if (!started) {
        this._stop(prevTo);
      }
    }
    if (!hasAsyncTo) {
      if (started || hasFluidValue(prevTo)) {
        anim.values = node.getPayload();
        anim.toValues = hasFluidValue(to22) ? null : goalType == AnimatedString ? [1] : toArray(goal);
      }
      if (anim.immediate != immediate) {
        anim.immediate = immediate;
        if (!immediate && !reset) {
          this._set(prevTo);
        }
      }
      if (started) {
        const { onRest } = anim;
        each(ACTIVE_EVENTS, (type) => mergeActiveFn(this, props, type));
        const result = getFinishedResult(this, checkFinished(this, prevTo));
        flushCalls(this._pendingCalls, result);
        this._pendingCalls.add(resolve);
        if (anim.changed)
          raf.batchedUpdates(() => {
            var _a;
            anim.changed = !reset;
            onRest == null ? void 0 : onRest(result, this);
            if (reset) {
              callProp(defaultProps.onRest, result);
            } else {
              (_a = anim.onStart) == null ? void 0 : _a.call(anim, result, this);
            }
          });
      }
    }
    if (reset) {
      this._set(value);
    }
    if (hasAsyncTo) {
      resolve(runAsync(props.to, props, this._state, this));
    } else if (started) {
      this._start();
    } else if (isAnimating(this) && !hasToChanged) {
      this._pendingCalls.add(resolve);
    } else {
      resolve(getNoopResult(value));
    }
  }
  /** Update the `animation.to` value, which might be a `FluidValue` */
  _focus(value) {
    const anim = this.animation;
    if (value !== anim.to) {
      if (getFluidObservers(this)) {
        this._detach();
      }
      anim.to = value;
      if (getFluidObservers(this)) {
        this._attach();
      }
    }
  }
  _attach() {
    let priority3 = 0;
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      addFluidObserver(to22, this);
      if (isFrameValue(to22)) {
        priority3 = to22.priority + 1;
      }
    }
    this.priority = priority3;
  }
  _detach() {
    const { to: to22 } = this.animation;
    if (hasFluidValue(to22)) {
      removeFluidObserver(to22, this);
    }
  }
  /**
   * Update the current value from outside the frameloop,
   * and return the `Animated` node.
   */
  _set(arg, idle = true) {
    const value = getFluidValue(arg);
    if (!is.und(value)) {
      const oldNode = getAnimated(this);
      if (!oldNode || !isEqual(value, oldNode.getValue())) {
        const nodeType = getAnimatedType(value);
        if (!oldNode || oldNode.constructor != nodeType) {
          setAnimated(this, nodeType.create(value));
        } else {
          oldNode.setValue(value);
        }
        if (oldNode) {
          raf.batchedUpdates(() => {
            this._onChange(value, idle);
          });
        }
      }
    }
    return getAnimated(this);
  }
  _onStart() {
    const anim = this.animation;
    if (!anim.changed) {
      anim.changed = true;
      sendEvent(
        this,
        "onStart",
        getFinishedResult(this, checkFinished(this, anim.to)),
        this
      );
    }
  }
  _onChange(value, idle) {
    if (!idle) {
      this._onStart();
      callProp(this.animation.onChange, value, this);
    }
    callProp(this.defaultProps.onChange, value, this);
    super._onChange(value, idle);
  }
  // This method resets the animation state (even if already animating) to
  // ensure the latest from/to range is used, and it also ensures this spring
  // is added to the frameloop.
  _start() {
    const anim = this.animation;
    getAnimated(this).reset(getFluidValue(anim.to));
    if (!anim.immediate) {
      anim.fromValues = anim.values.map((node) => node.lastPosition);
    }
    if (!isAnimating(this)) {
      setActiveBit(this, true);
      if (!isPaused(this)) {
        this._resume();
      }
    }
  }
  _resume() {
    if (globals_exports.skipAnimation) {
      this.finish();
    } else {
      frameLoop.start(this);
    }
  }
  /**
   * Exit the frameloop and notify `onRest` listeners.
   *
   * Always wrap `_stop` calls with `batchedUpdates`.
   */
  _stop(goal, cancel) {
    if (isAnimating(this)) {
      setActiveBit(this, false);
      const anim = this.animation;
      each(anim.values, (node) => {
        node.done = true;
      });
      if (anim.toValues) {
        anim.onChange = anim.onPause = anim.onResume = void 0;
      }
      callFluidObservers(this, {
        type: "idle",
        parent: this
      });
      const result = cancel ? getCancelledResult(this.get()) : getFinishedResult(this.get(), checkFinished(this, goal ?? anim.to));
      flushCalls(this._pendingCalls, result);
      if (anim.changed) {
        anim.changed = false;
        sendEvent(this, "onRest", result, this);
      }
    }
  }
};
function checkFinished(target, to22) {
  const goal = computeGoal(to22);
  const value = computeGoal(target.get());
  return isEqual(value, goal);
}
function createLoopUpdate(props, loop3 = props.loop, to22 = props.to) {
  const loopRet = callProp(loop3);
  if (loopRet) {
    const overrides = loopRet !== true && inferTo(loopRet);
    const reverse4 = (overrides || props).reverse;
    const reset = !overrides || overrides.reset;
    return createUpdate({
      ...props,
      loop: loop3,
      // Avoid updating default props when looping.
      default: false,
      // Never loop the `pause` prop.
      pause: void 0,
      // For the "reverse" prop to loop as expected, the "to" prop
      // must be undefined. The "reverse" prop is ignored when the
      // "to" prop is an array or function.
      to: !reverse4 || isAsyncTo(to22) ? to22 : void 0,
      // Ignore the "from" prop except on reset.
      from: reset ? props.from : void 0,
      reset,
      // The "loop" prop can return a "useSpring" props object to
      // override any of the original props.
      ...overrides
    });
  }
}
function createUpdate(props) {
  const { to: to22, from } = props = inferTo(props);
  const keys = /* @__PURE__ */ new Set();
  if (is.obj(to22))
    findDefined(to22, keys);
  if (is.obj(from))
    findDefined(from, keys);
  props.keys = keys.size ? Array.from(keys) : null;
  return props;
}
function declareUpdate(props) {
  const update22 = createUpdate(props);
  if (is.und(update22.default)) {
    update22.default = getDefaultProps(update22);
  }
  return update22;
}
function findDefined(values, keys) {
  eachProp(values, (value, key) => value != null && keys.add(key));
}
var ACTIVE_EVENTS = [
  "onStart",
  "onRest",
  "onChange",
  "onPause",
  "onResume"
];
function mergeActiveFn(target, props, type) {
  target.animation[type] = props[type] !== getDefaultProp(props, type) ? resolveProp(props[type], target.key) : void 0;
}
function sendEvent(target, type, ...args) {
  var _a, _b, _c, _d;
  (_b = (_a = target.animation)[type]) == null ? void 0 : _b.call(_a, ...args);
  (_d = (_c = target.defaultProps)[type]) == null ? void 0 : _d.call(_c, ...args);
}
var BATCHED_EVENTS = ["onStart", "onChange", "onRest"];
var nextId2 = 1;
var Controller = class {
  constructor(props, flush3) {
    this.id = nextId2++;
    this.springs = {};
    this.queue = [];
    this._lastAsyncId = 0;
    this._active = /* @__PURE__ */ new Set();
    this._changed = /* @__PURE__ */ new Set();
    this._started = false;
    this._state = {
      paused: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._events = {
      onStart: /* @__PURE__ */ new Map(),
      onChange: /* @__PURE__ */ new Map(),
      onRest: /* @__PURE__ */ new Map()
    };
    this._onFrame = this._onFrame.bind(this);
    if (flush3) {
      this._flush = flush3;
    }
    if (props) {
      this.start({ default: true, ...props });
    }
  }
  /**
   * Equals `true` when no spring values are in the frameloop, and
   * no async animation is currently active.
   */
  get idle() {
    return !this._state.asyncTo && Object.values(this.springs).every((spring) => {
      return spring.idle && !spring.isDelayed && !spring.isPaused;
    });
  }
  get item() {
    return this._item;
  }
  set item(item) {
    this._item = item;
  }
  /** Get the current values of our springs */
  get() {
    const values = {};
    this.each((spring, key) => values[key] = spring.get());
    return values;
  }
  /** Set the current values without animating. */
  set(values) {
    for (const key in values) {
      const value = values[key];
      if (!is.und(value)) {
        this.springs[key].set(value);
      }
    }
  }
  /** Push an update onto the queue of each value. */
  update(props) {
    if (props) {
      this.queue.push(createUpdate(props));
    }
    return this;
  }
  /**
   * Start the queued animations for every spring, and resolve the returned
   * promise once all queued animations have finished or been cancelled.
   *
   * When you pass a queue (instead of nothing), that queue is used instead of
   * the queued animations added with the `update` method, which are left alone.
   */
  start(props) {
    let { queue } = this;
    if (props) {
      queue = toArray(props).map(createUpdate);
    } else {
      this.queue = [];
    }
    if (this._flush) {
      return this._flush(this, queue);
    }
    prepareKeys(this, queue);
    return flushUpdateQueue(this, queue);
  }
  /** @internal */
  stop(arg, keys) {
    if (arg !== !!arg) {
      keys = arg;
    }
    if (keys) {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].stop(!!arg));
    } else {
      stopAsync(this._state, this._lastAsyncId);
      this.each((spring) => spring.stop(!!arg));
    }
    return this;
  }
  /** Freeze the active animation in time */
  pause(keys) {
    if (is.und(keys)) {
      this.start({ pause: true });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].pause());
    }
    return this;
  }
  /** Resume the animation if paused. */
  resume(keys) {
    if (is.und(keys)) {
      this.start({ pause: false });
    } else {
      const springs = this.springs;
      each(toArray(keys), (key) => springs[key].resume());
    }
    return this;
  }
  /** Call a function once per spring value */
  each(iterator) {
    eachProp(this.springs, iterator);
  }
  /** @internal Called at the end of every animation frame */
  _onFrame() {
    const { onStart, onChange, onRest } = this._events;
    const active = this._active.size > 0;
    const changed = this._changed.size > 0;
    if (active && !this._started || changed && !this._started) {
      this._started = true;
      flush(onStart, ([onStart2, result]) => {
        result.value = this.get();
        onStart2(result, this, this._item);
      });
    }
    const idle = !active && this._started;
    const values = changed || idle && onRest.size ? this.get() : null;
    if (changed && onChange.size) {
      flush(onChange, ([onChange2, result]) => {
        result.value = values;
        onChange2(result, this, this._item);
      });
    }
    if (idle) {
      this._started = false;
      flush(onRest, ([onRest2, result]) => {
        result.value = values;
        onRest2(result, this, this._item);
      });
    }
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._changed.add(event.parent);
      if (!event.idle) {
        this._active.add(event.parent);
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else
      return;
    raf.onFrame(this._onFrame);
  }
};
function flushUpdateQueue(ctrl, queue) {
  return Promise.all(queue.map((props) => flushUpdate(ctrl, props))).then(
    (results) => getCombinedResult(ctrl, results)
  );
}
async function flushUpdate(ctrl, props, isLoop) {
  const { keys, to: to22, from, loop: loop3, onRest, onResolve } = props;
  const defaults22 = is.obj(props.default) && props.default;
  if (loop3) {
    props.loop = false;
  }
  if (to22 === false)
    props.to = null;
  if (from === false)
    props.from = null;
  const asyncTo = is.arr(to22) || is.fun(to22) ? to22 : void 0;
  if (asyncTo) {
    props.to = void 0;
    props.onRest = void 0;
    if (defaults22) {
      defaults22.onRest = void 0;
    }
  } else {
    each(BATCHED_EVENTS, (key) => {
      const handler = props[key];
      if (is.fun(handler)) {
        const queue = ctrl["_events"][key];
        props[key] = ({ finished, cancelled }) => {
          const result2 = queue.get(handler);
          if (result2) {
            if (!finished)
              result2.finished = false;
            if (cancelled)
              result2.cancelled = true;
          } else {
            queue.set(handler, {
              value: null,
              finished: finished || false,
              cancelled: cancelled || false
            });
          }
        };
        if (defaults22) {
          defaults22[key] = props[key];
        }
      }
    });
  }
  const state = ctrl["_state"];
  if (props.pause === !state.paused) {
    state.paused = props.pause;
    flushCalls(props.pause ? state.pauseQueue : state.resumeQueue);
  } else if (state.paused) {
    props.pause = true;
  }
  const promises = (keys || Object.keys(ctrl.springs)).map(
    (key) => ctrl.springs[key].start(props)
  );
  const cancel = props.cancel === true || getDefaultProp(props, "cancel") === true;
  if (asyncTo || cancel && state.asyncId) {
    promises.push(
      scheduleProps(++ctrl["_lastAsyncId"], {
        props,
        state,
        actions: {
          pause: noop,
          resume: noop,
          start(props2, resolve) {
            if (cancel) {
              stopAsync(state, ctrl["_lastAsyncId"]);
              resolve(getCancelledResult(ctrl));
            } else {
              props2.onRest = onRest;
              resolve(
                runAsync(
                  asyncTo,
                  props2,
                  state,
                  ctrl
                )
              );
            }
          }
        }
      })
    );
  }
  if (state.paused) {
    await new Promise((resume) => {
      state.resumeQueue.add(resume);
    });
  }
  const result = getCombinedResult(ctrl, await Promise.all(promises));
  if (loop3 && result.finished && !(isLoop && result.noop)) {
    const nextProps = createLoopUpdate(props, loop3, to22);
    if (nextProps) {
      prepareKeys(ctrl, [nextProps]);
      return flushUpdate(ctrl, nextProps, true);
    }
  }
  if (onResolve) {
    raf.batchedUpdates(() => onResolve(result, ctrl, ctrl.item));
  }
  return result;
}
function getSprings(ctrl, props) {
  const springs = { ...ctrl.springs };
  if (props) {
    each(toArray(props), (props2) => {
      if (is.und(props2.keys)) {
        props2 = createUpdate(props2);
      }
      if (!is.obj(props2.to)) {
        props2 = { ...props2, to: void 0 };
      }
      prepareSprings(springs, props2, (key) => {
        return createSpring(key);
      });
    });
  }
  setSprings(ctrl, springs);
  return springs;
}
function setSprings(ctrl, springs) {
  eachProp(springs, (spring, key) => {
    if (!ctrl.springs[key]) {
      ctrl.springs[key] = spring;
      addFluidObserver(spring, ctrl);
    }
  });
}
function createSpring(key, observer) {
  const spring = new SpringValue();
  spring.key = key;
  if (observer) {
    addFluidObserver(spring, observer);
  }
  return spring;
}
function prepareSprings(springs, props, create) {
  if (props.keys) {
    each(props.keys, (key) => {
      const spring = springs[key] || (springs[key] = create(key));
      spring["_prepareNode"](props);
    });
  }
}
function prepareKeys(ctrl, queue) {
  each(queue, (props) => {
    prepareSprings(ctrl.springs, props, (key) => {
      return createSpring(key, ctrl);
    });
  });
}
var SpringContext = ({
  children,
  ...props
}) => {
  const inherited = (0, import_react11.useContext)(ctx);
  const pause = props.pause || !!inherited.pause, immediate = props.immediate || !!inherited.immediate;
  props = useMemoOne(() => ({ pause, immediate }), [pause, immediate]);
  const { Provider } = ctx;
  return React2.createElement(Provider, { value: props }, children);
};
var ctx = makeContext(SpringContext, {});
SpringContext.Provider = ctx.Provider;
SpringContext.Consumer = ctx.Consumer;
function makeContext(target, init) {
  Object.assign(target, React2.createContext(init));
  target.Provider._context = target;
  target.Consumer._context = target;
  return target;
}
var SpringRef = () => {
  const current = [];
  const SpringRef22 = function(props) {
    deprecateDirectCall();
    const results = [];
    each(current, (ctrl, i5) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = _getProps(props, ctrl, i5);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.current = current;
  SpringRef22.add = function(ctrl) {
    if (!current.includes(ctrl)) {
      current.push(ctrl);
    }
  };
  SpringRef22.delete = function(ctrl) {
    const i5 = current.indexOf(ctrl);
    if (~i5)
      current.splice(i5, 1);
  };
  SpringRef22.pause = function() {
    each(current, (ctrl) => ctrl.pause(...arguments));
    return this;
  };
  SpringRef22.resume = function() {
    each(current, (ctrl) => ctrl.resume(...arguments));
    return this;
  };
  SpringRef22.set = function(values) {
    each(current, (ctrl, i5) => {
      const update22 = is.fun(values) ? values(i5, ctrl) : values;
      if (update22) {
        ctrl.set(update22);
      }
    });
  };
  SpringRef22.start = function(props) {
    const results = [];
    each(current, (ctrl, i5) => {
      if (is.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = this._getProps(props, ctrl, i5);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.stop = function() {
    each(current, (ctrl) => ctrl.stop(...arguments));
    return this;
  };
  SpringRef22.update = function(props) {
    each(current, (ctrl, i5) => ctrl.update(this._getProps(props, ctrl, i5)));
    return this;
  };
  const _getProps = function(arg, ctrl, index4) {
    return is.fun(arg) ? arg(index4, ctrl) : arg;
  };
  SpringRef22._getProps = _getProps;
  return SpringRef22;
};
function useSprings(length, props, deps) {
  const propsFn = is.fun(props) && props;
  if (propsFn && !deps)
    deps = [];
  const ref = (0, import_react10.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef() : void 0,
    []
  );
  const layoutId = (0, import_react10.useRef)(0);
  const forceUpdate = useForceUpdate();
  const state = (0, import_react10.useMemo)(
    () => ({
      ctrls: [],
      queue: [],
      flush(ctrl, updates2) {
        const springs2 = getSprings(ctrl, updates2);
        const canFlushSync = layoutId.current > 0 && !state.queue.length && !Object.keys(springs2).some((key) => !ctrl.springs[key]);
        return canFlushSync ? flushUpdateQueue(ctrl, updates2) : new Promise((resolve) => {
          setSprings(ctrl, springs2);
          state.queue.push(() => {
            resolve(flushUpdateQueue(ctrl, updates2));
          });
          forceUpdate();
        });
      }
    }),
    []
  );
  const ctrls = (0, import_react10.useRef)([...state.ctrls]);
  const updates = [];
  const prevLength = usePrev(length) || 0;
  (0, import_react10.useMemo)(() => {
    each(ctrls.current.slice(length, prevLength), (ctrl) => {
      detachRefs(ctrl, ref);
      ctrl.stop(true);
    });
    ctrls.current.length = length;
    declareUpdates(prevLength, length);
  }, [length]);
  (0, import_react10.useMemo)(() => {
    declareUpdates(0, Math.min(prevLength, length));
  }, deps);
  function declareUpdates(startIndex, endIndex) {
    for (let i5 = startIndex; i5 < endIndex; i5++) {
      const ctrl = ctrls.current[i5] || (ctrls.current[i5] = new Controller(null, state.flush));
      const update22 = propsFn ? propsFn(i5, ctrl) : props[i5];
      if (update22) {
        updates[i5] = declareUpdate(update22);
      }
    }
  }
  const springs = ctrls.current.map((ctrl, i5) => getSprings(ctrl, updates[i5]));
  const context = (0, import_react10.useContext)(SpringContext);
  const prevContext = usePrev(context);
  const hasContext = context !== prevContext && hasProps(context);
  useIsomorphicLayoutEffect(() => {
    layoutId.current++;
    state.ctrls = ctrls.current;
    const { queue } = state;
    if (queue.length) {
      state.queue = [];
      each(queue, (cb) => cb());
    }
    each(ctrls.current, (ctrl, i5) => {
      ref == null ? void 0 : ref.add(ctrl);
      if (hasContext) {
        ctrl.start({ default: context });
      }
      const update22 = updates[i5];
      if (update22) {
        replaceRef(ctrl, update22.ref);
        if (ctrl.ref) {
          ctrl.queue.push(update22);
        } else {
          ctrl.start(update22);
        }
      }
    });
  });
  useOnce(() => () => {
    each(state.ctrls, (ctrl) => ctrl.stop(true));
  });
  const values = springs.map((x4) => ({ ...x4 }));
  return ref ? [values, ref] : values;
}
function useSpring(props, deps) {
  const isFn = is.fun(props);
  const [[values], ref] = useSprings(
    1,
    isFn ? props : [props],
    isFn ? deps || [] : deps
  );
  return isFn || arguments.length == 2 ? [values, ref] : values;
}
var Interpolation = class extends FrameValue {
  constructor(source, args) {
    super();
    this.source = source;
    this.idle = true;
    this._active = /* @__PURE__ */ new Set();
    this.calc = createInterpolator(...args);
    const value = this._get();
    const nodeType = getAnimatedType(value);
    setAnimated(this, nodeType.create(value));
  }
  advance(_dt) {
    const value = this._get();
    const oldValue = this.get();
    if (!isEqual(value, oldValue)) {
      getAnimated(this).setValue(value);
      this._onChange(value, this.idle);
    }
    if (!this.idle && checkIdle(this._active)) {
      becomeIdle(this);
    }
  }
  _get() {
    const inputs = is.arr(this.source) ? this.source.map(getFluidValue) : toArray(getFluidValue(this.source));
    return this.calc(...inputs);
  }
  _start() {
    if (this.idle && !checkIdle(this._active)) {
      this.idle = false;
      each(getPayload(this), (node) => {
        node.done = false;
      });
      if (globals_exports.skipAnimation) {
        raf.batchedUpdates(() => this.advance());
        becomeIdle(this);
      } else {
        frameLoop.start(this);
      }
    }
  }
  // Observe our sources only when we're observed.
  _attach() {
    let priority3 = 1;
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        addFluidObserver(source, this);
      }
      if (isFrameValue(source)) {
        if (!source.idle) {
          this._active.add(source);
        }
        priority3 = Math.max(priority3, source.priority + 1);
      }
    });
    this.priority = priority3;
    this._start();
  }
  // Stop observing our sources once we have no observers.
  _detach() {
    each(toArray(this.source), (source) => {
      if (hasFluidValue(source)) {
        removeFluidObserver(source, this);
      }
    });
    this._active.clear();
    becomeIdle(this);
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      if (event.idle) {
        this.advance();
      } else {
        this._active.add(event.parent);
        this._start();
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else if (event.type == "priority") {
      this.priority = toArray(this.source).reduce(
        (highest, parent) => Math.max(highest, (isFrameValue(parent) ? parent.priority : 0) + 1),
        0
      );
    }
  }
};
function isIdle(source) {
  return source.idle !== false;
}
function checkIdle(active) {
  return !active.size || Array.from(active).every(isIdle);
}
function becomeIdle(self2) {
  if (!self2.idle) {
    self2.idle = true;
    each(getPayload(self2), (node) => {
      node.done = true;
    });
    callFluidObservers(self2, {
      type: "idle",
      parent: self2
    });
  }
}
globals_exports.assign({
  createStringInterpolator: createStringInterpolator2,
  to: (source, args) => new Interpolation(source, args)
});
var update2 = frameLoop.advance;

// node_modules/@nivo/core/node_modules/@react-spring/web/dist/react-spring_web.modern.mjs
var import_react_dom = __toESM(require_react_dom(), 1);
var isCustomPropRE = /^--/;
function dangerousStyleValue(name, value) {
  if (value == null || typeof value === "boolean" || value === "")
    return "";
  if (typeof value === "number" && value !== 0 && !isCustomPropRE.test(name) && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name]))
    return value + "px";
  return ("" + value).trim();
}
var attributeCache = {};
function applyAnimatedValues(instance, props) {
  if (!instance.nodeType || !instance.setAttribute) {
    return false;
  }
  const isFilterElement = instance.nodeName === "filter" || instance.parentNode && instance.parentNode.nodeName === "filter";
  const {
    className,
    style,
    children,
    scrollTop,
    scrollLeft,
    viewBox,
    ...attributes
  } = props;
  const values = Object.values(attributes);
  const names = Object.keys(attributes).map(
    (name) => isFilterElement || instance.hasAttribute(name) ? name : attributeCache[name] || (attributeCache[name] = name.replace(
      /([A-Z])/g,
      // Attributes are written in dash case
      (n5) => "-" + n5.toLowerCase()
    ))
  );
  if (children !== void 0) {
    instance.textContent = children;
  }
  for (const name in style) {
    if (style.hasOwnProperty(name)) {
      const value = dangerousStyleValue(name, style[name]);
      if (isCustomPropRE.test(name)) {
        instance.style.setProperty(name, value);
      } else {
        instance.style[name] = value;
      }
    }
  }
  names.forEach((name, i5) => {
    instance.setAttribute(name, values[i5]);
  });
  if (className !== void 0) {
    instance.className = className;
  }
  if (scrollTop !== void 0) {
    instance.scrollTop = scrollTop;
  }
  if (scrollLeft !== void 0) {
    instance.scrollLeft = scrollLeft;
  }
  if (viewBox !== void 0) {
    instance.setAttribute("viewBox", viewBox);
  }
}
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
var prefixKey = (prefix3, key) => prefix3 + key.charAt(0).toUpperCase() + key.substring(1);
var prefixes = ["Webkit", "Ms", "Moz", "O"];
isUnitlessNumber = Object.keys(isUnitlessNumber).reduce((acc, prop) => {
  prefixes.forEach((prefix3) => acc[prefixKey(prefix3, prop)] = acc[prop]);
  return acc;
}, isUnitlessNumber);
var domTransforms = /^(matrix|translate|scale|rotate|skew)/;
var pxTransforms = /^(translate)/;
var degTransforms = /^(rotate|skew)/;
var addUnit = (value, unit) => is.num(value) && value !== 0 ? value + unit : value;
var isValueIdentity = (value, id) => is.arr(value) ? value.every((v4) => isValueIdentity(v4, id)) : is.num(value) ? value === id : parseFloat(value) === id;
var AnimatedStyle = class extends AnimatedObject {
  constructor({ x: x4, y: y6, z: z3, ...style }) {
    const inputs = [];
    const transforms = [];
    if (x4 || y6 || z3) {
      inputs.push([x4 || 0, y6 || 0, z3 || 0]);
      transforms.push((xyz) => [
        `translate3d(${xyz.map((v4) => addUnit(v4, "px")).join(",")})`,
        // prettier-ignore
        isValueIdentity(xyz, 0)
      ]);
    }
    eachProp(style, (value, key) => {
      if (key === "transform") {
        inputs.push([value || ""]);
        transforms.push((transform) => [transform, transform === ""]);
      } else if (domTransforms.test(key)) {
        delete style[key];
        if (is.und(value))
          return;
        const unit = pxTransforms.test(key) ? "px" : degTransforms.test(key) ? "deg" : "";
        inputs.push(toArray(value));
        transforms.push(
          key === "rotate3d" ? ([x22, y22, z22, deg]) => [
            `rotate3d(${x22},${y22},${z22},${addUnit(deg, unit)})`,
            isValueIdentity(deg, 0)
          ] : (input) => [
            `${key}(${input.map((v4) => addUnit(v4, unit)).join(",")})`,
            isValueIdentity(input, key.startsWith("scale") ? 1 : 0)
          ]
        );
      }
    });
    if (inputs.length) {
      style.transform = new FluidTransform(inputs, transforms);
    }
    super(style);
  }
};
var FluidTransform = class extends FluidValue {
  constructor(inputs, transforms) {
    super();
    this.inputs = inputs;
    this.transforms = transforms;
    this._value = null;
  }
  get() {
    return this._value || (this._value = this._get());
  }
  _get() {
    let transform = "";
    let identity10 = true;
    each(this.inputs, (input, i5) => {
      const arg1 = getFluidValue(input[0]);
      const [t7, id] = this.transforms[i5](
        is.arr(arg1) ? arg1 : input.map(getFluidValue)
      );
      transform += " " + t7;
      identity10 = identity10 && id;
    });
    return identity10 ? "none" : transform;
  }
  // Start observing our inputs once we have an observer.
  observerAdded(count5) {
    if (count5 == 1)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && addFluidObserver(value, this)
        )
      );
  }
  // Stop observing our inputs once we have no observers.
  observerRemoved(count5) {
    if (count5 == 0)
      each(
        this.inputs,
        (input) => each(
          input,
          (value) => hasFluidValue(value) && removeFluidObserver(value, this)
        )
      );
  }
  eventObserved(event) {
    if (event.type == "change") {
      this._value = null;
    }
    callFluidObservers(this, event);
  }
};
var primitives = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan"
];
globals_exports.assign({
  batchedUpdates: import_react_dom.unstable_batchedUpdates,
  createStringInterpolator: createStringInterpolator2,
  colors: colors2
});
var host = createHost(primitives, {
  applyAnimatedValues,
  createAnimatedStyle: (style) => new AnimatedStyle(style),
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  getComponentProps: ({ scrollTop, scrollLeft, ...props }) => props
});
var animated = host.animated;

// node_modules/@nivo/core/node_modules/@nivo/tooltip/dist/nivo-tooltip.es.js
var import_jsx_runtime = __toESM(require_jsx_runtime());
function v() {
  return v = Object.assign ? Object.assign.bind() : function(t7) {
    for (var i5 = 1; i5 < arguments.length; i5++) {
      var o5 = arguments[i5];
      for (var n5 in o5) Object.prototype.hasOwnProperty.call(o5, n5) && (t7[n5] = o5[n5]);
    }
    return t7;
  }, v.apply(this, arguments);
}
var x = { pointerEvents: "none", position: "absolute", zIndex: 10, top: 0, left: 0 };
var m = function(t7, i5) {
  return "translate(" + t7 + "px, " + i5 + "px)";
};
var b = (0, import_react15.memo)(function(t7) {
  var o5, n5 = t7.position, r5 = t7.anchor, e7 = t7.children, l4 = zt(), d2 = Ur(), y6 = d2.animate, f3 = d2.config, b3 = kt(), g3 = b3[0], w3 = b3[1], T3 = (0, import_react15.useRef)(false), C7 = void 0, E5 = false, P3 = w3.width > 0 && w3.height > 0, j4 = Math.round(n5[0]), N4 = Math.round(n5[1]);
  P3 && ("top" === r5 ? (j4 -= w3.width / 2, N4 -= w3.height + 14) : "right" === r5 ? (j4 += 14, N4 -= w3.height / 2) : "bottom" === r5 ? (j4 -= w3.width / 2, N4 += 14) : "left" === r5 ? (j4 -= w3.width + 14, N4 -= w3.height / 2) : "center" === r5 && (j4 -= w3.width / 2, N4 -= w3.height / 2), C7 = { transform: m(j4, N4) }, T3.current || (E5 = true), T3.current = [j4, N4]);
  var O4 = useSpring({ to: C7, config: f3, immediate: !y6 || E5 }), V3 = v({}, x, l4.tooltip.wrapper, { transform: null != (o5 = O4.transform) ? o5 : m(j4, N4), opacity: O4.transform ? 1 : 0 });
  return (0, import_jsx_runtime.jsx)(animated.div, { ref: g3, style: V3, children: e7 });
});
b.displayName = "TooltipWrapper";
var g = (0, import_react15.memo)(function(t7) {
  var i5 = t7.size, o5 = void 0 === i5 ? 12 : i5, n5 = t7.color, r5 = t7.style;
  return (0, import_jsx_runtime.jsx)("span", { style: v({ display: "block", width: o5, height: o5, background: n5 }, void 0 === r5 ? {} : r5) });
});
var w = (0, import_react15.memo)(function(t7) {
  var i5, o5 = t7.id, n5 = t7.value, r5 = t7.format, e7 = t7.enableChip, l4 = void 0 !== e7 && e7, a4 = t7.color, c10 = t7.renderContent, h = zt(), u2 = Ot(r5);
  if ("function" == typeof c10) i5 = c10();
  else {
    var f3 = n5;
    void 0 !== u2 && void 0 !== f3 && (f3 = u2(f3)), i5 = (0, import_jsx_runtime.jsxs)("div", { style: h.tooltip.basic, children: [l4 && (0, import_jsx_runtime.jsx)(g, { color: a4, style: h.tooltip.chip }), void 0 !== f3 ? (0, import_jsx_runtime.jsxs)("span", { children: [o5, ": ", (0, import_jsx_runtime.jsx)("strong", { children: "" + f3 })] }) : o5] });
  }
  return (0, import_jsx_runtime.jsx)("div", { style: h.tooltip.container, children: i5 });
});
var T = { width: "100%", borderCollapse: "collapse" };
var C = (0, import_react15.memo)(function(t7) {
  var i5, o5 = t7.title, n5 = t7.rows, r5 = void 0 === n5 ? [] : n5, e7 = t7.renderContent, l4 = zt();
  return r5.length ? (i5 = "function" == typeof e7 ? e7() : (0, import_jsx_runtime.jsxs)("div", { children: [o5 && o5, (0, import_jsx_runtime.jsx)("table", { style: v({}, T, l4.tooltip.table), children: (0, import_jsx_runtime.jsx)("tbody", { children: r5.map(function(t8, i6) {
    return (0, import_jsx_runtime.jsx)("tr", { children: t8.map(function(t9, i7) {
      return (0, import_jsx_runtime.jsx)("td", { style: l4.tooltip.tableCell, children: t9 }, i7);
    }) }, i6);
  }) }) })] }), (0, import_jsx_runtime.jsx)("div", { style: l4.tooltip.container, children: i5 })) : null;
});
C.displayName = "TableTooltip";
var E = (0, import_react15.memo)(function(t7) {
  var i5 = t7.x0, n5 = t7.x1, r5 = t7.y0, e7 = t7.y1, l4 = zt(), u2 = Ur(), d2 = u2.animate, y6 = u2.config, f3 = (0, import_react15.useMemo)(function() {
    return v({}, l4.crosshair.line, { pointerEvents: "none" });
  }, [l4.crosshair.line]), x4 = useSpring({ x1: i5, x2: n5, y1: r5, y2: e7, config: y6, immediate: !d2 });
  return (0, import_jsx_runtime.jsx)(animated.line, v({}, x4, { fill: "none", style: f3 }));
});
E.displayName = "CrosshairLine";
var P = (0, import_react15.memo)(function(t7) {
  var i5, o5, n5 = t7.width, r5 = t7.height, e7 = t7.type, l4 = t7.x, a4 = t7.y;
  return "cross" === e7 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: r5 }, o5 = { x0: 0, x1: n5, y0: a4, y1: a4 }) : "top-left" === e7 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: a4 }, o5 = { x0: 0, x1: l4, y0: a4, y1: a4 }) : "top" === e7 ? i5 = { x0: l4, x1: l4, y0: 0, y1: a4 } : "top-right" === e7 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: a4 }, o5 = { x0: l4, x1: n5, y0: a4, y1: a4 }) : "right" === e7 ? o5 = { x0: l4, x1: n5, y0: a4, y1: a4 } : "bottom-right" === e7 ? (i5 = { x0: l4, x1: l4, y0: a4, y1: r5 }, o5 = { x0: l4, x1: n5, y0: a4, y1: a4 }) : "bottom" === e7 ? i5 = { x0: l4, x1: l4, y0: a4, y1: r5 } : "bottom-left" === e7 ? (i5 = { x0: l4, x1: l4, y0: a4, y1: r5 }, o5 = { x0: 0, x1: l4, y0: a4, y1: a4 }) : "left" === e7 ? o5 = { x0: 0, x1: l4, y0: a4, y1: a4 } : "x" === e7 ? i5 = { x0: l4, x1: l4, y0: 0, y1: r5 } : "y" === e7 && (o5 = { x0: 0, x1: n5, y0: a4, y1: a4 }), (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [i5 && (0, import_jsx_runtime.jsx)(E, { x0: i5.x0, x1: i5.x1, y0: i5.y0, y1: i5.y1 }), o5 && (0, import_jsx_runtime.jsx)(E, { x0: o5.x0, x1: o5.x1, y0: o5.y0, y1: o5.y1 })] });
});
P.displayName = "Crosshair";
var j = (0, import_react15.createContext)({ showTooltipAt: function() {
}, showTooltipFromEvent: function() {
}, hideTooltip: function() {
} });
var N = { isVisible: false, position: [null, null], content: null, anchor: null };
var O = (0, import_react15.createContext)(N);
var V = function(t7) {
  var i5 = (0, import_react15.useState)(N), n5 = i5[0], l4 = i5[1], a4 = (0, import_react15.useCallback)(function(t8, i6, o5) {
    var n6 = i6[0], r5 = i6[1];
    void 0 === o5 && (o5 = "top"), l4({ isVisible: true, position: [n6, r5], anchor: o5, content: t8 });
  }, [l4]), c10 = (0, import_react15.useCallback)(function(i6, o5, n6) {
    void 0 === n6 && (n6 = "top");
    var r5 = t7.current.getBoundingClientRect(), e7 = t7.current.offsetWidth, a5 = e7 === r5.width ? 1 : e7 / r5.width, c11 = "touches" in o5 ? o5.touches[0] : o5, s4 = c11.clientX, h = c11.clientY, u2 = (s4 - r5.left) * a5, d2 = (h - r5.top) * a5;
    "left" !== n6 && "right" !== n6 || (n6 = u2 < r5.width / 2 ? "right" : "left"), l4({ isVisible: true, position: [u2, d2], anchor: n6, content: i6 });
  }, [t7, l4]), s3 = (0, import_react15.useCallback)(function() {
    l4(N);
  }, [l4]);
  return { actions: (0, import_react15.useMemo)(function() {
    return { showTooltipAt: a4, showTooltipFromEvent: c10, hideTooltip: s3 };
  }, [a4, c10, s3]), state: n5 };
};
var z = function() {
  var t7 = (0, import_react15.useContext)(O);
  if (void 0 === t7) throw new Error("useTooltipState must be used within a TooltipProvider");
  return t7;
};
var A = function(t7) {
  return t7.isVisible;
};
var F = function() {
  var t7 = z();
  return A(t7) ? (0, import_jsx_runtime.jsx)(b, { position: t7.position, anchor: t7.anchor, children: t7.content }) : null;
};
var M = function(t7) {
  var i5 = t7.container, o5 = t7.children, n5 = V(i5), r5 = n5.actions, e7 = n5.state;
  return (0, import_jsx_runtime.jsx)(j.Provider, { value: r5, children: (0, import_jsx_runtime.jsx)(O.Provider, { value: e7, children: o5 }) });
};

// node_modules/@nivo/core/dist/nivo-core.es.js
var import_merge3 = __toESM(require_merge());
var import_get = __toESM(require_get());
var import_set2 = __toESM(require_set());

// node_modules/@nivo/core/node_modules/d3-color/src/define.js
function define_default(constructor, factory, prototype) {
  constructor.prototype = factory.prototype = prototype;
  prototype.constructor = constructor;
}
function extend(parent, definition) {
  var prototype = Object.create(parent.prototype);
  for (var key in definition) prototype[key] = definition[key];
  return prototype;
}

// node_modules/@nivo/core/node_modules/d3-color/src/color.js
function Color() {
}
var darker = 0.7;
var brighter = 1 / darker;
var reI = "\\s*([+-]?\\d+)\\s*";
var reN = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
var reP = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
var reHex = /^#([0-9a-f]{3,8})$/;
var reRgbInteger = new RegExp(`^rgb\\(${reI},${reI},${reI}\\)$`);
var reRgbPercent = new RegExp(`^rgb\\(${reP},${reP},${reP}\\)$`);
var reRgbaInteger = new RegExp(`^rgba\\(${reI},${reI},${reI},${reN}\\)$`);
var reRgbaPercent = new RegExp(`^rgba\\(${reP},${reP},${reP},${reN}\\)$`);
var reHslPercent = new RegExp(`^hsl\\(${reN},${reP},${reP}\\)$`);
var reHslaPercent = new RegExp(`^hsla\\(${reN},${reP},${reP},${reN}\\)$`);
var named = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
define_default(Color, color, {
  copy(channels) {
    return Object.assign(new this.constructor(), this, channels);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: color_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: color_formatHex,
  formatHex8: color_formatHex8,
  formatHsl: color_formatHsl,
  formatRgb: color_formatRgb,
  toString: color_formatRgb
});
function color_formatHex() {
  return this.rgb().formatHex();
}
function color_formatHex8() {
  return this.rgb().formatHex8();
}
function color_formatHsl() {
  return hslConvert(this).formatHsl();
}
function color_formatRgb() {
  return this.rgb().formatRgb();
}
function color(format3) {
  var m5, l4;
  format3 = (format3 + "").trim().toLowerCase();
  return (m5 = reHex.exec(format3)) ? (l4 = m5[1].length, m5 = parseInt(m5[1], 16), l4 === 6 ? rgbn(m5) : l4 === 3 ? new Rgb(m5 >> 8 & 15 | m5 >> 4 & 240, m5 >> 4 & 15 | m5 & 240, (m5 & 15) << 4 | m5 & 15, 1) : l4 === 8 ? rgba2(m5 >> 24 & 255, m5 >> 16 & 255, m5 >> 8 & 255, (m5 & 255) / 255) : l4 === 4 ? rgba2(m5 >> 12 & 15 | m5 >> 8 & 240, m5 >> 8 & 15 | m5 >> 4 & 240, m5 >> 4 & 15 | m5 & 240, ((m5 & 15) << 4 | m5 & 15) / 255) : null) : (m5 = reRgbInteger.exec(format3)) ? new Rgb(m5[1], m5[2], m5[3], 1) : (m5 = reRgbPercent.exec(format3)) ? new Rgb(m5[1] * 255 / 100, m5[2] * 255 / 100, m5[3] * 255 / 100, 1) : (m5 = reRgbaInteger.exec(format3)) ? rgba2(m5[1], m5[2], m5[3], m5[4]) : (m5 = reRgbaPercent.exec(format3)) ? rgba2(m5[1] * 255 / 100, m5[2] * 255 / 100, m5[3] * 255 / 100, m5[4]) : (m5 = reHslPercent.exec(format3)) ? hsla2(m5[1], m5[2] / 100, m5[3] / 100, 1) : (m5 = reHslaPercent.exec(format3)) ? hsla2(m5[1], m5[2] / 100, m5[3] / 100, m5[4]) : named.hasOwnProperty(format3) ? rgbn(named[format3]) : format3 === "transparent" ? new Rgb(NaN, NaN, NaN, 0) : null;
}
function rgbn(n5) {
  return new Rgb(n5 >> 16 & 255, n5 >> 8 & 255, n5 & 255, 1);
}
function rgba2(r5, g3, b3, a4) {
  if (a4 <= 0) r5 = g3 = b3 = NaN;
  return new Rgb(r5, g3, b3, a4);
}
function rgbConvert(o5) {
  if (!(o5 instanceof Color)) o5 = color(o5);
  if (!o5) return new Rgb();
  o5 = o5.rgb();
  return new Rgb(o5.r, o5.g, o5.b, o5.opacity);
}
function rgb2(r5, g3, b3, opacity) {
  return arguments.length === 1 ? rgbConvert(r5) : new Rgb(r5, g3, b3, opacity == null ? 1 : opacity);
}
function Rgb(r5, g3, b3, opacity) {
  this.r = +r5;
  this.g = +g3;
  this.b = +b3;
  this.opacity = +opacity;
}
define_default(Rgb, rgb2, extend(Color, {
  brighter(k3) {
    k3 = k3 == null ? brighter : Math.pow(brighter, k3);
    return new Rgb(this.r * k3, this.g * k3, this.b * k3, this.opacity);
  },
  darker(k3) {
    k3 = k3 == null ? darker : Math.pow(darker, k3);
    return new Rgb(this.r * k3, this.g * k3, this.b * k3, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Rgb(clampi(this.r), clampi(this.g), clampi(this.b), clampa(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && (-0.5 <= this.g && this.g < 255.5) && (-0.5 <= this.b && this.b < 255.5) && (0 <= this.opacity && this.opacity <= 1);
  },
  hex: rgb_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: rgb_formatHex,
  formatHex8: rgb_formatHex8,
  formatRgb: rgb_formatRgb,
  toString: rgb_formatRgb
}));
function rgb_formatHex() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}`;
}
function rgb_formatHex8() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}${hex((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function rgb_formatRgb() {
  const a4 = clampa(this.opacity);
  return `${a4 === 1 ? "rgb(" : "rgba("}${clampi(this.r)}, ${clampi(this.g)}, ${clampi(this.b)}${a4 === 1 ? ")" : `, ${a4})`}`;
}
function clampa(opacity) {
  return isNaN(opacity) ? 1 : Math.max(0, Math.min(1, opacity));
}
function clampi(value) {
  return Math.max(0, Math.min(255, Math.round(value) || 0));
}
function hex(value) {
  value = clampi(value);
  return (value < 16 ? "0" : "") + value.toString(16);
}
function hsla2(h, s3, l4, a4) {
  if (a4 <= 0) h = s3 = l4 = NaN;
  else if (l4 <= 0 || l4 >= 1) h = s3 = NaN;
  else if (s3 <= 0) h = NaN;
  return new Hsl(h, s3, l4, a4);
}
function hslConvert(o5) {
  if (o5 instanceof Hsl) return new Hsl(o5.h, o5.s, o5.l, o5.opacity);
  if (!(o5 instanceof Color)) o5 = color(o5);
  if (!o5) return new Hsl();
  if (o5 instanceof Hsl) return o5;
  o5 = o5.rgb();
  var r5 = o5.r / 255, g3 = o5.g / 255, b3 = o5.b / 255, min5 = Math.min(r5, g3, b3), max5 = Math.max(r5, g3, b3), h = NaN, s3 = max5 - min5, l4 = (max5 + min5) / 2;
  if (s3) {
    if (r5 === max5) h = (g3 - b3) / s3 + (g3 < b3) * 6;
    else if (g3 === max5) h = (b3 - r5) / s3 + 2;
    else h = (r5 - g3) / s3 + 4;
    s3 /= l4 < 0.5 ? max5 + min5 : 2 - max5 - min5;
    h *= 60;
  } else {
    s3 = l4 > 0 && l4 < 1 ? 0 : h;
  }
  return new Hsl(h, s3, l4, o5.opacity);
}
function hsl2(h, s3, l4, opacity) {
  return arguments.length === 1 ? hslConvert(h) : new Hsl(h, s3, l4, opacity == null ? 1 : opacity);
}
function Hsl(h, s3, l4, opacity) {
  this.h = +h;
  this.s = +s3;
  this.l = +l4;
  this.opacity = +opacity;
}
define_default(Hsl, hsl2, extend(Color, {
  brighter(k3) {
    k3 = k3 == null ? brighter : Math.pow(brighter, k3);
    return new Hsl(this.h, this.s, this.l * k3, this.opacity);
  },
  darker(k3) {
    k3 = k3 == null ? darker : Math.pow(darker, k3);
    return new Hsl(this.h, this.s, this.l * k3, this.opacity);
  },
  rgb() {
    var h = this.h % 360 + (this.h < 0) * 360, s3 = isNaN(h) || isNaN(this.s) ? 0 : this.s, l4 = this.l, m22 = l4 + (l4 < 0.5 ? l4 : 1 - l4) * s3, m1 = 2 * l4 - m22;
    return new Rgb(
      hsl2rgb(h >= 240 ? h - 240 : h + 120, m1, m22),
      hsl2rgb(h, m1, m22),
      hsl2rgb(h < 120 ? h + 240 : h - 120, m1, m22),
      this.opacity
    );
  },
  clamp() {
    return new Hsl(clamph(this.h), clampt(this.s), clampt(this.l), clampa(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && (0 <= this.l && this.l <= 1) && (0 <= this.opacity && this.opacity <= 1);
  },
  formatHsl() {
    const a4 = clampa(this.opacity);
    return `${a4 === 1 ? "hsl(" : "hsla("}${clamph(this.h)}, ${clampt(this.s) * 100}%, ${clampt(this.l) * 100}%${a4 === 1 ? ")" : `, ${a4})`}`;
  }
}));
function clamph(value) {
  value = (value || 0) % 360;
  return value < 0 ? value + 360 : value;
}
function clampt(value) {
  return Math.max(0, Math.min(1, value || 0));
}
function hsl2rgb(h, m1, m22) {
  return (h < 60 ? m1 + (m22 - m1) * h / 60 : h < 180 ? m22 : h < 240 ? m1 + (m22 - m1) * (240 - h) / 60 : m1) * 255;
}

// node_modules/@nivo/core/node_modules/d3-color/src/math.js
var radians = Math.PI / 180;
var degrees = 180 / Math.PI;

// node_modules/@nivo/core/node_modules/d3-color/src/lab.js
var K = 18;
var Xn = 0.96422;
var Yn = 1;
var Zn = 0.82521;
var t0 = 4 / 29;
var t1 = 6 / 29;
var t2 = 3 * t1 * t1;
var t3 = t1 * t1 * t1;
function labConvert(o5) {
  if (o5 instanceof Lab) return new Lab(o5.l, o5.a, o5.b, o5.opacity);
  if (o5 instanceof Hcl) return hcl2lab(o5);
  if (!(o5 instanceof Rgb)) o5 = rgbConvert(o5);
  var r5 = rgb2lrgb(o5.r), g3 = rgb2lrgb(o5.g), b3 = rgb2lrgb(o5.b), y6 = xyz2lab((0.2225045 * r5 + 0.7168786 * g3 + 0.0606169 * b3) / Yn), x4, z3;
  if (r5 === g3 && g3 === b3) x4 = z3 = y6;
  else {
    x4 = xyz2lab((0.4360747 * r5 + 0.3850649 * g3 + 0.1430804 * b3) / Xn);
    z3 = xyz2lab((0.0139322 * r5 + 0.0971045 * g3 + 0.7141733 * b3) / Zn);
  }
  return new Lab(116 * y6 - 16, 500 * (x4 - y6), 200 * (y6 - z3), o5.opacity);
}
function lab(l4, a4, b3, opacity) {
  return arguments.length === 1 ? labConvert(l4) : new Lab(l4, a4, b3, opacity == null ? 1 : opacity);
}
function Lab(l4, a4, b3, opacity) {
  this.l = +l4;
  this.a = +a4;
  this.b = +b3;
  this.opacity = +opacity;
}
define_default(Lab, lab, extend(Color, {
  brighter(k3) {
    return new Lab(this.l + K * (k3 == null ? 1 : k3), this.a, this.b, this.opacity);
  },
  darker(k3) {
    return new Lab(this.l - K * (k3 == null ? 1 : k3), this.a, this.b, this.opacity);
  },
  rgb() {
    var y6 = (this.l + 16) / 116, x4 = isNaN(this.a) ? y6 : y6 + this.a / 500, z3 = isNaN(this.b) ? y6 : y6 - this.b / 200;
    x4 = Xn * lab2xyz(x4);
    y6 = Yn * lab2xyz(y6);
    z3 = Zn * lab2xyz(z3);
    return new Rgb(
      lrgb2rgb(3.1338561 * x4 - 1.6168667 * y6 - 0.4906146 * z3),
      lrgb2rgb(-0.9787684 * x4 + 1.9161415 * y6 + 0.033454 * z3),
      lrgb2rgb(0.0719453 * x4 - 0.2289914 * y6 + 1.4052427 * z3),
      this.opacity
    );
  }
}));
function xyz2lab(t7) {
  return t7 > t3 ? Math.pow(t7, 1 / 3) : t7 / t2 + t0;
}
function lab2xyz(t7) {
  return t7 > t1 ? t7 * t7 * t7 : t2 * (t7 - t0);
}
function lrgb2rgb(x4) {
  return 255 * (x4 <= 31308e-7 ? 12.92 * x4 : 1.055 * Math.pow(x4, 1 / 2.4) - 0.055);
}
function rgb2lrgb(x4) {
  return (x4 /= 255) <= 0.04045 ? x4 / 12.92 : Math.pow((x4 + 0.055) / 1.055, 2.4);
}
function hclConvert(o5) {
  if (o5 instanceof Hcl) return new Hcl(o5.h, o5.c, o5.l, o5.opacity);
  if (!(o5 instanceof Lab)) o5 = labConvert(o5);
  if (o5.a === 0 && o5.b === 0) return new Hcl(NaN, 0 < o5.l && o5.l < 100 ? 0 : NaN, o5.l, o5.opacity);
  var h = Math.atan2(o5.b, o5.a) * degrees;
  return new Hcl(h < 0 ? h + 360 : h, Math.sqrt(o5.a * o5.a + o5.b * o5.b), o5.l, o5.opacity);
}
function hcl(h, c10, l4, opacity) {
  return arguments.length === 1 ? hclConvert(h) : new Hcl(h, c10, l4, opacity == null ? 1 : opacity);
}
function Hcl(h, c10, l4, opacity) {
  this.h = +h;
  this.c = +c10;
  this.l = +l4;
  this.opacity = +opacity;
}
function hcl2lab(o5) {
  if (isNaN(o5.h)) return new Lab(o5.l, 0, 0, o5.opacity);
  var h = o5.h * radians;
  return new Lab(o5.l, Math.cos(h) * o5.c, Math.sin(h) * o5.c, o5.opacity);
}
define_default(Hcl, hcl, extend(Color, {
  brighter(k3) {
    return new Hcl(this.h, this.c, this.l + K * (k3 == null ? 1 : k3), this.opacity);
  },
  darker(k3) {
    return new Hcl(this.h, this.c, this.l - K * (k3 == null ? 1 : k3), this.opacity);
  },
  rgb() {
    return hcl2lab(this).rgb();
  }
}));

// node_modules/@nivo/core/node_modules/d3-color/src/cubehelix.js
var A2 = -0.14861;
var B = 1.78277;
var C2 = -0.29227;
var D = -0.90649;
var E2 = 1.97294;
var ED = E2 * D;
var EB = E2 * B;
var BC_DA = B * C2 - D * A2;
function cubehelixConvert(o5) {
  if (o5 instanceof Cubehelix) return new Cubehelix(o5.h, o5.s, o5.l, o5.opacity);
  if (!(o5 instanceof Rgb)) o5 = rgbConvert(o5);
  var r5 = o5.r / 255, g3 = o5.g / 255, b3 = o5.b / 255, l4 = (BC_DA * b3 + ED * r5 - EB * g3) / (BC_DA + ED - EB), bl = b3 - l4, k3 = (E2 * (g3 - l4) - C2 * bl) / D, s3 = Math.sqrt(k3 * k3 + bl * bl) / (E2 * l4 * (1 - l4)), h = s3 ? Math.atan2(k3, bl) * degrees - 120 : NaN;
  return new Cubehelix(h < 0 ? h + 360 : h, s3, l4, o5.opacity);
}
function cubehelix(h, s3, l4, opacity) {
  return arguments.length === 1 ? cubehelixConvert(h) : new Cubehelix(h, s3, l4, opacity == null ? 1 : opacity);
}
function Cubehelix(h, s3, l4, opacity) {
  this.h = +h;
  this.s = +s3;
  this.l = +l4;
  this.opacity = +opacity;
}
define_default(Cubehelix, cubehelix, extend(Color, {
  brighter(k3) {
    k3 = k3 == null ? brighter : Math.pow(brighter, k3);
    return new Cubehelix(this.h, this.s, this.l * k3, this.opacity);
  },
  darker(k3) {
    k3 = k3 == null ? darker : Math.pow(darker, k3);
    return new Cubehelix(this.h, this.s, this.l * k3, this.opacity);
  },
  rgb() {
    var h = isNaN(this.h) ? 0 : (this.h + 120) * radians, l4 = +this.l, a4 = isNaN(this.s) ? 0 : this.s * l4 * (1 - l4), cosh4 = Math.cos(h), sinh4 = Math.sin(h);
    return new Rgb(
      255 * (l4 + a4 * (A2 * cosh4 + B * sinh4)),
      255 * (l4 + a4 * (C2 * cosh4 + D * sinh4)),
      255 * (l4 + a4 * (E2 * cosh4)),
      this.opacity
    );
  }
}));

// node_modules/@nivo/core/node_modules/d3-interpolate/src/basis.js
function basis(t16, v0, v1, v22, v32) {
  var t23 = t16 * t16, t33 = t23 * t16;
  return ((1 - 3 * t16 + 3 * t23 - t33) * v0 + (4 - 6 * t23 + 3 * t33) * v1 + (1 + 3 * t16 + 3 * t23 - 3 * t33) * v22 + t33 * v32) / 6;
}
function basis_default(values) {
  var n5 = values.length - 1;
  return function(t7) {
    var i5 = t7 <= 0 ? t7 = 0 : t7 >= 1 ? (t7 = 1, n5 - 1) : Math.floor(t7 * n5), v1 = values[i5], v22 = values[i5 + 1], v0 = i5 > 0 ? values[i5 - 1] : 2 * v1 - v22, v32 = i5 < n5 - 1 ? values[i5 + 2] : 2 * v22 - v1;
    return basis((t7 - i5 / n5) * n5, v0, v1, v22, v32);
  };
}

// node_modules/@nivo/core/node_modules/d3-interpolate/src/basisClosed.js
function basisClosed_default(values) {
  var n5 = values.length;
  return function(t7) {
    var i5 = Math.floor(((t7 %= 1) < 0 ? ++t7 : t7) * n5), v0 = values[(i5 + n5 - 1) % n5], v1 = values[i5 % n5], v22 = values[(i5 + 1) % n5], v32 = values[(i5 + 2) % n5];
    return basis((t7 - i5 / n5) * n5, v0, v1, v22, v32);
  };
}

// node_modules/@nivo/core/node_modules/d3-interpolate/src/constant.js
var constant_default = (x4) => () => x4;

// node_modules/@nivo/core/node_modules/d3-interpolate/src/color.js
function linear(a4, d2) {
  return function(t7) {
    return a4 + t7 * d2;
  };
}
function exponential(a4, b3, y6) {
  return a4 = Math.pow(a4, y6), b3 = Math.pow(b3, y6) - a4, y6 = 1 / y6, function(t7) {
    return Math.pow(a4 + t7 * b3, y6);
  };
}
function hue(a4, b3) {
  var d2 = b3 - a4;
  return d2 ? linear(a4, d2 > 180 || d2 < -180 ? d2 - 360 * Math.round(d2 / 360) : d2) : constant_default(isNaN(a4) ? b3 : a4);
}
function gamma(y6) {
  return (y6 = +y6) === 1 ? nogamma : function(a4, b3) {
    return b3 - a4 ? exponential(a4, b3, y6) : constant_default(isNaN(a4) ? b3 : a4);
  };
}
function nogamma(a4, b3) {
  var d2 = b3 - a4;
  return d2 ? linear(a4, d2) : constant_default(isNaN(a4) ? b3 : a4);
}

// node_modules/@nivo/core/node_modules/d3-interpolate/src/rgb.js
var rgb_default = function rgbGamma(y6) {
  var color3 = gamma(y6);
  function rgb5(start3, end) {
    var r5 = color3((start3 = rgb2(start3)).r, (end = rgb2(end)).r), g3 = color3(start3.g, end.g), b3 = color3(start3.b, end.b), opacity = nogamma(start3.opacity, end.opacity);
    return function(t7) {
      start3.r = r5(t7);
      start3.g = g3(t7);
      start3.b = b3(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  }
  rgb5.gamma = rgbGamma;
  return rgb5;
}(1);
function rgbSpline(spline) {
  return function(colors4) {
    var n5 = colors4.length, r5 = new Array(n5), g3 = new Array(n5), b3 = new Array(n5), i5, color3;
    for (i5 = 0; i5 < n5; ++i5) {
      color3 = rgb2(colors4[i5]);
      r5[i5] = color3.r || 0;
      g3[i5] = color3.g || 0;
      b3[i5] = color3.b || 0;
    }
    r5 = spline(r5);
    g3 = spline(g3);
    b3 = spline(b3);
    color3.opacity = 1;
    return function(t7) {
      color3.r = r5(t7);
      color3.g = g3(t7);
      color3.b = b3(t7);
      return color3 + "";
    };
  };
}
var rgbBasis = rgbSpline(basis_default);
var rgbBasisClosed = rgbSpline(basisClosed_default);

// node_modules/@nivo/core/node_modules/d3-interpolate/src/number.js
function number_default(a4, b3) {
  return a4 = +a4, b3 = +b3, function(t7) {
    return a4 * (1 - t7) + b3 * t7;
  };
}

// node_modules/@nivo/core/node_modules/d3-interpolate/src/string.js
var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var reB = new RegExp(reA.source, "g");

// node_modules/@nivo/core/node_modules/d3-interpolate/src/transform/decompose.js
var degrees2 = 180 / Math.PI;
var identity = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function decompose_default(a4, b3, c10, d2, e7, f3) {
  var scaleX, scaleY, skewX;
  if (scaleX = Math.sqrt(a4 * a4 + b3 * b3)) a4 /= scaleX, b3 /= scaleX;
  if (skewX = a4 * c10 + b3 * d2) c10 -= a4 * skewX, d2 -= b3 * skewX;
  if (scaleY = Math.sqrt(c10 * c10 + d2 * d2)) c10 /= scaleY, d2 /= scaleY, skewX /= scaleY;
  if (a4 * d2 < b3 * c10) a4 = -a4, b3 = -b3, skewX = -skewX, scaleX = -scaleX;
  return {
    translateX: e7,
    translateY: f3,
    rotate: Math.atan2(b3, a4) * degrees2,
    skewX: Math.atan(skewX) * degrees2,
    scaleX,
    scaleY
  };
}

// node_modules/@nivo/core/node_modules/d3-interpolate/src/transform/parse.js
var svgNode;
function parseCss(value) {
  const m5 = new (typeof DOMMatrix === "function" ? DOMMatrix : WebKitCSSMatrix)(value + "");
  return m5.isIdentity ? identity : decompose_default(m5.a, m5.b, m5.c, m5.d, m5.e, m5.f);
}
function parseSvg(value) {
  if (value == null) return identity;
  if (!svgNode) svgNode = document.createElementNS("http://www.w3.org/2000/svg", "g");
  svgNode.setAttribute("transform", value);
  if (!(value = svgNode.transform.baseVal.consolidate())) return identity;
  value = value.matrix;
  return decompose_default(value.a, value.b, value.c, value.d, value.e, value.f);
}

// node_modules/@nivo/core/node_modules/d3-interpolate/src/transform/index.js
function interpolateTransform(parse, pxComma, pxParen, degParen) {
  function pop(s3) {
    return s3.length ? s3.pop() + " " : "";
  }
  function translate(xa, ya, xb, yb, s3, q3) {
    if (xa !== xb || ya !== yb) {
      var i5 = s3.push("translate(", null, pxComma, null, pxParen);
      q3.push({ i: i5 - 4, x: number_default(xa, xb) }, { i: i5 - 2, x: number_default(ya, yb) });
    } else if (xb || yb) {
      s3.push("translate(" + xb + pxComma + yb + pxParen);
    }
  }
  function rotate(a4, b3, s3, q3) {
    if (a4 !== b3) {
      if (a4 - b3 > 180) b3 += 360;
      else if (b3 - a4 > 180) a4 += 360;
      q3.push({ i: s3.push(pop(s3) + "rotate(", null, degParen) - 2, x: number_default(a4, b3) });
    } else if (b3) {
      s3.push(pop(s3) + "rotate(" + b3 + degParen);
    }
  }
  function skewX(a4, b3, s3, q3) {
    if (a4 !== b3) {
      q3.push({ i: s3.push(pop(s3) + "skewX(", null, degParen) - 2, x: number_default(a4, b3) });
    } else if (b3) {
      s3.push(pop(s3) + "skewX(" + b3 + degParen);
    }
  }
  function scale(xa, ya, xb, yb, s3, q3) {
    if (xa !== xb || ya !== yb) {
      var i5 = s3.push(pop(s3) + "scale(", null, ",", null, ")");
      q3.push({ i: i5 - 4, x: number_default(xa, xb) }, { i: i5 - 2, x: number_default(ya, yb) });
    } else if (xb !== 1 || yb !== 1) {
      s3.push(pop(s3) + "scale(" + xb + "," + yb + ")");
    }
  }
  return function(a4, b3) {
    var s3 = [], q3 = [];
    a4 = parse(a4), b3 = parse(b3);
    translate(a4.translateX, a4.translateY, b3.translateX, b3.translateY, s3, q3);
    rotate(a4.rotate, b3.rotate, s3, q3);
    skewX(a4.skewX, b3.skewX, s3, q3);
    scale(a4.scaleX, a4.scaleY, b3.scaleX, b3.scaleY, s3, q3);
    a4 = b3 = null;
    return function(t7) {
      var i5 = -1, n5 = q3.length, o5;
      while (++i5 < n5) s3[(o5 = q3[i5]).i] = o5.x(t7);
      return s3.join("");
    };
  };
}
var interpolateTransformCss = interpolateTransform(parseCss, "px, ", "px)", "deg)");
var interpolateTransformSvg = interpolateTransform(parseSvg, ", ", ")", ")");

// node_modules/@nivo/core/node_modules/d3-interpolate/src/zoom.js
var epsilon2 = 1e-12;
function cosh(x4) {
  return ((x4 = Math.exp(x4)) + 1 / x4) / 2;
}
function sinh(x4) {
  return ((x4 = Math.exp(x4)) - 1 / x4) / 2;
}
function tanh(x4) {
  return ((x4 = Math.exp(2 * x4)) - 1) / (x4 + 1);
}
var zoom_default = function zoomRho(rho, rho2, rho4) {
  function zoom(p0, p1) {
    var ux0 = p0[0], uy0 = p0[1], w0 = p0[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx = ux1 - ux0, dy = uy1 - uy0, d2 = dx * dx + dy * dy, i5, S;
    if (d2 < epsilon2) {
      S = Math.log(w1 / w0) / rho;
      i5 = function(t7) {
        return [
          ux0 + t7 * dx,
          uy0 + t7 * dy,
          w0 * Math.exp(rho * t7 * S)
        ];
      };
    } else {
      var d1 = Math.sqrt(d2), b0 = (w1 * w1 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1), b1 = (w1 * w1 - w0 * w0 - rho4 * d2) / (2 * w1 * rho2 * d1), r0 = Math.log(Math.sqrt(b0 * b0 + 1) - b0), r1 = Math.log(Math.sqrt(b1 * b1 + 1) - b1);
      S = (r1 - r0) / rho;
      i5 = function(t7) {
        var s3 = t7 * S, coshr0 = cosh(r0), u2 = w0 / (rho2 * d1) * (coshr0 * tanh(rho * s3 + r0) - sinh(r0));
        return [
          ux0 + u2 * dx,
          uy0 + u2 * dy,
          w0 * coshr0 / cosh(rho * s3 + r0)
        ];
      };
    }
    i5.duration = S * 1e3 * rho / Math.SQRT2;
    return i5;
  }
  zoom.rho = function(_2) {
    var _1 = Math.max(1e-3, +_2), _22 = _1 * _1, _4 = _22 * _22;
    return zoomRho(_1, _22, _4);
  };
  return zoom;
}(Math.SQRT2, 2, 4);

// node_modules/@nivo/core/node_modules/d3-interpolate/src/hsl.js
function hsl3(hue4) {
  return function(start3, end) {
    var h = hue4((start3 = hsl2(start3)).h, (end = hsl2(end)).h), s3 = nogamma(start3.s, end.s), l4 = nogamma(start3.l, end.l), opacity = nogamma(start3.opacity, end.opacity);
    return function(t7) {
      start3.h = h(t7);
      start3.s = s3(t7);
      start3.l = l4(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  };
}
var hsl_default = hsl3(hue);
var hslLong = hsl3(nogamma);

// node_modules/@nivo/core/node_modules/d3-interpolate/src/hcl.js
function hcl2(hue4) {
  return function(start3, end) {
    var h = hue4((start3 = hcl(start3)).h, (end = hcl(end)).h), c10 = nogamma(start3.c, end.c), l4 = nogamma(start3.l, end.l), opacity = nogamma(start3.opacity, end.opacity);
    return function(t7) {
      start3.h = h(t7);
      start3.c = c10(t7);
      start3.l = l4(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  };
}
var hcl_default = hcl2(hue);
var hclLong = hcl2(nogamma);

// node_modules/@nivo/core/node_modules/d3-interpolate/src/cubehelix.js
function cubehelix2(hue4) {
  return function cubehelixGamma(y6) {
    y6 = +y6;
    function cubehelix6(start3, end) {
      var h = hue4((start3 = cubehelix(start3)).h, (end = cubehelix(end)).h), s3 = nogamma(start3.s, end.s), l4 = nogamma(start3.l, end.l), opacity = nogamma(start3.opacity, end.opacity);
      return function(t7) {
        start3.h = h(t7);
        start3.s = s3(t7);
        start3.l = l4(Math.pow(t7, y6));
        start3.opacity = opacity(t7);
        return start3 + "";
      };
    }
    cubehelix6.gamma = cubehelixGamma;
    return cubehelix6;
  }(1);
}
var cubehelix_default = cubehelix2(hue);
var cubehelixLong = cubehelix2(nogamma);

// node_modules/@nivo/core/dist/nivo-core.es.js
var import_isString = __toESM(require_isString());
var import_jsx_runtime2 = __toESM(require_jsx_runtime());
var import_last = __toESM(require_last());
var import_isArray = __toESM(require_isArray());

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/ascending.js
function ascending(a4, b3) {
  return a4 == null || b3 == null ? NaN : a4 < b3 ? -1 : a4 > b3 ? 1 : a4 >= b3 ? 0 : NaN;
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/descending.js
function descending(a4, b3) {
  return a4 == null || b3 == null ? NaN : b3 < a4 ? -1 : b3 > a4 ? 1 : b3 >= a4 ? 0 : NaN;
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/bisector.js
function bisector(f3) {
  let compare1, compare2, delta;
  if (f3.length !== 2) {
    compare1 = ascending;
    compare2 = (d2, x4) => ascending(f3(d2), x4);
    delta = (d2, x4) => f3(d2) - x4;
  } else {
    compare1 = f3 === ascending || f3 === descending ? f3 : zero;
    compare2 = f3;
    delta = f3;
  }
  function left(a4, x4, lo = 0, hi = a4.length) {
    if (lo < hi) {
      if (compare1(x4, x4) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a4[mid], x4) < 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function right(a4, x4, lo = 0, hi = a4.length) {
    if (lo < hi) {
      if (compare1(x4, x4) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a4[mid], x4) <= 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function center(a4, x4, lo = 0, hi = a4.length) {
    const i5 = left(a4, x4, lo, hi - 1);
    return i5 > lo && delta(a4[i5 - 1], x4) > -delta(a4[i5], x4) ? i5 - 1 : i5;
  }
  return { left, center, right };
}
function zero() {
  return 0;
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/number.js
function number(x4) {
  return x4 === null ? NaN : +x4;
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/bisect.js
var ascendingBisect = bisector(ascending);
var bisectRight = ascendingBisect.right;
var bisectLeft = ascendingBisect.left;
var bisectCenter = bisector(number).center;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/blur.js
var blur2 = Blur2(blurf);
var blurImage = Blur2(blurfImage);
function Blur2(blur4) {
  return function(data, rx, ry = rx) {
    if (!((rx = +rx) >= 0)) throw new RangeError("invalid rx");
    if (!((ry = +ry) >= 0)) throw new RangeError("invalid ry");
    let { data: values, width, height } = data;
    if (!((width = Math.floor(width)) >= 0)) throw new RangeError("invalid width");
    if (!((height = Math.floor(height !== void 0 ? height : values.length / width)) >= 0)) throw new RangeError("invalid height");
    if (!width || !height || !rx && !ry) return data;
    const blurx = rx && blur4(rx);
    const blury = ry && blur4(ry);
    const temp = values.slice();
    if (blurx && blury) {
      blurh(blurx, temp, values, width, height);
      blurh(blurx, values, temp, width, height);
      blurh(blurx, temp, values, width, height);
      blurv(blury, values, temp, width, height);
      blurv(blury, temp, values, width, height);
      blurv(blury, values, temp, width, height);
    } else if (blurx) {
      blurh(blurx, values, temp, width, height);
      blurh(blurx, temp, values, width, height);
      blurh(blurx, values, temp, width, height);
    } else if (blury) {
      blurv(blury, values, temp, width, height);
      blurv(blury, temp, values, width, height);
      blurv(blury, values, temp, width, height);
    }
    return data;
  };
}
function blurh(blur4, T3, S, w3, h) {
  for (let y6 = 0, n5 = w3 * h; y6 < n5; ) {
    blur4(T3, S, y6, y6 += w3, 1);
  }
}
function blurv(blur4, T3, S, w3, h) {
  for (let x4 = 0, n5 = w3 * h; x4 < w3; ++x4) {
    blur4(T3, S, x4, x4 + n5, w3);
  }
}
function blurfImage(radius) {
  const blur4 = blurf(radius);
  return (T3, S, start3, stop3, step) => {
    start3 <<= 2, stop3 <<= 2, step <<= 2;
    blur4(T3, S, start3 + 0, stop3 + 0, step);
    blur4(T3, S, start3 + 1, stop3 + 1, step);
    blur4(T3, S, start3 + 2, stop3 + 2, step);
    blur4(T3, S, start3 + 3, stop3 + 3, step);
  };
}
function blurf(radius) {
  const radius0 = Math.floor(radius);
  if (radius0 === radius) return bluri(radius);
  const t7 = radius - radius0;
  const w3 = 2 * radius + 1;
  return (T3, S, start3, stop3, step) => {
    if (!((stop3 -= step) >= start3)) return;
    let sum5 = radius0 * S[start3];
    const s0 = step * radius0;
    const s1 = s0 + step;
    for (let i5 = start3, j4 = start3 + s0; i5 < j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5)];
    }
    for (let i5 = start3, j4 = stop3; i5 <= j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5 + s0)];
      T3[i5] = (sum5 + t7 * (S[Math.max(start3, i5 - s1)] + S[Math.min(stop3, i5 + s1)])) / w3;
      sum5 -= S[Math.max(start3, i5 - s0)];
    }
  };
}
function bluri(radius) {
  const w3 = 2 * radius + 1;
  return (T3, S, start3, stop3, step) => {
    if (!((stop3 -= step) >= start3)) return;
    let sum5 = radius * S[start3];
    const s3 = step * radius;
    for (let i5 = start3, j4 = start3 + s3; i5 < j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5)];
    }
    for (let i5 = start3, j4 = stop3; i5 <= j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5 + s3)];
      T3[i5] = sum5 / w3;
      sum5 -= S[Math.max(start3, i5 - s3)];
    }
  };
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/node_modules/internmap/src/index.js
var InternMap = class extends Map {
  constructor(entries, key = keyof) {
    super();
    Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: key } });
    if (entries != null) for (const [key2, value] of entries) this.set(key2, value);
  }
  get(key) {
    return super.get(intern_get(this, key));
  }
  has(key) {
    return super.has(intern_get(this, key));
  }
  set(key, value) {
    return super.set(intern_set(this, key), value);
  }
  delete(key) {
    return super.delete(intern_delete(this, key));
  }
};
function intern_get({ _intern, _key }, value) {
  const key = _key(value);
  return _intern.has(key) ? _intern.get(key) : value;
}
function intern_set({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) return _intern.get(key);
  _intern.set(key, value);
  return value;
}
function intern_delete({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) {
    value = _intern.get(key);
    _intern.delete(key);
  }
  return value;
}
function keyof(value) {
  return value !== null && typeof value === "object" ? value.valueOf() : value;
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/array.js
var array = Array.prototype;
var slice = array.slice;
var map = array.map;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/ticks.js
var e10 = Math.sqrt(50);
var e5 = Math.sqrt(10);
var e2 = Math.sqrt(2);
function tickSpec(start3, stop3, count5) {
  const step = (stop3 - start3) / Math.max(0, count5), power = Math.floor(Math.log10(step)), error = step / Math.pow(10, power), factor = error >= e10 ? 10 : error >= e5 ? 5 : error >= e2 ? 2 : 1;
  let i1, i22, inc;
  if (power < 0) {
    inc = Math.pow(10, -power) / factor;
    i1 = Math.round(start3 * inc);
    i22 = Math.round(stop3 * inc);
    if (i1 / inc < start3) ++i1;
    if (i22 / inc > stop3) --i22;
    inc = -inc;
  } else {
    inc = Math.pow(10, power) * factor;
    i1 = Math.round(start3 / inc);
    i22 = Math.round(stop3 / inc);
    if (i1 * inc < start3) ++i1;
    if (i22 * inc > stop3) --i22;
  }
  if (i22 < i1 && 0.5 <= count5 && count5 < 2) return tickSpec(start3, stop3, count5 * 2);
  return [i1, i22, inc];
}
function tickIncrement(start3, stop3, count5) {
  stop3 = +stop3, start3 = +start3, count5 = +count5;
  return tickSpec(start3, stop3, count5)[2];
}
function tickStep(start3, stop3, count5) {
  stop3 = +stop3, start3 = +start3, count5 = +count5;
  const reverse4 = stop3 < start3, inc = reverse4 ? tickIncrement(stop3, start3, count5) : tickIncrement(start3, stop3, count5);
  return (reverse4 ? -1 : 1) * (inc < 0 ? 1 / -inc : inc);
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-array/src/shuffle.js
var shuffle_default = shuffler(Math.random);
function shuffler(random) {
  return function shuffle2(array4, i0 = 0, i1 = array4.length) {
    let m5 = i1 - (i0 = +i0);
    while (m5) {
      const i5 = random() * m5-- | 0, t7 = array4[m5 + i0];
      array4[m5 + i0] = array4[i5 + i0];
      array4[i5 + i0] = t7;
    }
    return array4;
  };
}

// node_modules/@nivo/core/node_modules/d3-scale/src/init.js
function initRange(domain, range3) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(domain);
      break;
    default:
      this.range(range3).domain(domain);
      break;
  }
  return this;
}

// node_modules/@nivo/core/node_modules/d3-scale/src/ordinal.js
var implicit = Symbol("implicit");
function ordinal() {
  var index4 = new InternMap(), domain = [], range3 = [], unknown = implicit;
  function scale(d2) {
    let i5 = index4.get(d2);
    if (i5 === void 0) {
      if (unknown !== implicit) return unknown;
      index4.set(d2, i5 = domain.push(d2) - 1);
    }
    return range3[i5 % range3.length];
  }
  scale.domain = function(_2) {
    if (!arguments.length) return domain.slice();
    domain = [], index4 = new InternMap();
    for (const value of _2) {
      if (index4.has(value)) continue;
      index4.set(value, domain.push(value) - 1);
    }
    return scale;
  };
  scale.range = function(_2) {
    return arguments.length ? (range3 = Array.from(_2), scale) : range3.slice();
  };
  scale.unknown = function(_2) {
    return arguments.length ? (unknown = _2, scale) : unknown;
  };
  scale.copy = function() {
    return ordinal(domain, range3).unknown(unknown);
  };
  initRange.apply(scale, arguments);
  return scale;
}

// node_modules/@nivo/core/node_modules/d3-format/src/formatDecimal.js
function formatDecimal_default(x4) {
  return Math.abs(x4 = Math.round(x4)) >= 1e21 ? x4.toLocaleString("en").replace(/,/g, "") : x4.toString(10);
}
function formatDecimalParts(x4, p3) {
  if ((i5 = (x4 = p3 ? x4.toExponential(p3 - 1) : x4.toExponential()).indexOf("e")) < 0) return null;
  var i5, coefficient = x4.slice(0, i5);
  return [
    coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
    +x4.slice(i5 + 1)
  ];
}

// node_modules/@nivo/core/node_modules/d3-format/src/exponent.js
function exponent_default(x4) {
  return x4 = formatDecimalParts(Math.abs(x4)), x4 ? x4[1] : NaN;
}

// node_modules/@nivo/core/node_modules/d3-format/src/formatGroup.js
function formatGroup_default(grouping, thousands) {
  return function(value, width) {
    var i5 = value.length, t7 = [], j4 = 0, g3 = grouping[0], length = 0;
    while (i5 > 0 && g3 > 0) {
      if (length + g3 + 1 > width) g3 = Math.max(1, width - length);
      t7.push(value.substring(i5 -= g3, i5 + g3));
      if ((length += g3 + 1) > width) break;
      g3 = grouping[j4 = (j4 + 1) % grouping.length];
    }
    return t7.reverse().join(thousands);
  };
}

// node_modules/@nivo/core/node_modules/d3-format/src/formatNumerals.js
function formatNumerals_default(numerals) {
  return function(value) {
    return value.replace(/[0-9]/g, function(i5) {
      return numerals[+i5];
    });
  };
}

// node_modules/@nivo/core/node_modules/d3-format/src/formatSpecifier.js
var re = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function formatSpecifier(specifier) {
  if (!(match = re.exec(specifier))) throw new Error("invalid format: " + specifier);
  var match;
  return new FormatSpecifier({
    fill: match[1],
    align: match[2],
    sign: match[3],
    symbol: match[4],
    zero: match[5],
    width: match[6],
    comma: match[7],
    precision: match[8] && match[8].slice(1),
    trim: match[9],
    type: match[10]
  });
}
formatSpecifier.prototype = FormatSpecifier.prototype;
function FormatSpecifier(specifier) {
  this.fill = specifier.fill === void 0 ? " " : specifier.fill + "";
  this.align = specifier.align === void 0 ? ">" : specifier.align + "";
  this.sign = specifier.sign === void 0 ? "-" : specifier.sign + "";
  this.symbol = specifier.symbol === void 0 ? "" : specifier.symbol + "";
  this.zero = !!specifier.zero;
  this.width = specifier.width === void 0 ? void 0 : +specifier.width;
  this.comma = !!specifier.comma;
  this.precision = specifier.precision === void 0 ? void 0 : +specifier.precision;
  this.trim = !!specifier.trim;
  this.type = specifier.type === void 0 ? "" : specifier.type + "";
}
FormatSpecifier.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};

// node_modules/@nivo/core/node_modules/d3-format/src/formatTrim.js
function formatTrim_default(s3) {
  out: for (var n5 = s3.length, i5 = 1, i0 = -1, i1; i5 < n5; ++i5) {
    switch (s3[i5]) {
      case ".":
        i0 = i1 = i5;
        break;
      case "0":
        if (i0 === 0) i0 = i5;
        i1 = i5;
        break;
      default:
        if (!+s3[i5]) break out;
        if (i0 > 0) i0 = 0;
        break;
    }
  }
  return i0 > 0 ? s3.slice(0, i0) + s3.slice(i1 + 1) : s3;
}

// node_modules/@nivo/core/node_modules/d3-format/src/formatPrefixAuto.js
var prefixExponent;
function formatPrefixAuto_default(x4, p3) {
  var d2 = formatDecimalParts(x4, p3);
  if (!d2) return x4 + "";
  var coefficient = d2[0], exponent = d2[1], i5 = exponent - (prefixExponent = Math.max(-8, Math.min(8, Math.floor(exponent / 3))) * 3) + 1, n5 = coefficient.length;
  return i5 === n5 ? coefficient : i5 > n5 ? coefficient + new Array(i5 - n5 + 1).join("0") : i5 > 0 ? coefficient.slice(0, i5) + "." + coefficient.slice(i5) : "0." + new Array(1 - i5).join("0") + formatDecimalParts(x4, Math.max(0, p3 + i5 - 1))[0];
}

// node_modules/@nivo/core/node_modules/d3-format/src/formatRounded.js
function formatRounded_default(x4, p3) {
  var d2 = formatDecimalParts(x4, p3);
  if (!d2) return x4 + "";
  var coefficient = d2[0], exponent = d2[1];
  return exponent < 0 ? "0." + new Array(-exponent).join("0") + coefficient : coefficient.length > exponent + 1 ? coefficient.slice(0, exponent + 1) + "." + coefficient.slice(exponent + 1) : coefficient + new Array(exponent - coefficient.length + 2).join("0");
}

// node_modules/@nivo/core/node_modules/d3-format/src/formatTypes.js
var formatTypes_default = {
  "%": function(x4, p3) {
    return (x4 * 100).toFixed(p3);
  },
  "b": function(x4) {
    return Math.round(x4).toString(2);
  },
  "c": function(x4) {
    return x4 + "";
  },
  "d": formatDecimal_default,
  "e": function(x4, p3) {
    return x4.toExponential(p3);
  },
  "f": function(x4, p3) {
    return x4.toFixed(p3);
  },
  "g": function(x4, p3) {
    return x4.toPrecision(p3);
  },
  "o": function(x4) {
    return Math.round(x4).toString(8);
  },
  "p": function(x4, p3) {
    return formatRounded_default(x4 * 100, p3);
  },
  "r": formatRounded_default,
  "s": formatPrefixAuto_default,
  "X": function(x4) {
    return Math.round(x4).toString(16).toUpperCase();
  },
  "x": function(x4) {
    return Math.round(x4).toString(16);
  }
};

// node_modules/@nivo/core/node_modules/d3-format/src/identity.js
function identity_default(x4) {
  return x4;
}

// node_modules/@nivo/core/node_modules/d3-format/src/locale.js
var map3 = Array.prototype.map;
var prefixes2 = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function locale_default(locale5) {
  var group4 = locale5.grouping === void 0 || locale5.thousands === void 0 ? identity_default : formatGroup_default(map3.call(locale5.grouping, Number), locale5.thousands + ""), currencyPrefix = locale5.currency === void 0 ? "" : locale5.currency[0] + "", currencySuffix = locale5.currency === void 0 ? "" : locale5.currency[1] + "", decimal = locale5.decimal === void 0 ? "." : locale5.decimal + "", numerals = locale5.numerals === void 0 ? identity_default : formatNumerals_default(map3.call(locale5.numerals, String)), percent = locale5.percent === void 0 ? "%" : locale5.percent + "", minus = locale5.minus === void 0 ? "-" : locale5.minus + "", nan = locale5.nan === void 0 ? "NaN" : locale5.nan + "";
  function newFormat(specifier) {
    specifier = formatSpecifier(specifier);
    var fill = specifier.fill, align = specifier.align, sign2 = specifier.sign, symbol = specifier.symbol, zero3 = specifier.zero, width = specifier.width, comma = specifier.comma, precision = specifier.precision, trim = specifier.trim, type = specifier.type;
    if (type === "n") comma = true, type = "g";
    else if (!formatTypes_default[type]) precision === void 0 && (precision = 12), trim = true, type = "g";
    if (zero3 || fill === "0" && align === "=") zero3 = true, fill = "0", align = "=";
    var prefix3 = symbol === "$" ? currencyPrefix : symbol === "#" && /[boxX]/.test(type) ? "0" + type.toLowerCase() : "", suffix = symbol === "$" ? currencySuffix : /[%p]/.test(type) ? percent : "";
    var formatType = formatTypes_default[type], maybeSuffix = /[defgprs%]/.test(type);
    precision = precision === void 0 ? 6 : /[gprs]/.test(type) ? Math.max(1, Math.min(21, precision)) : Math.max(0, Math.min(20, precision));
    function format3(value) {
      var valuePrefix = prefix3, valueSuffix = suffix, i5, n5, c10;
      if (type === "c") {
        valueSuffix = formatType(value) + valueSuffix;
        value = "";
      } else {
        value = +value;
        var valueNegative = value < 0 || 1 / value < 0;
        value = isNaN(value) ? nan : formatType(Math.abs(value), precision);
        if (trim) value = formatTrim_default(value);
        if (valueNegative && +value === 0 && sign2 !== "+") valueNegative = false;
        valuePrefix = (valueNegative ? sign2 === "(" ? sign2 : minus : sign2 === "-" || sign2 === "(" ? "" : sign2) + valuePrefix;
        valueSuffix = (type === "s" ? prefixes2[8 + prefixExponent / 3] : "") + valueSuffix + (valueNegative && sign2 === "(" ? ")" : "");
        if (maybeSuffix) {
          i5 = -1, n5 = value.length;
          while (++i5 < n5) {
            if (c10 = value.charCodeAt(i5), 48 > c10 || c10 > 57) {
              valueSuffix = (c10 === 46 ? decimal + value.slice(i5 + 1) : value.slice(i5)) + valueSuffix;
              value = value.slice(0, i5);
              break;
            }
          }
        }
      }
      if (comma && !zero3) value = group4(value, Infinity);
      var length = valuePrefix.length + value.length + valueSuffix.length, padding = length < width ? new Array(width - length + 1).join(fill) : "";
      if (comma && zero3) value = group4(padding + value, padding.length ? width - valueSuffix.length : Infinity), padding = "";
      switch (align) {
        case "<":
          value = valuePrefix + value + valueSuffix + padding;
          break;
        case "=":
          value = valuePrefix + padding + value + valueSuffix;
          break;
        case "^":
          value = padding.slice(0, length = padding.length >> 1) + valuePrefix + value + valueSuffix + padding.slice(length);
          break;
        default:
          value = padding + valuePrefix + value + valueSuffix;
          break;
      }
      return numerals(value);
    }
    format3.toString = function() {
      return specifier + "";
    };
    return format3;
  }
  function formatPrefix3(specifier, value) {
    var f3 = newFormat((specifier = formatSpecifier(specifier), specifier.type = "f", specifier)), e7 = Math.max(-8, Math.min(8, Math.floor(exponent_default(value) / 3))) * 3, k3 = Math.pow(10, -e7), prefix3 = prefixes2[8 + e7 / 3];
    return function(value2) {
      return f3(k3 * value2) + prefix3;
    };
  }
  return {
    format: newFormat,
    formatPrefix: formatPrefix3
  };
}

// node_modules/@nivo/core/node_modules/d3-format/src/defaultLocale.js
var locale;
var format;
var formatPrefix;
defaultLocale({
  decimal: ".",
  thousands: ",",
  grouping: [3],
  currency: ["$", ""],
  minus: "-"
});
function defaultLocale(definition) {
  locale = locale_default(definition);
  format = locale.format;
  formatPrefix = locale.formatPrefix;
  return locale;
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/interval.js
var t02 = /* @__PURE__ */ new Date();
var t12 = /* @__PURE__ */ new Date();
function timeInterval(floori, offseti, count5, field) {
  function interval(date) {
    return floori(date = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date)), date;
  }
  interval.floor = (date) => {
    return floori(date = /* @__PURE__ */ new Date(+date)), date;
  };
  interval.ceil = (date) => {
    return floori(date = new Date(date - 1)), offseti(date, 1), floori(date), date;
  };
  interval.round = (date) => {
    const d0 = interval(date), d1 = interval.ceil(date);
    return date - d0 < d1 - date ? d0 : d1;
  };
  interval.offset = (date, step) => {
    return offseti(date = /* @__PURE__ */ new Date(+date), step == null ? 1 : Math.floor(step)), date;
  };
  interval.range = (start3, stop3, step) => {
    const range3 = [];
    start3 = interval.ceil(start3);
    step = step == null ? 1 : Math.floor(step);
    if (!(start3 < stop3) || !(step > 0)) return range3;
    let previous;
    do
      range3.push(previous = /* @__PURE__ */ new Date(+start3)), offseti(start3, step), floori(start3);
    while (previous < start3 && start3 < stop3);
    return range3;
  };
  interval.filter = (test) => {
    return timeInterval((date) => {
      if (date >= date) while (floori(date), !test(date)) date.setTime(date - 1);
    }, (date, step) => {
      if (date >= date) {
        if (step < 0) while (++step <= 0) {
          while (offseti(date, -1), !test(date)) {
          }
        }
        else while (--step >= 0) {
          while (offseti(date, 1), !test(date)) {
          }
        }
      }
    });
  };
  if (count5) {
    interval.count = (start3, end) => {
      t02.setTime(+start3), t12.setTime(+end);
      floori(t02), floori(t12);
      return Math.floor(count5(t02, t12));
    };
    interval.every = (step) => {
      step = Math.floor(step);
      return !isFinite(step) || !(step > 0) ? null : !(step > 1) ? interval : interval.filter(field ? (d2) => field(d2) % step === 0 : (d2) => interval.count(0, d2) % step === 0);
    };
  }
  return interval;
}

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/millisecond.js
var millisecond = timeInterval(() => {
}, (date, step) => {
  date.setTime(+date + step);
}, (start3, end) => {
  return end - start3;
});
millisecond.every = (k3) => {
  k3 = Math.floor(k3);
  if (!isFinite(k3) || !(k3 > 0)) return null;
  if (!(k3 > 1)) return millisecond;
  return timeInterval((date) => {
    date.setTime(Math.floor(date / k3) * k3);
  }, (date, step) => {
    date.setTime(+date + step * k3);
  }, (start3, end) => {
    return (end - start3) / k3;
  });
};
var milliseconds = millisecond.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/duration.js
var durationSecond = 1e3;
var durationMinute = durationSecond * 60;
var durationHour = durationMinute * 60;
var durationDay = durationHour * 24;
var durationWeek = durationDay * 7;
var durationMonth = durationDay * 30;
var durationYear = durationDay * 365;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/second.js
var second = timeInterval((date) => {
  date.setTime(date - date.getMilliseconds());
}, (date, step) => {
  date.setTime(+date + step * durationSecond);
}, (start3, end) => {
  return (end - start3) / durationSecond;
}, (date) => {
  return date.getUTCSeconds();
});
var seconds = second.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/minute.js
var timeMinute = timeInterval((date) => {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond);
}, (date, step) => {
  date.setTime(+date + step * durationMinute);
}, (start3, end) => {
  return (end - start3) / durationMinute;
}, (date) => {
  return date.getMinutes();
});
var timeMinutes = timeMinute.range;
var utcMinute = timeInterval((date) => {
  date.setUTCSeconds(0, 0);
}, (date, step) => {
  date.setTime(+date + step * durationMinute);
}, (start3, end) => {
  return (end - start3) / durationMinute;
}, (date) => {
  return date.getUTCMinutes();
});
var utcMinutes = utcMinute.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/hour.js
var timeHour = timeInterval((date) => {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond - date.getMinutes() * durationMinute);
}, (date, step) => {
  date.setTime(+date + step * durationHour);
}, (start3, end) => {
  return (end - start3) / durationHour;
}, (date) => {
  return date.getHours();
});
var timeHours = timeHour.range;
var utcHour = timeInterval((date) => {
  date.setUTCMinutes(0, 0, 0);
}, (date, step) => {
  date.setTime(+date + step * durationHour);
}, (start3, end) => {
  return (end - start3) / durationHour;
}, (date) => {
  return date.getUTCHours();
});
var utcHours = utcHour.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/day.js
var timeDay = timeInterval(
  (date) => date.setHours(0, 0, 0, 0),
  (date, step) => date.setDate(date.getDate() + step),
  (start3, end) => (end - start3 - (end.getTimezoneOffset() - start3.getTimezoneOffset()) * durationMinute) / durationDay,
  (date) => date.getDate() - 1
);
var timeDays = timeDay.range;
var utcDay = timeInterval((date) => {
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCDate(date.getUTCDate() + step);
}, (start3, end) => {
  return (end - start3) / durationDay;
}, (date) => {
  return date.getUTCDate() - 1;
});
var utcDays = utcDay.range;
var unixDay = timeInterval((date) => {
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCDate(date.getUTCDate() + step);
}, (start3, end) => {
  return (end - start3) / durationDay;
}, (date) => {
  return Math.floor(date / durationDay);
});
var unixDays = unixDay.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/week.js
function timeWeekday(i5) {
  return timeInterval((date) => {
    date.setDate(date.getDate() - (date.getDay() + 7 - i5) % 7);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setDate(date.getDate() + step * 7);
  }, (start3, end) => {
    return (end - start3 - (end.getTimezoneOffset() - start3.getTimezoneOffset()) * durationMinute) / durationWeek;
  });
}
var timeSunday = timeWeekday(0);
var timeMonday = timeWeekday(1);
var timeTuesday = timeWeekday(2);
var timeWednesday = timeWeekday(3);
var timeThursday = timeWeekday(4);
var timeFriday = timeWeekday(5);
var timeSaturday = timeWeekday(6);
var timeSundays = timeSunday.range;
var timeMondays = timeMonday.range;
var timeTuesdays = timeTuesday.range;
var timeWednesdays = timeWednesday.range;
var timeThursdays = timeThursday.range;
var timeFridays = timeFriday.range;
var timeSaturdays = timeSaturday.range;
function utcWeekday(i5) {
  return timeInterval((date) => {
    date.setUTCDate(date.getUTCDate() - (date.getUTCDay() + 7 - i5) % 7);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCDate(date.getUTCDate() + step * 7);
  }, (start3, end) => {
    return (end - start3) / durationWeek;
  });
}
var utcSunday = utcWeekday(0);
var utcMonday = utcWeekday(1);
var utcTuesday = utcWeekday(2);
var utcWednesday = utcWeekday(3);
var utcThursday = utcWeekday(4);
var utcFriday = utcWeekday(5);
var utcSaturday = utcWeekday(6);
var utcSundays = utcSunday.range;
var utcMondays = utcMonday.range;
var utcTuesdays = utcTuesday.range;
var utcWednesdays = utcWednesday.range;
var utcThursdays = utcThursday.range;
var utcFridays = utcFriday.range;
var utcSaturdays = utcSaturday.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/month.js
var timeMonth = timeInterval((date) => {
  date.setDate(1);
  date.setHours(0, 0, 0, 0);
}, (date, step) => {
  date.setMonth(date.getMonth() + step);
}, (start3, end) => {
  return end.getMonth() - start3.getMonth() + (end.getFullYear() - start3.getFullYear()) * 12;
}, (date) => {
  return date.getMonth();
});
var timeMonths = timeMonth.range;
var utcMonth = timeInterval((date) => {
  date.setUTCDate(1);
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCMonth(date.getUTCMonth() + step);
}, (start3, end) => {
  return end.getUTCMonth() - start3.getUTCMonth() + (end.getUTCFullYear() - start3.getUTCFullYear()) * 12;
}, (date) => {
  return date.getUTCMonth();
});
var utcMonths = utcMonth.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/year.js
var timeYear = timeInterval((date) => {
  date.setMonth(0, 1);
  date.setHours(0, 0, 0, 0);
}, (date, step) => {
  date.setFullYear(date.getFullYear() + step);
}, (start3, end) => {
  return end.getFullYear() - start3.getFullYear();
}, (date) => {
  return date.getFullYear();
});
timeYear.every = (k3) => {
  return !isFinite(k3 = Math.floor(k3)) || !(k3 > 0) ? null : timeInterval((date) => {
    date.setFullYear(Math.floor(date.getFullYear() / k3) * k3);
    date.setMonth(0, 1);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setFullYear(date.getFullYear() + step * k3);
  });
};
var timeYears = timeYear.range;
var utcYear = timeInterval((date) => {
  date.setUTCMonth(0, 1);
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCFullYear(date.getUTCFullYear() + step);
}, (start3, end) => {
  return end.getUTCFullYear() - start3.getUTCFullYear();
}, (date) => {
  return date.getUTCFullYear();
});
utcYear.every = (k3) => {
  return !isFinite(k3 = Math.floor(k3)) || !(k3 > 0) ? null : timeInterval((date) => {
    date.setUTCFullYear(Math.floor(date.getUTCFullYear() / k3) * k3);
    date.setUTCMonth(0, 1);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCFullYear(date.getUTCFullYear() + step * k3);
  });
};
var utcYears = utcYear.range;

// node_modules/@nivo/core/node_modules/d3-scale/node_modules/d3-time/src/ticks.js
function ticker(year2, month2, week, day2, hour2, minute2) {
  const tickIntervals = [
    [second, 1, durationSecond],
    [second, 5, 5 * durationSecond],
    [second, 15, 15 * durationSecond],
    [second, 30, 30 * durationSecond],
    [minute2, 1, durationMinute],
    [minute2, 5, 5 * durationMinute],
    [minute2, 15, 15 * durationMinute],
    [minute2, 30, 30 * durationMinute],
    [hour2, 1, durationHour],
    [hour2, 3, 3 * durationHour],
    [hour2, 6, 6 * durationHour],
    [hour2, 12, 12 * durationHour],
    [day2, 1, durationDay],
    [day2, 2, 2 * durationDay],
    [week, 1, durationWeek],
    [month2, 1, durationMonth],
    [month2, 3, 3 * durationMonth],
    [year2, 1, durationYear]
  ];
  function ticks3(start3, stop3, count5) {
    const reverse4 = stop3 < start3;
    if (reverse4) [start3, stop3] = [stop3, start3];
    const interval = count5 && typeof count5.range === "function" ? count5 : tickInterval(start3, stop3, count5);
    const ticks4 = interval ? interval.range(start3, +stop3 + 1) : [];
    return reverse4 ? ticks4.reverse() : ticks4;
  }
  function tickInterval(start3, stop3, count5) {
    const target = Math.abs(stop3 - start3) / count5;
    const i5 = bisector(([, , step2]) => step2).right(tickIntervals, target);
    if (i5 === tickIntervals.length) return year2.every(tickStep(start3 / durationYear, stop3 / durationYear, count5));
    if (i5 === 0) return millisecond.every(Math.max(tickStep(start3, stop3, count5), 1));
    const [t7, step] = tickIntervals[target / tickIntervals[i5 - 1][2] < tickIntervals[i5][2] / target ? i5 - 1 : i5];
    return t7.every(step);
  }
  return [ticks3, tickInterval];
}
var [utcTicks, utcTickInterval] = ticker(utcYear, utcMonth, utcSunday, unixDay, utcHour, utcMinute);
var [timeTicks, timeTickInterval] = ticker(timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute);

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/interval.js
var t03 = /* @__PURE__ */ new Date();
var t13 = /* @__PURE__ */ new Date();
function newInterval(floori, offseti, count5, field) {
  function interval(date) {
    return floori(date = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date)), date;
  }
  interval.floor = function(date) {
    return floori(date = /* @__PURE__ */ new Date(+date)), date;
  };
  interval.ceil = function(date) {
    return floori(date = new Date(date - 1)), offseti(date, 1), floori(date), date;
  };
  interval.round = function(date) {
    var d0 = interval(date), d1 = interval.ceil(date);
    return date - d0 < d1 - date ? d0 : d1;
  };
  interval.offset = function(date, step) {
    return offseti(date = /* @__PURE__ */ new Date(+date), step == null ? 1 : Math.floor(step)), date;
  };
  interval.range = function(start3, stop3, step) {
    var range3 = [], previous;
    start3 = interval.ceil(start3);
    step = step == null ? 1 : Math.floor(step);
    if (!(start3 < stop3) || !(step > 0)) return range3;
    do
      range3.push(previous = /* @__PURE__ */ new Date(+start3)), offseti(start3, step), floori(start3);
    while (previous < start3 && start3 < stop3);
    return range3;
  };
  interval.filter = function(test) {
    return newInterval(function(date) {
      if (date >= date) while (floori(date), !test(date)) date.setTime(date - 1);
    }, function(date, step) {
      if (date >= date) {
        if (step < 0) while (++step <= 0) {
          while (offseti(date, -1), !test(date)) {
          }
        }
        else while (--step >= 0) {
          while (offseti(date, 1), !test(date)) {
          }
        }
      }
    });
  };
  if (count5) {
    interval.count = function(start3, end) {
      t03.setTime(+start3), t13.setTime(+end);
      floori(t03), floori(t13);
      return Math.floor(count5(t03, t13));
    };
    interval.every = function(step) {
      step = Math.floor(step);
      return !isFinite(step) || !(step > 0) ? null : !(step > 1) ? interval : interval.filter(field ? function(d2) {
        return field(d2) % step === 0;
      } : function(d2) {
        return interval.count(0, d2) % step === 0;
      });
    };
  }
  return interval;
}

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/millisecond.js
var millisecond2 = newInterval(function() {
}, function(date, step) {
  date.setTime(+date + step);
}, function(start3, end) {
  return end - start3;
});
millisecond2.every = function(k3) {
  k3 = Math.floor(k3);
  if (!isFinite(k3) || !(k3 > 0)) return null;
  if (!(k3 > 1)) return millisecond2;
  return newInterval(function(date) {
    date.setTime(Math.floor(date / k3) * k3);
  }, function(date, step) {
    date.setTime(+date + step * k3);
  }, function(start3, end) {
    return (end - start3) / k3;
  });
};
var millisecond_default = millisecond2;
var milliseconds2 = millisecond2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/duration.js
var durationSecond2 = 1e3;
var durationMinute2 = durationSecond2 * 60;
var durationHour2 = durationMinute2 * 60;
var durationDay2 = durationHour2 * 24;
var durationWeek2 = durationDay2 * 7;
var durationMonth2 = durationDay2 * 30;
var durationYear2 = durationDay2 * 365;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/second.js
var second2 = newInterval(function(date) {
  date.setTime(date - date.getMilliseconds());
}, function(date, step) {
  date.setTime(+date + step * durationSecond2);
}, function(start3, end) {
  return (end - start3) / durationSecond2;
}, function(date) {
  return date.getUTCSeconds();
});
var second_default = second2;
var seconds2 = second2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/minute.js
var minute = newInterval(function(date) {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond2);
}, function(date, step) {
  date.setTime(+date + step * durationMinute2);
}, function(start3, end) {
  return (end - start3) / durationMinute2;
}, function(date) {
  return date.getMinutes();
});
var minute_default = minute;
var minutes = minute.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/hour.js
var hour = newInterval(function(date) {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond2 - date.getMinutes() * durationMinute2);
}, function(date, step) {
  date.setTime(+date + step * durationHour2);
}, function(start3, end) {
  return (end - start3) / durationHour2;
}, function(date) {
  return date.getHours();
});
var hour_default = hour;
var hours = hour.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/day.js
var day = newInterval(
  (date) => date.setHours(0, 0, 0, 0),
  (date, step) => date.setDate(date.getDate() + step),
  (start3, end) => (end - start3 - (end.getTimezoneOffset() - start3.getTimezoneOffset()) * durationMinute2) / durationDay2,
  (date) => date.getDate() - 1
);
var day_default = day;
var days = day.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/week.js
function weekday(i5) {
  return newInterval(function(date) {
    date.setDate(date.getDate() - (date.getDay() + 7 - i5) % 7);
    date.setHours(0, 0, 0, 0);
  }, function(date, step) {
    date.setDate(date.getDate() + step * 7);
  }, function(start3, end) {
    return (end - start3 - (end.getTimezoneOffset() - start3.getTimezoneOffset()) * durationMinute2) / durationWeek2;
  });
}
var sunday = weekday(0);
var monday = weekday(1);
var tuesday = weekday(2);
var wednesday = weekday(3);
var thursday = weekday(4);
var friday = weekday(5);
var saturday = weekday(6);
var sundays = sunday.range;
var mondays = monday.range;
var tuesdays = tuesday.range;
var wednesdays = wednesday.range;
var thursdays = thursday.range;
var fridays = friday.range;
var saturdays = saturday.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/month.js
var month = newInterval(function(date) {
  date.setDate(1);
  date.setHours(0, 0, 0, 0);
}, function(date, step) {
  date.setMonth(date.getMonth() + step);
}, function(start3, end) {
  return end.getMonth() - start3.getMonth() + (end.getFullYear() - start3.getFullYear()) * 12;
}, function(date) {
  return date.getMonth();
});
var month_default = month;
var months = month.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/year.js
var year = newInterval(function(date) {
  date.setMonth(0, 1);
  date.setHours(0, 0, 0, 0);
}, function(date, step) {
  date.setFullYear(date.getFullYear() + step);
}, function(start3, end) {
  return end.getFullYear() - start3.getFullYear();
}, function(date) {
  return date.getFullYear();
});
year.every = function(k3) {
  return !isFinite(k3 = Math.floor(k3)) || !(k3 > 0) ? null : newInterval(function(date) {
    date.setFullYear(Math.floor(date.getFullYear() / k3) * k3);
    date.setMonth(0, 1);
    date.setHours(0, 0, 0, 0);
  }, function(date, step) {
    date.setFullYear(date.getFullYear() + step * k3);
  });
};
var year_default = year;
var years = year.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/utcMinute.js
var utcMinute2 = newInterval(function(date) {
  date.setUTCSeconds(0, 0);
}, function(date, step) {
  date.setTime(+date + step * durationMinute2);
}, function(start3, end) {
  return (end - start3) / durationMinute2;
}, function(date) {
  return date.getUTCMinutes();
});
var utcMinute_default = utcMinute2;
var utcMinutes2 = utcMinute2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/utcHour.js
var utcHour2 = newInterval(function(date) {
  date.setUTCMinutes(0, 0, 0);
}, function(date, step) {
  date.setTime(+date + step * durationHour2);
}, function(start3, end) {
  return (end - start3) / durationHour2;
}, function(date) {
  return date.getUTCHours();
});
var utcHour_default = utcHour2;
var utcHours2 = utcHour2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/utcDay.js
var utcDay2 = newInterval(function(date) {
  date.setUTCHours(0, 0, 0, 0);
}, function(date, step) {
  date.setUTCDate(date.getUTCDate() + step);
}, function(start3, end) {
  return (end - start3) / durationDay2;
}, function(date) {
  return date.getUTCDate() - 1;
});
var utcDay_default = utcDay2;
var utcDays2 = utcDay2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/utcWeek.js
function utcWeekday2(i5) {
  return newInterval(function(date) {
    date.setUTCDate(date.getUTCDate() - (date.getUTCDay() + 7 - i5) % 7);
    date.setUTCHours(0, 0, 0, 0);
  }, function(date, step) {
    date.setUTCDate(date.getUTCDate() + step * 7);
  }, function(start3, end) {
    return (end - start3) / durationWeek2;
  });
}
var utcSunday2 = utcWeekday2(0);
var utcMonday2 = utcWeekday2(1);
var utcTuesday2 = utcWeekday2(2);
var utcWednesday2 = utcWeekday2(3);
var utcThursday2 = utcWeekday2(4);
var utcFriday2 = utcWeekday2(5);
var utcSaturday2 = utcWeekday2(6);
var utcSundays2 = utcSunday2.range;
var utcMondays2 = utcMonday2.range;
var utcTuesdays2 = utcTuesday2.range;
var utcWednesdays2 = utcWednesday2.range;
var utcThursdays2 = utcThursday2.range;
var utcFridays2 = utcFriday2.range;
var utcSaturdays2 = utcSaturday2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/utcMonth.js
var utcMonth2 = newInterval(function(date) {
  date.setUTCDate(1);
  date.setUTCHours(0, 0, 0, 0);
}, function(date, step) {
  date.setUTCMonth(date.getUTCMonth() + step);
}, function(start3, end) {
  return end.getUTCMonth() - start3.getUTCMonth() + (end.getUTCFullYear() - start3.getUTCFullYear()) * 12;
}, function(date) {
  return date.getUTCMonth();
});
var utcMonth_default = utcMonth2;
var utcMonths2 = utcMonth2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/utcYear.js
var utcYear2 = newInterval(function(date) {
  date.setUTCMonth(0, 1);
  date.setUTCHours(0, 0, 0, 0);
}, function(date, step) {
  date.setUTCFullYear(date.getUTCFullYear() + step);
}, function(start3, end) {
  return end.getUTCFullYear() - start3.getUTCFullYear();
}, function(date) {
  return date.getUTCFullYear();
});
utcYear2.every = function(k3) {
  return !isFinite(k3 = Math.floor(k3)) || !(k3 > 0) ? null : newInterval(function(date) {
    date.setUTCFullYear(Math.floor(date.getUTCFullYear() / k3) * k3);
    date.setUTCMonth(0, 1);
    date.setUTCHours(0, 0, 0, 0);
  }, function(date, step) {
    date.setUTCFullYear(date.getUTCFullYear() + step * k3);
  });
};
var utcYear_default = utcYear2;
var utcYears2 = utcYear2.range;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/node_modules/d3-array/src/ascending.js
function ascending_default(a4, b3) {
  return a4 < b3 ? -1 : a4 > b3 ? 1 : a4 >= b3 ? 0 : NaN;
}

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/node_modules/d3-array/src/bisector.js
function bisector_default(f3) {
  let delta = f3;
  let compare = f3;
  if (f3.length === 1) {
    delta = (d2, x4) => f3(d2) - x4;
    compare = ascendingComparator(f3);
  }
  function left(a4, x4, lo, hi) {
    if (lo == null) lo = 0;
    if (hi == null) hi = a4.length;
    while (lo < hi) {
      const mid = lo + hi >>> 1;
      if (compare(a4[mid], x4) < 0) lo = mid + 1;
      else hi = mid;
    }
    return lo;
  }
  function right(a4, x4, lo, hi) {
    if (lo == null) lo = 0;
    if (hi == null) hi = a4.length;
    while (lo < hi) {
      const mid = lo + hi >>> 1;
      if (compare(a4[mid], x4) > 0) hi = mid;
      else lo = mid + 1;
    }
    return lo;
  }
  function center(a4, x4, lo, hi) {
    if (lo == null) lo = 0;
    if (hi == null) hi = a4.length;
    const i5 = left(a4, x4, lo, hi - 1);
    return i5 > lo && delta(a4[i5 - 1], x4) > -delta(a4[i5], x4) ? i5 - 1 : i5;
  }
  return { left, center, right };
}
function ascendingComparator(f3) {
  return (d2, x4) => ascending_default(f3(d2), x4);
}

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/node_modules/d3-array/src/number.js
function number_default2(x4) {
  return x4 === null ? NaN : +x4;
}

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/node_modules/d3-array/src/bisect.js
var ascendingBisect2 = bisector_default(ascending_default);
var bisectRight2 = ascendingBisect2.right;
var bisectLeft2 = ascendingBisect2.left;
var bisectCenter2 = bisector_default(number_default2).center;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/node_modules/d3-array/src/array.js
var array2 = Array.prototype;
var slice2 = array2.slice;
var map4 = array2.map;

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/node_modules/d3-array/src/ticks.js
var e102 = Math.sqrt(50);
var e52 = Math.sqrt(10);
var e22 = Math.sqrt(2);
function tickStep2(start3, stop3, count5) {
  var step0 = Math.abs(stop3 - start3) / Math.max(0, count5), step1 = Math.pow(10, Math.floor(Math.log(step0) / Math.LN10)), error = step0 / step1;
  if (error >= e102) step1 *= 10;
  else if (error >= e52) step1 *= 5;
  else if (error >= e22) step1 *= 2;
  return stop3 < start3 ? -step1 : step1;
}

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/node_modules/d3-array/src/shuffle.js
var shuffle_default2 = shuffler2(Math.random);
function shuffler2(random) {
  return function shuffle2(array4, i0 = 0, i1 = array4.length) {
    let m5 = i1 - (i0 = +i0);
    while (m5) {
      const i5 = random() * m5-- | 0, t7 = array4[m5 + i0];
      array4[m5 + i0] = array4[i5 + i0];
      array4[i5 + i0] = t7;
    }
    return array4;
  };
}

// node_modules/@nivo/core/node_modules/d3-time-format/node_modules/d3-time/src/ticks.js
function ticker2(year2, month2, week, day2, hour2, minute2) {
  const tickIntervals = [
    [second_default, 1, durationSecond2],
    [second_default, 5, 5 * durationSecond2],
    [second_default, 15, 15 * durationSecond2],
    [second_default, 30, 30 * durationSecond2],
    [minute2, 1, durationMinute2],
    [minute2, 5, 5 * durationMinute2],
    [minute2, 15, 15 * durationMinute2],
    [minute2, 30, 30 * durationMinute2],
    [hour2, 1, durationHour2],
    [hour2, 3, 3 * durationHour2],
    [hour2, 6, 6 * durationHour2],
    [hour2, 12, 12 * durationHour2],
    [day2, 1, durationDay2],
    [day2, 2, 2 * durationDay2],
    [week, 1, durationWeek2],
    [month2, 1, durationMonth2],
    [month2, 3, 3 * durationMonth2],
    [year2, 1, durationYear2]
  ];
  function ticks3(start3, stop3, count5) {
    const reverse4 = stop3 < start3;
    if (reverse4) [start3, stop3] = [stop3, start3];
    const interval = count5 && typeof count5.range === "function" ? count5 : tickInterval(start3, stop3, count5);
    const ticks4 = interval ? interval.range(start3, +stop3 + 1) : [];
    return reverse4 ? ticks4.reverse() : ticks4;
  }
  function tickInterval(start3, stop3, count5) {
    const target = Math.abs(stop3 - start3) / count5;
    const i5 = bisector_default(([, , step2]) => step2).right(tickIntervals, target);
    if (i5 === tickIntervals.length) return year2.every(tickStep2(start3 / durationYear2, stop3 / durationYear2, count5));
    if (i5 === 0) return millisecond_default.every(Math.max(tickStep2(start3, stop3, count5), 1));
    const [t7, step] = tickIntervals[target / tickIntervals[i5 - 1][2] < tickIntervals[i5][2] / target ? i5 - 1 : i5];
    return t7.every(step);
  }
  return [ticks3, tickInterval];
}
var [utcTicks2, utcTickInterval2] = ticker2(utcYear_default, utcMonth_default, utcSunday2, utcDay_default, utcHour_default, utcMinute_default);
var [timeTicks2, timeTickInterval2] = ticker2(year_default, month_default, sunday, day_default, hour_default, minute_default);

// node_modules/@nivo/core/node_modules/d3-time-format/src/locale.js
function localDate(d2) {
  if (0 <= d2.y && d2.y < 100) {
    var date = new Date(-1, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L);
    date.setFullYear(d2.y);
    return date;
  }
  return new Date(d2.y, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L);
}
function utcDate(d2) {
  if (0 <= d2.y && d2.y < 100) {
    var date = new Date(Date.UTC(-1, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L));
    date.setUTCFullYear(d2.y);
    return date;
  }
  return new Date(Date.UTC(d2.y, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L));
}
function newDate(y6, m5, d2) {
  return { y: y6, m: m5, d: d2, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale(locale5) {
  var locale_dateTime = locale5.dateTime, locale_date = locale5.date, locale_time = locale5.time, locale_periods = locale5.periods, locale_weekdays = locale5.days, locale_shortWeekdays = locale5.shortDays, locale_months = locale5.months, locale_shortMonths = locale5.shortMonths;
  var periodRe = formatRe(locale_periods), periodLookup = formatLookup(locale_periods), weekdayRe = formatRe(locale_weekdays), weekdayLookup = formatLookup(locale_weekdays), shortWeekdayRe = formatRe(locale_shortWeekdays), shortWeekdayLookup = formatLookup(locale_shortWeekdays), monthRe = formatRe(locale_months), monthLookup = formatLookup(locale_months), shortMonthRe = formatRe(locale_shortMonths), shortMonthLookup = formatLookup(locale_shortMonths);
  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth,
    "e": formatDayOfMonth,
    "f": formatMicroseconds,
    "g": formatYearISO,
    "G": formatFullYearISO,
    "H": formatHour24,
    "I": formatHour12,
    "j": formatDayOfYear,
    "L": formatMilliseconds,
    "m": formatMonthNumber,
    "M": formatMinutes,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatSeconds,
    "u": formatWeekdayNumberMonday,
    "U": formatWeekNumberSunday,
    "V": formatWeekNumberISO,
    "w": formatWeekdayNumberSunday,
    "W": formatWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatYear,
    "Y": formatFullYear,
    "Z": formatZone,
    "%": formatLiteralPercent
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth,
    "e": formatUTCDayOfMonth,
    "f": formatUTCMicroseconds,
    "g": formatUTCYearISO,
    "G": formatUTCFullYearISO,
    "H": formatUTCHour24,
    "I": formatUTCHour12,
    "j": formatUTCDayOfYear,
    "L": formatUTCMilliseconds,
    "m": formatUTCMonthNumber,
    "M": formatUTCMinutes,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatUTCSeconds,
    "u": formatUTCWeekdayNumberMonday,
    "U": formatUTCWeekNumberSunday,
    "V": formatUTCWeekNumberISO,
    "w": formatUTCWeekdayNumberSunday,
    "W": formatUTCWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatUTCYear,
    "Y": formatUTCFullYear,
    "Z": formatUTCZone,
    "%": formatLiteralPercent
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth,
    "e": parseDayOfMonth,
    "f": parseMicroseconds,
    "g": parseYear,
    "G": parseFullYear,
    "H": parseHour24,
    "I": parseHour24,
    "j": parseDayOfYear,
    "L": parseMilliseconds,
    "m": parseMonthNumber,
    "M": parseMinutes,
    "p": parsePeriod,
    "q": parseQuarter,
    "Q": parseUnixTimestamp,
    "s": parseUnixTimestampSeconds,
    "S": parseSeconds,
    "u": parseWeekdayNumberMonday,
    "U": parseWeekNumberSunday,
    "V": parseWeekNumberISO,
    "w": parseWeekdayNumberSunday,
    "W": parseWeekNumberMonday,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear,
    "Y": parseFullYear,
    "Z": parseZone,
    "%": parseLiteralPercent
  };
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats2) {
    return function(date) {
      var string = [], i5 = -1, j4 = 0, n5 = specifier.length, c10, pad3, format3;
      if (!(date instanceof Date)) date = /* @__PURE__ */ new Date(+date);
      while (++i5 < n5) {
        if (specifier.charCodeAt(i5) === 37) {
          string.push(specifier.slice(j4, i5));
          if ((pad3 = pads[c10 = specifier.charAt(++i5)]) != null) c10 = specifier.charAt(++i5);
          else pad3 = c10 === "e" ? " " : "0";
          if (format3 = formats2[c10]) c10 = format3(date, pad3);
          string.push(c10);
          j4 = i5 + 1;
        }
      }
      string.push(specifier.slice(j4, i5));
      return string.join("");
    };
  }
  function newParse(specifier, Z2) {
    return function(string) {
      var d2 = newDate(1900, void 0, 1), i5 = parseSpecifier(d2, specifier, string += "", 0), week, day2;
      if (i5 != string.length) return null;
      if ("Q" in d2) return new Date(d2.Q);
      if ("s" in d2) return new Date(d2.s * 1e3 + ("L" in d2 ? d2.L : 0));
      if (Z2 && !("Z" in d2)) d2.Z = 0;
      if ("p" in d2) d2.H = d2.H % 12 + d2.p * 12;
      if (d2.m === void 0) d2.m = "q" in d2 ? d2.q : 0;
      if ("V" in d2) {
        if (d2.V < 1 || d2.V > 53) return null;
        if (!("w" in d2)) d2.w = 1;
        if ("Z" in d2) {
          week = utcDate(newDate(d2.y, 0, 1)), day2 = week.getUTCDay();
          week = day2 > 4 || day2 === 0 ? utcMonday2.ceil(week) : utcMonday2(week);
          week = utcDay_default.offset(week, (d2.V - 1) * 7);
          d2.y = week.getUTCFullYear();
          d2.m = week.getUTCMonth();
          d2.d = week.getUTCDate() + (d2.w + 6) % 7;
        } else {
          week = localDate(newDate(d2.y, 0, 1)), day2 = week.getDay();
          week = day2 > 4 || day2 === 0 ? monday.ceil(week) : monday(week);
          week = day_default.offset(week, (d2.V - 1) * 7);
          d2.y = week.getFullYear();
          d2.m = week.getMonth();
          d2.d = week.getDate() + (d2.w + 6) % 7;
        }
      } else if ("W" in d2 || "U" in d2) {
        if (!("w" in d2)) d2.w = "u" in d2 ? d2.u % 7 : "W" in d2 ? 1 : 0;
        day2 = "Z" in d2 ? utcDate(newDate(d2.y, 0, 1)).getUTCDay() : localDate(newDate(d2.y, 0, 1)).getDay();
        d2.m = 0;
        d2.d = "W" in d2 ? (d2.w + 6) % 7 + d2.W * 7 - (day2 + 5) % 7 : d2.w + d2.U * 7 - (day2 + 6) % 7;
      }
      if ("Z" in d2) {
        d2.H += d2.Z / 100 | 0;
        d2.M += d2.Z % 100;
        return utcDate(d2);
      }
      return localDate(d2);
    };
  }
  function parseSpecifier(d2, specifier, string, j4) {
    var i5 = 0, n5 = specifier.length, m5 = string.length, c10, parse;
    while (i5 < n5) {
      if (j4 >= m5) return -1;
      c10 = specifier.charCodeAt(i5++);
      if (c10 === 37) {
        c10 = specifier.charAt(i5++);
        parse = parses[c10 in pads ? specifier.charAt(i5++) : c10];
        if (!parse || (j4 = parse(d2, string, j4)) < 0) return -1;
      } else if (c10 != string.charCodeAt(j4++)) {
        return -1;
      }
    }
    return j4;
  }
  function parsePeriod(d2, string, i5) {
    var n5 = periodRe.exec(string.slice(i5));
    return n5 ? (d2.p = periodLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseShortWeekday(d2, string, i5) {
    var n5 = shortWeekdayRe.exec(string.slice(i5));
    return n5 ? (d2.w = shortWeekdayLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseWeekday(d2, string, i5) {
    var n5 = weekdayRe.exec(string.slice(i5));
    return n5 ? (d2.w = weekdayLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseShortMonth(d2, string, i5) {
    var n5 = shortMonthRe.exec(string.slice(i5));
    return n5 ? (d2.m = shortMonthLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseMonth(d2, string, i5) {
    var n5 = monthRe.exec(string.slice(i5));
    return n5 ? (d2.m = monthLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseLocaleDateTime(d2, string, i5) {
    return parseSpecifier(d2, locale_dateTime, string, i5);
  }
  function parseLocaleDate(d2, string, i5) {
    return parseSpecifier(d2, locale_date, string, i5);
  }
  function parseLocaleTime(d2, string, i5) {
    return parseSpecifier(d2, locale_time, string, i5);
  }
  function formatShortWeekday(d2) {
    return locale_shortWeekdays[d2.getDay()];
  }
  function formatWeekday(d2) {
    return locale_weekdays[d2.getDay()];
  }
  function formatShortMonth(d2) {
    return locale_shortMonths[d2.getMonth()];
  }
  function formatMonth(d2) {
    return locale_months[d2.getMonth()];
  }
  function formatPeriod(d2) {
    return locale_periods[+(d2.getHours() >= 12)];
  }
  function formatQuarter(d2) {
    return 1 + ~~(d2.getMonth() / 3);
  }
  function formatUTCShortWeekday(d2) {
    return locale_shortWeekdays[d2.getUTCDay()];
  }
  function formatUTCWeekday(d2) {
    return locale_weekdays[d2.getUTCDay()];
  }
  function formatUTCShortMonth(d2) {
    return locale_shortMonths[d2.getUTCMonth()];
  }
  function formatUTCMonth(d2) {
    return locale_months[d2.getUTCMonth()];
  }
  function formatUTCPeriod(d2) {
    return locale_periods[+(d2.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d2) {
    return 1 + ~~(d2.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f3 = newFormat(specifier += "", formats);
      f3.toString = function() {
        return specifier;
      };
      return f3;
    },
    parse: function(specifier) {
      var p3 = newParse(specifier += "", false);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    },
    utcFormat: function(specifier) {
      var f3 = newFormat(specifier += "", utcFormats);
      f3.toString = function() {
        return specifier;
      };
      return f3;
    },
    utcParse: function(specifier) {
      var p3 = newParse(specifier += "", true);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    }
  };
}
var pads = { "-": "", "_": " ", "0": "0" };
var numberRe = /^\s*\d+/;
var percentRe = /^%/;
var requoteRe = /[\\^$*+?|[\]().{}]/g;
function pad(value, fill, width) {
  var sign2 = value < 0 ? "-" : "", string = (sign2 ? -value : value) + "", length = string.length;
  return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}
function requote(s3) {
  return s3.replace(requoteRe, "\\$&");
}
function formatRe(names) {
  return new RegExp("^(?:" + names.map(requote).join("|") + ")", "i");
}
function formatLookup(names) {
  return new Map(names.map((name, i5) => [name.toLowerCase(), i5]));
}
function parseWeekdayNumberSunday(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 1));
  return n5 ? (d2.w = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekdayNumberMonday(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 1));
  return n5 ? (d2.u = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekNumberSunday(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.U = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekNumberISO(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.V = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekNumberMonday(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.W = +n5[0], i5 + n5[0].length) : -1;
}
function parseFullYear(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 4));
  return n5 ? (d2.y = +n5[0], i5 + n5[0].length) : -1;
}
function parseYear(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.y = +n5[0] + (+n5[0] > 68 ? 1900 : 2e3), i5 + n5[0].length) : -1;
}
function parseZone(d2, string, i5) {
  var n5 = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i5, i5 + 6));
  return n5 ? (d2.Z = n5[1] ? 0 : -(n5[2] + (n5[3] || "00")), i5 + n5[0].length) : -1;
}
function parseQuarter(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 1));
  return n5 ? (d2.q = n5[0] * 3 - 3, i5 + n5[0].length) : -1;
}
function parseMonthNumber(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.m = n5[0] - 1, i5 + n5[0].length) : -1;
}
function parseDayOfMonth(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.d = +n5[0], i5 + n5[0].length) : -1;
}
function parseDayOfYear(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 3));
  return n5 ? (d2.m = 0, d2.d = +n5[0], i5 + n5[0].length) : -1;
}
function parseHour24(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.H = +n5[0], i5 + n5[0].length) : -1;
}
function parseMinutes(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.M = +n5[0], i5 + n5[0].length) : -1;
}
function parseSeconds(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.S = +n5[0], i5 + n5[0].length) : -1;
}
function parseMilliseconds(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 3));
  return n5 ? (d2.L = +n5[0], i5 + n5[0].length) : -1;
}
function parseMicroseconds(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5, i5 + 6));
  return n5 ? (d2.L = Math.floor(n5[0] / 1e3), i5 + n5[0].length) : -1;
}
function parseLiteralPercent(d2, string, i5) {
  var n5 = percentRe.exec(string.slice(i5, i5 + 1));
  return n5 ? i5 + n5[0].length : -1;
}
function parseUnixTimestamp(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5));
  return n5 ? (d2.Q = +n5[0], i5 + n5[0].length) : -1;
}
function parseUnixTimestampSeconds(d2, string, i5) {
  var n5 = numberRe.exec(string.slice(i5));
  return n5 ? (d2.s = +n5[0], i5 + n5[0].length) : -1;
}
function formatDayOfMonth(d2, p3) {
  return pad(d2.getDate(), p3, 2);
}
function formatHour24(d2, p3) {
  return pad(d2.getHours(), p3, 2);
}
function formatHour12(d2, p3) {
  return pad(d2.getHours() % 12 || 12, p3, 2);
}
function formatDayOfYear(d2, p3) {
  return pad(1 + day_default.count(year_default(d2), d2), p3, 3);
}
function formatMilliseconds(d2, p3) {
  return pad(d2.getMilliseconds(), p3, 3);
}
function formatMicroseconds(d2, p3) {
  return formatMilliseconds(d2, p3) + "000";
}
function formatMonthNumber(d2, p3) {
  return pad(d2.getMonth() + 1, p3, 2);
}
function formatMinutes(d2, p3) {
  return pad(d2.getMinutes(), p3, 2);
}
function formatSeconds(d2, p3) {
  return pad(d2.getSeconds(), p3, 2);
}
function formatWeekdayNumberMonday(d2) {
  var day2 = d2.getDay();
  return day2 === 0 ? 7 : day2;
}
function formatWeekNumberSunday(d2, p3) {
  return pad(sunday.count(year_default(d2) - 1, d2), p3, 2);
}
function dISO(d2) {
  var day2 = d2.getDay();
  return day2 >= 4 || day2 === 0 ? thursday(d2) : thursday.ceil(d2);
}
function formatWeekNumberISO(d2, p3) {
  d2 = dISO(d2);
  return pad(thursday.count(year_default(d2), d2) + (year_default(d2).getDay() === 4), p3, 2);
}
function formatWeekdayNumberSunday(d2) {
  return d2.getDay();
}
function formatWeekNumberMonday(d2, p3) {
  return pad(monday.count(year_default(d2) - 1, d2), p3, 2);
}
function formatYear(d2, p3) {
  return pad(d2.getFullYear() % 100, p3, 2);
}
function formatYearISO(d2, p3) {
  d2 = dISO(d2);
  return pad(d2.getFullYear() % 100, p3, 2);
}
function formatFullYear(d2, p3) {
  return pad(d2.getFullYear() % 1e4, p3, 4);
}
function formatFullYearISO(d2, p3) {
  var day2 = d2.getDay();
  d2 = day2 >= 4 || day2 === 0 ? thursday(d2) : thursday.ceil(d2);
  return pad(d2.getFullYear() % 1e4, p3, 4);
}
function formatZone(d2) {
  var z3 = d2.getTimezoneOffset();
  return (z3 > 0 ? "-" : (z3 *= -1, "+")) + pad(z3 / 60 | 0, "0", 2) + pad(z3 % 60, "0", 2);
}
function formatUTCDayOfMonth(d2, p3) {
  return pad(d2.getUTCDate(), p3, 2);
}
function formatUTCHour24(d2, p3) {
  return pad(d2.getUTCHours(), p3, 2);
}
function formatUTCHour12(d2, p3) {
  return pad(d2.getUTCHours() % 12 || 12, p3, 2);
}
function formatUTCDayOfYear(d2, p3) {
  return pad(1 + utcDay_default.count(utcYear_default(d2), d2), p3, 3);
}
function formatUTCMilliseconds(d2, p3) {
  return pad(d2.getUTCMilliseconds(), p3, 3);
}
function formatUTCMicroseconds(d2, p3) {
  return formatUTCMilliseconds(d2, p3) + "000";
}
function formatUTCMonthNumber(d2, p3) {
  return pad(d2.getUTCMonth() + 1, p3, 2);
}
function formatUTCMinutes(d2, p3) {
  return pad(d2.getUTCMinutes(), p3, 2);
}
function formatUTCSeconds(d2, p3) {
  return pad(d2.getUTCSeconds(), p3, 2);
}
function formatUTCWeekdayNumberMonday(d2) {
  var dow = d2.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday(d2, p3) {
  return pad(utcSunday2.count(utcYear_default(d2) - 1, d2), p3, 2);
}
function UTCdISO(d2) {
  var day2 = d2.getUTCDay();
  return day2 >= 4 || day2 === 0 ? utcThursday2(d2) : utcThursday2.ceil(d2);
}
function formatUTCWeekNumberISO(d2, p3) {
  d2 = UTCdISO(d2);
  return pad(utcThursday2.count(utcYear_default(d2), d2) + (utcYear_default(d2).getUTCDay() === 4), p3, 2);
}
function formatUTCWeekdayNumberSunday(d2) {
  return d2.getUTCDay();
}
function formatUTCWeekNumberMonday(d2, p3) {
  return pad(utcMonday2.count(utcYear_default(d2) - 1, d2), p3, 2);
}
function formatUTCYear(d2, p3) {
  return pad(d2.getUTCFullYear() % 100, p3, 2);
}
function formatUTCYearISO(d2, p3) {
  d2 = UTCdISO(d2);
  return pad(d2.getUTCFullYear() % 100, p3, 2);
}
function formatUTCFullYear(d2, p3) {
  return pad(d2.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCFullYearISO(d2, p3) {
  var day2 = d2.getUTCDay();
  d2 = day2 >= 4 || day2 === 0 ? utcThursday2(d2) : utcThursday2.ceil(d2);
  return pad(d2.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCZone() {
  return "+0000";
}
function formatLiteralPercent() {
  return "%";
}
function formatUnixTimestamp(d2) {
  return +d2;
}
function formatUnixTimestampSeconds(d2) {
  return Math.floor(+d2 / 1e3);
}

// node_modules/@nivo/core/node_modules/d3-time-format/src/defaultLocale.js
var locale2;
var timeFormat;
var timeParse;
var utcFormat;
var utcParse;
defaultLocale2({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale2(definition) {
  locale2 = formatLocale(definition);
  timeFormat = locale2.format;
  timeParse = locale2.parse;
  utcFormat = locale2.utcFormat;
  utcParse = locale2.utcParse;
  return locale2;
}

// node_modules/@nivo/core/node_modules/d3-time-format/src/isoFormat.js
var isoSpecifier = "%Y-%m-%dT%H:%M:%S.%LZ";
function formatIsoNative(date) {
  return date.toISOString();
}
var formatIso = Date.prototype.toISOString ? formatIsoNative : utcFormat(isoSpecifier);

// node_modules/@nivo/core/node_modules/d3-time-format/src/isoParse.js
function parseIsoNative(string) {
  var date = new Date(string);
  return isNaN(date) ? null : date;
}
var parseIso = +/* @__PURE__ */ new Date("2000-01-01T00:00:00.000Z") ? parseIsoNative : utcParse(isoSpecifier);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/colors.js
function colors_default(specifier) {
  var n5 = specifier.length / 6 | 0, colors4 = new Array(n5), i5 = 0;
  while (i5 < n5) colors4[i5] = "#" + specifier.slice(i5 * 6, ++i5 * 6);
  return colors4;
}

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/category10.js
var category10_default = colors_default("1f77b4ff7f0e2ca02cd627289467bd8c564be377c27f7f7fbcbd2217becf");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Accent.js
var Accent_default = colors_default("7fc97fbeaed4fdc086ffff99386cb0f0027fbf5b17666666");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Dark2.js
var Dark2_default = colors_default("1b9e77d95f027570b3e7298a66a61ee6ab02a6761d666666");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/observable10.js
var observable10_default = colors_default("4269d0efb118ff725c6cc5b03ca951ff8ab7a463f297bbf59c6b4e9498a0");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Paired.js
var Paired_default = colors_default("a6cee31f78b4b2df8a33a02cfb9a99e31a1cfdbf6fff7f00cab2d66a3d9affff99b15928");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Pastel1.js
var Pastel1_default = colors_default("fbb4aeb3cde3ccebc5decbe4fed9a6ffffcce5d8bdfddaecf2f2f2");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Pastel2.js
var Pastel2_default = colors_default("b3e2cdfdcdaccbd5e8f4cae4e6f5c9fff2aef1e2cccccccc");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Set1.js
var Set1_default = colors_default("e41a1c377eb84daf4a984ea3ff7f00ffff33a65628f781bf999999");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Set2.js
var Set2_default = colors_default("66c2a5fc8d628da0cbe78ac3a6d854ffd92fe5c494b3b3b3");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Set3.js
var Set3_default = colors_default("8dd3c7ffffb3bebadafb807280b1d3fdb462b3de69fccde5d9d9d9bc80bdccebc5ffed6f");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/categorical/Tableau10.js
var Tableau10_default = colors_default("4e79a7f28e2ce1575976b7b259a14fedc949af7aa1ff9da79c755fbab0ab");

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/ramp.js
var ramp_default = (scheme55) => rgbBasis(scheme55[scheme55.length - 1]);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/BrBG.js
var scheme = new Array(3).concat(
  "d8b365f5f5f55ab4ac",
  "a6611adfc27d80cdc1018571",
  "a6611adfc27df5f5f580cdc1018571",
  "8c510ad8b365f6e8c3c7eae55ab4ac01665e",
  "8c510ad8b365f6e8c3f5f5f5c7eae55ab4ac01665e",
  "8c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e",
  "8c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e",
  "5430058c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e003c30",
  "5430058c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e003c30"
).map(colors_default);
var BrBG_default = ramp_default(scheme);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/PRGn.js
var scheme2 = new Array(3).concat(
  "af8dc3f7f7f77fbf7b",
  "7b3294c2a5cfa6dba0008837",
  "7b3294c2a5cff7f7f7a6dba0008837",
  "762a83af8dc3e7d4e8d9f0d37fbf7b1b7837",
  "762a83af8dc3e7d4e8f7f7f7d9f0d37fbf7b1b7837",
  "762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b7837",
  "762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b7837",
  "40004b762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b783700441b",
  "40004b762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b783700441b"
).map(colors_default);
var PRGn_default = ramp_default(scheme2);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/PiYG.js
var scheme3 = new Array(3).concat(
  "e9a3c9f7f7f7a1d76a",
  "d01c8bf1b6dab8e1864dac26",
  "d01c8bf1b6daf7f7f7b8e1864dac26",
  "c51b7de9a3c9fde0efe6f5d0a1d76a4d9221",
  "c51b7de9a3c9fde0eff7f7f7e6f5d0a1d76a4d9221",
  "c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221",
  "c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221",
  "8e0152c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221276419",
  "8e0152c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221276419"
).map(colors_default);
var PiYG_default = ramp_default(scheme3);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/PuOr.js
var scheme4 = new Array(3).concat(
  "998ec3f7f7f7f1a340",
  "5e3c99b2abd2fdb863e66101",
  "5e3c99b2abd2f7f7f7fdb863e66101",
  "542788998ec3d8daebfee0b6f1a340b35806",
  "542788998ec3d8daebf7f7f7fee0b6f1a340b35806",
  "5427888073acb2abd2d8daebfee0b6fdb863e08214b35806",
  "5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b35806",
  "2d004b5427888073acb2abd2d8daebfee0b6fdb863e08214b358067f3b08",
  "2d004b5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b358067f3b08"
).map(colors_default);
var PuOr_default = ramp_default(scheme4);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/RdBu.js
var scheme5 = new Array(3).concat(
  "ef8a62f7f7f767a9cf",
  "ca0020f4a58292c5de0571b0",
  "ca0020f4a582f7f7f792c5de0571b0",
  "b2182bef8a62fddbc7d1e5f067a9cf2166ac",
  "b2182bef8a62fddbc7f7f7f7d1e5f067a9cf2166ac",
  "b2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac",
  "b2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac",
  "67001fb2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac053061",
  "67001fb2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac053061"
).map(colors_default);
var RdBu_default = ramp_default(scheme5);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/RdGy.js
var scheme6 = new Array(3).concat(
  "ef8a62ffffff999999",
  "ca0020f4a582bababa404040",
  "ca0020f4a582ffffffbababa404040",
  "b2182bef8a62fddbc7e0e0e09999994d4d4d",
  "b2182bef8a62fddbc7ffffffe0e0e09999994d4d4d",
  "b2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d",
  "b2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d",
  "67001fb2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d1a1a1a",
  "67001fb2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d1a1a1a"
).map(colors_default);
var RdGy_default = ramp_default(scheme6);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/RdYlBu.js
var scheme7 = new Array(3).concat(
  "fc8d59ffffbf91bfdb",
  "d7191cfdae61abd9e92c7bb6",
  "d7191cfdae61ffffbfabd9e92c7bb6",
  "d73027fc8d59fee090e0f3f891bfdb4575b4",
  "d73027fc8d59fee090ffffbfe0f3f891bfdb4575b4",
  "d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4",
  "d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4",
  "a50026d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4313695",
  "a50026d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4313695"
).map(colors_default);
var RdYlBu_default = ramp_default(scheme7);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/RdYlGn.js
var scheme8 = new Array(3).concat(
  "fc8d59ffffbf91cf60",
  "d7191cfdae61a6d96a1a9641",
  "d7191cfdae61ffffbfa6d96a1a9641",
  "d73027fc8d59fee08bd9ef8b91cf601a9850",
  "d73027fc8d59fee08bffffbfd9ef8b91cf601a9850",
  "d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850",
  "d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850",
  "a50026d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850006837",
  "a50026d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850006837"
).map(colors_default);
var RdYlGn_default = ramp_default(scheme8);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/diverging/Spectral.js
var scheme9 = new Array(3).concat(
  "fc8d59ffffbf99d594",
  "d7191cfdae61abdda42b83ba",
  "d7191cfdae61ffffbfabdda42b83ba",
  "d53e4ffc8d59fee08be6f59899d5943288bd",
  "d53e4ffc8d59fee08bffffbfe6f59899d5943288bd",
  "d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd",
  "d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd",
  "9e0142d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd5e4fa2",
  "9e0142d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd5e4fa2"
).map(colors_default);
var Spectral_default = ramp_default(scheme9);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/BuGn.js
var scheme10 = new Array(3).concat(
  "e5f5f999d8c92ca25f",
  "edf8fbb2e2e266c2a4238b45",
  "edf8fbb2e2e266c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45006d2c00441b"
).map(colors_default);
var BuGn_default = ramp_default(scheme10);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/BuPu.js
var scheme11 = new Array(3).concat(
  "e0ecf49ebcda8856a7",
  "edf8fbb3cde38c96c688419d",
  "edf8fbb3cde38c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d810f7c4d004b"
).map(colors_default);
var BuPu_default = ramp_default(scheme11);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/GnBu.js
var scheme12 = new Array(3).concat(
  "e0f3dba8ddb543a2ca",
  "f0f9e8bae4bc7bccc42b8cbe",
  "f0f9e8bae4bc7bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe0868ac084081"
).map(colors_default);
var GnBu_default = ramp_default(scheme12);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/OrRd.js
var scheme13 = new Array(3).concat(
  "fee8c8fdbb84e34a33",
  "fef0d9fdcc8afc8d59d7301f",
  "fef0d9fdcc8afc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301fb300007f0000"
).map(colors_default);
var OrRd_default = ramp_default(scheme13);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/PuBuGn.js
var scheme14 = new Array(3).concat(
  "ece2f0a6bddb1c9099",
  "f6eff7bdc9e167a9cf02818a",
  "f6eff7bdc9e167a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016c59014636"
).map(colors_default);
var PuBuGn_default = ramp_default(scheme14);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/PuBu.js
var scheme15 = new Array(3).concat(
  "ece7f2a6bddb2b8cbe",
  "f1eef6bdc9e174a9cf0570b0",
  "f1eef6bdc9e174a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0045a8d023858"
).map(colors_default);
var PuBu_default = ramp_default(scheme15);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/PuRd.js
var scheme16 = new Array(3).concat(
  "e7e1efc994c7dd1c77",
  "f1eef6d7b5d8df65b0ce1256",
  "f1eef6d7b5d8df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125698004367001f"
).map(colors_default);
var PuRd_default = ramp_default(scheme16);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/RdPu.js
var scheme17 = new Array(3).concat(
  "fde0ddfa9fb5c51b8a",
  "feebe2fbb4b9f768a1ae017e",
  "feebe2fbb4b9f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a017749006a"
).map(colors_default);
var RdPu_default = ramp_default(scheme17);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/YlGnBu.js
var scheme18 = new Array(3).concat(
  "edf8b17fcdbb2c7fb8",
  "ffffcca1dab441b6c4225ea8",
  "ffffcca1dab441b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea8253494081d58"
).map(colors_default);
var YlGnBu_default = ramp_default(scheme18);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/YlGn.js
var scheme19 = new Array(3).concat(
  "f7fcb9addd8e31a354",
  "ffffccc2e69978c679238443",
  "ffffccc2e69978c67931a354006837",
  "ffffccd9f0a3addd8e78c67931a354006837",
  "ffffccd9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443006837004529"
).map(colors_default);
var YlGn_default = ramp_default(scheme19);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/YlOrBr.js
var scheme20 = new Array(3).concat(
  "fff7bcfec44fd95f0e",
  "ffffd4fed98efe9929cc4c02",
  "ffffd4fed98efe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c02993404662506"
).map(colors_default);
var YlOrBr_default = ramp_default(scheme20);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/YlOrRd.js
var scheme21 = new Array(3).concat(
  "ffeda0feb24cf03b20",
  "ffffb2fecc5cfd8d3ce31a1c",
  "ffffb2fecc5cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cbd0026800026"
).map(colors_default);
var YlOrRd_default = ramp_default(scheme21);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-single/Blues.js
var scheme22 = new Array(3).concat(
  "deebf79ecae13182bd",
  "eff3ffbdd7e76baed62171b5",
  "eff3ffbdd7e76baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b508519c08306b"
).map(colors_default);
var Blues_default = ramp_default(scheme22);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-single/Greens.js
var scheme23 = new Array(3).concat(
  "e5f5e0a1d99b31a354",
  "edf8e9bae4b374c476238b45",
  "edf8e9bae4b374c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45006d2c00441b"
).map(colors_default);
var Greens_default = ramp_default(scheme23);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-single/Greys.js
var scheme24 = new Array(3).concat(
  "f0f0f0bdbdbd636363",
  "f7f7f7cccccc969696525252",
  "f7f7f7cccccc969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525000000"
).map(colors_default);
var Greys_default = ramp_default(scheme24);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-single/Purples.js
var scheme25 = new Array(3).concat(
  "efedf5bcbddc756bb1",
  "f2f0f7cbc9e29e9ac86a51a3",
  "f2f0f7cbc9e29e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a354278f3f007d"
).map(colors_default);
var Purples_default = ramp_default(scheme25);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-single/Reds.js
var scheme26 = new Array(3).concat(
  "fee0d2fc9272de2d26",
  "fee5d9fcae91fb6a4acb181d",
  "fee5d9fcae91fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181da50f1567000d"
).map(colors_default);
var Reds_default = ramp_default(scheme26);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-single/Oranges.js
var scheme27 = new Array(3).concat(
  "fee6cefdae6be6550d",
  "feeddefdbe85fd8d3cd94701",
  "feeddefdbe85fd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d94801a636037f2704"
).map(colors_default);
var Oranges_default = ramp_default(scheme27);

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/cubehelix.js
var cubehelix_default2 = cubehelixLong(cubehelix(300, 0.5, 0), cubehelix(-240, 0.5, 1));

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/rainbow.js
var warm = cubehelixLong(cubehelix(-100, 0.75, 0.35), cubehelix(80, 1.5, 0.8));
var cool = cubehelixLong(cubehelix(260, 0.75, 0.35), cubehelix(80, 1.5, 0.8));
var c = cubehelix();

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/sinebow.js
var c6 = rgb2();
var pi_1_3 = Math.PI / 3;
var pi_2_3 = Math.PI * 2 / 3;

// node_modules/@nivo/core/node_modules/d3-scale-chromatic/src/sequential-multi/viridis.js
function ramp(range3) {
  var n5 = range3.length;
  return function(t7) {
    return range3[Math.max(0, Math.min(n5 - 1, Math.floor(t7 * n5)))];
  };
}
var viridis_default = ramp(colors_default(""));
var magma = ramp(colors_default(""));
var inferno = ramp(colors_default(""));
var plasma = ramp(colors_default(""));

// node_modules/@nivo/core/dist/nivo-core.es.js
var import_isFunction = __toESM(require_isFunction());
var import_without = __toESM(require_without());

// node_modules/@nivo/core/node_modules/d3-shape/src/math.js
var cos = Math.cos;
var sin = Math.sin;
var sqrt2 = Math.sqrt;
var epsilon = 1e-12;
var pi = Math.PI;
var halfPi = pi / 2;
var tau = 2 * pi;

// node_modules/@nivo/core/node_modules/d3-shape/node_modules/d3-path/src/path.js
var pi2 = Math.PI;
var tau2 = 2 * pi2;
var epsilon3 = 1e-6;
var tauEpsilon = tau2 - epsilon3;
function append(strings) {
  this._ += strings[0];
  for (let i5 = 1, n5 = strings.length; i5 < n5; ++i5) {
    this._ += arguments[i5] + strings[i5];
  }
}
function appendRound(digits) {
  let d2 = Math.floor(digits);
  if (!(d2 >= 0)) throw new Error(`invalid digits: ${digits}`);
  if (d2 > 15) return append;
  const k3 = 10 ** d2;
  return function(strings) {
    this._ += strings[0];
    for (let i5 = 1, n5 = strings.length; i5 < n5; ++i5) {
      this._ += Math.round(arguments[i5] * k3) / k3 + strings[i5];
    }
  };
}
var Path = class {
  constructor(digits) {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null;
    this._ = "";
    this._append = digits == null ? append : appendRound(digits);
  }
  moveTo(x4, y6) {
    this._append`M${this._x0 = this._x1 = +x4},${this._y0 = this._y1 = +y6}`;
  }
  closePath() {
    if (this._x1 !== null) {
      this._x1 = this._x0, this._y1 = this._y0;
      this._append`Z`;
    }
  }
  lineTo(x4, y6) {
    this._append`L${this._x1 = +x4},${this._y1 = +y6}`;
  }
  quadraticCurveTo(x1, y1, x4, y6) {
    this._append`Q${+x1},${+y1},${this._x1 = +x4},${this._y1 = +y6}`;
  }
  bezierCurveTo(x1, y1, x22, y22, x4, y6) {
    this._append`C${+x1},${+y1},${+x22},${+y22},${this._x1 = +x4},${this._y1 = +y6}`;
  }
  arcTo(x1, y1, x22, y22, r5) {
    x1 = +x1, y1 = +y1, x22 = +x22, y22 = +y22, r5 = +r5;
    if (r5 < 0) throw new Error(`negative radius: ${r5}`);
    let x0 = this._x1, y0 = this._y1, x21 = x22 - x1, y21 = y22 - y1, x01 = x0 - x1, y01 = y0 - y1, l01_2 = x01 * x01 + y01 * y01;
    if (this._x1 === null) {
      this._append`M${this._x1 = x1},${this._y1 = y1}`;
    } else if (!(l01_2 > epsilon3)) ;
    else if (!(Math.abs(y01 * x21 - y21 * x01) > epsilon3) || !r5) {
      this._append`L${this._x1 = x1},${this._y1 = y1}`;
    } else {
      let x20 = x22 - x0, y20 = y22 - y0, l21_2 = x21 * x21 + y21 * y21, l20_2 = x20 * x20 + y20 * y20, l21 = Math.sqrt(l21_2), l01 = Math.sqrt(l01_2), l4 = r5 * Math.tan((pi2 - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2), t01 = l4 / l01, t21 = l4 / l21;
      if (Math.abs(t01 - 1) > epsilon3) {
        this._append`L${x1 + t01 * x01},${y1 + t01 * y01}`;
      }
      this._append`A${r5},${r5},0,0,${+(y01 * x20 > x01 * y20)},${this._x1 = x1 + t21 * x21},${this._y1 = y1 + t21 * y21}`;
    }
  }
  arc(x4, y6, r5, a0, a1, ccw) {
    x4 = +x4, y6 = +y6, r5 = +r5, ccw = !!ccw;
    if (r5 < 0) throw new Error(`negative radius: ${r5}`);
    let dx = r5 * Math.cos(a0), dy = r5 * Math.sin(a0), x0 = x4 + dx, y0 = y6 + dy, cw = 1 ^ ccw, da = ccw ? a0 - a1 : a1 - a0;
    if (this._x1 === null) {
      this._append`M${x0},${y0}`;
    } else if (Math.abs(this._x1 - x0) > epsilon3 || Math.abs(this._y1 - y0) > epsilon3) {
      this._append`L${x0},${y0}`;
    }
    if (!r5) return;
    if (da < 0) da = da % tau2 + tau2;
    if (da > tauEpsilon) {
      this._append`A${r5},${r5},0,1,${cw},${x4 - dx},${y6 - dy}A${r5},${r5},0,1,${cw},${this._x1 = x0},${this._y1 = y0}`;
    } else if (da > epsilon3) {
      this._append`A${r5},${r5},0,${+(da >= pi2)},${cw},${this._x1 = x4 + r5 * Math.cos(a1)},${this._y1 = y6 + r5 * Math.sin(a1)}`;
    }
  }
  rect(x4, y6, w3, h) {
    this._append`M${this._x0 = this._x1 = +x4},${this._y0 = this._y1 = +y6}h${w3 = +w3}v${+h}h${-w3}Z`;
  }
  toString() {
    return this._;
  }
};
function path() {
  return new Path();
}
path.prototype = Path.prototype;

// node_modules/@nivo/core/node_modules/d3-shape/src/array.js
var slice3 = Array.prototype.slice;

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/linear.js
function Linear(context) {
  this._context = context;
}
Linear.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y6) : this._context.moveTo(x4, y6);
        break;
      case 1:
        this._point = 2;
      default:
        this._context.lineTo(x4, y6);
        break;
    }
  }
};
function linear_default(context) {
  return new Linear(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/radial.js
var curveRadialLinear = curveRadial(linear_default);
function Radial(curve) {
  this._curve = curve;
}
Radial.prototype = {
  areaStart: function() {
    this._curve.areaStart();
  },
  areaEnd: function() {
    this._curve.areaEnd();
  },
  lineStart: function() {
    this._curve.lineStart();
  },
  lineEnd: function() {
    this._curve.lineEnd();
  },
  point: function(a4, r5) {
    this._curve.point(r5 * Math.sin(a4), r5 * -Math.cos(a4));
  }
};
function curveRadial(curve) {
  function radial3(context) {
    return new Radial(curve(context));
  }
  radial3._curve = curve;
  return radial3;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/asterisk.js
var sqrt3 = sqrt2(3);

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/diamond.js
var tan30 = sqrt2(1 / 3);
var tan30_2 = tan30 * 2;

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/star.js
var kr = sin(pi / 10) / sin(7 * pi / 10);
var kx = sin(tau / 10) * kr;
var ky = -cos(tau / 10) * kr;

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/triangle.js
var sqrt32 = sqrt2(3);

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/triangle2.js
var sqrt33 = sqrt2(3);

// node_modules/@nivo/core/node_modules/d3-shape/src/symbol/wye.js
var s = sqrt2(3) / 2;
var k = 1 / sqrt2(12);
var a = (k / 2 + 1) * 3;

// node_modules/@nivo/core/node_modules/d3-shape/src/noop.js
function noop_default() {
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/basis.js
function point2(that, x4, y6) {
  that._context.bezierCurveTo(
    (2 * that._x0 + that._x1) / 3,
    (2 * that._y0 + that._y1) / 3,
    (that._x0 + 2 * that._x1) / 3,
    (that._y0 + 2 * that._y1) / 3,
    (that._x0 + 4 * that._x1 + x4) / 6,
    (that._y0 + 4 * that._y1 + y6) / 6
  );
}
function Basis(context) {
  this._context = context;
}
Basis.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 3:
        point2(this, this._x1, this._y1);
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y6) : this._context.moveTo(x4, y6);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6);
      default:
        point2(this, x4, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y6;
  }
};
function basis_default2(context) {
  return new Basis(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/basisClosed.js
function BasisClosed(context) {
  this._context = context;
}
BasisClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x2, this._y2);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3);
        this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x2, this._y2);
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        break;
      }
    }
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x2 = x4, this._y2 = y6;
        break;
      case 1:
        this._point = 2;
        this._x3 = x4, this._y3 = y6;
        break;
      case 2:
        this._point = 3;
        this._x4 = x4, this._y4 = y6;
        this._context.moveTo((this._x0 + 4 * this._x1 + x4) / 6, (this._y0 + 4 * this._y1 + y6) / 6);
        break;
      default:
        point2(this, x4, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y6;
  }
};
function basisClosed_default2(context) {
  return new BasisClosed(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/basisOpen.js
function BasisOpen(context) {
  this._context = context;
}
BasisOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        var x0 = (this._x0 + 4 * this._x1 + x4) / 6, y0 = (this._y0 + 4 * this._y1 + y6) / 6;
        this._line ? this._context.lineTo(x0, y0) : this._context.moveTo(x0, y0);
        break;
      case 3:
        this._point = 4;
      default:
        point2(this, x4, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y6;
  }
};
function basisOpen_default(context) {
  return new BasisOpen(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/bundle.js
function Bundle(context, beta) {
  this._basis = new Basis(context);
  this._beta = beta;
}
Bundle.prototype = {
  lineStart: function() {
    this._x = [];
    this._y = [];
    this._basis.lineStart();
  },
  lineEnd: function() {
    var x4 = this._x, y6 = this._y, j4 = x4.length - 1;
    if (j4 > 0) {
      var x0 = x4[0], y0 = y6[0], dx = x4[j4] - x0, dy = y6[j4] - y0, i5 = -1, t7;
      while (++i5 <= j4) {
        t7 = i5 / j4;
        this._basis.point(
          this._beta * x4[i5] + (1 - this._beta) * (x0 + t7 * dx),
          this._beta * y6[i5] + (1 - this._beta) * (y0 + t7 * dy)
        );
      }
    }
    this._x = this._y = null;
    this._basis.lineEnd();
  },
  point: function(x4, y6) {
    this._x.push(+x4);
    this._y.push(+y6);
  }
};
var bundle_default = function custom(beta) {
  function bundle(context) {
    return beta === 1 ? new Basis(context) : new Bundle(context, beta);
  }
  bundle.beta = function(beta2) {
    return custom(+beta2);
  };
  return bundle;
}(0.85);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/cardinal.js
function point3(that, x4, y6) {
  that._context.bezierCurveTo(
    that._x1 + that._k * (that._x2 - that._x0),
    that._y1 + that._k * (that._y2 - that._y0),
    that._x2 + that._k * (that._x1 - x4),
    that._y2 + that._k * (that._y1 - y6),
    that._x2,
    that._y2
  );
}
function Cardinal(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
Cardinal.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        point3(this, this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y6) : this._context.moveTo(x4, y6);
        break;
      case 1:
        this._point = 2;
        this._x1 = x4, this._y1 = y6;
        break;
      case 2:
        this._point = 3;
      default:
        point3(this, x4, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var cardinal_default = function custom2(tension) {
  function cardinal(context) {
    return new Cardinal(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom2(+tension2);
  };
  return cardinal;
}(0);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/cardinalClosed.js
function CardinalClosed(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
CardinalClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x4, this._y3 = y6;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x4, this._y4 = y6);
        break;
      case 2:
        this._point = 3;
        this._x5 = x4, this._y5 = y6;
        break;
      default:
        point3(this, x4, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var cardinalClosed_default = function custom3(tension) {
  function cardinal(context) {
    return new CardinalClosed(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom3(+tension2);
  };
  return cardinal;
}(0);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/cardinalOpen.js
function CardinalOpen(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
CardinalOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      default:
        point3(this, x4, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var cardinalOpen_default = function custom4(tension) {
  function cardinal(context) {
    return new CardinalOpen(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom4(+tension2);
  };
  return cardinal;
}(0);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/catmullRom.js
function point4(that, x4, y6) {
  var x1 = that._x1, y1 = that._y1, x22 = that._x2, y22 = that._y2;
  if (that._l01_a > epsilon) {
    var a4 = 2 * that._l01_2a + 3 * that._l01_a * that._l12_a + that._l12_2a, n5 = 3 * that._l01_a * (that._l01_a + that._l12_a);
    x1 = (x1 * a4 - that._x0 * that._l12_2a + that._x2 * that._l01_2a) / n5;
    y1 = (y1 * a4 - that._y0 * that._l12_2a + that._y2 * that._l01_2a) / n5;
  }
  if (that._l23_a > epsilon) {
    var b3 = 2 * that._l23_2a + 3 * that._l23_a * that._l12_a + that._l12_2a, m5 = 3 * that._l23_a * (that._l23_a + that._l12_a);
    x22 = (x22 * b3 + that._x1 * that._l23_2a - x4 * that._l12_2a) / m5;
    y22 = (y22 * b3 + that._y1 * that._l23_2a - y6 * that._l12_2a) / m5;
  }
  that._context.bezierCurveTo(x1, y1, x22, y22, that._x2, that._y2);
}
function CatmullRom(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRom.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        this.point(this._x2, this._y2);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    if (this._point) {
      var x23 = this._x2 - x4, y23 = this._y2 - y6;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y6) : this._context.moveTo(x4, y6);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
      default:
        point4(this, x4, y6);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var catmullRom_default = function custom5(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRom(context, alpha) : new Cardinal(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom5(+alpha2);
  };
  return catmullRom;
}(0.5);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/catmullRomClosed.js
function CatmullRomClosed(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRomClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    if (this._point) {
      var x23 = this._x2 - x4, y23 = this._y2 - y6;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x4, this._y3 = y6;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x4, this._y4 = y6);
        break;
      case 2:
        this._point = 3;
        this._x5 = x4, this._y5 = y6;
        break;
      default:
        point4(this, x4, y6);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var catmullRomClosed_default = function custom6(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRomClosed(context, alpha) : new CardinalClosed(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom6(+alpha2);
  };
  return catmullRom;
}(0.5);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/catmullRomOpen.js
function CatmullRomOpen(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRomOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    if (this._point) {
      var x23 = this._x2 - x4, y23 = this._y2 - y6;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      default:
        point4(this, x4, y6);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x4;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var catmullRomOpen_default = function custom7(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRomOpen(context, alpha) : new CardinalOpen(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom7(+alpha2);
  };
  return catmullRom;
}(0.5);

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/linearClosed.js
function LinearClosed(context) {
  this._context = context;
}
LinearClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._point) this._context.closePath();
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    if (this._point) this._context.lineTo(x4, y6);
    else this._point = 1, this._context.moveTo(x4, y6);
  }
};
function linearClosed_default(context) {
  return new LinearClosed(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/monotone.js
function sign(x4) {
  return x4 < 0 ? -1 : 1;
}
function slope3(that, x22, y22) {
  var h0 = that._x1 - that._x0, h1 = x22 - that._x1, s0 = (that._y1 - that._y0) / (h0 || h1 < 0 && -0), s1 = (y22 - that._y1) / (h1 || h0 < 0 && -0), p3 = (s0 * h1 + s1 * h0) / (h0 + h1);
  return (sign(s0) + sign(s1)) * Math.min(Math.abs(s0), Math.abs(s1), 0.5 * Math.abs(p3)) || 0;
}
function slope2(that, t7) {
  var h = that._x1 - that._x0;
  return h ? (3 * (that._y1 - that._y0) / h - t7) / 2 : t7;
}
function point5(that, t06, t16) {
  var x0 = that._x0, y0 = that._y0, x1 = that._x1, y1 = that._y1, dx = (x1 - x0) / 3;
  that._context.bezierCurveTo(x0 + dx, y0 + dx * t06, x1 - dx, y1 - dx * t16, x1, y1);
}
function MonotoneX(context) {
  this._context = context;
}
MonotoneX.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
      case 3:
        point5(this, this._t0, slope2(this, this._t0));
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    var t16 = NaN;
    x4 = +x4, y6 = +y6;
    if (x4 === this._x1 && y6 === this._y1) return;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y6) : this._context.moveTo(x4, y6);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        point5(this, slope2(this, t16 = slope3(this, x4, y6)), t16);
        break;
      default:
        point5(this, this._t0, t16 = slope3(this, x4, y6));
        break;
    }
    this._x0 = this._x1, this._x1 = x4;
    this._y0 = this._y1, this._y1 = y6;
    this._t0 = t16;
  }
};
function MonotoneY(context) {
  this._context = new ReflectContext(context);
}
(MonotoneY.prototype = Object.create(MonotoneX.prototype)).point = function(x4, y6) {
  MonotoneX.prototype.point.call(this, y6, x4);
};
function ReflectContext(context) {
  this._context = context;
}
ReflectContext.prototype = {
  moveTo: function(x4, y6) {
    this._context.moveTo(y6, x4);
  },
  closePath: function() {
    this._context.closePath();
  },
  lineTo: function(x4, y6) {
    this._context.lineTo(y6, x4);
  },
  bezierCurveTo: function(x1, y1, x22, y22, x4, y6) {
    this._context.bezierCurveTo(y1, x1, y22, x22, y6, x4);
  }
};
function monotoneX(context) {
  return new MonotoneX(context);
}
function monotoneY(context) {
  return new MonotoneY(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/natural.js
function Natural(context) {
  this._context = context;
}
Natural.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = [];
    this._y = [];
  },
  lineEnd: function() {
    var x4 = this._x, y6 = this._y, n5 = x4.length;
    if (n5) {
      this._line ? this._context.lineTo(x4[0], y6[0]) : this._context.moveTo(x4[0], y6[0]);
      if (n5 === 2) {
        this._context.lineTo(x4[1], y6[1]);
      } else {
        var px = controlPoints(x4), py = controlPoints(y6);
        for (var i0 = 0, i1 = 1; i1 < n5; ++i0, ++i1) {
          this._context.bezierCurveTo(px[0][i0], py[0][i0], px[1][i0], py[1][i0], x4[i1], y6[i1]);
        }
      }
    }
    if (this._line || this._line !== 0 && n5 === 1) this._context.closePath();
    this._line = 1 - this._line;
    this._x = this._y = null;
  },
  point: function(x4, y6) {
    this._x.push(+x4);
    this._y.push(+y6);
  }
};
function controlPoints(x4) {
  var i5, n5 = x4.length - 1, m5, a4 = new Array(n5), b3 = new Array(n5), r5 = new Array(n5);
  a4[0] = 0, b3[0] = 2, r5[0] = x4[0] + 2 * x4[1];
  for (i5 = 1; i5 < n5 - 1; ++i5) a4[i5] = 1, b3[i5] = 4, r5[i5] = 4 * x4[i5] + 2 * x4[i5 + 1];
  a4[n5 - 1] = 2, b3[n5 - 1] = 7, r5[n5 - 1] = 8 * x4[n5 - 1] + x4[n5];
  for (i5 = 1; i5 < n5; ++i5) m5 = a4[i5] / b3[i5 - 1], b3[i5] -= m5, r5[i5] -= m5 * r5[i5 - 1];
  a4[n5 - 1] = r5[n5 - 1] / b3[n5 - 1];
  for (i5 = n5 - 2; i5 >= 0; --i5) a4[i5] = (r5[i5] - a4[i5 + 1]) / b3[i5];
  b3[n5 - 1] = (x4[n5] + a4[n5 - 1]) / 2;
  for (i5 = 0; i5 < n5 - 1; ++i5) b3[i5] = 2 * x4[i5 + 1] - a4[i5 + 1];
  return [a4, b3];
}
function natural_default(context) {
  return new Natural(context);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/curve/step.js
function Step(context, t7) {
  this._context = context;
  this._t = t7;
}
Step.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = this._y = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (0 < this._t && this._t < 1 && this._point === 2) this._context.lineTo(this._x, this._y);
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    if (this._line >= 0) this._t = 1 - this._t, this._line = 1 - this._line;
  },
  point: function(x4, y6) {
    x4 = +x4, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x4, y6) : this._context.moveTo(x4, y6);
        break;
      case 1:
        this._point = 2;
      default: {
        if (this._t <= 0) {
          this._context.lineTo(this._x, y6);
          this._context.lineTo(x4, y6);
        } else {
          var x1 = this._x * (1 - this._t) + x4 * this._t;
          this._context.lineTo(x1, this._y);
          this._context.lineTo(x1, y6);
        }
        break;
      }
    }
    this._x = x4, this._y = y6;
  }
};
function step_default(context) {
  return new Step(context, 0.5);
}
function stepBefore(context) {
  return new Step(context, 0);
}
function stepAfter(context) {
  return new Step(context, 1);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/none.js
function none_default(series, order) {
  if (!((n5 = series.length) > 1)) return;
  for (var i5 = 1, j4, s0, s1 = series[order[0]], n5, m5 = s1.length; i5 < n5; ++i5) {
    s0 = s1, s1 = series[order[i5]];
    for (j4 = 0; j4 < m5; ++j4) {
      s1[j4][1] += s1[j4][0] = isNaN(s0[j4][1]) ? s0[j4][0] : s0[j4][1];
    }
  }
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/none.js
function none_default2(series) {
  var n5 = series.length, o5 = new Array(n5);
  while (--n5 >= 0) o5[n5] = n5;
  return o5;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/expand.js
function expand_default(series, order) {
  if (!((n5 = series.length) > 0)) return;
  for (var i5, n5, j4 = 0, m5 = series[0].length, y6; j4 < m5; ++j4) {
    for (y6 = i5 = 0; i5 < n5; ++i5) y6 += series[i5][j4][1] || 0;
    if (y6) for (i5 = 0; i5 < n5; ++i5) series[i5][j4][1] /= y6;
  }
  none_default(series, order);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/diverging.js
function diverging_default(series, order) {
  if (!((n5 = series.length) > 0)) return;
  for (var i5, j4 = 0, d2, dy, yp, yn2, n5, m5 = series[order[0]].length; j4 < m5; ++j4) {
    for (yp = yn2 = 0, i5 = 0; i5 < n5; ++i5) {
      if ((dy = (d2 = series[order[i5]][j4])[1] - d2[0]) > 0) {
        d2[0] = yp, d2[1] = yp += dy;
      } else if (dy < 0) {
        d2[1] = yn2, d2[0] = yn2 += dy;
      } else {
        d2[0] = 0, d2[1] = dy;
      }
    }
  }
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/silhouette.js
function silhouette_default(series, order) {
  if (!((n5 = series.length) > 0)) return;
  for (var j4 = 0, s0 = series[order[0]], n5, m5 = s0.length; j4 < m5; ++j4) {
    for (var i5 = 0, y6 = 0; i5 < n5; ++i5) y6 += series[i5][j4][1] || 0;
    s0[j4][1] += s0[j4][0] = -y6 / 2;
  }
  none_default(series, order);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/offset/wiggle.js
function wiggle_default(series, order) {
  if (!((n5 = series.length) > 0) || !((m5 = (s0 = series[order[0]]).length) > 0)) return;
  for (var y6 = 0, j4 = 1, s0, m5, n5; j4 < m5; ++j4) {
    for (var i5 = 0, s1 = 0, s22 = 0; i5 < n5; ++i5) {
      var si = series[order[i5]], sij0 = si[j4][1] || 0, sij1 = si[j4 - 1][1] || 0, s3 = (sij0 - sij1) / 2;
      for (var k3 = 0; k3 < i5; ++k3) {
        var sk = series[order[k3]], skj0 = sk[j4][1] || 0, skj1 = sk[j4 - 1][1] || 0;
        s3 += skj0 - skj1;
      }
      s1 += sij0, s22 += s3 * sij0;
    }
    s0[j4 - 1][1] += s0[j4 - 1][0] = y6;
    if (s1) y6 -= s22 / s1;
  }
  s0[j4 - 1][1] += s0[j4 - 1][0] = y6;
  none_default(series, order);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/appearance.js
function appearance_default(series) {
  var peaks = series.map(peak);
  return none_default2(series).sort(function(a4, b3) {
    return peaks[a4] - peaks[b3];
  });
}
function peak(series) {
  var i5 = -1, j4 = 0, n5 = series.length, vi, vj = -Infinity;
  while (++i5 < n5) if ((vi = +series[i5][1]) > vj) vj = vi, j4 = i5;
  return j4;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/ascending.js
function ascending_default2(series) {
  var sums = series.map(sum3);
  return none_default2(series).sort(function(a4, b3) {
    return sums[a4] - sums[b3];
  });
}
function sum3(series) {
  var s3 = 0, i5 = -1, n5 = series.length, v4;
  while (++i5 < n5) if (v4 = +series[i5][1]) s3 += v4;
  return s3;
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/descending.js
function descending_default3(series) {
  return ascending_default2(series).reverse();
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/insideOut.js
function insideOut_default(series) {
  var n5 = series.length, i5, j4, sums = series.map(sum3), order = appearance_default(series), top = 0, bottom = 0, tops = [], bottoms = [];
  for (i5 = 0; i5 < n5; ++i5) {
    j4 = order[i5];
    if (top < bottom) {
      top += sums[j4];
      tops.push(j4);
    } else {
      bottom += sums[j4];
      bottoms.push(j4);
    }
  }
  return bottoms.reverse().concat(tops);
}

// node_modules/@nivo/core/node_modules/d3-shape/src/order/reverse.js
function reverse_default(series) {
  return none_default2(series).reverse();
}

// node_modules/@nivo/core/dist/nivo-core.es.js
var import_isPlainObject = __toESM(require_isPlainObject());
var import_pick = __toESM(require_pick());
var import_isEqual = __toESM(require_isEqual());
var Pr = { background: "transparent", text: { fontFamily: "sans-serif", fontSize: 11, fill: "#333333", outlineWidth: 0, outlineColor: "transparent", outlineOpacity: 1 }, axis: { domain: { line: { stroke: "transparent", strokeWidth: 1 } }, ticks: { line: { stroke: "#777777", strokeWidth: 1 }, text: {} }, legend: { text: { fontSize: 12 } } }, grid: { line: { stroke: "#dddddd", strokeWidth: 1 } }, legends: { hidden: { symbol: { fill: "#333333", opacity: 0.6 }, text: { fill: "#333333", opacity: 0.6 } }, text: {}, ticks: { line: { stroke: "#777777", strokeWidth: 1 }, text: { fontSize: 10 } }, title: { text: {} } }, labels: { text: {} }, markers: { lineColor: "#000000", lineStrokeWidth: 1, text: {} }, dots: { text: {} }, tooltip: { container: { background: "white", color: "inherit", fontSize: "inherit", borderRadius: "2px", boxShadow: "0 1px 2px rgba(0, 0, 0, 0.25)", padding: "5px 9px" }, basic: { whiteSpace: "pre", display: "flex", alignItems: "center" }, chip: { marginRight: 7 }, table: {}, tableCell: { padding: "3px 5px" }, tableCellValue: { fontWeight: "bold" } }, crosshair: { line: { stroke: "#000000", strokeWidth: 1, strokeOpacity: 0.75, strokeDasharray: "6 6" } }, annotations: { text: { fontSize: 13, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, link: { stroke: "#000000", strokeWidth: 1, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, outline: { fill: "none", stroke: "#000000", strokeWidth: 2, outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 }, symbol: { fill: "#000000", outlineWidth: 2, outlineColor: "#ffffff", outlineOpacity: 1 } } };
function jr() {
  return jr = Object.assign ? Object.assign.bind() : function(e7) {
    for (var r5 = 1; r5 < arguments.length; r5++) {
      var t7 = arguments[r5];
      for (var n5 in t7) Object.prototype.hasOwnProperty.call(t7, n5) && (e7[n5] = t7[n5]);
    }
    return e7;
  }, jr.apply(this, arguments);
}
function Br(e7, r5) {
  if (null == e7) return {};
  var t7, n5, i5 = {}, o5 = Object.keys(e7);
  for (n5 = 0; n5 < o5.length; n5++) t7 = o5[n5], r5.indexOf(t7) >= 0 || (i5[t7] = e7[t7]);
  return i5;
}
var Gr = ["axis.ticks.text", "axis.legend.text", "legends.title.text", "legends.text", "legends.ticks.text", "legends.title.text", "labels.text", "dots.text", "markers.text", "annotations.text"];
var Lr = function(e7, r5) {
  return jr({}, r5, e7);
};
var Ir = function(e7, r5) {
  var t7 = (0, import_merge3.default)({}, e7, r5);
  return Gr.forEach(function(e8) {
    (0, import_set2.default)(t7, e8, Lr((0, import_get.default)(t7, e8), t7.text));
  }), t7;
};
var Yr = (0, import_react16.createContext)();
var Ar = function(e7) {
  var t7 = e7.children, n5 = e7.animate, i5 = void 0 === n5 || n5, o5 = e7.config, l4 = void 0 === o5 ? "default" : o5, a4 = (0, import_react16.useMemo)(function() {
    var e8 = (0, import_isString.default)(l4) ? config[l4] : l4;
    return { animate: i5, config: e8 };
  }, [i5, l4]);
  return (0, import_jsx_runtime2.jsx)(Yr.Provider, { value: a4, children: t7 });
};
var Er = { animate: import_prop_types.default.bool, motionConfig: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(Object.keys(config)), import_prop_types.default.shape({ mass: import_prop_types.default.number, tension: import_prop_types.default.number, friction: import_prop_types.default.number, clamp: import_prop_types.default.bool, precision: import_prop_types.default.number, velocity: import_prop_types.default.number, duration: import_prop_types.default.number, easing: import_prop_types.default.func })]) };
Ar.propTypes = { children: import_prop_types.default.node.isRequired, animate: Er.animate, config: Er.motionConfig };
var Ur = function() {
  return (0, import_react16.useContext)(Yr);
};
var Xr = { nivo: ["#d76445", "#f47560", "#e8c1a0", "#97e3d5", "#61cdbb", "#00b0a7"], BrBG: (0, import_last.default)(scheme), PRGn: (0, import_last.default)(scheme2), PiYG: (0, import_last.default)(scheme3), PuOr: (0, import_last.default)(scheme4), RdBu: (0, import_last.default)(scheme5), RdGy: (0, import_last.default)(scheme6), RdYlBu: (0, import_last.default)(scheme7), RdYlGn: (0, import_last.default)(scheme8), spectral: (0, import_last.default)(scheme9), blues: (0, import_last.default)(scheme22), greens: (0, import_last.default)(scheme23), greys: (0, import_last.default)(scheme24), oranges: (0, import_last.default)(scheme27), purples: (0, import_last.default)(scheme25), reds: (0, import_last.default)(scheme26), BuGn: (0, import_last.default)(scheme10), BuPu: (0, import_last.default)(scheme11), GnBu: (0, import_last.default)(scheme12), OrRd: (0, import_last.default)(scheme13), PuBuGn: (0, import_last.default)(scheme14), PuBu: (0, import_last.default)(scheme15), PuRd: (0, import_last.default)(scheme16), RdPu: (0, import_last.default)(scheme17), YlGnBu: (0, import_last.default)(scheme18), YlGn: (0, import_last.default)(scheme19), YlOrBr: (0, import_last.default)(scheme20), YlOrRd: (0, import_last.default)(scheme21) };
var Hr = Object.keys(Xr);
var Nr = { nivo: ["#e8c1a0", "#f47560", "#f1e15b", "#e8a838", "#61cdbb", "#97e3d5"], category10: category10_default, accent: Accent_default, dark2: Dark2_default, paired: Paired_default, pastel1: Pastel1_default, pastel2: Pastel2_default, set1: Set1_default, set2: Set2_default, set3: Set3_default, brown_blueGreen: (0, import_last.default)(scheme), purpleRed_green: (0, import_last.default)(scheme2), pink_yellowGreen: (0, import_last.default)(scheme3), purple_orange: (0, import_last.default)(scheme4), red_blue: (0, import_last.default)(scheme5), red_grey: (0, import_last.default)(scheme6), red_yellow_blue: (0, import_last.default)(scheme7), red_yellow_green: (0, import_last.default)(scheme8), spectral: (0, import_last.default)(scheme9), blues: (0, import_last.default)(scheme22), greens: (0, import_last.default)(scheme23), greys: (0, import_last.default)(scheme24), oranges: (0, import_last.default)(scheme27), purples: (0, import_last.default)(scheme25), reds: (0, import_last.default)(scheme26), blue_green: (0, import_last.default)(scheme10), blue_purple: (0, import_last.default)(scheme11), green_blue: (0, import_last.default)(scheme12), orange_red: (0, import_last.default)(scheme13), purple_blue_green: (0, import_last.default)(scheme14), purple_blue: (0, import_last.default)(scheme15), purple_red: (0, import_last.default)(scheme16), red_purple: (0, import_last.default)(scheme17), yellow_green_blue: (0, import_last.default)(scheme18), yellow_green: (0, import_last.default)(scheme19), yellow_orange_brown: (0, import_last.default)(scheme20), yellow_orange_red: (0, import_last.default)(scheme21) };
var et = import_prop_types.default.oneOfType([import_prop_types.default.oneOf(Hr), import_prop_types.default.func, import_prop_types.default.arrayOf(import_prop_types.default.string)]);
var rt = { basis: basis_default2, basisClosed: basisClosed_default2, basisOpen: basisOpen_default, bundle: bundle_default, cardinal: cardinal_default, cardinalClosed: cardinalClosed_default, cardinalOpen: cardinalOpen_default, catmullRom: catmullRom_default, catmullRomClosed: catmullRomClosed_default, catmullRomOpen: catmullRomOpen_default, linear: linear_default, linearClosed: linearClosed_default, monotoneX, monotoneY, natural: natural_default, step: step_default, stepAfter, stepBefore };
var tt = Object.keys(rt);
var nt = tt.filter(function(e7) {
  return e7.endsWith("Closed");
});
var it = (0, import_without.default)(tt, "bundle", "basisClosed", "basisOpen", "cardinalClosed", "cardinalOpen", "catmullRomClosed", "catmullRomOpen", "linearClosed");
var ot = (0, import_without.default)(tt, "bundle", "basisClosed", "basisOpen", "cardinalClosed", "cardinalOpen", "catmullRomClosed", "catmullRomOpen", "linearClosed");
var at = { ascending: ascending_default2, descending: descending_default3, insideOut: insideOut_default, none: none_default2, reverse: reverse_default };
var dt = Object.keys(at);
var ut = { expand: expand_default, diverging: diverging_default, none: none_default, silhouette: silhouette_default, wiggle: wiggle_default };
var ct = Object.keys(ut);
var pt = import_prop_types.default.shape({ top: import_prop_types.default.number, right: import_prop_types.default.number, bottom: import_prop_types.default.number, left: import_prop_types.default.number }).isRequired;
var ht = ["normal", "multiply", "screen", "overlay", "darken", "lighten", "color-dodge", "color-burn", "hard-light", "soft-light", "difference", "exclusion", "hue", "saturation", "color", "luminosity"];
var gt = import_prop_types.default.oneOf(ht);
var vt = ordinal(Set3_default);
var _t = { top: 0, right: 0, bottom: 0, left: 0 };
var wt = function(e7, t7, n5) {
  return void 0 === n5 && (n5 = {}), (0, import_react16.useMemo)(function() {
    var r5 = jr({}, _t, n5);
    return { margin: r5, innerWidth: e7 - r5.left - r5.right, innerHeight: t7 - r5.top - r5.bottom, outerWidth: e7, outerHeight: t7 };
  }, [e7, t7, n5.top, n5.right, n5.bottom, n5.left]);
};
var kt = function() {
  var e7 = (0, import_react16.useRef)(null), r5 = (0, import_react16.useState)({ left: 0, top: 0, width: 0, height: 0 }), t7 = r5[0], l4 = r5[1], a4 = (0, import_react16.useState)(function() {
    return "undefined" == typeof ResizeObserver ? null : new ResizeObserver(function(e8) {
      var r6 = e8[0];
      return l4(r6.contentRect);
    });
  })[0];
  return (0, import_react16.useEffect)(function() {
    return e7.current && null !== a4 && a4.observe(e7.current), function() {
      null !== a4 && a4.disconnect();
    };
  }, []), [e7, t7];
};
var Rt = function(e7) {
  return (0, import_react16.useMemo)(function() {
    return Ir(Pr, e7);
  }, [e7]);
};
var xt = function(e7) {
  return "function" == typeof e7 ? e7 : "string" == typeof e7 ? 0 === e7.indexOf("time:") ? timeFormat(e7.slice("5")) : format(e7) : function(e8) {
    return "" + e8;
  };
};
var Ot = function(e7) {
  return (0, import_react16.useMemo)(function() {
    return xt(e7);
  }, [e7]);
};
var qt = (0, import_react16.createContext)();
var Ct = {};
var Wt = function(e7) {
  var r5 = e7.theme, t7 = void 0 === r5 ? Ct : r5, n5 = e7.children, i5 = Rt(t7);
  return (0, import_jsx_runtime2.jsx)(qt.Provider, { value: i5, children: n5 });
};
Wt.propTypes = { children: import_prop_types.default.node.isRequired, theme: import_prop_types.default.object };
var zt = function() {
  return (0, import_react16.useContext)(qt);
};
var Tt = ["outlineWidth", "outlineColor", "outlineOpacity"];
var Mt = function(e7) {
  return e7.outlineWidth, e7.outlineColor, e7.outlineOpacity, Br(e7, Tt);
};
var Pt = function(e7) {
  var r5 = e7.children, t7 = e7.condition, n5 = e7.wrapper;
  return t7 ? (0, import_react16.cloneElement)(n5, {}, r5) : r5;
};
Pt.propTypes = { children: import_prop_types.default.node.isRequired, condition: import_prop_types.default.bool.isRequired, wrapper: import_prop_types.default.element.isRequired };
var jt = { position: "relative" };
var St = function(e7) {
  var r5 = e7.children, t7 = e7.theme, i5 = e7.renderWrapper, o5 = void 0 === i5 || i5, l4 = e7.isInteractive, a4 = void 0 === l4 || l4, d2 = e7.animate, s3 = e7.motionConfig, u2 = (0, import_react16.useRef)(null);
  return (0, import_jsx_runtime2.jsx)(Wt, { theme: t7, children: (0, import_jsx_runtime2.jsx)(Ar, { animate: d2, config: s3, children: (0, import_jsx_runtime2.jsx)(M, { container: u2, children: (0, import_jsx_runtime2.jsxs)(Pt, { condition: o5, wrapper: (0, import_jsx_runtime2.jsx)("div", { style: jt, ref: u2 }), children: [r5, a4 && (0, import_jsx_runtime2.jsx)(F, {})] }) }) }) });
};
St.propTypes = { children: import_prop_types.default.element.isRequired, isInteractive: import_prop_types.default.bool, renderWrapper: import_prop_types.default.bool, theme: import_prop_types.default.object, animate: import_prop_types.default.bool, motionConfig: import_prop_types.default.string };
var Bt = function() {
};
var Gt = { position: "relative" };
var Lt = function(e7) {
  var t7 = e7.children, i5 = e7.theme, o5 = e7.isInteractive, l4 = void 0 === o5 || o5, d2 = e7.renderWrapper, s3 = void 0 === d2 || d2, u2 = e7.animate, c10 = e7.motionConfig, f3 = (0, import_react16.useRef)(null), m5 = V(f3), y6 = m5.actions, v4 = m5.state, _2 = (0, import_react16.useCallback)(function(e8, r5) {
    return y6.showTooltipFromEvent(e8, r5);
  }, [y6.showTooltipFromEvent]), w3 = (0, import_react16.useMemo)(function() {
    return { showTooltip: l4 ? _2 : Bt, hideTooltip: l4 ? y6.hideTooltip : Bt };
  }, [y6.hideTooltip, l4, _2]);
  return (0, import_jsx_runtime2.jsx)(Wt, { theme: i5, children: (0, import_jsx_runtime2.jsx)(Ar, { animate: u2, config: c10, children: (0, import_jsx_runtime2.jsx)(j.Provider, { value: y6, children: (0, import_jsx_runtime2.jsx)(O.Provider, { value: v4, children: (0, import_jsx_runtime2.jsxs)(Pt, { condition: s3, wrapper: (0, import_jsx_runtime2.jsx)("div", { style: Gt, ref: f3 }), children: [t7(w3), l4 && (0, import_jsx_runtime2.jsx)(F, {})] }) }) }) }) });
};
Lt.propTypes = { children: import_prop_types.default.func.isRequired, isInteractive: import_prop_types.default.bool, renderWrapper: import_prop_types.default.bool, theme: import_prop_types.default.object.isRequired, animate: import_prop_types.default.bool.isRequired, motionConfig: import_prop_types.default.string };
var It = function(e7) {
  var r5 = e7.children, t7 = kt(), n5 = t7[0], i5 = t7[1], o5 = i5.width > 0 && i5.height > 0;
  return (0, import_jsx_runtime2.jsx)("div", { ref: n5, style: { width: "100%", height: "100%" }, children: o5 && r5({ width: i5.width, height: i5.height }) });
};
It.propTypes = { children: import_prop_types.default.func.isRequired };
var Yt = ["id", "colors"];
var Dt = function(e7) {
  var r5 = e7.id, t7 = e7.colors, n5 = Br(e7, Yt);
  return (0, import_jsx_runtime2.jsx)("linearGradient", jr({ id: r5, x1: 0, x2: 0, y1: 0, y2: 1 }, n5, { children: t7.map(function(e8) {
    var r6 = e8.offset, t8 = e8.color, n6 = e8.opacity;
    return (0, import_jsx_runtime2.jsx)("stop", { offset: r6 + "%", stopColor: t8, stopOpacity: void 0 !== n6 ? n6 : 1 }, r6);
  }) }));
};
Dt.propTypes = { id: import_prop_types.default.string.isRequired, colors: import_prop_types.default.arrayOf(import_prop_types.default.shape({ offset: import_prop_types.default.number.isRequired, color: import_prop_types.default.string.isRequired, opacity: import_prop_types.default.number })).isRequired, gradientTransform: import_prop_types.default.string };
var Et = { linearGradient: Dt };
var Ut = { color: "#000000", background: "#ffffff", size: 4, padding: 4, stagger: false };
var Ft = (0, import_react16.memo)(function(e7) {
  var r5 = e7.id, t7 = e7.background, n5 = void 0 === t7 ? Ut.background : t7, i5 = e7.color, o5 = void 0 === i5 ? Ut.color : i5, l4 = e7.size, a4 = void 0 === l4 ? Ut.size : l4, d2 = e7.padding, s3 = void 0 === d2 ? Ut.padding : d2, u2 = e7.stagger, c10 = void 0 === u2 ? Ut.stagger : u2, f3 = a4 + s3, p3 = a4 / 2, h = s3 / 2;
  return true === c10 && (f3 = 2 * a4 + 2 * s3), (0, import_jsx_runtime2.jsxs)("pattern", { id: r5, width: f3, height: f3, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime2.jsx)("rect", { width: f3, height: f3, fill: n5 }), (0, import_jsx_runtime2.jsx)("circle", { cx: h + p3, cy: h + p3, r: p3, fill: o5 }), c10 && (0, import_jsx_runtime2.jsx)("circle", { cx: 1.5 * s3 + a4 + p3, cy: 1.5 * s3 + a4 + p3, r: p3, fill: o5 })] });
});
Ft.displayName = "PatternDots", Ft.propTypes = { id: import_prop_types.default.string.isRequired, color: import_prop_types.default.string.isRequired, background: import_prop_types.default.string.isRequired, size: import_prop_types.default.number.isRequired, padding: import_prop_types.default.number.isRequired, stagger: import_prop_types.default.bool.isRequired };
var Ht = 2 * Math.PI;
var Kt = function(e7) {
  return e7 * Math.PI / 180;
};
var nn = { spacing: 5, rotation: 0, background: "#000000", color: "#ffffff", lineWidth: 2 };
var on = (0, import_react16.memo)(function(e7) {
  var r5 = e7.id, t7 = e7.spacing, n5 = void 0 === t7 ? nn.spacing : t7, i5 = e7.rotation, o5 = void 0 === i5 ? nn.rotation : i5, l4 = e7.background, a4 = void 0 === l4 ? nn.background : l4, d2 = e7.color, s3 = void 0 === d2 ? nn.color : d2, u2 = e7.lineWidth, c10 = void 0 === u2 ? nn.lineWidth : u2, f3 = Math.round(o5) % 360, p3 = Math.abs(n5);
  f3 > 180 ? f3 -= 360 : f3 > 90 ? f3 -= 180 : f3 < -180 ? f3 += 360 : f3 < -90 && (f3 += 180);
  var h, g3 = p3, b3 = p3;
  return 0 === f3 ? h = "\n                M 0 0 L " + g3 + " 0\n                M 0 " + b3 + " L " + g3 + " " + b3 + "\n            " : 90 === f3 ? h = "\n                M 0 0 L 0 " + b3 + "\n                M " + g3 + " 0 L " + g3 + " " + b3 + "\n            " : (g3 = Math.abs(p3 / Math.sin(Kt(f3))), b3 = p3 / Math.sin(Kt(90 - f3)), h = f3 > 0 ? "\n                    M 0 " + -b3 + " L " + 2 * g3 + " " + b3 + "\n                    M " + -g3 + " " + -b3 + " L " + g3 + " " + b3 + "\n                    M " + -g3 + " 0 L " + g3 + " " + 2 * b3 + "\n                " : "\n                    M " + -g3 + " " + b3 + " L " + g3 + " " + -b3 + "\n                    M " + -g3 + " " + 2 * b3 + " L " + 2 * g3 + " " + -b3 + "\n                    M 0 " + 2 * b3 + " L " + 2 * g3 + " 0\n                "), (0, import_jsx_runtime2.jsxs)("pattern", { id: r5, width: g3, height: b3, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime2.jsx)("rect", { width: g3, height: b3, fill: a4, stroke: "rgba(255, 0, 0, 0.1)", strokeWidth: 0 }), (0, import_jsx_runtime2.jsx)("path", { d: h, strokeWidth: c10, stroke: s3, strokeLinecap: "square" })] });
});
on.displayName = "PatternLines", on.propTypes = { id: import_prop_types.default.string.isRequired, spacing: import_prop_types.default.number.isRequired, rotation: import_prop_types.default.number.isRequired, background: import_prop_types.default.string.isRequired, color: import_prop_types.default.string.isRequired, lineWidth: import_prop_types.default.number.isRequired };
var an = { color: "#000000", background: "#ffffff", size: 4, padding: 4, stagger: false };
var dn = (0, import_react16.memo)(function(e7) {
  var r5 = e7.id, t7 = e7.color, n5 = void 0 === t7 ? an.color : t7, i5 = e7.background, o5 = void 0 === i5 ? an.background : i5, l4 = e7.size, a4 = void 0 === l4 ? an.size : l4, d2 = e7.padding, s3 = void 0 === d2 ? an.padding : d2, u2 = e7.stagger, c10 = void 0 === u2 ? an.stagger : u2, f3 = a4 + s3, p3 = s3 / 2;
  return true === c10 && (f3 = 2 * a4 + 2 * s3), (0, import_jsx_runtime2.jsxs)("pattern", { id: r5, width: f3, height: f3, patternUnits: "userSpaceOnUse", children: [(0, import_jsx_runtime2.jsx)("rect", { width: f3, height: f3, fill: o5 }), (0, import_jsx_runtime2.jsx)("rect", { x: p3, y: p3, width: a4, height: a4, fill: n5 }), c10 && (0, import_jsx_runtime2.jsx)("rect", { x: 1.5 * s3 + a4, y: 1.5 * s3 + a4, width: a4, height: a4, fill: n5 })] });
});
dn.displayName = "PatternSquares", dn.propTypes = { id: import_prop_types.default.string.isRequired, color: import_prop_types.default.string.isRequired, background: import_prop_types.default.string.isRequired, size: import_prop_types.default.number.isRequired, padding: import_prop_types.default.number.isRequired, stagger: import_prop_types.default.bool.isRequired };
var un = { patternDots: Ft, patternLines: on, patternSquares: dn };
var cn = ["type"];
var fn = jr({}, Et, un);
var pn = function(e7) {
  var r5 = e7.defs;
  return !r5 || r5.length < 1 ? null : (0, import_jsx_runtime2.jsx)("defs", { "aria-hidden": true, children: r5.map(function(e8) {
    var r6 = e8.type, t7 = Br(e8, cn);
    return fn[r6] ? (0, import_react16.createElement)(fn[r6], jr({ key: t7.id }, t7)) : null;
  }) });
};
pn.propTypes = { defs: import_prop_types.default.arrayOf(import_prop_types.default.shape({ type: import_prop_types.default.oneOf(Object.keys(fn)).isRequired, id: import_prop_types.default.string.isRequired })) };
var hn = (0, import_react16.memo)(pn);
var gn = function(e7) {
  var r5 = e7.width, t7 = e7.height, n5 = e7.margin, i5 = e7.defs, o5 = e7.children, l4 = e7.role, a4 = e7.ariaLabel, d2 = e7.ariaLabelledBy, s3 = e7.ariaDescribedBy, u2 = e7.isFocusable, c10 = zt();
  return (0, import_jsx_runtime2.jsxs)("svg", { xmlns: "http://www.w3.org/2000/svg", width: r5, height: t7, role: l4, "aria-label": a4, "aria-labelledby": d2, "aria-describedby": s3, focusable: u2, tabIndex: u2 ? 0 : void 0, children: [(0, import_jsx_runtime2.jsx)(hn, { defs: i5 }), (0, import_jsx_runtime2.jsx)("rect", { width: r5, height: t7, fill: c10.background }), (0, import_jsx_runtime2.jsx)("g", { transform: "translate(" + n5.left + "," + n5.top + ")", children: o5 })] });
};
gn.propTypes = { width: import_prop_types.default.number.isRequired, height: import_prop_types.default.number.isRequired, margin: import_prop_types.default.shape({ top: import_prop_types.default.number.isRequired, left: import_prop_types.default.number.isRequired }).isRequired, defs: import_prop_types.default.array, children: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.node), import_prop_types.default.node]).isRequired, role: import_prop_types.default.string, isFocusable: import_prop_types.default.bool, ariaLabel: import_prop_types.default.string, ariaLabelledBy: import_prop_types.default.string, ariaDescribedBy: import_prop_types.default.string };
var bn = function(e7) {
  var r5 = e7.size, t7 = e7.color, n5 = e7.borderWidth, i5 = e7.borderColor;
  return (0, import_jsx_runtime2.jsx)("circle", { r: r5 / 2, fill: t7, stroke: i5, strokeWidth: n5, style: { pointerEvents: "none" } });
};
bn.propTypes = { size: import_prop_types.default.number.isRequired, color: import_prop_types.default.string.isRequired, borderWidth: import_prop_types.default.number.isRequired, borderColor: import_prop_types.default.string.isRequired };
var mn = (0, import_react16.memo)(bn);
var yn = function(e7) {
  var r5 = e7.x, t7 = e7.y, n5 = e7.symbol, i5 = void 0 === n5 ? mn : n5, o5 = e7.size, l4 = e7.datum, a4 = e7.color, d2 = e7.borderWidth, u2 = e7.borderColor, c10 = e7.label, f3 = e7.labelTextAnchor, p3 = void 0 === f3 ? "middle" : f3, h = e7.labelYOffset, g3 = void 0 === h ? -12 : h, b3 = zt(), m5 = Ur(), y6 = m5.animate, v4 = m5.config, _2 = useSpring({ transform: "translate(" + r5 + ", " + t7 + ")", config: v4, immediate: !y6 });
  return (0, import_jsx_runtime2.jsxs)(animated.g, { transform: _2.transform, style: { pointerEvents: "none" }, children: [(0, import_react16.createElement)(i5, { size: o5, color: a4, datum: l4, borderWidth: d2, borderColor: u2 }), c10 && (0, import_jsx_runtime2.jsx)("text", { textAnchor: p3, y: g3, style: Mt(b3.dots.text), children: c10 })] });
};
yn.propTypes = { x: import_prop_types.default.number.isRequired, y: import_prop_types.default.number.isRequired, datum: import_prop_types.default.object.isRequired, size: import_prop_types.default.number.isRequired, color: import_prop_types.default.string.isRequired, borderWidth: import_prop_types.default.number.isRequired, borderColor: import_prop_types.default.string.isRequired, symbol: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.object]), label: import_prop_types.default.oneOfType([import_prop_types.default.string, import_prop_types.default.number]), labelTextAnchor: import_prop_types.default.oneOf(["start", "middle", "end"]), labelYOffset: import_prop_types.default.number };
var vn = (0, import_react16.memo)(yn);
var _n = function(e7) {
  var r5 = e7.width, t7 = e7.height, n5 = e7.axis, i5 = e7.scale, o5 = e7.value, l4 = e7.lineStyle, a4 = e7.textStyle, d2 = e7.legend, s3 = e7.legendPosition, u2 = void 0 === s3 ? "top-right" : s3, c10 = e7.legendOffsetX, f3 = void 0 === c10 ? 14 : c10, p3 = e7.legendOffsetY, h = void 0 === p3 ? 14 : p3, g3 = e7.legendOrientation, b3 = void 0 === g3 ? "horizontal" : g3, m5 = zt(), y6 = 0, v4 = 0, _2 = 0, w3 = 0;
  "y" === n5 ? (_2 = i5(o5), v4 = r5) : (y6 = i5(o5), w3 = t7);
  var k3 = null;
  if (d2) {
    var R = function(e8) {
      var r6 = e8.axis, t8 = e8.width, n6 = e8.height, i6 = e8.position, o6 = e8.offsetX, l5 = e8.offsetY, a5 = e8.orientation, d3 = 0, s4 = 0, u3 = "vertical" === a5 ? -90 : 0, c11 = "start";
      if ("x" === r6) switch (i6) {
        case "top-left":
          d3 = -o6, s4 = l5, c11 = "end";
          break;
        case "top":
          s4 = -l5, c11 = "horizontal" === a5 ? "middle" : "start";
          break;
        case "top-right":
          d3 = o6, s4 = l5, c11 = "horizontal" === a5 ? "start" : "end";
          break;
        case "right":
          d3 = o6, s4 = n6 / 2, c11 = "horizontal" === a5 ? "start" : "middle";
          break;
        case "bottom-right":
          d3 = o6, s4 = n6 - l5, c11 = "start";
          break;
        case "bottom":
          s4 = n6 + l5, c11 = "horizontal" === a5 ? "middle" : "end";
          break;
        case "bottom-left":
          s4 = n6 - l5, d3 = -o6, c11 = "horizontal" === a5 ? "end" : "start";
          break;
        case "left":
          d3 = -o6, s4 = n6 / 2, c11 = "horizontal" === a5 ? "end" : "middle";
      }
      else switch (i6) {
        case "top-left":
          d3 = o6, s4 = -l5, c11 = "start";
          break;
        case "top":
          d3 = t8 / 2, s4 = -l5, c11 = "horizontal" === a5 ? "middle" : "start";
          break;
        case "top-right":
          d3 = t8 - o6, s4 = -l5, c11 = "horizontal" === a5 ? "end" : "start";
          break;
        case "right":
          d3 = t8 + o6, c11 = "horizontal" === a5 ? "start" : "middle";
          break;
        case "bottom-right":
          d3 = t8 - o6, s4 = l5, c11 = "end";
          break;
        case "bottom":
          d3 = t8 / 2, s4 = l5, c11 = "horizontal" === a5 ? "middle" : "end";
          break;
        case "bottom-left":
          d3 = o6, s4 = l5, c11 = "horizontal" === a5 ? "start" : "end";
          break;
        case "left":
          d3 = -o6, c11 = "horizontal" === a5 ? "end" : "middle";
      }
      return { x: d3, y: s4, rotation: u3, textAnchor: c11 };
    }({ axis: n5, width: r5, height: t7, position: u2, offsetX: f3, offsetY: h, orientation: b3 });
    k3 = (0, import_jsx_runtime2.jsx)("text", { transform: "translate(" + R.x + ", " + R.y + ") rotate(" + R.rotation + ")", textAnchor: R.textAnchor, dominantBaseline: "central", style: a4, children: d2 });
  }
  return (0, import_jsx_runtime2.jsxs)("g", { transform: "translate(" + y6 + ", " + _2 + ")", children: [(0, import_jsx_runtime2.jsx)("line", { x1: 0, x2: v4, y1: 0, y2: w3, stroke: m5.markers.lineColor, strokeWidth: m5.markers.lineStrokeWidth, style: l4 }), k3] });
};
_n.propTypes = { width: import_prop_types.default.number.isRequired, height: import_prop_types.default.number.isRequired, axis: import_prop_types.default.oneOf(["x", "y"]).isRequired, scale: import_prop_types.default.func.isRequired, value: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string, import_prop_types.default.instanceOf(Date)]).isRequired, lineStyle: import_prop_types.default.object, textStyle: import_prop_types.default.object, legend: import_prop_types.default.string, legendPosition: import_prop_types.default.oneOf(["top-left", "top", "top-right", "right", "bottom-right", "bottom", "bottom-left", "left"]), legendOffsetX: import_prop_types.default.number.isRequired, legendOffsetY: import_prop_types.default.number.isRequired, legendOrientation: import_prop_types.default.oneOf(["horizontal", "vertical"]).isRequired };
var wn = (0, import_react16.memo)(_n);
var kn = function(e7) {
  var r5 = e7.markers, t7 = e7.width, n5 = e7.height, i5 = e7.xScale, o5 = e7.yScale;
  return r5 && 0 !== r5.length ? r5.map(function(e8, r6) {
    return (0, import_jsx_runtime2.jsx)(wn, jr({}, e8, { width: t7, height: n5, scale: "y" === e8.axis ? o5 : i5 }), r6);
  }) : null;
};
kn.propTypes = { width: import_prop_types.default.number.isRequired, height: import_prop_types.default.number.isRequired, xScale: import_prop_types.default.func.isRequired, yScale: import_prop_types.default.func.isRequired, markers: import_prop_types.default.arrayOf(import_prop_types.default.shape({ axis: import_prop_types.default.oneOf(["x", "y"]).isRequired, value: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string, import_prop_types.default.instanceOf(Date)]).isRequired, lineStyle: import_prop_types.default.object, textStyle: import_prop_types.default.object })) };
var Rn = (0, import_react16.memo)(kn);
var Cn = function(e7) {
  return (0, import_isFunction.default)(e7) ? e7 : function(r5) {
    return (0, import_get.default)(r5, e7);
  };
};
var Wn = function(e7) {
  return (0, import_react16.useMemo)(function() {
    return Cn(e7);
  }, [e7]);
};
var jn = function(e7, r5, t7, n5, i5, o5) {
  return e7 <= i5 && i5 <= e7 + t7 && r5 <= o5 && o5 <= r5 + n5;
};
var Sn = function(e7, r5) {
  var t7, n5 = "touches" in r5 ? r5.touches[0] : r5, i5 = n5.clientX, o5 = n5.clientY, l4 = e7.getBoundingClientRect(), a4 = (t7 = void 0 !== e7.getBBox ? e7.getBBox() : { width: e7.offsetWidth || 0, height: e7.offsetHeight || 0 }).width === l4.width ? 1 : t7.width / l4.width;
  return [(i5 - l4.left) * a4, (o5 - l4.top) * a4];
};
var Bn = Object.keys(Et);
var Gn = Object.keys(un);
var Ln = function(e7, r5, t7) {
  if ("*" === e7) return true;
  if ((0, import_isFunction.default)(e7)) return e7(r5);
  if ((0, import_isPlainObject.default)(e7)) {
    var n5 = t7 ? (0, import_get.default)(r5, t7) : r5;
    return (0, import_isEqual.default)((0, import_pick.default)(n5, Object.keys(e7)), e7);
  }
  return false;
};
var In = function(e7, r5, t7, n5) {
  var i5 = void 0 === n5 ? {} : n5, o5 = i5.dataKey, l4 = i5.colorKey, a4 = void 0 === l4 ? "color" : l4, d2 = i5.targetKey, s3 = void 0 === d2 ? "fill" : d2, u2 = [], c10 = {};
  return e7.length && r5.length && (u2 = [].concat(e7), r5.forEach(function(r6) {
    for (var n6 = function() {
      var n7 = t7[i6], l5 = n7.id, d3 = n7.match;
      if (Ln(d3, r6, o5)) {
        var f3 = e7.find(function(e8) {
          return e8.id === l5;
        });
        if (f3) {
          if (Gn.includes(f3.type)) if ("inherit" === f3.background || "inherit" === f3.color) {
            var p3 = (0, import_get.default)(r6, a4), h = f3.background, g3 = f3.color, b3 = l5;
            "inherit" === f3.background && (b3 = b3 + ".bg." + p3, h = p3), "inherit" === f3.color && (b3 = b3 + ".fg." + p3, g3 = p3), (0, import_set2.default)(r6, s3, "url(#" + b3 + ")"), c10[b3] || (u2.push(jr({}, f3, { id: b3, background: h, color: g3 })), c10[b3] = 1);
          } else (0, import_set2.default)(r6, s3, "url(#" + l5 + ")");
          else if (Bn.includes(f3.type)) {
            if (f3.colors.map(function(e8) {
              return e8.color;
            }).includes("inherit")) {
              var m5 = (0, import_get.default)(r6, a4), _2 = l5, w3 = jr({}, f3, { colors: f3.colors.map(function(e8, r7) {
                return "inherit" !== e8.color ? e8 : (_2 = _2 + "." + r7 + "." + m5, jr({}, e8, { color: "inherit" === e8.color ? m5 : e8.color }));
              }) });
              w3.id = _2, (0, import_set2.default)(r6, s3, "url(#" + _2 + ")"), c10[_2] || (u2.push(w3), c10[_2] = 1);
            } else (0, import_set2.default)(r6, s3, "url(#" + l5 + ")");
          }
        }
        return "break";
      }
    }, i6 = 0; i6 < t7.length; i6++) {
      if ("break" === n6()) break;
    }
  })), u2;
};

// node_modules/@nivo/treemap/dist/nivo-treemap.es.js
var import_omit = __toESM(require_omit());
var import_cloneDeep = __toESM(require_cloneDeep());
var import_startCase = __toESM(require_startCase());

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/count.js
function count3(node) {
  var sum5 = 0, children = node.children, i5 = children && children.length;
  if (!i5) sum5 = 1;
  else while (--i5 >= 0) sum5 += children[i5].value;
  node.value = sum5;
}
function count_default() {
  return this.eachAfter(count3);
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/each.js
function each_default(callback) {
  var node = this, current, next = [node], children, i5, n5;
  do {
    current = next.reverse(), next = [];
    while (node = current.pop()) {
      callback(node), children = node.children;
      if (children) for (i5 = 0, n5 = children.length; i5 < n5; ++i5) {
        next.push(children[i5]);
      }
    }
  } while (next.length);
  return this;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/eachBefore.js
function eachBefore_default(callback) {
  var node = this, nodes = [node], children, i5;
  while (node = nodes.pop()) {
    callback(node), children = node.children;
    if (children) for (i5 = children.length - 1; i5 >= 0; --i5) {
      nodes.push(children[i5]);
    }
  }
  return this;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/eachAfter.js
function eachAfter_default(callback) {
  var node = this, nodes = [node], next = [], children, i5, n5;
  while (node = nodes.pop()) {
    next.push(node), children = node.children;
    if (children) for (i5 = 0, n5 = children.length; i5 < n5; ++i5) {
      nodes.push(children[i5]);
    }
  }
  while (node = next.pop()) {
    callback(node);
  }
  return this;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/sum.js
function sum_default(value) {
  return this.eachAfter(function(node) {
    var sum5 = +value(node.data) || 0, children = node.children, i5 = children && children.length;
    while (--i5 >= 0) sum5 += children[i5].value;
    node.value = sum5;
  });
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/sort.js
function sort_default(compare) {
  return this.eachBefore(function(node) {
    if (node.children) {
      node.children.sort(compare);
    }
  });
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/path.js
function path_default(end) {
  var start3 = this, ancestor = leastCommonAncestor(start3, end), nodes = [start3];
  while (start3 !== ancestor) {
    start3 = start3.parent;
    nodes.push(start3);
  }
  var k3 = nodes.length;
  while (end !== ancestor) {
    nodes.splice(k3, 0, end);
    end = end.parent;
  }
  return nodes;
}
function leastCommonAncestor(a4, b3) {
  if (a4 === b3) return a4;
  var aNodes = a4.ancestors(), bNodes = b3.ancestors(), c10 = null;
  a4 = aNodes.pop();
  b3 = bNodes.pop();
  while (a4 === b3) {
    c10 = a4;
    a4 = aNodes.pop();
    b3 = bNodes.pop();
  }
  return c10;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/ancestors.js
function ancestors_default() {
  var node = this, nodes = [node];
  while (node = node.parent) {
    nodes.push(node);
  }
  return nodes;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/descendants.js
function descendants_default() {
  var nodes = [];
  this.each(function(node) {
    nodes.push(node);
  });
  return nodes;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/leaves.js
function leaves_default() {
  var leaves = [];
  this.eachBefore(function(node) {
    if (!node.children) {
      leaves.push(node);
    }
  });
  return leaves;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/links.js
function links_default() {
  var root = this, links = [];
  root.each(function(node) {
    if (node !== root) {
      links.push({ source: node.parent, target: node });
    }
  });
  return links;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/hierarchy/index.js
function hierarchy(data, children) {
  var root = new Node(data), valued = +data.value && (root.value = data.value), node, nodes = [root], child, childs, i5, n5;
  if (children == null) children = defaultChildren;
  while (node = nodes.pop()) {
    if (valued) node.value = +node.data.value;
    if ((childs = children(node.data)) && (n5 = childs.length)) {
      node.children = new Array(n5);
      for (i5 = n5 - 1; i5 >= 0; --i5) {
        nodes.push(child = node.children[i5] = new Node(childs[i5]));
        child.parent = node;
        child.depth = node.depth + 1;
      }
    }
  }
  return root.eachBefore(computeHeight);
}
function node_copy() {
  return hierarchy(this).eachBefore(copyData);
}
function defaultChildren(d2) {
  return d2.children;
}
function copyData(node) {
  node.data = node.data.data;
}
function computeHeight(node) {
  var height = 0;
  do
    node.height = height;
  while ((node = node.parent) && node.height < ++height);
}
function Node(data) {
  this.data = data;
  this.depth = this.height = 0;
  this.parent = null;
}
Node.prototype = hierarchy.prototype = {
  constructor: Node,
  count: count_default,
  each: each_default,
  eachAfter: eachAfter_default,
  eachBefore: eachBefore_default,
  sum: sum_default,
  sort: sort_default,
  path: path_default,
  ancestors: ancestors_default,
  descendants: descendants_default,
  leaves: leaves_default,
  links: links_default,
  copy: node_copy
};

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/array.js
var slice4 = Array.prototype.slice;

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/accessors.js
function required(f3) {
  if (typeof f3 !== "function") throw new Error();
  return f3;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/constant.js
function constantZero() {
  return 0;
}
function constant_default4(x4) {
  return function() {
    return x4;
  };
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/round.js
function round_default2(node) {
  node.x0 = Math.round(node.x0);
  node.y0 = Math.round(node.y0);
  node.x1 = Math.round(node.x1);
  node.y1 = Math.round(node.y1);
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/dice.js
function dice_default(parent, x0, y0, x1, y1) {
  var nodes = parent.children, node, i5 = -1, n5 = nodes.length, k3 = parent.value && (x1 - x0) / parent.value;
  while (++i5 < n5) {
    node = nodes[i5], node.y0 = y0, node.y1 = y1;
    node.x0 = x0, node.x1 = x0 += node.value * k3;
  }
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/tree.js
function TreeNode(node, i5) {
  this._ = node;
  this.parent = null;
  this.children = null;
  this.A = null;
  this.a = this;
  this.z = 0;
  this.m = 0;
  this.c = 0;
  this.s = 0;
  this.t = null;
  this.i = i5;
}
TreeNode.prototype = Object.create(Node.prototype);

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/slice.js
function slice_default(parent, x0, y0, x1, y1) {
  var nodes = parent.children, node, i5 = -1, n5 = nodes.length, k3 = parent.value && (y1 - y0) / parent.value;
  while (++i5 < n5) {
    node = nodes[i5], node.x0 = x0, node.x1 = x1;
    node.y0 = y0, node.y1 = y0 += node.value * k3;
  }
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/squarify.js
var phi = (1 + Math.sqrt(5)) / 2;
function squarifyRatio(ratio, parent, x0, y0, x1, y1) {
  var rows = [], nodes = parent.children, row, nodeValue, i0 = 0, i1 = 0, n5 = nodes.length, dx, dy, value = parent.value, sumValue, minValue, maxValue, newRatio, minRatio, alpha, beta;
  while (i0 < n5) {
    dx = x1 - x0, dy = y1 - y0;
    do
      sumValue = nodes[i1++].value;
    while (!sumValue && i1 < n5);
    minValue = maxValue = sumValue;
    alpha = Math.max(dy / dx, dx / dy) / (value * ratio);
    beta = sumValue * sumValue * alpha;
    minRatio = Math.max(maxValue / beta, beta / minValue);
    for (; i1 < n5; ++i1) {
      sumValue += nodeValue = nodes[i1].value;
      if (nodeValue < minValue) minValue = nodeValue;
      if (nodeValue > maxValue) maxValue = nodeValue;
      beta = sumValue * sumValue * alpha;
      newRatio = Math.max(maxValue / beta, beta / minValue);
      if (newRatio > minRatio) {
        sumValue -= nodeValue;
        break;
      }
      minRatio = newRatio;
    }
    rows.push(row = { value: sumValue, dice: dx < dy, children: nodes.slice(i0, i1) });
    if (row.dice) dice_default(row, x0, y0, x1, value ? y0 += dy * sumValue / value : y1);
    else slice_default(row, x0, y0, value ? x0 += dx * sumValue / value : x1, y1);
    value -= sumValue, i0 = i1;
  }
  return rows;
}
var squarify_default = function custom8(ratio) {
  function squarify(parent, x0, y0, x1, y1) {
    squarifyRatio(ratio, parent, x0, y0, x1, y1);
  }
  squarify.ratio = function(x4) {
    return custom8((x4 = +x4) > 1 ? x4 : 1);
  };
  return squarify;
}(phi);

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/index.js
function treemap_default() {
  var tile = squarify_default, round = false, dx = 1, dy = 1, paddingStack = [0], paddingInner = constantZero, paddingTop = constantZero, paddingRight = constantZero, paddingBottom = constantZero, paddingLeft = constantZero;
  function treemap(root) {
    root.x0 = root.y0 = 0;
    root.x1 = dx;
    root.y1 = dy;
    root.eachBefore(positionNode);
    paddingStack = [0];
    if (round) root.eachBefore(round_default2);
    return root;
  }
  function positionNode(node) {
    var p3 = paddingStack[node.depth], x0 = node.x0 + p3, y0 = node.y0 + p3, x1 = node.x1 - p3, y1 = node.y1 - p3;
    if (x1 < x0) x0 = x1 = (x0 + x1) / 2;
    if (y1 < y0) y0 = y1 = (y0 + y1) / 2;
    node.x0 = x0;
    node.y0 = y0;
    node.x1 = x1;
    node.y1 = y1;
    if (node.children) {
      p3 = paddingStack[node.depth + 1] = paddingInner(node) / 2;
      x0 += paddingLeft(node) - p3;
      y0 += paddingTop(node) - p3;
      x1 -= paddingRight(node) - p3;
      y1 -= paddingBottom(node) - p3;
      if (x1 < x0) x0 = x1 = (x0 + x1) / 2;
      if (y1 < y0) y0 = y1 = (y0 + y1) / 2;
      tile(node, x0, y0, x1, y1);
    }
  }
  treemap.round = function(x4) {
    return arguments.length ? (round = !!x4, treemap) : round;
  };
  treemap.size = function(x4) {
    return arguments.length ? (dx = +x4[0], dy = +x4[1], treemap) : [dx, dy];
  };
  treemap.tile = function(x4) {
    return arguments.length ? (tile = required(x4), treemap) : tile;
  };
  treemap.padding = function(x4) {
    return arguments.length ? treemap.paddingInner(x4).paddingOuter(x4) : treemap.paddingInner();
  };
  treemap.paddingInner = function(x4) {
    return arguments.length ? (paddingInner = typeof x4 === "function" ? x4 : constant_default4(+x4), treemap) : paddingInner;
  };
  treemap.paddingOuter = function(x4) {
    return arguments.length ? treemap.paddingTop(x4).paddingRight(x4).paddingBottom(x4).paddingLeft(x4) : treemap.paddingTop();
  };
  treemap.paddingTop = function(x4) {
    return arguments.length ? (paddingTop = typeof x4 === "function" ? x4 : constant_default4(+x4), treemap) : paddingTop;
  };
  treemap.paddingRight = function(x4) {
    return arguments.length ? (paddingRight = typeof x4 === "function" ? x4 : constant_default4(+x4), treemap) : paddingRight;
  };
  treemap.paddingBottom = function(x4) {
    return arguments.length ? (paddingBottom = typeof x4 === "function" ? x4 : constant_default4(+x4), treemap) : paddingBottom;
  };
  treemap.paddingLeft = function(x4) {
    return arguments.length ? (paddingLeft = typeof x4 === "function" ? x4 : constant_default4(+x4), treemap) : paddingLeft;
  };
  return treemap;
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/binary.js
function binary_default(parent, x0, y0, x1, y1) {
  var nodes = parent.children, i5, n5 = nodes.length, sum5, sums = new Array(n5 + 1);
  for (sums[0] = sum5 = i5 = 0; i5 < n5; ++i5) {
    sums[i5 + 1] = sum5 += nodes[i5].value;
  }
  partition(0, n5, parent.value, x0, y0, x1, y1);
  function partition(i6, j4, value, x02, y02, x12, y12) {
    if (i6 >= j4 - 1) {
      var node = nodes[i6];
      node.x0 = x02, node.y0 = y02;
      node.x1 = x12, node.y1 = y12;
      return;
    }
    var valueOffset = sums[i6], valueTarget = value / 2 + valueOffset, k3 = i6 + 1, hi = j4 - 1;
    while (k3 < hi) {
      var mid = k3 + hi >>> 1;
      if (sums[mid] < valueTarget) k3 = mid + 1;
      else hi = mid;
    }
    if (valueTarget - sums[k3 - 1] < sums[k3] - valueTarget && i6 + 1 < k3) --k3;
    var valueLeft = sums[k3] - valueOffset, valueRight = value - valueLeft;
    if (x12 - x02 > y12 - y02) {
      var xk = (x02 * valueRight + x12 * valueLeft) / value;
      partition(i6, k3, valueLeft, x02, y02, xk, y12);
      partition(k3, j4, valueRight, xk, y02, x12, y12);
    } else {
      var yk = (y02 * valueRight + y12 * valueLeft) / value;
      partition(i6, k3, valueLeft, x02, y02, x12, yk);
      partition(k3, j4, valueRight, x02, yk, x12, y12);
    }
  }
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/sliceDice.js
function sliceDice_default(parent, x0, y0, x1, y1) {
  (parent.depth & 1 ? slice_default : dice_default)(parent, x0, y0, x1, y1);
}

// node_modules/@nivo/treemap/node_modules/d3-hierarchy/src/treemap/resquarify.js
var resquarify_default = function custom9(ratio) {
  function resquarify(parent, x0, y0, x1, y1) {
    if ((rows = parent._squarify) && rows.ratio === ratio) {
      var rows, row, nodes, i5, j4 = -1, n5, m5 = rows.length, value = parent.value;
      while (++j4 < m5) {
        row = rows[j4], nodes = row.children;
        for (i5 = row.value = 0, n5 = nodes.length; i5 < n5; ++i5) row.value += nodes[i5].value;
        if (row.dice) dice_default(row, x0, y0, x1, y0 += (y1 - y0) * row.value / value);
        else slice_default(row, x0, y0, x0 += (x1 - x0) * row.value / value, y1);
        value -= row.value;
      }
    } else {
      parent._squarify = rows = squarifyRatio(ratio, parent, x0, y0, x1, y1);
      rows.ratio = ratio;
    }
  }
  resquarify.ratio = function(x4) {
    return custom9((x4 = +x4) > 1 ? x4 : 1);
  };
  return resquarify;
}(phi);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/colors.js
function colors_default2(specifier) {
  var n5 = specifier.length / 6 | 0, colors4 = new Array(n5), i5 = 0;
  while (i5 < n5) colors4[i5] = "#" + specifier.slice(i5 * 6, ++i5 * 6);
  return colors4;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/category10.js
var category10_default2 = colors_default2("1f77b4ff7f0e2ca02cd627289467bd8c564be377c27f7f7fbcbd2217becf");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Accent.js
var Accent_default2 = colors_default2("7fc97fbeaed4fdc086ffff99386cb0f0027fbf5b17666666");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Dark2.js
var Dark2_default2 = colors_default2("1b9e77d95f027570b3e7298a66a61ee6ab02a6761d666666");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/observable10.js
var observable10_default2 = colors_default2("4269d0efb118ff725c6cc5b03ca951ff8ab7a463f297bbf59c6b4e9498a0");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Paired.js
var Paired_default2 = colors_default2("a6cee31f78b4b2df8a33a02cfb9a99e31a1cfdbf6fff7f00cab2d66a3d9affff99b15928");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Pastel1.js
var Pastel1_default2 = colors_default2("fbb4aeb3cde3ccebc5decbe4fed9a6ffffcce5d8bdfddaecf2f2f2");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Pastel2.js
var Pastel2_default2 = colors_default2("b3e2cdfdcdaccbd5e8f4cae4e6f5c9fff2aef1e2cccccccc");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Set1.js
var Set1_default2 = colors_default2("e41a1c377eb84daf4a984ea3ff7f00ffff33a65628f781bf999999");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Set2.js
var Set2_default2 = colors_default2("66c2a5fc8d628da0cbe78ac3a6d854ffd92fe5c494b3b3b3");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Set3.js
var Set3_default2 = colors_default2("8dd3c7ffffb3bebadafb807280b1d3fdb462b3de69fccde5d9d9d9bc80bdccebc5ffed6f");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/categorical/Tableau10.js
var Tableau10_default2 = colors_default2("4e79a7f28e2ce1575976b7b259a14fedc949af7aa1ff9da79c755fbab0ab");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-color/src/define.js
function define_default2(constructor, factory, prototype) {
  constructor.prototype = factory.prototype = prototype;
  prototype.constructor = constructor;
}
function extend2(parent, definition) {
  var prototype = Object.create(parent.prototype);
  for (var key in definition) prototype[key] = definition[key];
  return prototype;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-color/src/color.js
function Color2() {
}
var darker2 = 0.7;
var brighter2 = 1 / darker2;
var reI2 = "\\s*([+-]?\\d+)\\s*";
var reN2 = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
var reP2 = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
var reHex2 = /^#([0-9a-f]{3,8})$/;
var reRgbInteger2 = new RegExp(`^rgb\\(${reI2},${reI2},${reI2}\\)$`);
var reRgbPercent2 = new RegExp(`^rgb\\(${reP2},${reP2},${reP2}\\)$`);
var reRgbaInteger2 = new RegExp(`^rgba\\(${reI2},${reI2},${reI2},${reN2}\\)$`);
var reRgbaPercent2 = new RegExp(`^rgba\\(${reP2},${reP2},${reP2},${reN2}\\)$`);
var reHslPercent2 = new RegExp(`^hsl\\(${reN2},${reP2},${reP2}\\)$`);
var reHslaPercent2 = new RegExp(`^hsla\\(${reN2},${reP2},${reP2},${reN2}\\)$`);
var named2 = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
define_default2(Color2, color2, {
  copy(channels) {
    return Object.assign(new this.constructor(), this, channels);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: color_formatHex2,
  // Deprecated! Use color.formatHex.
  formatHex: color_formatHex2,
  formatHex8: color_formatHex82,
  formatHsl: color_formatHsl2,
  formatRgb: color_formatRgb2,
  toString: color_formatRgb2
});
function color_formatHex2() {
  return this.rgb().formatHex();
}
function color_formatHex82() {
  return this.rgb().formatHex8();
}
function color_formatHsl2() {
  return hslConvert2(this).formatHsl();
}
function color_formatRgb2() {
  return this.rgb().formatRgb();
}
function color2(format3) {
  var m5, l4;
  format3 = (format3 + "").trim().toLowerCase();
  return (m5 = reHex2.exec(format3)) ? (l4 = m5[1].length, m5 = parseInt(m5[1], 16), l4 === 6 ? rgbn2(m5) : l4 === 3 ? new Rgb2(m5 >> 8 & 15 | m5 >> 4 & 240, m5 >> 4 & 15 | m5 & 240, (m5 & 15) << 4 | m5 & 15, 1) : l4 === 8 ? rgba3(m5 >> 24 & 255, m5 >> 16 & 255, m5 >> 8 & 255, (m5 & 255) / 255) : l4 === 4 ? rgba3(m5 >> 12 & 15 | m5 >> 8 & 240, m5 >> 8 & 15 | m5 >> 4 & 240, m5 >> 4 & 15 | m5 & 240, ((m5 & 15) << 4 | m5 & 15) / 255) : null) : (m5 = reRgbInteger2.exec(format3)) ? new Rgb2(m5[1], m5[2], m5[3], 1) : (m5 = reRgbPercent2.exec(format3)) ? new Rgb2(m5[1] * 255 / 100, m5[2] * 255 / 100, m5[3] * 255 / 100, 1) : (m5 = reRgbaInteger2.exec(format3)) ? rgba3(m5[1], m5[2], m5[3], m5[4]) : (m5 = reRgbaPercent2.exec(format3)) ? rgba3(m5[1] * 255 / 100, m5[2] * 255 / 100, m5[3] * 255 / 100, m5[4]) : (m5 = reHslPercent2.exec(format3)) ? hsla3(m5[1], m5[2] / 100, m5[3] / 100, 1) : (m5 = reHslaPercent2.exec(format3)) ? hsla3(m5[1], m5[2] / 100, m5[3] / 100, m5[4]) : named2.hasOwnProperty(format3) ? rgbn2(named2[format3]) : format3 === "transparent" ? new Rgb2(NaN, NaN, NaN, 0) : null;
}
function rgbn2(n5) {
  return new Rgb2(n5 >> 16 & 255, n5 >> 8 & 255, n5 & 255, 1);
}
function rgba3(r5, g3, b3, a4) {
  if (a4 <= 0) r5 = g3 = b3 = NaN;
  return new Rgb2(r5, g3, b3, a4);
}
function rgbConvert2(o5) {
  if (!(o5 instanceof Color2)) o5 = color2(o5);
  if (!o5) return new Rgb2();
  o5 = o5.rgb();
  return new Rgb2(o5.r, o5.g, o5.b, o5.opacity);
}
function rgb3(r5, g3, b3, opacity) {
  return arguments.length === 1 ? rgbConvert2(r5) : new Rgb2(r5, g3, b3, opacity == null ? 1 : opacity);
}
function Rgb2(r5, g3, b3, opacity) {
  this.r = +r5;
  this.g = +g3;
  this.b = +b3;
  this.opacity = +opacity;
}
define_default2(Rgb2, rgb3, extend2(Color2, {
  brighter(k3) {
    k3 = k3 == null ? brighter2 : Math.pow(brighter2, k3);
    return new Rgb2(this.r * k3, this.g * k3, this.b * k3, this.opacity);
  },
  darker(k3) {
    k3 = k3 == null ? darker2 : Math.pow(darker2, k3);
    return new Rgb2(this.r * k3, this.g * k3, this.b * k3, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Rgb2(clampi2(this.r), clampi2(this.g), clampi2(this.b), clampa2(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && (-0.5 <= this.g && this.g < 255.5) && (-0.5 <= this.b && this.b < 255.5) && (0 <= this.opacity && this.opacity <= 1);
  },
  hex: rgb_formatHex2,
  // Deprecated! Use color.formatHex.
  formatHex: rgb_formatHex2,
  formatHex8: rgb_formatHex82,
  formatRgb: rgb_formatRgb2,
  toString: rgb_formatRgb2
}));
function rgb_formatHex2() {
  return `#${hex2(this.r)}${hex2(this.g)}${hex2(this.b)}`;
}
function rgb_formatHex82() {
  return `#${hex2(this.r)}${hex2(this.g)}${hex2(this.b)}${hex2((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function rgb_formatRgb2() {
  const a4 = clampa2(this.opacity);
  return `${a4 === 1 ? "rgb(" : "rgba("}${clampi2(this.r)}, ${clampi2(this.g)}, ${clampi2(this.b)}${a4 === 1 ? ")" : `, ${a4})`}`;
}
function clampa2(opacity) {
  return isNaN(opacity) ? 1 : Math.max(0, Math.min(1, opacity));
}
function clampi2(value) {
  return Math.max(0, Math.min(255, Math.round(value) || 0));
}
function hex2(value) {
  value = clampi2(value);
  return (value < 16 ? "0" : "") + value.toString(16);
}
function hsla3(h, s3, l4, a4) {
  if (a4 <= 0) h = s3 = l4 = NaN;
  else if (l4 <= 0 || l4 >= 1) h = s3 = NaN;
  else if (s3 <= 0) h = NaN;
  return new Hsl2(h, s3, l4, a4);
}
function hslConvert2(o5) {
  if (o5 instanceof Hsl2) return new Hsl2(o5.h, o5.s, o5.l, o5.opacity);
  if (!(o5 instanceof Color2)) o5 = color2(o5);
  if (!o5) return new Hsl2();
  if (o5 instanceof Hsl2) return o5;
  o5 = o5.rgb();
  var r5 = o5.r / 255, g3 = o5.g / 255, b3 = o5.b / 255, min5 = Math.min(r5, g3, b3), max5 = Math.max(r5, g3, b3), h = NaN, s3 = max5 - min5, l4 = (max5 + min5) / 2;
  if (s3) {
    if (r5 === max5) h = (g3 - b3) / s3 + (g3 < b3) * 6;
    else if (g3 === max5) h = (b3 - r5) / s3 + 2;
    else h = (r5 - g3) / s3 + 4;
    s3 /= l4 < 0.5 ? max5 + min5 : 2 - max5 - min5;
    h *= 60;
  } else {
    s3 = l4 > 0 && l4 < 1 ? 0 : h;
  }
  return new Hsl2(h, s3, l4, o5.opacity);
}
function hsl4(h, s3, l4, opacity) {
  return arguments.length === 1 ? hslConvert2(h) : new Hsl2(h, s3, l4, opacity == null ? 1 : opacity);
}
function Hsl2(h, s3, l4, opacity) {
  this.h = +h;
  this.s = +s3;
  this.l = +l4;
  this.opacity = +opacity;
}
define_default2(Hsl2, hsl4, extend2(Color2, {
  brighter(k3) {
    k3 = k3 == null ? brighter2 : Math.pow(brighter2, k3);
    return new Hsl2(this.h, this.s, this.l * k3, this.opacity);
  },
  darker(k3) {
    k3 = k3 == null ? darker2 : Math.pow(darker2, k3);
    return new Hsl2(this.h, this.s, this.l * k3, this.opacity);
  },
  rgb() {
    var h = this.h % 360 + (this.h < 0) * 360, s3 = isNaN(h) || isNaN(this.s) ? 0 : this.s, l4 = this.l, m22 = l4 + (l4 < 0.5 ? l4 : 1 - l4) * s3, m1 = 2 * l4 - m22;
    return new Rgb2(
      hsl2rgb2(h >= 240 ? h - 240 : h + 120, m1, m22),
      hsl2rgb2(h, m1, m22),
      hsl2rgb2(h < 120 ? h + 240 : h - 120, m1, m22),
      this.opacity
    );
  },
  clamp() {
    return new Hsl2(clamph2(this.h), clampt2(this.s), clampt2(this.l), clampa2(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && (0 <= this.l && this.l <= 1) && (0 <= this.opacity && this.opacity <= 1);
  },
  formatHsl() {
    const a4 = clampa2(this.opacity);
    return `${a4 === 1 ? "hsl(" : "hsla("}${clamph2(this.h)}, ${clampt2(this.s) * 100}%, ${clampt2(this.l) * 100}%${a4 === 1 ? ")" : `, ${a4})`}`;
  }
}));
function clamph2(value) {
  value = (value || 0) % 360;
  return value < 0 ? value + 360 : value;
}
function clampt2(value) {
  return Math.max(0, Math.min(1, value || 0));
}
function hsl2rgb2(h, m1, m22) {
  return (h < 60 ? m1 + (m22 - m1) * h / 60 : h < 180 ? m22 : h < 240 ? m1 + (m22 - m1) * (240 - h) / 60 : m1) * 255;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-color/src/math.js
var radians2 = Math.PI / 180;
var degrees3 = 180 / Math.PI;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-color/src/lab.js
var K2 = 18;
var Xn2 = 0.96422;
var Yn2 = 1;
var Zn2 = 0.82521;
var t04 = 4 / 29;
var t14 = 6 / 29;
var t22 = 3 * t14 * t14;
var t32 = t14 * t14 * t14;
function labConvert2(o5) {
  if (o5 instanceof Lab2) return new Lab2(o5.l, o5.a, o5.b, o5.opacity);
  if (o5 instanceof Hcl2) return hcl2lab2(o5);
  if (!(o5 instanceof Rgb2)) o5 = rgbConvert2(o5);
  var r5 = rgb2lrgb2(o5.r), g3 = rgb2lrgb2(o5.g), b3 = rgb2lrgb2(o5.b), y6 = xyz2lab2((0.2225045 * r5 + 0.7168786 * g3 + 0.0606169 * b3) / Yn2), x4, z3;
  if (r5 === g3 && g3 === b3) x4 = z3 = y6;
  else {
    x4 = xyz2lab2((0.4360747 * r5 + 0.3850649 * g3 + 0.1430804 * b3) / Xn2);
    z3 = xyz2lab2((0.0139322 * r5 + 0.0971045 * g3 + 0.7141733 * b3) / Zn2);
  }
  return new Lab2(116 * y6 - 16, 500 * (x4 - y6), 200 * (y6 - z3), o5.opacity);
}
function lab3(l4, a4, b3, opacity) {
  return arguments.length === 1 ? labConvert2(l4) : new Lab2(l4, a4, b3, opacity == null ? 1 : opacity);
}
function Lab2(l4, a4, b3, opacity) {
  this.l = +l4;
  this.a = +a4;
  this.b = +b3;
  this.opacity = +opacity;
}
define_default2(Lab2, lab3, extend2(Color2, {
  brighter(k3) {
    return new Lab2(this.l + K2 * (k3 == null ? 1 : k3), this.a, this.b, this.opacity);
  },
  darker(k3) {
    return new Lab2(this.l - K2 * (k3 == null ? 1 : k3), this.a, this.b, this.opacity);
  },
  rgb() {
    var y6 = (this.l + 16) / 116, x4 = isNaN(this.a) ? y6 : y6 + this.a / 500, z3 = isNaN(this.b) ? y6 : y6 - this.b / 200;
    x4 = Xn2 * lab2xyz2(x4);
    y6 = Yn2 * lab2xyz2(y6);
    z3 = Zn2 * lab2xyz2(z3);
    return new Rgb2(
      lrgb2rgb2(3.1338561 * x4 - 1.6168667 * y6 - 0.4906146 * z3),
      lrgb2rgb2(-0.9787684 * x4 + 1.9161415 * y6 + 0.033454 * z3),
      lrgb2rgb2(0.0719453 * x4 - 0.2289914 * y6 + 1.4052427 * z3),
      this.opacity
    );
  }
}));
function xyz2lab2(t7) {
  return t7 > t32 ? Math.pow(t7, 1 / 3) : t7 / t22 + t04;
}
function lab2xyz2(t7) {
  return t7 > t14 ? t7 * t7 * t7 : t22 * (t7 - t04);
}
function lrgb2rgb2(x4) {
  return 255 * (x4 <= 31308e-7 ? 12.92 * x4 : 1.055 * Math.pow(x4, 1 / 2.4) - 0.055);
}
function rgb2lrgb2(x4) {
  return (x4 /= 255) <= 0.04045 ? x4 / 12.92 : Math.pow((x4 + 0.055) / 1.055, 2.4);
}
function hclConvert2(o5) {
  if (o5 instanceof Hcl2) return new Hcl2(o5.h, o5.c, o5.l, o5.opacity);
  if (!(o5 instanceof Lab2)) o5 = labConvert2(o5);
  if (o5.a === 0 && o5.b === 0) return new Hcl2(NaN, 0 < o5.l && o5.l < 100 ? 0 : NaN, o5.l, o5.opacity);
  var h = Math.atan2(o5.b, o5.a) * degrees3;
  return new Hcl2(h < 0 ? h + 360 : h, Math.sqrt(o5.a * o5.a + o5.b * o5.b), o5.l, o5.opacity);
}
function hcl3(h, c10, l4, opacity) {
  return arguments.length === 1 ? hclConvert2(h) : new Hcl2(h, c10, l4, opacity == null ? 1 : opacity);
}
function Hcl2(h, c10, l4, opacity) {
  this.h = +h;
  this.c = +c10;
  this.l = +l4;
  this.opacity = +opacity;
}
function hcl2lab2(o5) {
  if (isNaN(o5.h)) return new Lab2(o5.l, 0, 0, o5.opacity);
  var h = o5.h * radians2;
  return new Lab2(o5.l, Math.cos(h) * o5.c, Math.sin(h) * o5.c, o5.opacity);
}
define_default2(Hcl2, hcl3, extend2(Color2, {
  brighter(k3) {
    return new Hcl2(this.h, this.c, this.l + K2 * (k3 == null ? 1 : k3), this.opacity);
  },
  darker(k3) {
    return new Hcl2(this.h, this.c, this.l - K2 * (k3 == null ? 1 : k3), this.opacity);
  },
  rgb() {
    return hcl2lab2(this).rgb();
  }
}));

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-color/src/cubehelix.js
var A3 = -0.14861;
var B2 = 1.78277;
var C4 = -0.29227;
var D2 = -0.90649;
var E3 = 1.97294;
var ED2 = E3 * D2;
var EB2 = E3 * B2;
var BC_DA2 = B2 * C4 - D2 * A3;
function cubehelixConvert2(o5) {
  if (o5 instanceof Cubehelix2) return new Cubehelix2(o5.h, o5.s, o5.l, o5.opacity);
  if (!(o5 instanceof Rgb2)) o5 = rgbConvert2(o5);
  var r5 = o5.r / 255, g3 = o5.g / 255, b3 = o5.b / 255, l4 = (BC_DA2 * b3 + ED2 * r5 - EB2 * g3) / (BC_DA2 + ED2 - EB2), bl = b3 - l4, k3 = (E3 * (g3 - l4) - C4 * bl) / D2, s3 = Math.sqrt(k3 * k3 + bl * bl) / (E3 * l4 * (1 - l4)), h = s3 ? Math.atan2(k3, bl) * degrees3 - 120 : NaN;
  return new Cubehelix2(h < 0 ? h + 360 : h, s3, l4, o5.opacity);
}
function cubehelix3(h, s3, l4, opacity) {
  return arguments.length === 1 ? cubehelixConvert2(h) : new Cubehelix2(h, s3, l4, opacity == null ? 1 : opacity);
}
function Cubehelix2(h, s3, l4, opacity) {
  this.h = +h;
  this.s = +s3;
  this.l = +l4;
  this.opacity = +opacity;
}
define_default2(Cubehelix2, cubehelix3, extend2(Color2, {
  brighter(k3) {
    k3 = k3 == null ? brighter2 : Math.pow(brighter2, k3);
    return new Cubehelix2(this.h, this.s, this.l * k3, this.opacity);
  },
  darker(k3) {
    k3 = k3 == null ? darker2 : Math.pow(darker2, k3);
    return new Cubehelix2(this.h, this.s, this.l * k3, this.opacity);
  },
  rgb() {
    var h = isNaN(this.h) ? 0 : (this.h + 120) * radians2, l4 = +this.l, a4 = isNaN(this.s) ? 0 : this.s * l4 * (1 - l4), cosh4 = Math.cos(h), sinh4 = Math.sin(h);
    return new Rgb2(
      255 * (l4 + a4 * (A3 * cosh4 + B2 * sinh4)),
      255 * (l4 + a4 * (C4 * cosh4 + D2 * sinh4)),
      255 * (l4 + a4 * (E3 * cosh4)),
      this.opacity
    );
  }
}));

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/basis.js
function basis2(t16, v0, v1, v22, v32) {
  var t23 = t16 * t16, t33 = t23 * t16;
  return ((1 - 3 * t16 + 3 * t23 - t33) * v0 + (4 - 6 * t23 + 3 * t33) * v1 + (1 + 3 * t16 + 3 * t23 - 3 * t33) * v22 + t33 * v32) / 6;
}
function basis_default3(values) {
  var n5 = values.length - 1;
  return function(t7) {
    var i5 = t7 <= 0 ? t7 = 0 : t7 >= 1 ? (t7 = 1, n5 - 1) : Math.floor(t7 * n5), v1 = values[i5], v22 = values[i5 + 1], v0 = i5 > 0 ? values[i5 - 1] : 2 * v1 - v22, v32 = i5 < n5 - 1 ? values[i5 + 2] : 2 * v22 - v1;
    return basis2((t7 - i5 / n5) * n5, v0, v1, v22, v32);
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/basisClosed.js
function basisClosed_default3(values) {
  var n5 = values.length;
  return function(t7) {
    var i5 = Math.floor(((t7 %= 1) < 0 ? ++t7 : t7) * n5), v0 = values[(i5 + n5 - 1) % n5], v1 = values[i5 % n5], v22 = values[(i5 + 1) % n5], v32 = values[(i5 + 2) % n5];
    return basis2((t7 - i5 / n5) * n5, v0, v1, v22, v32);
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/constant.js
var constant_default5 = (x4) => () => x4;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/color.js
function linear3(a4, d2) {
  return function(t7) {
    return a4 + t7 * d2;
  };
}
function exponential2(a4, b3, y6) {
  return a4 = Math.pow(a4, y6), b3 = Math.pow(b3, y6) - a4, y6 = 1 / y6, function(t7) {
    return Math.pow(a4 + t7 * b3, y6);
  };
}
function hue2(a4, b3) {
  var d2 = b3 - a4;
  return d2 ? linear3(a4, d2 > 180 || d2 < -180 ? d2 - 360 * Math.round(d2 / 360) : d2) : constant_default5(isNaN(a4) ? b3 : a4);
}
function gamma2(y6) {
  return (y6 = +y6) === 1 ? nogamma2 : function(a4, b3) {
    return b3 - a4 ? exponential2(a4, b3, y6) : constant_default5(isNaN(a4) ? b3 : a4);
  };
}
function nogamma2(a4, b3) {
  var d2 = b3 - a4;
  return d2 ? linear3(a4, d2) : constant_default5(isNaN(a4) ? b3 : a4);
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/rgb.js
var rgb_default2 = function rgbGamma2(y6) {
  var color3 = gamma2(y6);
  function rgb5(start3, end) {
    var r5 = color3((start3 = rgb3(start3)).r, (end = rgb3(end)).r), g3 = color3(start3.g, end.g), b3 = color3(start3.b, end.b), opacity = nogamma2(start3.opacity, end.opacity);
    return function(t7) {
      start3.r = r5(t7);
      start3.g = g3(t7);
      start3.b = b3(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  }
  rgb5.gamma = rgbGamma2;
  return rgb5;
}(1);
function rgbSpline2(spline) {
  return function(colors4) {
    var n5 = colors4.length, r5 = new Array(n5), g3 = new Array(n5), b3 = new Array(n5), i5, color3;
    for (i5 = 0; i5 < n5; ++i5) {
      color3 = rgb3(colors4[i5]);
      r5[i5] = color3.r || 0;
      g3[i5] = color3.g || 0;
      b3[i5] = color3.b || 0;
    }
    r5 = spline(r5);
    g3 = spline(g3);
    b3 = spline(b3);
    color3.opacity = 1;
    return function(t7) {
      color3.r = r5(t7);
      color3.g = g3(t7);
      color3.b = b3(t7);
      return color3 + "";
    };
  };
}
var rgbBasis2 = rgbSpline2(basis_default3);
var rgbBasisClosed2 = rgbSpline2(basisClosed_default3);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/number.js
function number_default3(a4, b3) {
  return a4 = +a4, b3 = +b3, function(t7) {
    return a4 * (1 - t7) + b3 * t7;
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/string.js
var reA2 = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var reB2 = new RegExp(reA2.source, "g");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/transform/decompose.js
var degrees4 = 180 / Math.PI;
var identity5 = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function decompose_default2(a4, b3, c10, d2, e7, f3) {
  var scaleX, scaleY, skewX;
  if (scaleX = Math.sqrt(a4 * a4 + b3 * b3)) a4 /= scaleX, b3 /= scaleX;
  if (skewX = a4 * c10 + b3 * d2) c10 -= a4 * skewX, d2 -= b3 * skewX;
  if (scaleY = Math.sqrt(c10 * c10 + d2 * d2)) c10 /= scaleY, d2 /= scaleY, skewX /= scaleY;
  if (a4 * d2 < b3 * c10) a4 = -a4, b3 = -b3, skewX = -skewX, scaleX = -scaleX;
  return {
    translateX: e7,
    translateY: f3,
    rotate: Math.atan2(b3, a4) * degrees4,
    skewX: Math.atan(skewX) * degrees4,
    scaleX,
    scaleY
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/transform/parse.js
var svgNode2;
function parseCss2(value) {
  const m5 = new (typeof DOMMatrix === "function" ? DOMMatrix : WebKitCSSMatrix)(value + "");
  return m5.isIdentity ? identity5 : decompose_default2(m5.a, m5.b, m5.c, m5.d, m5.e, m5.f);
}
function parseSvg2(value) {
  if (value == null) return identity5;
  if (!svgNode2) svgNode2 = document.createElementNS("http://www.w3.org/2000/svg", "g");
  svgNode2.setAttribute("transform", value);
  if (!(value = svgNode2.transform.baseVal.consolidate())) return identity5;
  value = value.matrix;
  return decompose_default2(value.a, value.b, value.c, value.d, value.e, value.f);
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/transform/index.js
function interpolateTransform2(parse, pxComma, pxParen, degParen) {
  function pop(s3) {
    return s3.length ? s3.pop() + " " : "";
  }
  function translate(xa, ya, xb, yb, s3, q3) {
    if (xa !== xb || ya !== yb) {
      var i5 = s3.push("translate(", null, pxComma, null, pxParen);
      q3.push({ i: i5 - 4, x: number_default3(xa, xb) }, { i: i5 - 2, x: number_default3(ya, yb) });
    } else if (xb || yb) {
      s3.push("translate(" + xb + pxComma + yb + pxParen);
    }
  }
  function rotate(a4, b3, s3, q3) {
    if (a4 !== b3) {
      if (a4 - b3 > 180) b3 += 360;
      else if (b3 - a4 > 180) a4 += 360;
      q3.push({ i: s3.push(pop(s3) + "rotate(", null, degParen) - 2, x: number_default3(a4, b3) });
    } else if (b3) {
      s3.push(pop(s3) + "rotate(" + b3 + degParen);
    }
  }
  function skewX(a4, b3, s3, q3) {
    if (a4 !== b3) {
      q3.push({ i: s3.push(pop(s3) + "skewX(", null, degParen) - 2, x: number_default3(a4, b3) });
    } else if (b3) {
      s3.push(pop(s3) + "skewX(" + b3 + degParen);
    }
  }
  function scale(xa, ya, xb, yb, s3, q3) {
    if (xa !== xb || ya !== yb) {
      var i5 = s3.push(pop(s3) + "scale(", null, ",", null, ")");
      q3.push({ i: i5 - 4, x: number_default3(xa, xb) }, { i: i5 - 2, x: number_default3(ya, yb) });
    } else if (xb !== 1 || yb !== 1) {
      s3.push(pop(s3) + "scale(" + xb + "," + yb + ")");
    }
  }
  return function(a4, b3) {
    var s3 = [], q3 = [];
    a4 = parse(a4), b3 = parse(b3);
    translate(a4.translateX, a4.translateY, b3.translateX, b3.translateY, s3, q3);
    rotate(a4.rotate, b3.rotate, s3, q3);
    skewX(a4.skewX, b3.skewX, s3, q3);
    scale(a4.scaleX, a4.scaleY, b3.scaleX, b3.scaleY, s3, q3);
    a4 = b3 = null;
    return function(t7) {
      var i5 = -1, n5 = q3.length, o5;
      while (++i5 < n5) s3[(o5 = q3[i5]).i] = o5.x(t7);
      return s3.join("");
    };
  };
}
var interpolateTransformCss2 = interpolateTransform2(parseCss2, "px, ", "px)", "deg)");
var interpolateTransformSvg2 = interpolateTransform2(parseSvg2, ", ", ")", ")");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/zoom.js
var epsilon22 = 1e-12;
function cosh2(x4) {
  return ((x4 = Math.exp(x4)) + 1 / x4) / 2;
}
function sinh2(x4) {
  return ((x4 = Math.exp(x4)) - 1 / x4) / 2;
}
function tanh2(x4) {
  return ((x4 = Math.exp(2 * x4)) - 1) / (x4 + 1);
}
var zoom_default2 = function zoomRho2(rho, rho2, rho4) {
  function zoom(p0, p1) {
    var ux0 = p0[0], uy0 = p0[1], w0 = p0[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx = ux1 - ux0, dy = uy1 - uy0, d2 = dx * dx + dy * dy, i5, S;
    if (d2 < epsilon22) {
      S = Math.log(w1 / w0) / rho;
      i5 = function(t7) {
        return [
          ux0 + t7 * dx,
          uy0 + t7 * dy,
          w0 * Math.exp(rho * t7 * S)
        ];
      };
    } else {
      var d1 = Math.sqrt(d2), b0 = (w1 * w1 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1), b1 = (w1 * w1 - w0 * w0 - rho4 * d2) / (2 * w1 * rho2 * d1), r0 = Math.log(Math.sqrt(b0 * b0 + 1) - b0), r1 = Math.log(Math.sqrt(b1 * b1 + 1) - b1);
      S = (r1 - r0) / rho;
      i5 = function(t7) {
        var s3 = t7 * S, coshr0 = cosh2(r0), u2 = w0 / (rho2 * d1) * (coshr0 * tanh2(rho * s3 + r0) - sinh2(r0));
        return [
          ux0 + u2 * dx,
          uy0 + u2 * dy,
          w0 * coshr0 / cosh2(rho * s3 + r0)
        ];
      };
    }
    i5.duration = S * 1e3 * rho / Math.SQRT2;
    return i5;
  }
  zoom.rho = function(_2) {
    var _1 = Math.max(1e-3, +_2), _22 = _1 * _1, _4 = _22 * _22;
    return zoomRho2(_1, _22, _4);
  };
  return zoom;
}(Math.SQRT2, 2, 4);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/hsl.js
function hsl5(hue4) {
  return function(start3, end) {
    var h = hue4((start3 = hsl4(start3)).h, (end = hsl4(end)).h), s3 = nogamma2(start3.s, end.s), l4 = nogamma2(start3.l, end.l), opacity = nogamma2(start3.opacity, end.opacity);
    return function(t7) {
      start3.h = h(t7);
      start3.s = s3(t7);
      start3.l = l4(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  };
}
var hsl_default2 = hsl5(hue2);
var hslLong2 = hsl5(nogamma2);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/hcl.js
function hcl4(hue4) {
  return function(start3, end) {
    var h = hue4((start3 = hcl3(start3)).h, (end = hcl3(end)).h), c10 = nogamma2(start3.c, end.c), l4 = nogamma2(start3.l, end.l), opacity = nogamma2(start3.opacity, end.opacity);
    return function(t7) {
      start3.h = h(t7);
      start3.c = c10(t7);
      start3.l = l4(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  };
}
var hcl_default2 = hcl4(hue2);
var hclLong2 = hcl4(nogamma2);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/node_modules/d3-interpolate/src/cubehelix.js
function cubehelix4(hue4) {
  return function cubehelixGamma(y6) {
    y6 = +y6;
    function cubehelix6(start3, end) {
      var h = hue4((start3 = cubehelix3(start3)).h, (end = cubehelix3(end)).h), s3 = nogamma2(start3.s, end.s), l4 = nogamma2(start3.l, end.l), opacity = nogamma2(start3.opacity, end.opacity);
      return function(t7) {
        start3.h = h(t7);
        start3.s = s3(t7);
        start3.l = l4(Math.pow(t7, y6));
        start3.opacity = opacity(t7);
        return start3 + "";
      };
    }
    cubehelix6.gamma = cubehelixGamma;
    return cubehelix6;
  }(1);
}
var cubehelix_default3 = cubehelix4(hue2);
var cubehelixLong2 = cubehelix4(nogamma2);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/ramp.js
var ramp_default2 = (scheme55) => rgbBasis2(scheme55[scheme55.length - 1]);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/BrBG.js
var scheme28 = new Array(3).concat(
  "d8b365f5f5f55ab4ac",
  "a6611adfc27d80cdc1018571",
  "a6611adfc27df5f5f580cdc1018571",
  "8c510ad8b365f6e8c3c7eae55ab4ac01665e",
  "8c510ad8b365f6e8c3f5f5f5c7eae55ab4ac01665e",
  "8c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e",
  "8c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e",
  "5430058c510abf812ddfc27df6e8c3c7eae580cdc135978f01665e003c30",
  "5430058c510abf812ddfc27df6e8c3f5f5f5c7eae580cdc135978f01665e003c30"
).map(colors_default2);
var BrBG_default2 = ramp_default2(scheme28);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/PRGn.js
var scheme29 = new Array(3).concat(
  "af8dc3f7f7f77fbf7b",
  "7b3294c2a5cfa6dba0008837",
  "7b3294c2a5cff7f7f7a6dba0008837",
  "762a83af8dc3e7d4e8d9f0d37fbf7b1b7837",
  "762a83af8dc3e7d4e8f7f7f7d9f0d37fbf7b1b7837",
  "762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b7837",
  "762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b7837",
  "40004b762a839970abc2a5cfe7d4e8d9f0d3a6dba05aae611b783700441b",
  "40004b762a839970abc2a5cfe7d4e8f7f7f7d9f0d3a6dba05aae611b783700441b"
).map(colors_default2);
var PRGn_default2 = ramp_default2(scheme29);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/PiYG.js
var scheme30 = new Array(3).concat(
  "e9a3c9f7f7f7a1d76a",
  "d01c8bf1b6dab8e1864dac26",
  "d01c8bf1b6daf7f7f7b8e1864dac26",
  "c51b7de9a3c9fde0efe6f5d0a1d76a4d9221",
  "c51b7de9a3c9fde0eff7f7f7e6f5d0a1d76a4d9221",
  "c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221",
  "c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221",
  "8e0152c51b7dde77aef1b6dafde0efe6f5d0b8e1867fbc414d9221276419",
  "8e0152c51b7dde77aef1b6dafde0eff7f7f7e6f5d0b8e1867fbc414d9221276419"
).map(colors_default2);
var PiYG_default2 = ramp_default2(scheme30);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/PuOr.js
var scheme31 = new Array(3).concat(
  "998ec3f7f7f7f1a340",
  "5e3c99b2abd2fdb863e66101",
  "5e3c99b2abd2f7f7f7fdb863e66101",
  "542788998ec3d8daebfee0b6f1a340b35806",
  "542788998ec3d8daebf7f7f7fee0b6f1a340b35806",
  "5427888073acb2abd2d8daebfee0b6fdb863e08214b35806",
  "5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b35806",
  "2d004b5427888073acb2abd2d8daebfee0b6fdb863e08214b358067f3b08",
  "2d004b5427888073acb2abd2d8daebf7f7f7fee0b6fdb863e08214b358067f3b08"
).map(colors_default2);
var PuOr_default2 = ramp_default2(scheme31);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/RdBu.js
var scheme32 = new Array(3).concat(
  "ef8a62f7f7f767a9cf",
  "ca0020f4a58292c5de0571b0",
  "ca0020f4a582f7f7f792c5de0571b0",
  "b2182bef8a62fddbc7d1e5f067a9cf2166ac",
  "b2182bef8a62fddbc7f7f7f7d1e5f067a9cf2166ac",
  "b2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac",
  "b2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac",
  "67001fb2182bd6604df4a582fddbc7d1e5f092c5de4393c32166ac053061",
  "67001fb2182bd6604df4a582fddbc7f7f7f7d1e5f092c5de4393c32166ac053061"
).map(colors_default2);
var RdBu_default2 = ramp_default2(scheme32);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/RdGy.js
var scheme33 = new Array(3).concat(
  "ef8a62ffffff999999",
  "ca0020f4a582bababa404040",
  "ca0020f4a582ffffffbababa404040",
  "b2182bef8a62fddbc7e0e0e09999994d4d4d",
  "b2182bef8a62fddbc7ffffffe0e0e09999994d4d4d",
  "b2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d",
  "b2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d",
  "67001fb2182bd6604df4a582fddbc7e0e0e0bababa8787874d4d4d1a1a1a",
  "67001fb2182bd6604df4a582fddbc7ffffffe0e0e0bababa8787874d4d4d1a1a1a"
).map(colors_default2);
var RdGy_default2 = ramp_default2(scheme33);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/RdYlBu.js
var scheme34 = new Array(3).concat(
  "fc8d59ffffbf91bfdb",
  "d7191cfdae61abd9e92c7bb6",
  "d7191cfdae61ffffbfabd9e92c7bb6",
  "d73027fc8d59fee090e0f3f891bfdb4575b4",
  "d73027fc8d59fee090ffffbfe0f3f891bfdb4575b4",
  "d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4",
  "d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4",
  "a50026d73027f46d43fdae61fee090e0f3f8abd9e974add14575b4313695",
  "a50026d73027f46d43fdae61fee090ffffbfe0f3f8abd9e974add14575b4313695"
).map(colors_default2);
var RdYlBu_default2 = ramp_default2(scheme34);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/RdYlGn.js
var scheme35 = new Array(3).concat(
  "fc8d59ffffbf91cf60",
  "d7191cfdae61a6d96a1a9641",
  "d7191cfdae61ffffbfa6d96a1a9641",
  "d73027fc8d59fee08bd9ef8b91cf601a9850",
  "d73027fc8d59fee08bffffbfd9ef8b91cf601a9850",
  "d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850",
  "d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850",
  "a50026d73027f46d43fdae61fee08bd9ef8ba6d96a66bd631a9850006837",
  "a50026d73027f46d43fdae61fee08bffffbfd9ef8ba6d96a66bd631a9850006837"
).map(colors_default2);
var RdYlGn_default2 = ramp_default2(scheme35);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/diverging/Spectral.js
var scheme36 = new Array(3).concat(
  "fc8d59ffffbf99d594",
  "d7191cfdae61abdda42b83ba",
  "d7191cfdae61ffffbfabdda42b83ba",
  "d53e4ffc8d59fee08be6f59899d5943288bd",
  "d53e4ffc8d59fee08bffffbfe6f59899d5943288bd",
  "d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd",
  "d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd",
  "9e0142d53e4ff46d43fdae61fee08be6f598abdda466c2a53288bd5e4fa2",
  "9e0142d53e4ff46d43fdae61fee08bffffbfe6f598abdda466c2a53288bd5e4fa2"
).map(colors_default2);
var Spectral_default2 = ramp_default2(scheme36);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/BuGn.js
var scheme37 = new Array(3).concat(
  "e5f5f999d8c92ca25f",
  "edf8fbb2e2e266c2a4238b45",
  "edf8fbb2e2e266c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a42ca25f006d2c",
  "edf8fbccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45005824",
  "f7fcfde5f5f9ccece699d8c966c2a441ae76238b45006d2c00441b"
).map(colors_default2);
var BuGn_default2 = ramp_default2(scheme37);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/BuPu.js
var scheme38 = new Array(3).concat(
  "e0ecf49ebcda8856a7",
  "edf8fbb3cde38c96c688419d",
  "edf8fbb3cde38c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68856a7810f7c",
  "edf8fbbfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d6e016b",
  "f7fcfde0ecf4bfd3e69ebcda8c96c68c6bb188419d810f7c4d004b"
).map(colors_default2);
var BuPu_default2 = ramp_default2(scheme38);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/GnBu.js
var scheme39 = new Array(3).concat(
  "e0f3dba8ddb543a2ca",
  "f0f9e8bae4bc7bccc42b8cbe",
  "f0f9e8bae4bc7bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc443a2ca0868ac",
  "f0f9e8ccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe08589e",
  "f7fcf0e0f3dbccebc5a8ddb57bccc44eb3d32b8cbe0868ac084081"
).map(colors_default2);
var GnBu_default2 = ramp_default2(scheme39);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/OrRd.js
var scheme40 = new Array(3).concat(
  "fee8c8fdbb84e34a33",
  "fef0d9fdcc8afc8d59d7301f",
  "fef0d9fdcc8afc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59e34a33b30000",
  "fef0d9fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301f990000",
  "fff7ecfee8c8fdd49efdbb84fc8d59ef6548d7301fb300007f0000"
).map(colors_default2);
var OrRd_default2 = ramp_default2(scheme40);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/PuBuGn.js
var scheme41 = new Array(3).concat(
  "ece2f0a6bddb1c9099",
  "f6eff7bdc9e167a9cf02818a",
  "f6eff7bdc9e167a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf1c9099016c59",
  "f6eff7d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016450",
  "fff7fbece2f0d0d1e6a6bddb67a9cf3690c002818a016c59014636"
).map(colors_default2);
var PuBuGn_default2 = ramp_default2(scheme41);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/PuBu.js
var scheme42 = new Array(3).concat(
  "ece7f2a6bddb2b8cbe",
  "f1eef6bdc9e174a9cf0570b0",
  "f1eef6bdc9e174a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf2b8cbe045a8d",
  "f1eef6d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0034e7b",
  "fff7fbece7f2d0d1e6a6bddb74a9cf3690c00570b0045a8d023858"
).map(colors_default2);
var PuBu_default2 = ramp_default2(scheme42);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/PuRd.js
var scheme43 = new Array(3).concat(
  "e7e1efc994c7dd1c77",
  "f1eef6d7b5d8df65b0ce1256",
  "f1eef6d7b5d8df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0dd1c77980043",
  "f1eef6d4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125691003f",
  "f7f4f9e7e1efd4b9dac994c7df65b0e7298ace125698004367001f"
).map(colors_default2);
var PuRd_default2 = ramp_default2(scheme43);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/RdPu.js
var scheme44 = new Array(3).concat(
  "fde0ddfa9fb5c51b8a",
  "feebe2fbb4b9f768a1ae017e",
  "feebe2fbb4b9f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1c51b8a7a0177",
  "feebe2fcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a0177",
  "fff7f3fde0ddfcc5c0fa9fb5f768a1dd3497ae017e7a017749006a"
).map(colors_default2);
var RdPu_default2 = ramp_default2(scheme44);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/YlGnBu.js
var scheme45 = new Array(3).concat(
  "edf8b17fcdbb2c7fb8",
  "ffffcca1dab441b6c4225ea8",
  "ffffcca1dab441b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c42c7fb8253494",
  "ffffccc7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea80c2c84",
  "ffffd9edf8b1c7e9b47fcdbb41b6c41d91c0225ea8253494081d58"
).map(colors_default2);
var YlGnBu_default2 = ramp_default2(scheme45);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/YlGn.js
var scheme46 = new Array(3).concat(
  "f7fcb9addd8e31a354",
  "ffffccc2e69978c679238443",
  "ffffccc2e69978c67931a354006837",
  "ffffccd9f0a3addd8e78c67931a354006837",
  "ffffccd9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443005a32",
  "ffffe5f7fcb9d9f0a3addd8e78c67941ab5d238443006837004529"
).map(colors_default2);
var YlGn_default2 = ramp_default2(scheme46);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/YlOrBr.js
var scheme47 = new Array(3).concat(
  "fff7bcfec44fd95f0e",
  "ffffd4fed98efe9929cc4c02",
  "ffffd4fed98efe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929d95f0e993404",
  "ffffd4fee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c028c2d04",
  "ffffe5fff7bcfee391fec44ffe9929ec7014cc4c02993404662506"
).map(colors_default2);
var YlOrBr_default2 = ramp_default2(scheme47);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/YlOrRd.js
var scheme48 = new Array(3).concat(
  "ffeda0feb24cf03b20",
  "ffffb2fecc5cfd8d3ce31a1c",
  "ffffb2fecc5cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cf03b20bd0026",
  "ffffb2fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cb10026",
  "ffffccffeda0fed976feb24cfd8d3cfc4e2ae31a1cbd0026800026"
).map(colors_default2);
var YlOrRd_default2 = ramp_default2(scheme48);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-single/Blues.js
var scheme49 = new Array(3).concat(
  "deebf79ecae13182bd",
  "eff3ffbdd7e76baed62171b5",
  "eff3ffbdd7e76baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed63182bd08519c",
  "eff3ffc6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b5084594",
  "f7fbffdeebf7c6dbef9ecae16baed64292c62171b508519c08306b"
).map(colors_default2);
var Blues_default2 = ramp_default2(scheme49);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-single/Greens.js
var scheme50 = new Array(3).concat(
  "e5f5e0a1d99b31a354",
  "edf8e9bae4b374c476238b45",
  "edf8e9bae4b374c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47631a354006d2c",
  "edf8e9c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45005a32",
  "f7fcf5e5f5e0c7e9c0a1d99b74c47641ab5d238b45006d2c00441b"
).map(colors_default2);
var Greens_default2 = ramp_default2(scheme50);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-single/Greys.js
var scheme51 = new Array(3).concat(
  "f0f0f0bdbdbd636363",
  "f7f7f7cccccc969696525252",
  "f7f7f7cccccc969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696636363252525",
  "f7f7f7d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525",
  "fffffff0f0f0d9d9d9bdbdbd969696737373525252252525000000"
).map(colors_default2);
var Greys_default2 = ramp_default2(scheme51);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-single/Purples.js
var scheme52 = new Array(3).concat(
  "efedf5bcbddc756bb1",
  "f2f0f7cbc9e29e9ac86a51a3",
  "f2f0f7cbc9e29e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8756bb154278f",
  "f2f0f7dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a34a1486",
  "fcfbfdefedf5dadaebbcbddc9e9ac8807dba6a51a354278f3f007d"
).map(colors_default2);
var Purples_default2 = ramp_default2(scheme52);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-single/Reds.js
var scheme53 = new Array(3).concat(
  "fee0d2fc9272de2d26",
  "fee5d9fcae91fb6a4acb181d",
  "fee5d9fcae91fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4ade2d26a50f15",
  "fee5d9fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181d99000d",
  "fff5f0fee0d2fcbba1fc9272fb6a4aef3b2ccb181da50f1567000d"
).map(colors_default2);
var Reds_default2 = ramp_default2(scheme53);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-single/Oranges.js
var scheme54 = new Array(3).concat(
  "fee6cefdae6be6550d",
  "feeddefdbe85fd8d3cd94701",
  "feeddefdbe85fd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3ce6550da63603",
  "feeddefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d948018c2d04",
  "fff5ebfee6cefdd0a2fdae6bfd8d3cf16913d94801a636037f2704"
).map(colors_default2);
var Oranges_default2 = ramp_default2(scheme54);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/cividis.js
function cividis_default2(t7) {
  t7 = Math.max(0, Math.min(1, t7));
  return "rgb(" + Math.max(0, Math.min(255, Math.round(-4.54 - t7 * (35.34 - t7 * (2381.73 - t7 * (6402.7 - t7 * (7024.72 - t7 * 2710.57))))))) + ", " + Math.max(0, Math.min(255, Math.round(32.49 + t7 * (170.73 + t7 * (52.82 - t7 * (131.46 - t7 * (176.58 - t7 * 67.37))))))) + ", " + Math.max(0, Math.min(255, Math.round(81.24 + t7 * (442.36 - t7 * (2482.43 - t7 * (6167.24 - t7 * (6614.94 - t7 * 2475.67))))))) + ")";
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/cubehelix.js
var cubehelix_default4 = cubehelixLong2(cubehelix3(300, 0.5, 0), cubehelix3(-240, 0.5, 1));

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/rainbow.js
var warm2 = cubehelixLong2(cubehelix3(-100, 0.75, 0.35), cubehelix3(80, 1.5, 0.8));
var cool2 = cubehelixLong2(cubehelix3(260, 0.75, 0.35), cubehelix3(80, 1.5, 0.8));
var c8 = cubehelix3();
function rainbow_default2(t7) {
  if (t7 < 0 || t7 > 1) t7 -= Math.floor(t7);
  var ts3 = Math.abs(t7 - 0.5);
  c8.h = 360 * t7 - 100;
  c8.s = 1.5 - 1.5 * ts3;
  c8.l = 0.8 - 0.9 * ts3;
  return c8 + "";
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/sinebow.js
var c9 = rgb3();
var pi_1_32 = Math.PI / 3;
var pi_2_32 = Math.PI * 2 / 3;
function sinebow_default2(t7) {
  var x4;
  t7 = (0.5 - t7) * Math.PI;
  c9.r = 255 * (x4 = Math.sin(t7)) * x4;
  c9.g = 255 * (x4 = Math.sin(t7 + pi_1_32)) * x4;
  c9.b = 255 * (x4 = Math.sin(t7 + pi_2_32)) * x4;
  return c9 + "";
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/turbo.js
function turbo_default2(t7) {
  t7 = Math.max(0, Math.min(1, t7));
  return "rgb(" + Math.max(0, Math.min(255, Math.round(34.61 + t7 * (1172.33 - t7 * (10793.56 - t7 * (33300.12 - t7 * (38394.49 - t7 * 14825.05))))))) + ", " + Math.max(0, Math.min(255, Math.round(23.31 + t7 * (557.33 + t7 * (1225.33 - t7 * (3574.96 - t7 * (1073.77 + t7 * 707.56))))))) + ", " + Math.max(0, Math.min(255, Math.round(27.2 + t7 * (3211.1 - t7 * (15327.97 - t7 * (27814 - t7 * (22569.18 - t7 * 6838.66))))))) + ")";
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale-chromatic/src/sequential-multi/viridis.js
function ramp2(range3) {
  var n5 = range3.length;
  return function(t7) {
    return range3[Math.max(0, Math.min(n5 - 1, Math.floor(t7 * n5)))];
  };
}
var viridis_default2 = ramp2(colors_default2(""));
var magma2 = ramp2(colors_default2(""));
var inferno2 = ramp2(colors_default2(""));
var plasma2 = ramp2(colors_default2(""));

// node_modules/@nivo/treemap/node_modules/@nivo/colors/dist/nivo-colors.es.js
var import_react17 = __toESM(require_react());
var import_get2 = __toESM(require_get2());
var import_isPlainObject2 = __toESM(require_isPlainObject2());
var import_prop_types2 = __toESM(require_prop_types2());

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/ascending.js
function ascending2(a4, b3) {
  return a4 == null || b3 == null ? NaN : a4 < b3 ? -1 : a4 > b3 ? 1 : a4 >= b3 ? 0 : NaN;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/descending.js
function descending2(a4, b3) {
  return a4 == null || b3 == null ? NaN : b3 < a4 ? -1 : b3 > a4 ? 1 : b3 >= a4 ? 0 : NaN;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/bisector.js
function bisector2(f3) {
  let compare1, compare2, delta;
  if (f3.length !== 2) {
    compare1 = ascending2;
    compare2 = (d2, x4) => ascending2(f3(d2), x4);
    delta = (d2, x4) => f3(d2) - x4;
  } else {
    compare1 = f3 === ascending2 || f3 === descending2 ? f3 : zero2;
    compare2 = f3;
    delta = f3;
  }
  function left(a4, x4, lo = 0, hi = a4.length) {
    if (lo < hi) {
      if (compare1(x4, x4) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a4[mid], x4) < 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function right(a4, x4, lo = 0, hi = a4.length) {
    if (lo < hi) {
      if (compare1(x4, x4) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a4[mid], x4) <= 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function center(a4, x4, lo = 0, hi = a4.length) {
    const i5 = left(a4, x4, lo, hi - 1);
    return i5 > lo && delta(a4[i5 - 1], x4) > -delta(a4[i5], x4) ? i5 - 1 : i5;
  }
  return { left, center, right };
}
function zero2() {
  return 0;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/number.js
function number3(x4) {
  return x4 === null ? NaN : +x4;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/bisect.js
var ascendingBisect3 = bisector2(ascending2);
var bisectRight3 = ascendingBisect3.right;
var bisectLeft3 = ascendingBisect3.left;
var bisectCenter3 = bisector2(number3).center;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/blur.js
var blur22 = Blur22(blurf2);
var blurImage2 = Blur22(blurfImage2);
function Blur22(blur4) {
  return function(data, rx, ry = rx) {
    if (!((rx = +rx) >= 0)) throw new RangeError("invalid rx");
    if (!((ry = +ry) >= 0)) throw new RangeError("invalid ry");
    let { data: values, width, height } = data;
    if (!((width = Math.floor(width)) >= 0)) throw new RangeError("invalid width");
    if (!((height = Math.floor(height !== void 0 ? height : values.length / width)) >= 0)) throw new RangeError("invalid height");
    if (!width || !height || !rx && !ry) return data;
    const blurx = rx && blur4(rx);
    const blury = ry && blur4(ry);
    const temp = values.slice();
    if (blurx && blury) {
      blurh2(blurx, temp, values, width, height);
      blurh2(blurx, values, temp, width, height);
      blurh2(blurx, temp, values, width, height);
      blurv2(blury, values, temp, width, height);
      blurv2(blury, temp, values, width, height);
      blurv2(blury, values, temp, width, height);
    } else if (blurx) {
      blurh2(blurx, values, temp, width, height);
      blurh2(blurx, temp, values, width, height);
      blurh2(blurx, values, temp, width, height);
    } else if (blury) {
      blurv2(blury, values, temp, width, height);
      blurv2(blury, temp, values, width, height);
      blurv2(blury, values, temp, width, height);
    }
    return data;
  };
}
function blurh2(blur4, T3, S, w3, h) {
  for (let y6 = 0, n5 = w3 * h; y6 < n5; ) {
    blur4(T3, S, y6, y6 += w3, 1);
  }
}
function blurv2(blur4, T3, S, w3, h) {
  for (let x4 = 0, n5 = w3 * h; x4 < w3; ++x4) {
    blur4(T3, S, x4, x4 + n5, w3);
  }
}
function blurfImage2(radius) {
  const blur4 = blurf2(radius);
  return (T3, S, start3, stop3, step) => {
    start3 <<= 2, stop3 <<= 2, step <<= 2;
    blur4(T3, S, start3 + 0, stop3 + 0, step);
    blur4(T3, S, start3 + 1, stop3 + 1, step);
    blur4(T3, S, start3 + 2, stop3 + 2, step);
    blur4(T3, S, start3 + 3, stop3 + 3, step);
  };
}
function blurf2(radius) {
  const radius0 = Math.floor(radius);
  if (radius0 === radius) return bluri2(radius);
  const t7 = radius - radius0;
  const w3 = 2 * radius + 1;
  return (T3, S, start3, stop3, step) => {
    if (!((stop3 -= step) >= start3)) return;
    let sum5 = radius0 * S[start3];
    const s0 = step * radius0;
    const s1 = s0 + step;
    for (let i5 = start3, j4 = start3 + s0; i5 < j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5)];
    }
    for (let i5 = start3, j4 = stop3; i5 <= j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5 + s0)];
      T3[i5] = (sum5 + t7 * (S[Math.max(start3, i5 - s1)] + S[Math.min(stop3, i5 + s1)])) / w3;
      sum5 -= S[Math.max(start3, i5 - s0)];
    }
  };
}
function bluri2(radius) {
  const w3 = 2 * radius + 1;
  return (T3, S, start3, stop3, step) => {
    if (!((stop3 -= step) >= start3)) return;
    let sum5 = radius * S[start3];
    const s3 = step * radius;
    for (let i5 = start3, j4 = start3 + s3; i5 < j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5)];
    }
    for (let i5 = start3, j4 = stop3; i5 <= j4; i5 += step) {
      sum5 += S[Math.min(stop3, i5 + s3)];
      T3[i5] = sum5 / w3;
      sum5 -= S[Math.max(start3, i5 - s3)];
    }
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/node_modules/internmap/src/index.js
var InternMap3 = class extends Map {
  constructor(entries, key = keyof2) {
    super();
    Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: key } });
    if (entries != null) for (const [key2, value] of entries) this.set(key2, value);
  }
  get(key) {
    return super.get(intern_get2(this, key));
  }
  has(key) {
    return super.has(intern_get2(this, key));
  }
  set(key, value) {
    return super.set(intern_set2(this, key), value);
  }
  delete(key) {
    return super.delete(intern_delete2(this, key));
  }
};
function intern_get2({ _intern, _key }, value) {
  const key = _key(value);
  return _intern.has(key) ? _intern.get(key) : value;
}
function intern_set2({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) return _intern.get(key);
  _intern.set(key, value);
  return value;
}
function intern_delete2({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) {
    value = _intern.get(key);
    _intern.delete(key);
  }
  return value;
}
function keyof2(value) {
  return value !== null && typeof value === "object" ? value.valueOf() : value;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/array.js
var array3 = Array.prototype;
var slice5 = array3.slice;
var map6 = array3.map;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/ticks.js
var e103 = Math.sqrt(50);
var e53 = Math.sqrt(10);
var e23 = Math.sqrt(2);
function tickSpec2(start3, stop3, count5) {
  const step = (stop3 - start3) / Math.max(0, count5), power = Math.floor(Math.log10(step)), error = step / Math.pow(10, power), factor = error >= e103 ? 10 : error >= e53 ? 5 : error >= e23 ? 2 : 1;
  let i1, i22, inc;
  if (power < 0) {
    inc = Math.pow(10, -power) / factor;
    i1 = Math.round(start3 * inc);
    i22 = Math.round(stop3 * inc);
    if (i1 / inc < start3) ++i1;
    if (i22 / inc > stop3) --i22;
    inc = -inc;
  } else {
    inc = Math.pow(10, power) * factor;
    i1 = Math.round(start3 / inc);
    i22 = Math.round(stop3 / inc);
    if (i1 * inc < start3) ++i1;
    if (i22 * inc > stop3) --i22;
  }
  if (i22 < i1 && 0.5 <= count5 && count5 < 2) return tickSpec2(start3, stop3, count5 * 2);
  return [i1, i22, inc];
}
function tickIncrement3(start3, stop3, count5) {
  stop3 = +stop3, start3 = +start3, count5 = +count5;
  return tickSpec2(start3, stop3, count5)[2];
}
function tickStep3(start3, stop3, count5) {
  stop3 = +stop3, start3 = +start3, count5 = +count5;
  const reverse4 = stop3 < start3, inc = reverse4 ? tickIncrement3(stop3, start3, count5) : tickIncrement3(start3, stop3, count5);
  return (reverse4 ? -1 : 1) * (inc < 0 ? 1 / -inc : inc);
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-array/src/shuffle.js
var shuffle_default3 = shuffler3(Math.random);
function shuffler3(random) {
  return function shuffle2(array4, i0 = 0, i1 = array4.length) {
    let m5 = i1 - (i0 = +i0);
    while (m5) {
      const i5 = random() * m5-- | 0, t7 = array4[m5 + i0];
      array4[m5 + i0] = array4[i5 + i0];
      array4[i5 + i0] = t7;
    }
    return array4;
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/src/init.js
function initRange2(domain, range3) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(domain);
      break;
    default:
      this.range(range3).domain(domain);
      break;
  }
  return this;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/src/ordinal.js
var implicit2 = Symbol("implicit");
function ordinal2() {
  var index4 = new InternMap3(), domain = [], range3 = [], unknown = implicit2;
  function scale(d2) {
    let i5 = index4.get(d2);
    if (i5 === void 0) {
      if (unknown !== implicit2) return unknown;
      index4.set(d2, i5 = domain.push(d2) - 1);
    }
    return range3[i5 % range3.length];
  }
  scale.domain = function(_2) {
    if (!arguments.length) return domain.slice();
    domain = [], index4 = new InternMap3();
    for (const value of _2) {
      if (index4.has(value)) continue;
      index4.set(value, domain.push(value) - 1);
    }
    return scale;
  };
  scale.range = function(_2) {
    return arguments.length ? (range3 = Array.from(_2), scale) : range3.slice();
  };
  scale.unknown = function(_2) {
    return arguments.length ? (unknown = _2, scale) : unknown;
  };
  scale.copy = function() {
    return ordinal2(domain, range3).unknown(unknown);
  };
  initRange2.apply(scale, arguments);
  return scale;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/basis.js
function basis3(t16, v0, v1, v22, v32) {
  var t23 = t16 * t16, t33 = t23 * t16;
  return ((1 - 3 * t16 + 3 * t23 - t33) * v0 + (4 - 6 * t23 + 3 * t33) * v1 + (1 + 3 * t16 + 3 * t23 - 3 * t33) * v22 + t33 * v32) / 6;
}
function basis_default4(values) {
  var n5 = values.length - 1;
  return function(t7) {
    var i5 = t7 <= 0 ? t7 = 0 : t7 >= 1 ? (t7 = 1, n5 - 1) : Math.floor(t7 * n5), v1 = values[i5], v22 = values[i5 + 1], v0 = i5 > 0 ? values[i5 - 1] : 2 * v1 - v22, v32 = i5 < n5 - 1 ? values[i5 + 2] : 2 * v22 - v1;
    return basis3((t7 - i5 / n5) * n5, v0, v1, v22, v32);
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/basisClosed.js
function basisClosed_default4(values) {
  var n5 = values.length;
  return function(t7) {
    var i5 = Math.floor(((t7 %= 1) < 0 ? ++t7 : t7) * n5), v0 = values[(i5 + n5 - 1) % n5], v1 = values[i5 % n5], v22 = values[(i5 + 1) % n5], v32 = values[(i5 + 2) % n5];
    return basis3((t7 - i5 / n5) * n5, v0, v1, v22, v32);
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/constant.js
var constant_default6 = (x4) => () => x4;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/color.js
function linear4(a4, d2) {
  return function(t7) {
    return a4 + t7 * d2;
  };
}
function exponential3(a4, b3, y6) {
  return a4 = Math.pow(a4, y6), b3 = Math.pow(b3, y6) - a4, y6 = 1 / y6, function(t7) {
    return Math.pow(a4 + t7 * b3, y6);
  };
}
function hue3(a4, b3) {
  var d2 = b3 - a4;
  return d2 ? linear4(a4, d2 > 180 || d2 < -180 ? d2 - 360 * Math.round(d2 / 360) : d2) : constant_default6(isNaN(a4) ? b3 : a4);
}
function gamma3(y6) {
  return (y6 = +y6) === 1 ? nogamma3 : function(a4, b3) {
    return b3 - a4 ? exponential3(a4, b3, y6) : constant_default6(isNaN(a4) ? b3 : a4);
  };
}
function nogamma3(a4, b3) {
  var d2 = b3 - a4;
  return d2 ? linear4(a4, d2) : constant_default6(isNaN(a4) ? b3 : a4);
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/rgb.js
var rgb_default3 = function rgbGamma3(y6) {
  var color3 = gamma3(y6);
  function rgb5(start3, end) {
    var r5 = color3((start3 = rgb3(start3)).r, (end = rgb3(end)).r), g3 = color3(start3.g, end.g), b3 = color3(start3.b, end.b), opacity = nogamma3(start3.opacity, end.opacity);
    return function(t7) {
      start3.r = r5(t7);
      start3.g = g3(t7);
      start3.b = b3(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  }
  rgb5.gamma = rgbGamma3;
  return rgb5;
}(1);
function rgbSpline3(spline) {
  return function(colors4) {
    var n5 = colors4.length, r5 = new Array(n5), g3 = new Array(n5), b3 = new Array(n5), i5, color3;
    for (i5 = 0; i5 < n5; ++i5) {
      color3 = rgb3(colors4[i5]);
      r5[i5] = color3.r || 0;
      g3[i5] = color3.g || 0;
      b3[i5] = color3.b || 0;
    }
    r5 = spline(r5);
    g3 = spline(g3);
    b3 = spline(b3);
    color3.opacity = 1;
    return function(t7) {
      color3.r = r5(t7);
      color3.g = g3(t7);
      color3.b = b3(t7);
      return color3 + "";
    };
  };
}
var rgbBasis3 = rgbSpline3(basis_default4);
var rgbBasisClosed3 = rgbSpline3(basisClosed_default4);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/number.js
function number_default4(a4, b3) {
  return a4 = +a4, b3 = +b3, function(t7) {
    return a4 * (1 - t7) + b3 * t7;
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/string.js
var reA3 = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var reB3 = new RegExp(reA3.source, "g");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/transform/decompose.js
var degrees5 = 180 / Math.PI;
var identity7 = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function decompose_default3(a4, b3, c10, d2, e7, f3) {
  var scaleX, scaleY, skewX;
  if (scaleX = Math.sqrt(a4 * a4 + b3 * b3)) a4 /= scaleX, b3 /= scaleX;
  if (skewX = a4 * c10 + b3 * d2) c10 -= a4 * skewX, d2 -= b3 * skewX;
  if (scaleY = Math.sqrt(c10 * c10 + d2 * d2)) c10 /= scaleY, d2 /= scaleY, skewX /= scaleY;
  if (a4 * d2 < b3 * c10) a4 = -a4, b3 = -b3, skewX = -skewX, scaleX = -scaleX;
  return {
    translateX: e7,
    translateY: f3,
    rotate: Math.atan2(b3, a4) * degrees5,
    skewX: Math.atan(skewX) * degrees5,
    scaleX,
    scaleY
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/transform/parse.js
var svgNode3;
function parseCss3(value) {
  const m5 = new (typeof DOMMatrix === "function" ? DOMMatrix : WebKitCSSMatrix)(value + "");
  return m5.isIdentity ? identity7 : decompose_default3(m5.a, m5.b, m5.c, m5.d, m5.e, m5.f);
}
function parseSvg3(value) {
  if (value == null) return identity7;
  if (!svgNode3) svgNode3 = document.createElementNS("http://www.w3.org/2000/svg", "g");
  svgNode3.setAttribute("transform", value);
  if (!(value = svgNode3.transform.baseVal.consolidate())) return identity7;
  value = value.matrix;
  return decompose_default3(value.a, value.b, value.c, value.d, value.e, value.f);
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/transform/index.js
function interpolateTransform3(parse, pxComma, pxParen, degParen) {
  function pop(s3) {
    return s3.length ? s3.pop() + " " : "";
  }
  function translate(xa, ya, xb, yb, s3, q3) {
    if (xa !== xb || ya !== yb) {
      var i5 = s3.push("translate(", null, pxComma, null, pxParen);
      q3.push({ i: i5 - 4, x: number_default4(xa, xb) }, { i: i5 - 2, x: number_default4(ya, yb) });
    } else if (xb || yb) {
      s3.push("translate(" + xb + pxComma + yb + pxParen);
    }
  }
  function rotate(a4, b3, s3, q3) {
    if (a4 !== b3) {
      if (a4 - b3 > 180) b3 += 360;
      else if (b3 - a4 > 180) a4 += 360;
      q3.push({ i: s3.push(pop(s3) + "rotate(", null, degParen) - 2, x: number_default4(a4, b3) });
    } else if (b3) {
      s3.push(pop(s3) + "rotate(" + b3 + degParen);
    }
  }
  function skewX(a4, b3, s3, q3) {
    if (a4 !== b3) {
      q3.push({ i: s3.push(pop(s3) + "skewX(", null, degParen) - 2, x: number_default4(a4, b3) });
    } else if (b3) {
      s3.push(pop(s3) + "skewX(" + b3 + degParen);
    }
  }
  function scale(xa, ya, xb, yb, s3, q3) {
    if (xa !== xb || ya !== yb) {
      var i5 = s3.push(pop(s3) + "scale(", null, ",", null, ")");
      q3.push({ i: i5 - 4, x: number_default4(xa, xb) }, { i: i5 - 2, x: number_default4(ya, yb) });
    } else if (xb !== 1 || yb !== 1) {
      s3.push(pop(s3) + "scale(" + xb + "," + yb + ")");
    }
  }
  return function(a4, b3) {
    var s3 = [], q3 = [];
    a4 = parse(a4), b3 = parse(b3);
    translate(a4.translateX, a4.translateY, b3.translateX, b3.translateY, s3, q3);
    rotate(a4.rotate, b3.rotate, s3, q3);
    skewX(a4.skewX, b3.skewX, s3, q3);
    scale(a4.scaleX, a4.scaleY, b3.scaleX, b3.scaleY, s3, q3);
    a4 = b3 = null;
    return function(t7) {
      var i5 = -1, n5 = q3.length, o5;
      while (++i5 < n5) s3[(o5 = q3[i5]).i] = o5.x(t7);
      return s3.join("");
    };
  };
}
var interpolateTransformCss3 = interpolateTransform3(parseCss3, "px, ", "px)", "deg)");
var interpolateTransformSvg3 = interpolateTransform3(parseSvg3, ", ", ")", ")");

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/zoom.js
var epsilon23 = 1e-12;
function cosh3(x4) {
  return ((x4 = Math.exp(x4)) + 1 / x4) / 2;
}
function sinh3(x4) {
  return ((x4 = Math.exp(x4)) - 1 / x4) / 2;
}
function tanh3(x4) {
  return ((x4 = Math.exp(2 * x4)) - 1) / (x4 + 1);
}
var zoom_default3 = function zoomRho3(rho, rho2, rho4) {
  function zoom(p0, p1) {
    var ux0 = p0[0], uy0 = p0[1], w0 = p0[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx = ux1 - ux0, dy = uy1 - uy0, d2 = dx * dx + dy * dy, i5, S;
    if (d2 < epsilon23) {
      S = Math.log(w1 / w0) / rho;
      i5 = function(t7) {
        return [
          ux0 + t7 * dx,
          uy0 + t7 * dy,
          w0 * Math.exp(rho * t7 * S)
        ];
      };
    } else {
      var d1 = Math.sqrt(d2), b0 = (w1 * w1 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1), b1 = (w1 * w1 - w0 * w0 - rho4 * d2) / (2 * w1 * rho2 * d1), r0 = Math.log(Math.sqrt(b0 * b0 + 1) - b0), r1 = Math.log(Math.sqrt(b1 * b1 + 1) - b1);
      S = (r1 - r0) / rho;
      i5 = function(t7) {
        var s3 = t7 * S, coshr0 = cosh3(r0), u2 = w0 / (rho2 * d1) * (coshr0 * tanh3(rho * s3 + r0) - sinh3(r0));
        return [
          ux0 + u2 * dx,
          uy0 + u2 * dy,
          w0 * coshr0 / cosh3(rho * s3 + r0)
        ];
      };
    }
    i5.duration = S * 1e3 * rho / Math.SQRT2;
    return i5;
  }
  zoom.rho = function(_2) {
    var _1 = Math.max(1e-3, +_2), _22 = _1 * _1, _4 = _22 * _22;
    return zoomRho3(_1, _22, _4);
  };
  return zoom;
}(Math.SQRT2, 2, 4);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/hsl.js
function hsl6(hue4) {
  return function(start3, end) {
    var h = hue4((start3 = hsl4(start3)).h, (end = hsl4(end)).h), s3 = nogamma3(start3.s, end.s), l4 = nogamma3(start3.l, end.l), opacity = nogamma3(start3.opacity, end.opacity);
    return function(t7) {
      start3.h = h(t7);
      start3.s = s3(t7);
      start3.l = l4(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  };
}
var hsl_default3 = hsl6(hue3);
var hslLong3 = hsl6(nogamma3);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/hcl.js
function hcl5(hue4) {
  return function(start3, end) {
    var h = hue4((start3 = hcl3(start3)).h, (end = hcl3(end)).h), c10 = nogamma3(start3.c, end.c), l4 = nogamma3(start3.l, end.l), opacity = nogamma3(start3.opacity, end.opacity);
    return function(t7) {
      start3.h = h(t7);
      start3.c = c10(t7);
      start3.l = l4(t7);
      start3.opacity = opacity(t7);
      return start3 + "";
    };
  };
}
var hcl_default3 = hcl5(hue3);
var hclLong3 = hcl5(nogamma3);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-interpolate/src/cubehelix.js
function cubehelix5(hue4) {
  return function cubehelixGamma(y6) {
    y6 = +y6;
    function cubehelix6(start3, end) {
      var h = hue4((start3 = cubehelix3(start3)).h, (end = cubehelix3(end)).h), s3 = nogamma3(start3.s, end.s), l4 = nogamma3(start3.l, end.l), opacity = nogamma3(start3.opacity, end.opacity);
      return function(t7) {
        start3.h = h(t7);
        start3.s = s3(t7);
        start3.l = l4(Math.pow(t7, y6));
        start3.opacity = opacity(t7);
        return start3 + "";
      };
    }
    cubehelix6.gamma = cubehelixGamma;
    return cubehelix6;
  }(1);
}
var cubehelix_default5 = cubehelix5(hue3);
var cubehelixLong3 = cubehelix5(nogamma3);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatDecimal.js
function formatDecimal_default2(x4) {
  return Math.abs(x4 = Math.round(x4)) >= 1e21 ? x4.toLocaleString("en").replace(/,/g, "") : x4.toString(10);
}
function formatDecimalParts2(x4, p3) {
  if (!isFinite(x4) || x4 === 0) return null;
  var i5 = (x4 = p3 ? x4.toExponential(p3 - 1) : x4.toExponential()).indexOf("e"), coefficient = x4.slice(0, i5);
  return [
    coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
    +x4.slice(i5 + 1)
  ];
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/exponent.js
function exponent_default2(x4) {
  return x4 = formatDecimalParts2(Math.abs(x4)), x4 ? x4[1] : NaN;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatGroup.js
function formatGroup_default2(grouping, thousands) {
  return function(value, width) {
    var i5 = value.length, t7 = [], j4 = 0, g3 = grouping[0], length = 0;
    while (i5 > 0 && g3 > 0) {
      if (length + g3 + 1 > width) g3 = Math.max(1, width - length);
      t7.push(value.substring(i5 -= g3, i5 + g3));
      if ((length += g3 + 1) > width) break;
      g3 = grouping[j4 = (j4 + 1) % grouping.length];
    }
    return t7.reverse().join(thousands);
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatNumerals.js
function formatNumerals_default2(numerals) {
  return function(value) {
    return value.replace(/[0-9]/g, function(i5) {
      return numerals[+i5];
    });
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatSpecifier.js
var re2 = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function formatSpecifier2(specifier) {
  if (!(match = re2.exec(specifier))) throw new Error("invalid format: " + specifier);
  var match;
  return new FormatSpecifier2({
    fill: match[1],
    align: match[2],
    sign: match[3],
    symbol: match[4],
    zero: match[5],
    width: match[6],
    comma: match[7],
    precision: match[8] && match[8].slice(1),
    trim: match[9],
    type: match[10]
  });
}
formatSpecifier2.prototype = FormatSpecifier2.prototype;
function FormatSpecifier2(specifier) {
  this.fill = specifier.fill === void 0 ? " " : specifier.fill + "";
  this.align = specifier.align === void 0 ? ">" : specifier.align + "";
  this.sign = specifier.sign === void 0 ? "-" : specifier.sign + "";
  this.symbol = specifier.symbol === void 0 ? "" : specifier.symbol + "";
  this.zero = !!specifier.zero;
  this.width = specifier.width === void 0 ? void 0 : +specifier.width;
  this.comma = !!specifier.comma;
  this.precision = specifier.precision === void 0 ? void 0 : +specifier.precision;
  this.trim = !!specifier.trim;
  this.type = specifier.type === void 0 ? "" : specifier.type + "";
}
FormatSpecifier2.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatTrim.js
function formatTrim_default2(s3) {
  out: for (var n5 = s3.length, i5 = 1, i0 = -1, i1; i5 < n5; ++i5) {
    switch (s3[i5]) {
      case ".":
        i0 = i1 = i5;
        break;
      case "0":
        if (i0 === 0) i0 = i5;
        i1 = i5;
        break;
      default:
        if (!+s3[i5]) break out;
        if (i0 > 0) i0 = 0;
        break;
    }
  }
  return i0 > 0 ? s3.slice(0, i0) + s3.slice(i1 + 1) : s3;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatPrefixAuto.js
var prefixExponent2;
function formatPrefixAuto_default2(x4, p3) {
  var d2 = formatDecimalParts2(x4, p3);
  if (!d2) return prefixExponent2 = void 0, x4.toPrecision(p3);
  var coefficient = d2[0], exponent = d2[1], i5 = exponent - (prefixExponent2 = Math.max(-8, Math.min(8, Math.floor(exponent / 3))) * 3) + 1, n5 = coefficient.length;
  return i5 === n5 ? coefficient : i5 > n5 ? coefficient + new Array(i5 - n5 + 1).join("0") : i5 > 0 ? coefficient.slice(0, i5) + "." + coefficient.slice(i5) : "0." + new Array(1 - i5).join("0") + formatDecimalParts2(x4, Math.max(0, p3 + i5 - 1))[0];
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatRounded.js
function formatRounded_default2(x4, p3) {
  var d2 = formatDecimalParts2(x4, p3);
  if (!d2) return x4 + "";
  var coefficient = d2[0], exponent = d2[1];
  return exponent < 0 ? "0." + new Array(-exponent).join("0") + coefficient : coefficient.length > exponent + 1 ? coefficient.slice(0, exponent + 1) + "." + coefficient.slice(exponent + 1) : coefficient + new Array(exponent - coefficient.length + 2).join("0");
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/formatTypes.js
var formatTypes_default2 = {
  "%": (x4, p3) => (x4 * 100).toFixed(p3),
  "b": (x4) => Math.round(x4).toString(2),
  "c": (x4) => x4 + "",
  "d": formatDecimal_default2,
  "e": (x4, p3) => x4.toExponential(p3),
  "f": (x4, p3) => x4.toFixed(p3),
  "g": (x4, p3) => x4.toPrecision(p3),
  "o": (x4) => Math.round(x4).toString(8),
  "p": (x4, p3) => formatRounded_default2(x4 * 100, p3),
  "r": formatRounded_default2,
  "s": formatPrefixAuto_default2,
  "X": (x4) => Math.round(x4).toString(16).toUpperCase(),
  "x": (x4) => Math.round(x4).toString(16)
};

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/identity.js
function identity_default4(x4) {
  return x4;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/locale.js
var map8 = Array.prototype.map;
var prefixes3 = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function locale_default2(locale5) {
  var group4 = locale5.grouping === void 0 || locale5.thousands === void 0 ? identity_default4 : formatGroup_default2(map8.call(locale5.grouping, Number), locale5.thousands + ""), currencyPrefix = locale5.currency === void 0 ? "" : locale5.currency[0] + "", currencySuffix = locale5.currency === void 0 ? "" : locale5.currency[1] + "", decimal = locale5.decimal === void 0 ? "." : locale5.decimal + "", numerals = locale5.numerals === void 0 ? identity_default4 : formatNumerals_default2(map8.call(locale5.numerals, String)), percent = locale5.percent === void 0 ? "%" : locale5.percent + "", minus = locale5.minus === void 0 ? "−" : locale5.minus + "", nan = locale5.nan === void 0 ? "NaN" : locale5.nan + "";
  function newFormat(specifier, options) {
    specifier = formatSpecifier2(specifier);
    var fill = specifier.fill, align = specifier.align, sign2 = specifier.sign, symbol = specifier.symbol, zero3 = specifier.zero, width = specifier.width, comma = specifier.comma, precision = specifier.precision, trim = specifier.trim, type = specifier.type;
    if (type === "n") comma = true, type = "g";
    else if (!formatTypes_default2[type]) precision === void 0 && (precision = 12), trim = true, type = "g";
    if (zero3 || fill === "0" && align === "=") zero3 = true, fill = "0", align = "=";
    var prefix3 = (options && options.prefix !== void 0 ? options.prefix : "") + (symbol === "$" ? currencyPrefix : symbol === "#" && /[boxX]/.test(type) ? "0" + type.toLowerCase() : ""), suffix = (symbol === "$" ? currencySuffix : /[%p]/.test(type) ? percent : "") + (options && options.suffix !== void 0 ? options.suffix : "");
    var formatType = formatTypes_default2[type], maybeSuffix = /[defgprs%]/.test(type);
    precision = precision === void 0 ? 6 : /[gprs]/.test(type) ? Math.max(1, Math.min(21, precision)) : Math.max(0, Math.min(20, precision));
    function format3(value) {
      var valuePrefix = prefix3, valueSuffix = suffix, i5, n5, c10;
      if (type === "c") {
        valueSuffix = formatType(value) + valueSuffix;
        value = "";
      } else {
        value = +value;
        var valueNegative = value < 0 || 1 / value < 0;
        value = isNaN(value) ? nan : formatType(Math.abs(value), precision);
        if (trim) value = formatTrim_default2(value);
        if (valueNegative && +value === 0 && sign2 !== "+") valueNegative = false;
        valuePrefix = (valueNegative ? sign2 === "(" ? sign2 : minus : sign2 === "-" || sign2 === "(" ? "" : sign2) + valuePrefix;
        valueSuffix = (type === "s" && !isNaN(value) && prefixExponent2 !== void 0 ? prefixes3[8 + prefixExponent2 / 3] : "") + valueSuffix + (valueNegative && sign2 === "(" ? ")" : "");
        if (maybeSuffix) {
          i5 = -1, n5 = value.length;
          while (++i5 < n5) {
            if (c10 = value.charCodeAt(i5), 48 > c10 || c10 > 57) {
              valueSuffix = (c10 === 46 ? decimal + value.slice(i5 + 1) : value.slice(i5)) + valueSuffix;
              value = value.slice(0, i5);
              break;
            }
          }
        }
      }
      if (comma && !zero3) value = group4(value, Infinity);
      var length = valuePrefix.length + value.length + valueSuffix.length, padding = length < width ? new Array(width - length + 1).join(fill) : "";
      if (comma && zero3) value = group4(padding + value, padding.length ? width - valueSuffix.length : Infinity), padding = "";
      switch (align) {
        case "<":
          value = valuePrefix + value + valueSuffix + padding;
          break;
        case "=":
          value = valuePrefix + padding + value + valueSuffix;
          break;
        case "^":
          value = padding.slice(0, length = padding.length >> 1) + valuePrefix + value + valueSuffix + padding.slice(length);
          break;
        default:
          value = padding + valuePrefix + value + valueSuffix;
          break;
      }
      return numerals(value);
    }
    format3.toString = function() {
      return specifier + "";
    };
    return format3;
  }
  function formatPrefix3(specifier, value) {
    var e7 = Math.max(-8, Math.min(8, Math.floor(exponent_default2(value) / 3))) * 3, k3 = Math.pow(10, -e7), f3 = newFormat((specifier = formatSpecifier2(specifier), specifier.type = "f", specifier), { suffix: prefixes3[8 + e7 / 3] });
    return function(value2) {
      return f3(k3 * value2);
    };
  }
  return {
    format: newFormat,
    formatPrefix: formatPrefix3
  };
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-format/src/defaultLocale.js
var locale3;
var format2;
var formatPrefix2;
defaultLocale3({
  thousands: ",",
  grouping: [3],
  currency: ["$", ""]
});
function defaultLocale3(definition) {
  locale3 = locale_default2(definition);
  format2 = locale3.format;
  formatPrefix2 = locale3.formatPrefix;
  return locale3;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/interval.js
var t05 = /* @__PURE__ */ new Date();
var t15 = /* @__PURE__ */ new Date();
function timeInterval2(floori, offseti, count5, field) {
  function interval(date) {
    return floori(date = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date)), date;
  }
  interval.floor = (date) => {
    return floori(date = /* @__PURE__ */ new Date(+date)), date;
  };
  interval.ceil = (date) => {
    return floori(date = new Date(date - 1)), offseti(date, 1), floori(date), date;
  };
  interval.round = (date) => {
    const d0 = interval(date), d1 = interval.ceil(date);
    return date - d0 < d1 - date ? d0 : d1;
  };
  interval.offset = (date, step) => {
    return offseti(date = /* @__PURE__ */ new Date(+date), step == null ? 1 : Math.floor(step)), date;
  };
  interval.range = (start3, stop3, step) => {
    const range3 = [];
    start3 = interval.ceil(start3);
    step = step == null ? 1 : Math.floor(step);
    if (!(start3 < stop3) || !(step > 0)) return range3;
    let previous;
    do
      range3.push(previous = /* @__PURE__ */ new Date(+start3)), offseti(start3, step), floori(start3);
    while (previous < start3 && start3 < stop3);
    return range3;
  };
  interval.filter = (test) => {
    return timeInterval2((date) => {
      if (date >= date) while (floori(date), !test(date)) date.setTime(date - 1);
    }, (date, step) => {
      if (date >= date) {
        if (step < 0) while (++step <= 0) {
          while (offseti(date, -1), !test(date)) {
          }
        }
        else while (--step >= 0) {
          while (offseti(date, 1), !test(date)) {
          }
        }
      }
    });
  };
  if (count5) {
    interval.count = (start3, end) => {
      t05.setTime(+start3), t15.setTime(+end);
      floori(t05), floori(t15);
      return Math.floor(count5(t05, t15));
    };
    interval.every = (step) => {
      step = Math.floor(step);
      return !isFinite(step) || !(step > 0) ? null : !(step > 1) ? interval : interval.filter(field ? (d2) => field(d2) % step === 0 : (d2) => interval.count(0, d2) % step === 0);
    };
  }
  return interval;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/millisecond.js
var millisecond3 = timeInterval2(() => {
}, (date, step) => {
  date.setTime(+date + step);
}, (start3, end) => {
  return end - start3;
});
millisecond3.every = (k3) => {
  k3 = Math.floor(k3);
  if (!isFinite(k3) || !(k3 > 0)) return null;
  if (!(k3 > 1)) return millisecond3;
  return timeInterval2((date) => {
    date.setTime(Math.floor(date / k3) * k3);
  }, (date, step) => {
    date.setTime(+date + step * k3);
  }, (start3, end) => {
    return (end - start3) / k3;
  });
};
var milliseconds3 = millisecond3.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/duration.js
var durationSecond3 = 1e3;
var durationMinute3 = durationSecond3 * 60;
var durationHour3 = durationMinute3 * 60;
var durationDay3 = durationHour3 * 24;
var durationWeek3 = durationDay3 * 7;
var durationMonth3 = durationDay3 * 30;
var durationYear3 = durationDay3 * 365;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/second.js
var second3 = timeInterval2((date) => {
  date.setTime(date - date.getMilliseconds());
}, (date, step) => {
  date.setTime(+date + step * durationSecond3);
}, (start3, end) => {
  return (end - start3) / durationSecond3;
}, (date) => {
  return date.getUTCSeconds();
});
var seconds3 = second3.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/minute.js
var timeMinute2 = timeInterval2((date) => {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond3);
}, (date, step) => {
  date.setTime(+date + step * durationMinute3);
}, (start3, end) => {
  return (end - start3) / durationMinute3;
}, (date) => {
  return date.getMinutes();
});
var timeMinutes2 = timeMinute2.range;
var utcMinute3 = timeInterval2((date) => {
  date.setUTCSeconds(0, 0);
}, (date, step) => {
  date.setTime(+date + step * durationMinute3);
}, (start3, end) => {
  return (end - start3) / durationMinute3;
}, (date) => {
  return date.getUTCMinutes();
});
var utcMinutes3 = utcMinute3.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/hour.js
var timeHour2 = timeInterval2((date) => {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond3 - date.getMinutes() * durationMinute3);
}, (date, step) => {
  date.setTime(+date + step * durationHour3);
}, (start3, end) => {
  return (end - start3) / durationHour3;
}, (date) => {
  return date.getHours();
});
var timeHours2 = timeHour2.range;
var utcHour3 = timeInterval2((date) => {
  date.setUTCMinutes(0, 0, 0);
}, (date, step) => {
  date.setTime(+date + step * durationHour3);
}, (start3, end) => {
  return (end - start3) / durationHour3;
}, (date) => {
  return date.getUTCHours();
});
var utcHours3 = utcHour3.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/day.js
var timeDay2 = timeInterval2(
  (date) => date.setHours(0, 0, 0, 0),
  (date, step) => date.setDate(date.getDate() + step),
  (start3, end) => (end - start3 - (end.getTimezoneOffset() - start3.getTimezoneOffset()) * durationMinute3) / durationDay3,
  (date) => date.getDate() - 1
);
var timeDays2 = timeDay2.range;
var utcDay3 = timeInterval2((date) => {
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCDate(date.getUTCDate() + step);
}, (start3, end) => {
  return (end - start3) / durationDay3;
}, (date) => {
  return date.getUTCDate() - 1;
});
var utcDays3 = utcDay3.range;
var unixDay2 = timeInterval2((date) => {
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCDate(date.getUTCDate() + step);
}, (start3, end) => {
  return (end - start3) / durationDay3;
}, (date) => {
  return Math.floor(date / durationDay3);
});
var unixDays2 = unixDay2.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/week.js
function timeWeekday2(i5) {
  return timeInterval2((date) => {
    date.setDate(date.getDate() - (date.getDay() + 7 - i5) % 7);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setDate(date.getDate() + step * 7);
  }, (start3, end) => {
    return (end - start3 - (end.getTimezoneOffset() - start3.getTimezoneOffset()) * durationMinute3) / durationWeek3;
  });
}
var timeSunday2 = timeWeekday2(0);
var timeMonday2 = timeWeekday2(1);
var timeTuesday2 = timeWeekday2(2);
var timeWednesday2 = timeWeekday2(3);
var timeThursday2 = timeWeekday2(4);
var timeFriday2 = timeWeekday2(5);
var timeSaturday2 = timeWeekday2(6);
var timeSundays2 = timeSunday2.range;
var timeMondays2 = timeMonday2.range;
var timeTuesdays2 = timeTuesday2.range;
var timeWednesdays2 = timeWednesday2.range;
var timeThursdays2 = timeThursday2.range;
var timeFridays2 = timeFriday2.range;
var timeSaturdays2 = timeSaturday2.range;
function utcWeekday3(i5) {
  return timeInterval2((date) => {
    date.setUTCDate(date.getUTCDate() - (date.getUTCDay() + 7 - i5) % 7);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCDate(date.getUTCDate() + step * 7);
  }, (start3, end) => {
    return (end - start3) / durationWeek3;
  });
}
var utcSunday3 = utcWeekday3(0);
var utcMonday3 = utcWeekday3(1);
var utcTuesday3 = utcWeekday3(2);
var utcWednesday3 = utcWeekday3(3);
var utcThursday3 = utcWeekday3(4);
var utcFriday3 = utcWeekday3(5);
var utcSaturday3 = utcWeekday3(6);
var utcSundays3 = utcSunday3.range;
var utcMondays3 = utcMonday3.range;
var utcTuesdays3 = utcTuesday3.range;
var utcWednesdays3 = utcWednesday3.range;
var utcThursdays3 = utcThursday3.range;
var utcFridays3 = utcFriday3.range;
var utcSaturdays3 = utcSaturday3.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/month.js
var timeMonth2 = timeInterval2((date) => {
  date.setDate(1);
  date.setHours(0, 0, 0, 0);
}, (date, step) => {
  date.setMonth(date.getMonth() + step);
}, (start3, end) => {
  return end.getMonth() - start3.getMonth() + (end.getFullYear() - start3.getFullYear()) * 12;
}, (date) => {
  return date.getMonth();
});
var timeMonths2 = timeMonth2.range;
var utcMonth3 = timeInterval2((date) => {
  date.setUTCDate(1);
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCMonth(date.getUTCMonth() + step);
}, (start3, end) => {
  return end.getUTCMonth() - start3.getUTCMonth() + (end.getUTCFullYear() - start3.getUTCFullYear()) * 12;
}, (date) => {
  return date.getUTCMonth();
});
var utcMonths3 = utcMonth3.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/year.js
var timeYear2 = timeInterval2((date) => {
  date.setMonth(0, 1);
  date.setHours(0, 0, 0, 0);
}, (date, step) => {
  date.setFullYear(date.getFullYear() + step);
}, (start3, end) => {
  return end.getFullYear() - start3.getFullYear();
}, (date) => {
  return date.getFullYear();
});
timeYear2.every = (k3) => {
  return !isFinite(k3 = Math.floor(k3)) || !(k3 > 0) ? null : timeInterval2((date) => {
    date.setFullYear(Math.floor(date.getFullYear() / k3) * k3);
    date.setMonth(0, 1);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setFullYear(date.getFullYear() + step * k3);
  });
};
var timeYears2 = timeYear2.range;
var utcYear3 = timeInterval2((date) => {
  date.setUTCMonth(0, 1);
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCFullYear(date.getUTCFullYear() + step);
}, (start3, end) => {
  return end.getUTCFullYear() - start3.getUTCFullYear();
}, (date) => {
  return date.getUTCFullYear();
});
utcYear3.every = (k3) => {
  return !isFinite(k3 = Math.floor(k3)) || !(k3 > 0) ? null : timeInterval2((date) => {
    date.setUTCFullYear(Math.floor(date.getUTCFullYear() / k3) * k3);
    date.setUTCMonth(0, 1);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCFullYear(date.getUTCFullYear() + step * k3);
  });
};
var utcYears3 = utcYear3.range;

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time/src/ticks.js
function ticker3(year2, month2, week, day2, hour2, minute2) {
  const tickIntervals = [
    [second3, 1, durationSecond3],
    [second3, 5, 5 * durationSecond3],
    [second3, 15, 15 * durationSecond3],
    [second3, 30, 30 * durationSecond3],
    [minute2, 1, durationMinute3],
    [minute2, 5, 5 * durationMinute3],
    [minute2, 15, 15 * durationMinute3],
    [minute2, 30, 30 * durationMinute3],
    [hour2, 1, durationHour3],
    [hour2, 3, 3 * durationHour3],
    [hour2, 6, 6 * durationHour3],
    [hour2, 12, 12 * durationHour3],
    [day2, 1, durationDay3],
    [day2, 2, 2 * durationDay3],
    [week, 1, durationWeek3],
    [month2, 1, durationMonth3],
    [month2, 3, 3 * durationMonth3],
    [year2, 1, durationYear3]
  ];
  function ticks3(start3, stop3, count5) {
    const reverse4 = stop3 < start3;
    if (reverse4) [start3, stop3] = [stop3, start3];
    const interval = count5 && typeof count5.range === "function" ? count5 : tickInterval(start3, stop3, count5);
    const ticks4 = interval ? interval.range(start3, +stop3 + 1) : [];
    return reverse4 ? ticks4.reverse() : ticks4;
  }
  function tickInterval(start3, stop3, count5) {
    const target = Math.abs(stop3 - start3) / count5;
    const i5 = bisector2(([, , step2]) => step2).right(tickIntervals, target);
    if (i5 === tickIntervals.length) return year2.every(tickStep3(start3 / durationYear3, stop3 / durationYear3, count5));
    if (i5 === 0) return millisecond3.every(Math.max(tickStep3(start3, stop3, count5), 1));
    const [t7, step] = tickIntervals[target / tickIntervals[i5 - 1][2] < tickIntervals[i5][2] / target ? i5 - 1 : i5];
    return t7.every(step);
  }
  return [ticks3, tickInterval];
}
var [utcTicks3, utcTickInterval3] = ticker3(utcYear3, utcMonth3, utcSunday3, unixDay2, utcHour3, utcMinute3);
var [timeTicks3, timeTickInterval3] = ticker3(timeYear2, timeMonth2, timeSunday2, timeDay2, timeHour2, timeMinute2);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time-format/src/locale.js
function localDate2(d2) {
  if (0 <= d2.y && d2.y < 100) {
    var date = new Date(-1, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L);
    date.setFullYear(d2.y);
    return date;
  }
  return new Date(d2.y, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L);
}
function utcDate2(d2) {
  if (0 <= d2.y && d2.y < 100) {
    var date = new Date(Date.UTC(-1, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L));
    date.setUTCFullYear(d2.y);
    return date;
  }
  return new Date(Date.UTC(d2.y, d2.m, d2.d, d2.H, d2.M, d2.S, d2.L));
}
function newDate2(y6, m5, d2) {
  return { y: y6, m: m5, d: d2, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale2(locale5) {
  var locale_dateTime = locale5.dateTime, locale_date = locale5.date, locale_time = locale5.time, locale_periods = locale5.periods, locale_weekdays = locale5.days, locale_shortWeekdays = locale5.shortDays, locale_months = locale5.months, locale_shortMonths = locale5.shortMonths;
  var periodRe = formatRe2(locale_periods), periodLookup = formatLookup2(locale_periods), weekdayRe = formatRe2(locale_weekdays), weekdayLookup = formatLookup2(locale_weekdays), shortWeekdayRe = formatRe2(locale_shortWeekdays), shortWeekdayLookup = formatLookup2(locale_shortWeekdays), monthRe = formatRe2(locale_months), monthLookup = formatLookup2(locale_months), shortMonthRe = formatRe2(locale_shortMonths), shortMonthLookup = formatLookup2(locale_shortMonths);
  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth2,
    "e": formatDayOfMonth2,
    "f": formatMicroseconds2,
    "g": formatYearISO2,
    "G": formatFullYearISO2,
    "H": formatHour242,
    "I": formatHour122,
    "j": formatDayOfYear2,
    "L": formatMilliseconds2,
    "m": formatMonthNumber2,
    "M": formatMinutes2,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp2,
    "s": formatUnixTimestampSeconds2,
    "S": formatSeconds2,
    "u": formatWeekdayNumberMonday2,
    "U": formatWeekNumberSunday2,
    "V": formatWeekNumberISO2,
    "w": formatWeekdayNumberSunday2,
    "W": formatWeekNumberMonday2,
    "x": null,
    "X": null,
    "y": formatYear2,
    "Y": formatFullYear2,
    "Z": formatZone2,
    "%": formatLiteralPercent2
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth2,
    "e": formatUTCDayOfMonth2,
    "f": formatUTCMicroseconds2,
    "g": formatUTCYearISO2,
    "G": formatUTCFullYearISO2,
    "H": formatUTCHour242,
    "I": formatUTCHour122,
    "j": formatUTCDayOfYear2,
    "L": formatUTCMilliseconds2,
    "m": formatUTCMonthNumber2,
    "M": formatUTCMinutes2,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp2,
    "s": formatUnixTimestampSeconds2,
    "S": formatUTCSeconds2,
    "u": formatUTCWeekdayNumberMonday2,
    "U": formatUTCWeekNumberSunday2,
    "V": formatUTCWeekNumberISO2,
    "w": formatUTCWeekdayNumberSunday2,
    "W": formatUTCWeekNumberMonday2,
    "x": null,
    "X": null,
    "y": formatUTCYear2,
    "Y": formatUTCFullYear2,
    "Z": formatUTCZone2,
    "%": formatLiteralPercent2
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth2,
    "e": parseDayOfMonth2,
    "f": parseMicroseconds2,
    "g": parseYear2,
    "G": parseFullYear2,
    "H": parseHour242,
    "I": parseHour242,
    "j": parseDayOfYear2,
    "L": parseMilliseconds2,
    "m": parseMonthNumber2,
    "M": parseMinutes2,
    "p": parsePeriod,
    "q": parseQuarter2,
    "Q": parseUnixTimestamp2,
    "s": parseUnixTimestampSeconds2,
    "S": parseSeconds2,
    "u": parseWeekdayNumberMonday2,
    "U": parseWeekNumberSunday2,
    "V": parseWeekNumberISO2,
    "w": parseWeekdayNumberSunday2,
    "W": parseWeekNumberMonday2,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear2,
    "Y": parseFullYear2,
    "Z": parseZone2,
    "%": parseLiteralPercent2
  };
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats2) {
    return function(date) {
      var string = [], i5 = -1, j4 = 0, n5 = specifier.length, c10, pad3, format3;
      if (!(date instanceof Date)) date = /* @__PURE__ */ new Date(+date);
      while (++i5 < n5) {
        if (specifier.charCodeAt(i5) === 37) {
          string.push(specifier.slice(j4, i5));
          if ((pad3 = pads2[c10 = specifier.charAt(++i5)]) != null) c10 = specifier.charAt(++i5);
          else pad3 = c10 === "e" ? " " : "0";
          if (format3 = formats2[c10]) c10 = format3(date, pad3);
          string.push(c10);
          j4 = i5 + 1;
        }
      }
      string.push(specifier.slice(j4, i5));
      return string.join("");
    };
  }
  function newParse(specifier, Z2) {
    return function(string) {
      var d2 = newDate2(1900, void 0, 1), i5 = parseSpecifier(d2, specifier, string += "", 0), week, day2;
      if (i5 != string.length) return null;
      if ("Q" in d2) return new Date(d2.Q);
      if ("s" in d2) return new Date(d2.s * 1e3 + ("L" in d2 ? d2.L : 0));
      if (Z2 && !("Z" in d2)) d2.Z = 0;
      if ("p" in d2) d2.H = d2.H % 12 + d2.p * 12;
      if (d2.m === void 0) d2.m = "q" in d2 ? d2.q : 0;
      if ("V" in d2) {
        if (d2.V < 1 || d2.V > 53) return null;
        if (!("w" in d2)) d2.w = 1;
        if ("Z" in d2) {
          week = utcDate2(newDate2(d2.y, 0, 1)), day2 = week.getUTCDay();
          week = day2 > 4 || day2 === 0 ? utcMonday3.ceil(week) : utcMonday3(week);
          week = utcDay3.offset(week, (d2.V - 1) * 7);
          d2.y = week.getUTCFullYear();
          d2.m = week.getUTCMonth();
          d2.d = week.getUTCDate() + (d2.w + 6) % 7;
        } else {
          week = localDate2(newDate2(d2.y, 0, 1)), day2 = week.getDay();
          week = day2 > 4 || day2 === 0 ? timeMonday2.ceil(week) : timeMonday2(week);
          week = timeDay2.offset(week, (d2.V - 1) * 7);
          d2.y = week.getFullYear();
          d2.m = week.getMonth();
          d2.d = week.getDate() + (d2.w + 6) % 7;
        }
      } else if ("W" in d2 || "U" in d2) {
        if (!("w" in d2)) d2.w = "u" in d2 ? d2.u % 7 : "W" in d2 ? 1 : 0;
        day2 = "Z" in d2 ? utcDate2(newDate2(d2.y, 0, 1)).getUTCDay() : localDate2(newDate2(d2.y, 0, 1)).getDay();
        d2.m = 0;
        d2.d = "W" in d2 ? (d2.w + 6) % 7 + d2.W * 7 - (day2 + 5) % 7 : d2.w + d2.U * 7 - (day2 + 6) % 7;
      }
      if ("Z" in d2) {
        d2.H += d2.Z / 100 | 0;
        d2.M += d2.Z % 100;
        return utcDate2(d2);
      }
      return localDate2(d2);
    };
  }
  function parseSpecifier(d2, specifier, string, j4) {
    var i5 = 0, n5 = specifier.length, m5 = string.length, c10, parse;
    while (i5 < n5) {
      if (j4 >= m5) return -1;
      c10 = specifier.charCodeAt(i5++);
      if (c10 === 37) {
        c10 = specifier.charAt(i5++);
        parse = parses[c10 in pads2 ? specifier.charAt(i5++) : c10];
        if (!parse || (j4 = parse(d2, string, j4)) < 0) return -1;
      } else if (c10 != string.charCodeAt(j4++)) {
        return -1;
      }
    }
    return j4;
  }
  function parsePeriod(d2, string, i5) {
    var n5 = periodRe.exec(string.slice(i5));
    return n5 ? (d2.p = periodLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseShortWeekday(d2, string, i5) {
    var n5 = shortWeekdayRe.exec(string.slice(i5));
    return n5 ? (d2.w = shortWeekdayLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseWeekday(d2, string, i5) {
    var n5 = weekdayRe.exec(string.slice(i5));
    return n5 ? (d2.w = weekdayLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseShortMonth(d2, string, i5) {
    var n5 = shortMonthRe.exec(string.slice(i5));
    return n5 ? (d2.m = shortMonthLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseMonth(d2, string, i5) {
    var n5 = monthRe.exec(string.slice(i5));
    return n5 ? (d2.m = monthLookup.get(n5[0].toLowerCase()), i5 + n5[0].length) : -1;
  }
  function parseLocaleDateTime(d2, string, i5) {
    return parseSpecifier(d2, locale_dateTime, string, i5);
  }
  function parseLocaleDate(d2, string, i5) {
    return parseSpecifier(d2, locale_date, string, i5);
  }
  function parseLocaleTime(d2, string, i5) {
    return parseSpecifier(d2, locale_time, string, i5);
  }
  function formatShortWeekday(d2) {
    return locale_shortWeekdays[d2.getDay()];
  }
  function formatWeekday(d2) {
    return locale_weekdays[d2.getDay()];
  }
  function formatShortMonth(d2) {
    return locale_shortMonths[d2.getMonth()];
  }
  function formatMonth(d2) {
    return locale_months[d2.getMonth()];
  }
  function formatPeriod(d2) {
    return locale_periods[+(d2.getHours() >= 12)];
  }
  function formatQuarter(d2) {
    return 1 + ~~(d2.getMonth() / 3);
  }
  function formatUTCShortWeekday(d2) {
    return locale_shortWeekdays[d2.getUTCDay()];
  }
  function formatUTCWeekday(d2) {
    return locale_weekdays[d2.getUTCDay()];
  }
  function formatUTCShortMonth(d2) {
    return locale_shortMonths[d2.getUTCMonth()];
  }
  function formatUTCMonth(d2) {
    return locale_months[d2.getUTCMonth()];
  }
  function formatUTCPeriod(d2) {
    return locale_periods[+(d2.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d2) {
    return 1 + ~~(d2.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f3 = newFormat(specifier += "", formats);
      f3.toString = function() {
        return specifier;
      };
      return f3;
    },
    parse: function(specifier) {
      var p3 = newParse(specifier += "", false);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    },
    utcFormat: function(specifier) {
      var f3 = newFormat(specifier += "", utcFormats);
      f3.toString = function() {
        return specifier;
      };
      return f3;
    },
    utcParse: function(specifier) {
      var p3 = newParse(specifier += "", true);
      p3.toString = function() {
        return specifier;
      };
      return p3;
    }
  };
}
var pads2 = { "-": "", "_": " ", "0": "0" };
var numberRe2 = /^\s*\d+/;
var percentRe2 = /^%/;
var requoteRe2 = /[\\^$*+?|[\]().{}]/g;
function pad2(value, fill, width) {
  var sign2 = value < 0 ? "-" : "", string = (sign2 ? -value : value) + "", length = string.length;
  return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}
function requote2(s3) {
  return s3.replace(requoteRe2, "\\$&");
}
function formatRe2(names) {
  return new RegExp("^(?:" + names.map(requote2).join("|") + ")", "i");
}
function formatLookup2(names) {
  return new Map(names.map((name, i5) => [name.toLowerCase(), i5]));
}
function parseWeekdayNumberSunday2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 1));
  return n5 ? (d2.w = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekdayNumberMonday2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 1));
  return n5 ? (d2.u = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekNumberSunday2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.U = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekNumberISO2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.V = +n5[0], i5 + n5[0].length) : -1;
}
function parseWeekNumberMonday2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.W = +n5[0], i5 + n5[0].length) : -1;
}
function parseFullYear2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 4));
  return n5 ? (d2.y = +n5[0], i5 + n5[0].length) : -1;
}
function parseYear2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.y = +n5[0] + (+n5[0] > 68 ? 1900 : 2e3), i5 + n5[0].length) : -1;
}
function parseZone2(d2, string, i5) {
  var n5 = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i5, i5 + 6));
  return n5 ? (d2.Z = n5[1] ? 0 : -(n5[2] + (n5[3] || "00")), i5 + n5[0].length) : -1;
}
function parseQuarter2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 1));
  return n5 ? (d2.q = n5[0] * 3 - 3, i5 + n5[0].length) : -1;
}
function parseMonthNumber2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.m = n5[0] - 1, i5 + n5[0].length) : -1;
}
function parseDayOfMonth2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.d = +n5[0], i5 + n5[0].length) : -1;
}
function parseDayOfYear2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 3));
  return n5 ? (d2.m = 0, d2.d = +n5[0], i5 + n5[0].length) : -1;
}
function parseHour242(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.H = +n5[0], i5 + n5[0].length) : -1;
}
function parseMinutes2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.M = +n5[0], i5 + n5[0].length) : -1;
}
function parseSeconds2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 2));
  return n5 ? (d2.S = +n5[0], i5 + n5[0].length) : -1;
}
function parseMilliseconds2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 3));
  return n5 ? (d2.L = +n5[0], i5 + n5[0].length) : -1;
}
function parseMicroseconds2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5, i5 + 6));
  return n5 ? (d2.L = Math.floor(n5[0] / 1e3), i5 + n5[0].length) : -1;
}
function parseLiteralPercent2(d2, string, i5) {
  var n5 = percentRe2.exec(string.slice(i5, i5 + 1));
  return n5 ? i5 + n5[0].length : -1;
}
function parseUnixTimestamp2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5));
  return n5 ? (d2.Q = +n5[0], i5 + n5[0].length) : -1;
}
function parseUnixTimestampSeconds2(d2, string, i5) {
  var n5 = numberRe2.exec(string.slice(i5));
  return n5 ? (d2.s = +n5[0], i5 + n5[0].length) : -1;
}
function formatDayOfMonth2(d2, p3) {
  return pad2(d2.getDate(), p3, 2);
}
function formatHour242(d2, p3) {
  return pad2(d2.getHours(), p3, 2);
}
function formatHour122(d2, p3) {
  return pad2(d2.getHours() % 12 || 12, p3, 2);
}
function formatDayOfYear2(d2, p3) {
  return pad2(1 + timeDay2.count(timeYear2(d2), d2), p3, 3);
}
function formatMilliseconds2(d2, p3) {
  return pad2(d2.getMilliseconds(), p3, 3);
}
function formatMicroseconds2(d2, p3) {
  return formatMilliseconds2(d2, p3) + "000";
}
function formatMonthNumber2(d2, p3) {
  return pad2(d2.getMonth() + 1, p3, 2);
}
function formatMinutes2(d2, p3) {
  return pad2(d2.getMinutes(), p3, 2);
}
function formatSeconds2(d2, p3) {
  return pad2(d2.getSeconds(), p3, 2);
}
function formatWeekdayNumberMonday2(d2) {
  var day2 = d2.getDay();
  return day2 === 0 ? 7 : day2;
}
function formatWeekNumberSunday2(d2, p3) {
  return pad2(timeSunday2.count(timeYear2(d2) - 1, d2), p3, 2);
}
function dISO2(d2) {
  var day2 = d2.getDay();
  return day2 >= 4 || day2 === 0 ? timeThursday2(d2) : timeThursday2.ceil(d2);
}
function formatWeekNumberISO2(d2, p3) {
  d2 = dISO2(d2);
  return pad2(timeThursday2.count(timeYear2(d2), d2) + (timeYear2(d2).getDay() === 4), p3, 2);
}
function formatWeekdayNumberSunday2(d2) {
  return d2.getDay();
}
function formatWeekNumberMonday2(d2, p3) {
  return pad2(timeMonday2.count(timeYear2(d2) - 1, d2), p3, 2);
}
function formatYear2(d2, p3) {
  return pad2(d2.getFullYear() % 100, p3, 2);
}
function formatYearISO2(d2, p3) {
  d2 = dISO2(d2);
  return pad2(d2.getFullYear() % 100, p3, 2);
}
function formatFullYear2(d2, p3) {
  return pad2(d2.getFullYear() % 1e4, p3, 4);
}
function formatFullYearISO2(d2, p3) {
  var day2 = d2.getDay();
  d2 = day2 >= 4 || day2 === 0 ? timeThursday2(d2) : timeThursday2.ceil(d2);
  return pad2(d2.getFullYear() % 1e4, p3, 4);
}
function formatZone2(d2) {
  var z3 = d2.getTimezoneOffset();
  return (z3 > 0 ? "-" : (z3 *= -1, "+")) + pad2(z3 / 60 | 0, "0", 2) + pad2(z3 % 60, "0", 2);
}
function formatUTCDayOfMonth2(d2, p3) {
  return pad2(d2.getUTCDate(), p3, 2);
}
function formatUTCHour242(d2, p3) {
  return pad2(d2.getUTCHours(), p3, 2);
}
function formatUTCHour122(d2, p3) {
  return pad2(d2.getUTCHours() % 12 || 12, p3, 2);
}
function formatUTCDayOfYear2(d2, p3) {
  return pad2(1 + utcDay3.count(utcYear3(d2), d2), p3, 3);
}
function formatUTCMilliseconds2(d2, p3) {
  return pad2(d2.getUTCMilliseconds(), p3, 3);
}
function formatUTCMicroseconds2(d2, p3) {
  return formatUTCMilliseconds2(d2, p3) + "000";
}
function formatUTCMonthNumber2(d2, p3) {
  return pad2(d2.getUTCMonth() + 1, p3, 2);
}
function formatUTCMinutes2(d2, p3) {
  return pad2(d2.getUTCMinutes(), p3, 2);
}
function formatUTCSeconds2(d2, p3) {
  return pad2(d2.getUTCSeconds(), p3, 2);
}
function formatUTCWeekdayNumberMonday2(d2) {
  var dow = d2.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday2(d2, p3) {
  return pad2(utcSunday3.count(utcYear3(d2) - 1, d2), p3, 2);
}
function UTCdISO2(d2) {
  var day2 = d2.getUTCDay();
  return day2 >= 4 || day2 === 0 ? utcThursday3(d2) : utcThursday3.ceil(d2);
}
function formatUTCWeekNumberISO2(d2, p3) {
  d2 = UTCdISO2(d2);
  return pad2(utcThursday3.count(utcYear3(d2), d2) + (utcYear3(d2).getUTCDay() === 4), p3, 2);
}
function formatUTCWeekdayNumberSunday2(d2) {
  return d2.getUTCDay();
}
function formatUTCWeekNumberMonday2(d2, p3) {
  return pad2(utcMonday3.count(utcYear3(d2) - 1, d2), p3, 2);
}
function formatUTCYear2(d2, p3) {
  return pad2(d2.getUTCFullYear() % 100, p3, 2);
}
function formatUTCYearISO2(d2, p3) {
  d2 = UTCdISO2(d2);
  return pad2(d2.getUTCFullYear() % 100, p3, 2);
}
function formatUTCFullYear2(d2, p3) {
  return pad2(d2.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCFullYearISO2(d2, p3) {
  var day2 = d2.getUTCDay();
  d2 = day2 >= 4 || day2 === 0 ? utcThursday3(d2) : utcThursday3.ceil(d2);
  return pad2(d2.getUTCFullYear() % 1e4, p3, 4);
}
function formatUTCZone2() {
  return "+0000";
}
function formatLiteralPercent2() {
  return "%";
}
function formatUnixTimestamp2(d2) {
  return +d2;
}
function formatUnixTimestampSeconds2(d2) {
  return Math.floor(+d2 / 1e3);
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time-format/src/defaultLocale.js
var locale4;
var timeFormat2;
var timeParse2;
var utcFormat2;
var utcParse2;
defaultLocale4({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale4(definition) {
  locale4 = formatLocale2(definition);
  timeFormat2 = locale4.format;
  timeParse2 = locale4.parse;
  utcFormat2 = locale4.utcFormat;
  utcParse2 = locale4.utcParse;
  return locale4;
}

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time-format/src/isoFormat.js
var isoSpecifier2 = "%Y-%m-%dT%H:%M:%S.%LZ";
function formatIsoNative2(date) {
  return date.toISOString();
}
var formatIso2 = Date.prototype.toISOString ? formatIsoNative2 : utcFormat2(isoSpecifier2);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/node_modules/d3-scale/node_modules/d3-time-format/src/isoParse.js
function parseIsoNative2(string) {
  var date = new Date(string);
  return isNaN(date) ? null : date;
}
var parseIso2 = +/* @__PURE__ */ new Date("2000-01-01T00:00:00.000Z") ? parseIsoNative2 : utcParse2(isoSpecifier2);

// node_modules/@nivo/treemap/node_modules/@nivo/colors/dist/nivo-colors.es.js
function qe() {
  return qe = Object.assign ? Object.assign.bind() : function(e7) {
    for (var r5 = 1; r5 < arguments.length; r5++) {
      var n5 = arguments[r5];
      for (var t7 in n5) Object.prototype.hasOwnProperty.call(n5, t7) && (e7[t7] = n5[t7]);
    }
    return e7;
  }, qe.apply(this, arguments);
}
function Ce(e7, r5) {
  (null == r5 || r5 > e7.length) && (r5 = e7.length);
  for (var n5 = 0, t7 = new Array(r5); n5 < r5; n5++) t7[n5] = e7[n5];
  return t7;
}
function Ge(e7, r5) {
  var n5 = "undefined" != typeof Symbol && e7[Symbol.iterator] || e7["@@iterator"];
  if (n5) return (n5 = n5.call(e7)).next.bind(n5);
  if (Array.isArray(e7) || (n5 = function(e8, r6) {
    if (e8) {
      if ("string" == typeof e8) return Ce(e8, r6);
      var n6 = Object.prototype.toString.call(e8).slice(8, -1);
      return "Object" === n6 && e8.constructor && (n6 = e8.constructor.name), "Map" === n6 || "Set" === n6 ? Array.from(e8) : "Arguments" === n6 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n6) ? Ce(e8, r6) : void 0;
    }
  }(e7)) || r5 && e7 && "number" == typeof e7.length) {
    n5 && (e7 = n5);
    var t7 = 0;
    return function() {
      return t7 >= e7.length ? { done: true } : { done: false, value: e7[t7++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
var Re = { nivo: ["#e8c1a0", "#f47560", "#f1e15b", "#e8a838", "#61cdbb", "#97e3d5"], category10: category10_default2, accent: Accent_default2, dark2: Dark2_default2, paired: Paired_default2, pastel1: Pastel1_default2, pastel2: Pastel2_default2, set1: Set1_default2, set2: Set2_default2, set3: Set3_default2, tableau10: Tableau10_default2 };
var Ve = Object.keys(Re);
var Pe = { brown_blueGreen: scheme28, purpleRed_green: scheme29, pink_yellowGreen: scheme30, purple_orange: scheme31, red_blue: scheme32, red_grey: scheme33, red_yellow_blue: scheme34, red_yellow_green: scheme35, spectral: scheme36 };
var Te = Object.keys(Pe);
var Ue = { brown_blueGreen: BrBG_default2, purpleRed_green: PRGn_default2, pink_yellowGreen: PiYG_default2, purple_orange: PuOr_default2, red_blue: RdBu_default2, red_grey: RdGy_default2, red_yellow_blue: RdYlBu_default2, red_yellow_green: RdYlGn_default2, spectral: Spectral_default2 };
var De = { blues: scheme49, greens: scheme50, greys: scheme51, oranges: scheme54, purples: scheme52, reds: scheme53, blue_green: scheme37, blue_purple: scheme38, green_blue: scheme39, orange_red: scheme40, purple_blue_green: scheme41, purple_blue: scheme42, purple_red: scheme43, red_purple: scheme44, yellow_green_blue: scheme45, yellow_green: scheme46, yellow_orange_brown: scheme47, yellow_orange_red: scheme48 };
var Me = Object.keys(De);
var $e = { blues: Blues_default2, greens: Greens_default2, greys: Greys_default2, oranges: Oranges_default2, purples: Purples_default2, reds: Reds_default2, turbo: turbo_default2, viridis: viridis_default2, inferno: inferno2, magma: magma2, plasma: plasma2, cividis: cividis_default2, warm: warm2, cool: cool2, cubehelixDefault: cubehelix_default4, blue_green: BuGn_default2, blue_purple: BuPu_default2, green_blue: GnBu_default2, orange_red: OrRd_default2, purple_blue_green: PuBuGn_default2, purple_blue: PuBu_default2, purple_red: PuRd_default2, red_purple: RdPu_default2, yellow_green_blue: YlGnBu_default2, yellow_green: YlGn_default2, yellow_orange_brown: YlOrBr_default2, yellow_orange_red: YlOrRd_default2 };
var Be = qe({}, Re, Pe, De);
var Fe = Object.keys(Be);
var He = function(e7) {
  return Ve.includes(e7);
};
var Je = function(e7) {
  return Te.includes(e7);
};
var Ke = function(e7) {
  return Me.includes(e7);
};
var Le = { rainbow: rainbow_default2, sinebow: sinebow_default2 };
var Ne = qe({}, Ue, $e, Le);
var Qe2 = Object.keys(Ne);
var We = function(e7, r5) {
  if ("function" == typeof e7) return e7;
  if ((0, import_isPlainObject2.default)(e7)) {
    if (function(e8) {
      return void 0 !== e8.theme;
    }(e7)) {
      if (void 0 === r5) throw new Error("Unable to use color from theme as no theme was provided");
      var n5 = (0, import_get2.default)(r5, e7.theme);
      if (void 0 === n5) throw new Error("Color from theme is undefined at path: '" + e7.theme + "'");
      return function() {
        return n5;
      };
    }
    if (function(e8) {
      return void 0 !== e8.from;
    }(e7)) {
      var t7 = function(r6) {
        return (0, import_get2.default)(r6, e7.from);
      };
      if (Array.isArray(e7.modifiers)) {
        for (var o5, i5 = [], u2 = function() {
          var e8 = o5.value, r6 = e8[0], n6 = e8[1];
          if ("brighter" === r6) i5.push(function(e9) {
            return e9.brighter(n6);
          });
          else if ("darker" === r6) i5.push(function(e9) {
            return e9.darker(n6);
          });
          else {
            if ("opacity" !== r6) throw new Error("Invalid color modifier: '" + r6 + "', must be one of: 'brighter', 'darker', 'opacity'");
            i5.push(function(e9) {
              return e9.opacity = n6, e9;
            });
          }
        }, a4 = Ge(e7.modifiers); !(o5 = a4()).done; ) u2();
        return 0 === i5.length ? t7 : function(e8) {
          return i5.reduce(function(e9, r6) {
            return r6(e9);
          }, rgb3(t7(e8))).toString();
        };
      }
      return t7;
    }
    throw new Error("Invalid color spec, you should either specify 'theme' or 'from' when using a config object");
  }
  return function() {
    return e7;
  };
};
var Xe = function(e7, r5) {
  return (0, import_react17.useMemo)(function() {
    return We(e7, r5);
  }, [e7, r5]);
};
var Ye = import_prop_types2.default.oneOfType([import_prop_types2.default.string, import_prop_types2.default.func, import_prop_types2.default.shape({ theme: import_prop_types2.default.string.isRequired }), import_prop_types2.default.shape({ from: import_prop_types2.default.string.isRequired, modifiers: import_prop_types2.default.arrayOf(import_prop_types2.default.array) })]);
var fr = function(e7, r5) {
  if ("function" == typeof e7) return e7;
  var n5 = "function" == typeof r5 ? r5 : function(e8) {
    return (0, import_get2.default)(e8, r5);
  };
  if (Array.isArray(e7)) {
    var t7 = ordinal2(e7), o5 = function(e8) {
      return t7(n5(e8));
    };
    return o5.scale = t7, o5;
  }
  if ((0, import_isPlainObject2.default)(e7)) {
    if (function(e8) {
      return void 0 !== e8.datum;
    }(e7)) return function(r6) {
      return (0, import_get2.default)(r6, e7.datum);
    };
    if (function(e8) {
      return void 0 !== e8.scheme;
    }(e7)) {
      if (He(e7.scheme)) {
        var i5 = ordinal2(Be[e7.scheme]), u2 = function(e8) {
          return i5(n5(e8));
        };
        return u2.scale = i5, u2;
      }
      if (Je(e7.scheme)) {
        if (void 0 !== e7.size && (e7.size < 3 || e7.size > 11)) throw new Error("Invalid size '" + e7.size + "' for diverging color scheme '" + e7.scheme + "', must be between 3~11");
        var a4 = ordinal2(Be[e7.scheme][e7.size || 11]), l4 = function(e8) {
          return a4(n5(e8));
        };
        return l4.scale = a4, l4;
      }
      if (Ke(e7.scheme)) {
        if (void 0 !== e7.size && (e7.size < 3 || e7.size > 9)) throw new Error("Invalid size '" + e7.size + "' for sequential color scheme '" + e7.scheme + "', must be between 3~9");
        var c10 = ordinal2(Be[e7.scheme][e7.size || 9]), s3 = function(e8) {
          return c10(n5(e8));
        };
        return s3.scale = c10, s3;
      }
    }
    throw new Error("Invalid colors, when using an object, you should either pass a 'datum' or a 'scheme' property");
  }
  return function() {
    return e7;
  };
};
var pr = function(e7, r5) {
  return (0, import_react17.useMemo)(function() {
    return fr(e7, r5);
  }, [e7, r5]);
};

// node_modules/@nivo/treemap/node_modules/@nivo/tooltip/dist/nivo-tooltip.es.js
var import_react32 = __toESM(require_react());

// node_modules/@nivo/treemap/node_modules/@react-spring/web/node_modules/@react-spring/shared/node_modules/@react-spring/rafz/dist/react-spring_rafz.modern.mjs
var updateQueue2 = makeQueue2();
var raf2 = (fn2) => schedule2(fn2, updateQueue2);
var writeQueue2 = makeQueue2();
raf2.write = (fn2) => schedule2(fn2, writeQueue2);
var onStartQueue2 = makeQueue2();
raf2.onStart = (fn2) => schedule2(fn2, onStartQueue2);
var onFrameQueue2 = makeQueue2();
raf2.onFrame = (fn2) => schedule2(fn2, onFrameQueue2);
var onFinishQueue2 = makeQueue2();
raf2.onFinish = (fn2) => schedule2(fn2, onFinishQueue2);
var timeouts2 = [];
raf2.setTimeout = (handler, ms) => {
  const time3 = raf2.now() + ms;
  const cancel = () => {
    const i5 = timeouts2.findIndex((t7) => t7.cancel == cancel);
    if (~i5)
      timeouts2.splice(i5, 1);
    pendingCount2 -= ~i5 ? 1 : 0;
  };
  const timeout = { time: time3, handler, cancel };
  timeouts2.splice(findTimeout2(time3), 0, timeout);
  pendingCount2 += 1;
  start2();
  return timeout;
};
var findTimeout2 = (time3) => ~(~timeouts2.findIndex((t7) => t7.time > time3) || ~timeouts2.length);
raf2.cancel = (fn2) => {
  onStartQueue2.delete(fn2);
  onFrameQueue2.delete(fn2);
  onFinishQueue2.delete(fn2);
  updateQueue2.delete(fn2);
  writeQueue2.delete(fn2);
};
raf2.sync = (fn2) => {
  sync2 = true;
  raf2.batchedUpdates(fn2);
  sync2 = false;
};
raf2.throttle = (fn2) => {
  let lastArgs;
  function queuedFn() {
    try {
      fn2(...lastArgs);
    } finally {
      lastArgs = null;
    }
  }
  function throttled(...args) {
    lastArgs = args;
    raf2.onStart(queuedFn);
  }
  throttled.handler = fn2;
  throttled.cancel = () => {
    onStartQueue2.delete(queuedFn);
    lastArgs = null;
  };
  return throttled;
};
var nativeRaf2 = typeof window != "undefined" ? window.requestAnimationFrame : (
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  () => {
  }
);
raf2.use = (impl) => nativeRaf2 = impl;
raf2.now = typeof performance != "undefined" ? () => performance.now() : Date.now;
raf2.batchedUpdates = (fn2) => fn2();
raf2.catch = console.error;
raf2.frameLoop = "always";
raf2.advance = () => {
  if (raf2.frameLoop !== "demand") {
    console.warn(
      "Cannot call the manual advancement of rafz whilst frameLoop is not set as demand"
    );
  } else {
    update3();
  }
};
var ts2 = -1;
var pendingCount2 = 0;
var sync2 = false;
function schedule2(fn2, queue) {
  if (sync2) {
    queue.delete(fn2);
    fn2(0);
  } else {
    queue.add(fn2);
    start2();
  }
}
function start2() {
  if (ts2 < 0) {
    ts2 = 0;
    if (raf2.frameLoop !== "demand") {
      nativeRaf2(loop2);
    }
  }
}
function stop2() {
  ts2 = -1;
}
function loop2() {
  if (~ts2) {
    nativeRaf2(loop2);
    raf2.batchedUpdates(update3);
  }
}
function update3() {
  const prevTs = ts2;
  ts2 = raf2.now();
  const count5 = findTimeout2(ts2);
  if (count5) {
    eachSafely2(timeouts2.splice(0, count5), (t7) => t7.handler());
    pendingCount2 -= count5;
  }
  if (!pendingCount2) {
    stop2();
    return;
  }
  onStartQueue2.flush();
  updateQueue2.flush(prevTs ? Math.min(64, ts2 - prevTs) : 16.667);
  onFrameQueue2.flush();
  writeQueue2.flush();
  onFinishQueue2.flush();
}
function makeQueue2() {
  let next = /* @__PURE__ */ new Set();
  let current = next;
  return {
    add(fn2) {
      pendingCount2 += current == next && !next.has(fn2) ? 1 : 0;
      next.add(fn2);
    },
    delete(fn2) {
      pendingCount2 -= current == next && next.has(fn2) ? 1 : 0;
      return next.delete(fn2);
    },
    flush(arg) {
      if (current.size) {
        next = /* @__PURE__ */ new Set();
        pendingCount2 -= current.size;
        eachSafely2(current, (fn2) => fn2(arg) && next.add(fn2));
        pendingCount2 += next.size;
        current = next;
      }
    }
  };
}
function eachSafely2(values, each3) {
  values.forEach((value) => {
    try {
      each3(value);
    } catch (e7) {
      raf2.catch(e7);
    }
  });
}

// node_modules/@nivo/treemap/node_modules/@react-spring/web/node_modules/@react-spring/shared/dist/react-spring_shared.modern.mjs
var import_react18 = __toESM(require_react(), 1);
var import_react19 = __toESM(require_react(), 1);
var import_react20 = __toESM(require_react(), 1);
var import_react21 = __toESM(require_react(), 1);
var import_react22 = __toESM(require_react(), 1);
var import_react23 = __toESM(require_react(), 1);
var import_react24 = __toESM(require_react(), 1);
var import_react25 = __toESM(require_react(), 1);
var __defProp2 = Object.defineProperty;
var __export2 = (target, all) => {
  for (var name in all)
    __defProp2(target, name, { get: all[name], enumerable: true });
};
var globals_exports2 = {};
__export2(globals_exports2, {
  assign: () => assign2,
  colors: () => colors3,
  createStringInterpolator: () => createStringInterpolator3,
  skipAnimation: () => skipAnimation2,
  to: () => to3,
  willAdvance: () => willAdvance2
});
function noop2() {
}
var defineHidden2 = (obj, key, value) => Object.defineProperty(obj, key, { value, writable: true, configurable: true });
var is2 = {
  arr: Array.isArray,
  obj: (a4) => !!a4 && a4.constructor.name === "Object",
  fun: (a4) => typeof a4 === "function",
  str: (a4) => typeof a4 === "string",
  num: (a4) => typeof a4 === "number",
  und: (a4) => a4 === void 0
};
function isEqual2(a4, b3) {
  if (is2.arr(a4)) {
    if (!is2.arr(b3) || a4.length !== b3.length)
      return false;
    for (let i5 = 0; i5 < a4.length; i5++) {
      if (a4[i5] !== b3[i5])
        return false;
    }
    return true;
  }
  return a4 === b3;
}
var each2 = (obj, fn2) => obj.forEach(fn2);
function eachProp2(obj, fn2, ctx3) {
  if (is2.arr(obj)) {
    for (let i5 = 0; i5 < obj.length; i5++) {
      fn2.call(ctx3, obj[i5], `${i5}`);
    }
    return;
  }
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      fn2.call(ctx3, obj[key], key);
    }
  }
}
var toArray2 = (a4) => is2.und(a4) ? [] : is2.arr(a4) ? a4 : [a4];
function flush2(queue, iterator) {
  if (queue.size) {
    const items = Array.from(queue);
    queue.clear();
    each2(items, iterator);
  }
}
var flushCalls2 = (queue, ...args) => flush2(queue, (fn2) => fn2(...args));
var isSSR2 = () => typeof window === "undefined" || !window.navigator || /ServerSideRendering|^Deno\//.test(window.navigator.userAgent);
var createStringInterpolator3;
var to3;
var colors3 = null;
var skipAnimation2 = false;
var willAdvance2 = noop2;
var assign2 = (globals) => {
  if (globals.to)
    to3 = globals.to;
  if (globals.now)
    raf2.now = globals.now;
  if (globals.colors !== void 0)
    colors3 = globals.colors;
  if (globals.skipAnimation != null)
    skipAnimation2 = globals.skipAnimation;
  if (globals.createStringInterpolator)
    createStringInterpolator3 = globals.createStringInterpolator;
  if (globals.requestAnimationFrame)
    raf2.use(globals.requestAnimationFrame);
  if (globals.batchedUpdates)
    raf2.batchedUpdates = globals.batchedUpdates;
  if (globals.willAdvance)
    willAdvance2 = globals.willAdvance;
  if (globals.frameLoop)
    raf2.frameLoop = globals.frameLoop;
};
var startQueue2 = /* @__PURE__ */ new Set();
var currentFrame2 = [];
var prevFrame2 = [];
var priority2 = 0;
var frameLoop2 = {
  get idle() {
    return !startQueue2.size && !currentFrame2.length;
  },
  /** Advance the given animation on every frame until idle. */
  start(animation) {
    if (priority2 > animation.priority) {
      startQueue2.add(animation);
      raf2.onStart(flushStartQueue2);
    } else {
      startSafely2(animation);
      raf2(advance2);
    }
  },
  /** Advance all animations by the given time. */
  advance: advance2,
  /** Call this when an animation's priority changes. */
  sort(animation) {
    if (priority2) {
      raf2.onFrame(() => frameLoop2.sort(animation));
    } else {
      const prevIndex = currentFrame2.indexOf(animation);
      if (~prevIndex) {
        currentFrame2.splice(prevIndex, 1);
        startUnsafely2(animation);
      }
    }
  },
  /**
   * Clear all animations. For testing purposes.
   *
   * ☠️ Never call this from within the frameloop.
   */
  clear() {
    currentFrame2 = [];
    startQueue2.clear();
  }
};
function flushStartQueue2() {
  startQueue2.forEach(startSafely2);
  startQueue2.clear();
  raf2(advance2);
}
function startSafely2(animation) {
  if (!currentFrame2.includes(animation))
    startUnsafely2(animation);
}
function startUnsafely2(animation) {
  currentFrame2.splice(
    findIndex2(currentFrame2, (other) => other.priority > animation.priority),
    0,
    animation
  );
}
function advance2(dt2) {
  const nextFrame = prevFrame2;
  for (let i5 = 0; i5 < currentFrame2.length; i5++) {
    const animation = currentFrame2[i5];
    priority2 = animation.priority;
    if (!animation.idle) {
      willAdvance2(animation);
      animation.advance(dt2);
      if (!animation.idle) {
        nextFrame.push(animation);
      }
    }
  }
  priority2 = 0;
  prevFrame2 = currentFrame2;
  prevFrame2.length = 0;
  currentFrame2 = nextFrame;
  return currentFrame2.length > 0;
}
function findIndex2(arr, test) {
  const index4 = arr.findIndex(test);
  return index4 < 0 ? arr.length : index4;
}
var clamp2 = (min5, max5, v4) => Math.min(Math.max(v4, min5), max5);
var colors22 = {
  transparent: 0,
  aliceblue: 4042850303,
  antiquewhite: 4209760255,
  aqua: 16777215,
  aquamarine: 2147472639,
  azure: 4043309055,
  beige: 4126530815,
  bisque: 4293182719,
  black: 255,
  blanchedalmond: 4293643775,
  blue: 65535,
  blueviolet: 2318131967,
  brown: 2771004159,
  burlywood: 3736635391,
  burntsienna: 3934150143,
  cadetblue: 1604231423,
  chartreuse: 2147418367,
  chocolate: 3530104575,
  coral: 4286533887,
  cornflowerblue: 1687547391,
  cornsilk: 4294499583,
  crimson: 3692313855,
  cyan: 16777215,
  darkblue: 35839,
  darkcyan: 9145343,
  darkgoldenrod: 3095792639,
  darkgray: 2846468607,
  darkgreen: 6553855,
  darkgrey: 2846468607,
  darkkhaki: 3182914559,
  darkmagenta: 2332068863,
  darkolivegreen: 1433087999,
  darkorange: 4287365375,
  darkorchid: 2570243327,
  darkred: 2332033279,
  darksalmon: 3918953215,
  darkseagreen: 2411499519,
  darkslateblue: 1211993087,
  darkslategray: 793726975,
  darkslategrey: 793726975,
  darkturquoise: 13554175,
  darkviolet: 2483082239,
  deeppink: 4279538687,
  deepskyblue: 12582911,
  dimgray: 1768516095,
  dimgrey: 1768516095,
  dodgerblue: 512819199,
  firebrick: 2988581631,
  floralwhite: 4294635775,
  forestgreen: 579543807,
  fuchsia: 4278255615,
  gainsboro: 3705462015,
  ghostwhite: 4177068031,
  gold: 4292280575,
  goldenrod: 3668254975,
  gray: 2155905279,
  green: 8388863,
  greenyellow: 2919182335,
  grey: 2155905279,
  honeydew: 4043305215,
  hotpink: 4285117695,
  indianred: 3445382399,
  indigo: 1258324735,
  ivory: 4294963455,
  khaki: 4041641215,
  lavender: 3873897215,
  lavenderblush: 4293981695,
  lawngreen: 2096890111,
  lemonchiffon: 4294626815,
  lightblue: 2916673279,
  lightcoral: 4034953471,
  lightcyan: 3774873599,
  lightgoldenrodyellow: 4210742015,
  lightgray: 3553874943,
  lightgreen: 2431553791,
  lightgrey: 3553874943,
  lightpink: 4290167295,
  lightsalmon: 4288707327,
  lightseagreen: 548580095,
  lightskyblue: 2278488831,
  lightslategray: 2005441023,
  lightslategrey: 2005441023,
  lightsteelblue: 2965692159,
  lightyellow: 4294959359,
  lime: 16711935,
  limegreen: 852308735,
  linen: 4210091775,
  magenta: 4278255615,
  maroon: 2147483903,
  mediumaquamarine: 1724754687,
  mediumblue: 52735,
  mediumorchid: 3126187007,
  mediumpurple: 2473647103,
  mediumseagreen: 1018393087,
  mediumslateblue: 2070474495,
  mediumspringgreen: 16423679,
  mediumturquoise: 1221709055,
  mediumvioletred: 3340076543,
  midnightblue: 421097727,
  mintcream: 4127193855,
  mistyrose: 4293190143,
  moccasin: 4293178879,
  navajowhite: 4292783615,
  navy: 33023,
  oldlace: 4260751103,
  olive: 2155872511,
  olivedrab: 1804477439,
  orange: 4289003775,
  orangered: 4282712319,
  orchid: 3664828159,
  palegoldenrod: 4008225535,
  palegreen: 2566625535,
  paleturquoise: 2951671551,
  palevioletred: 3681588223,
  papayawhip: 4293907967,
  peachpuff: 4292524543,
  peru: 3448061951,
  pink: 4290825215,
  plum: 3718307327,
  powderblue: 2967529215,
  purple: 2147516671,
  rebeccapurple: 1714657791,
  red: 4278190335,
  rosybrown: 3163525119,
  royalblue: 1097458175,
  saddlebrown: 2336560127,
  salmon: 4202722047,
  sandybrown: 4104413439,
  seagreen: 780883967,
  seashell: 4294307583,
  sienna: 2689740287,
  silver: 3233857791,
  skyblue: 2278484991,
  slateblue: 1784335871,
  slategray: 1887473919,
  slategrey: 1887473919,
  snow: 4294638335,
  springgreen: 16744447,
  steelblue: 1182971135,
  tan: 3535047935,
  teal: 8421631,
  thistle: 3636451583,
  tomato: 4284696575,
  turquoise: 1088475391,
  violet: 4001558271,
  wheat: 4125012991,
  white: 4294967295,
  whitesmoke: 4126537215,
  yellow: 4294902015,
  yellowgreen: 2597139199
};
var NUMBER2 = "[-+]?\\d*\\.?\\d+";
var PERCENTAGE2 = NUMBER2 + "%";
function call2(...parts) {
  return "\\(\\s*(" + parts.join(")\\s*,\\s*(") + ")\\s*\\)";
}
var rgb4 = new RegExp("rgb" + call2(NUMBER2, NUMBER2, NUMBER2));
var rgba4 = new RegExp("rgba" + call2(NUMBER2, NUMBER2, NUMBER2, NUMBER2));
var hsl7 = new RegExp("hsl" + call2(NUMBER2, PERCENTAGE2, PERCENTAGE2));
var hsla4 = new RegExp(
  "hsla" + call2(NUMBER2, PERCENTAGE2, PERCENTAGE2, NUMBER2)
);
var hex32 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex42 = /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/;
var hex62 = /^#([0-9a-fA-F]{6})$/;
var hex82 = /^#([0-9a-fA-F]{8})$/;
function normalizeColor2(color3) {
  let match;
  if (typeof color3 === "number") {
    return color3 >>> 0 === color3 && color3 >= 0 && color3 <= 4294967295 ? color3 : null;
  }
  if (match = hex62.exec(color3))
    return parseInt(match[1] + "ff", 16) >>> 0;
  if (colors3 && colors3[color3] !== void 0) {
    return colors3[color3];
  }
  if (match = rgb4.exec(color3)) {
    return (parse2552(match[1]) << 24 | // r
    parse2552(match[2]) << 16 | // g
    parse2552(match[3]) << 8 | // b
    255) >>> // a
    0;
  }
  if (match = rgba4.exec(color3)) {
    return (parse2552(match[1]) << 24 | // r
    parse2552(match[2]) << 16 | // g
    parse2552(match[3]) << 8 | // b
    parse12(match[4])) >>> // a
    0;
  }
  if (match = hex32.exec(color3)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      "ff",
      // a
      16
    ) >>> 0;
  }
  if (match = hex82.exec(color3))
    return parseInt(match[1], 16) >>> 0;
  if (match = hex42.exec(color3)) {
    return parseInt(
      match[1] + match[1] + // r
      match[2] + match[2] + // g
      match[3] + match[3] + // b
      match[4] + match[4],
      // a
      16
    ) >>> 0;
  }
  if (match = hsl7.exec(color3)) {
    return (hslToRgb2(
      parse3602(match[1]),
      // h
      parsePercentage2(match[2]),
      // s
      parsePercentage2(match[3])
      // l
    ) | 255) >>> // a
    0;
  }
  if (match = hsla4.exec(color3)) {
    return (hslToRgb2(
      parse3602(match[1]),
      // h
      parsePercentage2(match[2]),
      // s
      parsePercentage2(match[3])
      // l
    ) | parse12(match[4])) >>> // a
    0;
  }
  return null;
}
function hue2rgb2(p3, q3, t7) {
  if (t7 < 0)
    t7 += 1;
  if (t7 > 1)
    t7 -= 1;
  if (t7 < 1 / 6)
    return p3 + (q3 - p3) * 6 * t7;
  if (t7 < 1 / 2)
    return q3;
  if (t7 < 2 / 3)
    return p3 + (q3 - p3) * (2 / 3 - t7) * 6;
  return p3;
}
function hslToRgb2(h, s3, l4) {
  const q3 = l4 < 0.5 ? l4 * (1 + s3) : l4 + s3 - l4 * s3;
  const p3 = 2 * l4 - q3;
  const r5 = hue2rgb2(p3, q3, h + 1 / 3);
  const g3 = hue2rgb2(p3, q3, h);
  const b3 = hue2rgb2(p3, q3, h - 1 / 3);
  return Math.round(r5 * 255) << 24 | Math.round(g3 * 255) << 16 | Math.round(b3 * 255) << 8;
}
function parse2552(str) {
  const int = parseInt(str, 10);
  if (int < 0)
    return 0;
  if (int > 255)
    return 255;
  return int;
}
function parse3602(str) {
  const int = parseFloat(str);
  return (int % 360 + 360) % 360 / 360;
}
function parse12(str) {
  const num = parseFloat(str);
  if (num < 0)
    return 0;
  if (num > 1)
    return 255;
  return Math.round(num * 255);
}
function parsePercentage2(str) {
  const int = parseFloat(str);
  if (int < 0)
    return 0;
  if (int > 100)
    return 1;
  return int / 100;
}
function colorToRgba2(input) {
  let int32Color = normalizeColor2(input);
  if (int32Color === null)
    return input;
  int32Color = int32Color || 0;
  const r5 = (int32Color & 4278190080) >>> 24;
  const g3 = (int32Color & 16711680) >>> 16;
  const b3 = (int32Color & 65280) >>> 8;
  const a4 = (int32Color & 255) / 255;
  return `rgba(${r5}, ${g3}, ${b3}, ${a4})`;
}
var createInterpolator2 = (range3, output, extrapolate) => {
  if (is2.fun(range3)) {
    return range3;
  }
  if (is2.arr(range3)) {
    return createInterpolator2({
      range: range3,
      output,
      extrapolate
    });
  }
  if (is2.str(range3.output[0])) {
    return createStringInterpolator3(range3);
  }
  const config3 = range3;
  const outputRange = config3.output;
  const inputRange = config3.range || [0, 1];
  const extrapolateLeft = config3.extrapolateLeft || config3.extrapolate || "extend";
  const extrapolateRight = config3.extrapolateRight || config3.extrapolate || "extend";
  const easing = config3.easing || ((t7) => t7);
  return (input) => {
    const range22 = findRange2(input, inputRange);
    return interpolate2(
      input,
      inputRange[range22],
      inputRange[range22 + 1],
      outputRange[range22],
      outputRange[range22 + 1],
      easing,
      extrapolateLeft,
      extrapolateRight,
      config3.map
    );
  };
};
function interpolate2(input, inputMin, inputMax, outputMin, outputMax, easing, extrapolateLeft, extrapolateRight, map9) {
  let result = map9 ? map9(input) : input;
  if (result < inputMin) {
    if (extrapolateLeft === "identity")
      return result;
    else if (extrapolateLeft === "clamp")
      result = inputMin;
  }
  if (result > inputMax) {
    if (extrapolateRight === "identity")
      return result;
    else if (extrapolateRight === "clamp")
      result = inputMax;
  }
  if (outputMin === outputMax)
    return outputMin;
  if (inputMin === inputMax)
    return input <= inputMin ? outputMin : outputMax;
  if (inputMin === -Infinity)
    result = -result;
  else if (inputMax === Infinity)
    result = result - inputMin;
  else
    result = (result - inputMin) / (inputMax - inputMin);
  result = easing(result);
  if (outputMin === -Infinity)
    result = -result;
  else if (outputMax === Infinity)
    result = result + outputMin;
  else
    result = result * (outputMax - outputMin) + outputMin;
  return result;
}
function findRange2(input, inputRange) {
  for (var i5 = 1; i5 < inputRange.length - 1; ++i5)
    if (inputRange[i5] >= input)
      break;
  return i5 - 1;
}
var steps2 = (steps22, direction = "end") => (progress2) => {
  progress2 = direction === "end" ? Math.min(progress2, 0.999) : Math.max(progress2, 1e-3);
  const expanded = progress2 * steps22;
  const rounded = direction === "end" ? Math.floor(expanded) : Math.ceil(expanded);
  return clamp2(0, 1, rounded / steps22);
};
var c12 = 1.70158;
var c22 = c12 * 1.525;
var c32 = c12 + 1;
var c42 = 2 * Math.PI / 3;
var c52 = 2 * Math.PI / 4.5;
var bounceOut2 = (x4) => {
  const n1 = 7.5625;
  const d1 = 2.75;
  if (x4 < 1 / d1) {
    return n1 * x4 * x4;
  } else if (x4 < 2 / d1) {
    return n1 * (x4 -= 1.5 / d1) * x4 + 0.75;
  } else if (x4 < 2.5 / d1) {
    return n1 * (x4 -= 2.25 / d1) * x4 + 0.9375;
  } else {
    return n1 * (x4 -= 2.625 / d1) * x4 + 0.984375;
  }
};
var easings2 = {
  linear: (x4) => x4,
  easeInQuad: (x4) => x4 * x4,
  easeOutQuad: (x4) => 1 - (1 - x4) * (1 - x4),
  easeInOutQuad: (x4) => x4 < 0.5 ? 2 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 2) / 2,
  easeInCubic: (x4) => x4 * x4 * x4,
  easeOutCubic: (x4) => 1 - Math.pow(1 - x4, 3),
  easeInOutCubic: (x4) => x4 < 0.5 ? 4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 3) / 2,
  easeInQuart: (x4) => x4 * x4 * x4 * x4,
  easeOutQuart: (x4) => 1 - Math.pow(1 - x4, 4),
  easeInOutQuart: (x4) => x4 < 0.5 ? 8 * x4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 4) / 2,
  easeInQuint: (x4) => x4 * x4 * x4 * x4 * x4,
  easeOutQuint: (x4) => 1 - Math.pow(1 - x4, 5),
  easeInOutQuint: (x4) => x4 < 0.5 ? 16 * x4 * x4 * x4 * x4 * x4 : 1 - Math.pow(-2 * x4 + 2, 5) / 2,
  easeInSine: (x4) => 1 - Math.cos(x4 * Math.PI / 2),
  easeOutSine: (x4) => Math.sin(x4 * Math.PI / 2),
  easeInOutSine: (x4) => -(Math.cos(Math.PI * x4) - 1) / 2,
  easeInExpo: (x4) => x4 === 0 ? 0 : Math.pow(2, 10 * x4 - 10),
  easeOutExpo: (x4) => x4 === 1 ? 1 : 1 - Math.pow(2, -10 * x4),
  easeInOutExpo: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : x4 < 0.5 ? Math.pow(2, 20 * x4 - 10) / 2 : (2 - Math.pow(2, -20 * x4 + 10)) / 2,
  easeInCirc: (x4) => 1 - Math.sqrt(1 - Math.pow(x4, 2)),
  easeOutCirc: (x4) => Math.sqrt(1 - Math.pow(x4 - 1, 2)),
  easeInOutCirc: (x4) => x4 < 0.5 ? (1 - Math.sqrt(1 - Math.pow(2 * x4, 2))) / 2 : (Math.sqrt(1 - Math.pow(-2 * x4 + 2, 2)) + 1) / 2,
  easeInBack: (x4) => c32 * x4 * x4 * x4 - c12 * x4 * x4,
  easeOutBack: (x4) => 1 + c32 * Math.pow(x4 - 1, 3) + c12 * Math.pow(x4 - 1, 2),
  easeInOutBack: (x4) => x4 < 0.5 ? Math.pow(2 * x4, 2) * ((c22 + 1) * 2 * x4 - c22) / 2 : (Math.pow(2 * x4 - 2, 2) * ((c22 + 1) * (x4 * 2 - 2) + c22) + 2) / 2,
  easeInElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : -Math.pow(2, 10 * x4 - 10) * Math.sin((x4 * 10 - 10.75) * c42),
  easeOutElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : Math.pow(2, -10 * x4) * Math.sin((x4 * 10 - 0.75) * c42) + 1,
  easeInOutElastic: (x4) => x4 === 0 ? 0 : x4 === 1 ? 1 : x4 < 0.5 ? -(Math.pow(2, 20 * x4 - 10) * Math.sin((20 * x4 - 11.125) * c52)) / 2 : Math.pow(2, -20 * x4 + 10) * Math.sin((20 * x4 - 11.125) * c52) / 2 + 1,
  easeInBounce: (x4) => 1 - bounceOut2(1 - x4),
  easeOutBounce: bounceOut2,
  easeInOutBounce: (x4) => x4 < 0.5 ? (1 - bounceOut2(1 - 2 * x4)) / 2 : (1 + bounceOut2(2 * x4 - 1)) / 2,
  steps: steps2
};
var $get2 = Symbol.for("FluidValue.get");
var $observers2 = Symbol.for("FluidValue.observers");
var hasFluidValue2 = (arg) => Boolean(arg && arg[$get2]);
var getFluidValue2 = (arg) => arg && arg[$get2] ? arg[$get2]() : arg;
var getFluidObservers2 = (target) => target[$observers2] || null;
function callFluidObserver2(observer2, event) {
  if (observer2.eventObserved) {
    observer2.eventObserved(event);
  } else {
    observer2(event);
  }
}
function callFluidObservers2(target, event) {
  const observers = target[$observers2];
  if (observers) {
    observers.forEach((observer2) => {
      callFluidObserver2(observer2, event);
    });
  }
}
var FluidValue2 = class {
  constructor(get) {
    if (!get && !(get = this.get)) {
      throw Error("Unknown getter");
    }
    setFluidGetter2(this, get);
  }
};
var setFluidGetter2 = (target, get) => setHidden2(target, $get2, get);
function addFluidObserver2(target, observer2) {
  if (target[$get2]) {
    let observers = target[$observers2];
    if (!observers) {
      setHidden2(target, $observers2, observers = /* @__PURE__ */ new Set());
    }
    if (!observers.has(observer2)) {
      observers.add(observer2);
      if (target.observerAdded) {
        target.observerAdded(observers.size, observer2);
      }
    }
  }
  return observer2;
}
function removeFluidObserver2(target, observer2) {
  const observers = target[$observers2];
  if (observers && observers.has(observer2)) {
    const count5 = observers.size - 1;
    if (count5) {
      observers.delete(observer2);
    } else {
      target[$observers2] = null;
    }
    if (target.observerRemoved) {
      target.observerRemoved(count5, observer2);
    }
  }
}
var setHidden2 = (target, key, value) => Object.defineProperty(target, key, {
  value,
  writable: true,
  configurable: true
});
var numberRegex2 = /[+\-]?(?:0|[1-9]\d*)(?:\.\d*)?(?:[eE][+\-]?\d+)?/g;
var colorRegex2 = /(#(?:[0-9a-f]{2}){2,4}|(#[0-9a-f]{3})|(rgb|hsl)a?\((-?\d+%?[,\s]+){2,3}\s*[\d\.]+%?\))/gi;
var unitRegex2 = new RegExp(`(${numberRegex2.source})(%|[a-z]+)`, "i");
var rgbaRegex2 = /rgba\(([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+), ([0-9\.-]+)\)/gi;
var cssVariableRegex2 = /var\((--[a-zA-Z0-9-_]+),? ?([a-zA-Z0-9 ()%#.,-]+)?\)/;
var variableToRgba2 = (input) => {
  const [token, fallback] = parseCSSVariable2(input);
  if (!token || isSSR2()) {
    return input;
  }
  const value = window.getComputedStyle(document.documentElement).getPropertyValue(token);
  if (value) {
    return value.trim();
  } else if (fallback && fallback.startsWith("--")) {
    const value2 = window.getComputedStyle(document.documentElement).getPropertyValue(fallback);
    if (value2) {
      return value2;
    } else {
      return input;
    }
  } else if (fallback && cssVariableRegex2.test(fallback)) {
    return variableToRgba2(fallback);
  } else if (fallback) {
    return fallback;
  }
  return input;
};
var parseCSSVariable2 = (current) => {
  const match = cssVariableRegex2.exec(current);
  if (!match)
    return [,];
  const [, token, fallback] = match;
  return [token, fallback];
};
var namedColorRegex2;
var rgbaRound2 = (_2, p1, p22, p3, p4) => `rgba(${Math.round(p1)}, ${Math.round(p22)}, ${Math.round(p3)}, ${p4})`;
var createStringInterpolator22 = (config3) => {
  if (!namedColorRegex2)
    namedColorRegex2 = colors3 ? (
      // match color names, ignore partial matches
      new RegExp(`(${Object.keys(colors3).join("|")})(?!\\w)`, "g")
    ) : (
      // never match
      /^\b$/
    );
  const output = config3.output.map((value) => {
    return getFluidValue2(value).replace(cssVariableRegex2, variableToRgba2).replace(colorRegex2, colorToRgba2).replace(namedColorRegex2, colorToRgba2);
  });
  const keyframes = output.map((value) => value.match(numberRegex2).map(Number));
  const outputRanges = keyframes[0].map(
    (_2, i5) => keyframes.map((values) => {
      if (!(i5 in values)) {
        throw Error('The arity of each "output" value must be equal');
      }
      return values[i5];
    })
  );
  const interpolators = outputRanges.map(
    (output2) => createInterpolator2({ ...config3, output: output2 })
  );
  return (input) => {
    var _a;
    const missingUnit = !unitRegex2.test(output[0]) && ((_a = output.find((value) => unitRegex2.test(value))) == null ? void 0 : _a.replace(numberRegex2, ""));
    let i5 = 0;
    return output[0].replace(
      numberRegex2,
      () => `${interpolators[i5++](input)}${missingUnit || ""}`
    ).replace(rgbaRegex2, rgbaRound2);
  };
};
var prefix2 = "react-spring: ";
var once2 = (fn2) => {
  const func = fn2;
  let called = false;
  if (typeof func != "function") {
    throw new TypeError(`${prefix2}once requires a function parameter`);
  }
  return (...args) => {
    if (!called) {
      func(...args);
      called = true;
    }
  };
};
var warnInterpolate2 = once2(console.warn);
function deprecateInterpolate2() {
  warnInterpolate2(
    `${prefix2}The "interpolate" function is deprecated in v9 (use "to" instead)`
  );
}
var warnDirectCall2 = once2(console.warn);
function deprecateDirectCall2() {
  warnDirectCall2(
    `${prefix2}Directly calling start instead of using the api object is deprecated in v9 (use ".start" instead), this will be removed in later 0.X.0 versions`
  );
}
function isAnimatedString2(value) {
  return is2.str(value) && (value[0] == "#" || /\d/.test(value) || // Do not identify a CSS variable as an AnimatedString if its SSR
  !isSSR2() && cssVariableRegex2.test(value) || value in (colors3 || {}));
}
var useIsomorphicLayoutEffect2 = isSSR2() ? import_react21.useEffect : import_react21.useLayoutEffect;
var useIsMounted2 = () => {
  const isMounted = (0, import_react20.useRef)(false);
  useIsomorphicLayoutEffect2(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};
function useForceUpdate2() {
  const update5 = (0, import_react19.useState)()[1];
  const isMounted = useIsMounted2();
  return () => {
    if (isMounted.current) {
      update5(Math.random());
    }
  };
}
function useMemoOne2(getResult, inputs) {
  const [initial] = (0, import_react22.useState)(
    () => ({
      inputs,
      result: getResult()
    })
  );
  const committed = (0, import_react22.useRef)();
  const prevCache = committed.current;
  let cache = prevCache;
  if (cache) {
    const useCache = Boolean(
      inputs && cache.inputs && areInputsEqual2(inputs, cache.inputs)
    );
    if (!useCache) {
      cache = {
        inputs,
        result: getResult()
      };
    }
  } else {
    cache = initial;
  }
  (0, import_react22.useEffect)(() => {
    committed.current = cache;
    if (prevCache == initial) {
      initial.inputs = initial.result = void 0;
    }
  }, [cache]);
  return cache.result;
}
function areInputsEqual2(next, prev) {
  if (next.length !== prev.length) {
    return false;
  }
  for (let i5 = 0; i5 < next.length; i5++) {
    if (next[i5] !== prev[i5]) {
      return false;
    }
  }
  return true;
}
var useOnce2 = (effect) => (0, import_react23.useEffect)(effect, emptyDeps2);
var emptyDeps2 = [];
function usePrev2(value) {
  const prevRef = (0, import_react24.useRef)();
  (0, import_react24.useEffect)(() => {
    prevRef.current = value;
  });
  return prevRef.current;
}

// node_modules/@nivo/treemap/node_modules/@react-spring/web/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var import_react27 = __toESM(require_react(), 1);

// node_modules/@nivo/treemap/node_modules/@react-spring/web/node_modules/@react-spring/animated/dist/react-spring_animated.modern.mjs
var React3 = __toESM(require_react(), 1);
var import_react26 = __toESM(require_react(), 1);
var $node2 = Symbol.for("Animated:node");
var isAnimated2 = (value) => !!value && value[$node2] === value;
var getAnimated2 = (owner) => owner && owner[$node2];
var setAnimated2 = (owner, node) => defineHidden2(owner, $node2, node);
var getPayload2 = (owner) => owner && owner[$node2] && owner[$node2].getPayload();
var Animated2 = class {
  constructor() {
    setAnimated2(this, this);
  }
  /** Get every `AnimatedValue` used by this node. */
  getPayload() {
    return this.payload || [];
  }
};
var AnimatedValue2 = class extends Animated2 {
  constructor(_value) {
    super();
    this._value = _value;
    this.done = true;
    this.durationProgress = 0;
    if (is2.num(this._value)) {
      this.lastPosition = this._value;
    }
  }
  /** @internal */
  static create(value) {
    return new AnimatedValue2(value);
  }
  getPayload() {
    return [this];
  }
  getValue() {
    return this._value;
  }
  setValue(value, step) {
    if (is2.num(value)) {
      this.lastPosition = value;
      if (step) {
        value = Math.round(value / step) * step;
        if (this.done) {
          this.lastPosition = value;
        }
      }
    }
    if (this._value === value) {
      return false;
    }
    this._value = value;
    return true;
  }
  reset() {
    const { done } = this;
    this.done = false;
    if (is2.num(this._value)) {
      this.elapsedTime = 0;
      this.durationProgress = 0;
      this.lastPosition = this._value;
      if (done)
        this.lastVelocity = null;
      this.v0 = null;
    }
  }
};
var AnimatedString2 = class extends AnimatedValue2 {
  constructor(value) {
    super(0);
    this._string = null;
    this._toString = createInterpolator2({
      output: [value, value]
    });
  }
  /** @internal */
  static create(value) {
    return new AnimatedString2(value);
  }
  getValue() {
    const value = this._string;
    return value == null ? this._string = this._toString(this._value) : value;
  }
  setValue(value) {
    if (is2.str(value)) {
      if (value == this._string) {
        return false;
      }
      this._string = value;
      this._value = 1;
    } else if (super.setValue(value)) {
      this._string = null;
    } else {
      return false;
    }
    return true;
  }
  reset(goal) {
    if (goal) {
      this._toString = createInterpolator2({
        output: [this.getValue(), goal]
      });
    }
    this._value = 0;
    super.reset();
  }
};
var TreeContext2 = { dependencies: null };
var AnimatedObject2 = class extends Animated2 {
  constructor(source) {
    super();
    this.source = source;
    this.setValue(source);
  }
  getValue(animated3) {
    const values = {};
    eachProp2(this.source, (source, key) => {
      if (isAnimated2(source)) {
        values[key] = source.getValue(animated3);
      } else if (hasFluidValue2(source)) {
        values[key] = getFluidValue2(source);
      } else if (!animated3) {
        values[key] = source;
      }
    });
    return values;
  }
  /** Replace the raw object data */
  setValue(source) {
    this.source = source;
    this.payload = this._makePayload(source);
  }
  reset() {
    if (this.payload) {
      each2(this.payload, (node) => node.reset());
    }
  }
  /** Create a payload set. */
  _makePayload(source) {
    if (source) {
      const payload = /* @__PURE__ */ new Set();
      eachProp2(source, this._addToPayload, payload);
      return Array.from(payload);
    }
  }
  /** Add to a payload set. */
  _addToPayload(source) {
    if (TreeContext2.dependencies && hasFluidValue2(source)) {
      TreeContext2.dependencies.add(source);
    }
    const payload = getPayload2(source);
    if (payload) {
      each2(payload, (node) => this.add(node));
    }
  }
};
var AnimatedArray2 = class extends AnimatedObject2 {
  constructor(source) {
    super(source);
  }
  /** @internal */
  static create(source) {
    return new AnimatedArray2(source);
  }
  getValue() {
    return this.source.map((node) => node.getValue());
  }
  setValue(source) {
    const payload = this.getPayload();
    if (source.length == payload.length) {
      return payload.map((node, i5) => node.setValue(source[i5])).some(Boolean);
    }
    super.setValue(source.map(makeAnimated2));
    return true;
  }
};
function makeAnimated2(value) {
  const nodeType = isAnimatedString2(value) ? AnimatedString2 : AnimatedValue2;
  return nodeType.create(value);
}
function getAnimatedType2(value) {
  const parentNode = getAnimated2(value);
  return parentNode ? parentNode.constructor : is2.arr(value) ? AnimatedArray2 : isAnimatedString2(value) ? AnimatedString2 : AnimatedValue2;
}
var withAnimated2 = (Component, host3) => {
  const hasInstance = (
    // Function components must use "forwardRef" to avoid being
    // re-rendered on every animation frame.
    !is2.fun(Component) || Component.prototype && Component.prototype.isReactComponent
  );
  return (0, import_react26.forwardRef)((givenProps, givenRef) => {
    const instanceRef = (0, import_react26.useRef)(null);
    const ref = hasInstance && // eslint-disable-next-line react-hooks/rules-of-hooks
    (0, import_react26.useCallback)(
      (value) => {
        instanceRef.current = updateRef2(givenRef, value);
      },
      [givenRef]
    );
    const [props, deps] = getAnimatedState2(givenProps, host3);
    const forceUpdate = useForceUpdate2();
    const callback = () => {
      const instance = instanceRef.current;
      if (hasInstance && !instance) {
        return;
      }
      const didUpdate = instance ? host3.applyAnimatedValues(instance, props.getValue(true)) : false;
      if (didUpdate === false) {
        forceUpdate();
      }
    };
    const observer = new PropsObserver2(callback, deps);
    const observerRef = (0, import_react26.useRef)();
    useIsomorphicLayoutEffect2(() => {
      observerRef.current = observer;
      each2(deps, (dep) => addFluidObserver2(dep, observer));
      return () => {
        if (observerRef.current) {
          each2(
            observerRef.current.deps,
            (dep) => removeFluidObserver2(dep, observerRef.current)
          );
          raf2.cancel(observerRef.current.update);
        }
      };
    });
    (0, import_react26.useEffect)(callback, []);
    useOnce2(() => () => {
      const observer2 = observerRef.current;
      each2(observer2.deps, (dep) => removeFluidObserver2(dep, observer2));
    });
    const usedProps = host3.getComponentProps(props.getValue());
    return React3.createElement(Component, { ...usedProps, ref });
  });
};
var PropsObserver2 = class {
  constructor(update5, deps) {
    this.update = update5;
    this.deps = deps;
  }
  eventObserved(event) {
    if (event.type == "change") {
      raf2.write(this.update);
    }
  }
};
function getAnimatedState2(props, host3) {
  const dependencies = /* @__PURE__ */ new Set();
  TreeContext2.dependencies = dependencies;
  if (props.style)
    props = {
      ...props,
      style: host3.createAnimatedStyle(props.style)
    };
  props = new AnimatedObject2(props);
  TreeContext2.dependencies = null;
  return [props, dependencies];
}
function updateRef2(ref, value) {
  if (ref) {
    if (is2.fun(ref))
      ref(value);
    else
      ref.current = value;
  }
  return value;
}
var cacheKey2 = Symbol.for("AnimatedComponent");
var createHost2 = (components, {
  applyAnimatedValues: applyAnimatedValues3 = () => false,
  createAnimatedStyle = (style) => new AnimatedObject2(style),
  getComponentProps = (props) => props
} = {}) => {
  const hostConfig = {
    applyAnimatedValues: applyAnimatedValues3,
    createAnimatedStyle,
    getComponentProps
  };
  const animated3 = (Component) => {
    const displayName = getDisplayName2(Component) || "Anonymous";
    if (is2.str(Component)) {
      Component = animated3[Component] || (animated3[Component] = withAnimated2(Component, hostConfig));
    } else {
      Component = Component[cacheKey2] || (Component[cacheKey2] = withAnimated2(Component, hostConfig));
    }
    Component.displayName = `Animated(${displayName})`;
    return Component;
  };
  eachProp2(components, (Component, key) => {
    if (is2.arr(components)) {
      key = getDisplayName2(Component);
    }
    animated3[key] = animated3(Component);
  });
  return {
    animated: animated3
  };
};
var getDisplayName2 = (arg) => is2.str(arg) ? arg : arg && is2.str(arg.displayName) ? arg.displayName : is2.fun(arg) && arg.name || null;

// node_modules/@nivo/treemap/node_modules/@react-spring/web/node_modules/@react-spring/core/dist/react-spring_core.modern.mjs
var React4 = __toESM(require_react(), 1);
var import_react28 = __toESM(require_react(), 1);
var import_react29 = __toESM(require_react(), 1);
var React23 = __toESM(require_react(), 1);
var import_react30 = __toESM(require_react(), 1);
var import_react31 = __toESM(require_react(), 1);
function callProp2(value, ...args) {
  return is2.fun(value) ? value(...args) : value;
}
var matchProp2 = (value, key) => value === true || !!(key && value && (is2.fun(value) ? value(key) : toArray2(value).includes(key)));
var resolveProp2 = (prop, key) => is2.obj(prop) ? key && prop[key] : prop;
var getDefaultProp2 = (props, key) => props.default === true ? props[key] : props.default ? props.default[key] : void 0;
var noopTransform2 = (value) => value;
var getDefaultProps2 = (props, transform = noopTransform2) => {
  let keys = DEFAULT_PROPS2;
  if (props.default && props.default !== true) {
    props = props.default;
    keys = Object.keys(props);
  }
  const defaults22 = {};
  for (const key of keys) {
    const value = transform(props[key], key);
    if (!is2.und(value)) {
      defaults22[key] = value;
    }
  }
  return defaults22;
};
var DEFAULT_PROPS2 = [
  "config",
  "onProps",
  "onStart",
  "onChange",
  "onPause",
  "onResume",
  "onRest"
];
var RESERVED_PROPS2 = {
  config: 1,
  from: 1,
  to: 1,
  ref: 1,
  loop: 1,
  reset: 1,
  pause: 1,
  cancel: 1,
  reverse: 1,
  immediate: 1,
  default: 1,
  delay: 1,
  onProps: 1,
  onStart: 1,
  onChange: 1,
  onPause: 1,
  onResume: 1,
  onRest: 1,
  onResolve: 1,
  // Transition props
  items: 1,
  trail: 1,
  sort: 1,
  expires: 1,
  initial: 1,
  enter: 1,
  update: 1,
  leave: 1,
  children: 1,
  onDestroyed: 1,
  // Internal props
  keys: 1,
  callId: 1,
  parentId: 1
};
function getForwardProps2(props) {
  const forward = {};
  let count5 = 0;
  eachProp2(props, (value, prop) => {
    if (!RESERVED_PROPS2[prop]) {
      forward[prop] = value;
      count5++;
    }
  });
  if (count5) {
    return forward;
  }
}
function inferTo2(props) {
  const to22 = getForwardProps2(props);
  if (to22) {
    const out = { to: to22 };
    eachProp2(props, (val, key) => key in to22 || (out[key] = val));
    return out;
  }
  return { ...props };
}
function computeGoal2(value) {
  value = getFluidValue2(value);
  return is2.arr(value) ? value.map(computeGoal2) : isAnimatedString2(value) ? globals_exports2.createStringInterpolator({
    range: [0, 1],
    output: [value, value]
  })(1) : value;
}
function hasProps2(props) {
  for (const _2 in props)
    return true;
  return false;
}
function isAsyncTo2(to22) {
  return is2.fun(to22) || is2.arr(to22) && is2.obj(to22[0]);
}
function detachRefs2(ctrl, ref) {
  var _a;
  (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
  ref == null ? void 0 : ref.delete(ctrl);
}
function replaceRef2(ctrl, ref) {
  var _a;
  if (ref && ctrl.ref !== ref) {
    (_a = ctrl.ref) == null ? void 0 : _a.delete(ctrl);
    ref.add(ctrl);
    ctrl.ref = ref;
  }
}
var config2 = {
  default: { tension: 170, friction: 26 },
  gentle: { tension: 120, friction: 14 },
  wobbly: { tension: 180, friction: 12 },
  stiff: { tension: 210, friction: 20 },
  slow: { tension: 280, friction: 60 },
  molasses: { tension: 280, friction: 120 }
};
var defaults2 = {
  ...config2.default,
  mass: 1,
  damping: 1,
  easing: easings2.linear,
  clamp: false
};
var AnimationConfig2 = class {
  constructor() {
    this.velocity = 0;
    Object.assign(this, defaults2);
  }
};
function mergeConfig2(config22, newConfig, defaultConfig) {
  if (defaultConfig) {
    defaultConfig = { ...defaultConfig };
    sanitizeConfig2(defaultConfig, newConfig);
    newConfig = { ...defaultConfig, ...newConfig };
  }
  sanitizeConfig2(config22, newConfig);
  Object.assign(config22, newConfig);
  for (const key in defaults2) {
    if (config22[key] == null) {
      config22[key] = defaults2[key];
    }
  }
  let { frequency, damping } = config22;
  const { mass } = config22;
  if (!is2.und(frequency)) {
    if (frequency < 0.01)
      frequency = 0.01;
    if (damping < 0)
      damping = 0;
    config22.tension = Math.pow(2 * Math.PI / frequency, 2) * mass;
    config22.friction = 4 * Math.PI * damping * mass / frequency;
  }
  return config22;
}
function sanitizeConfig2(config22, props) {
  if (!is2.und(props.decay)) {
    config22.duration = void 0;
  } else {
    const isTensionConfig = !is2.und(props.tension) || !is2.und(props.friction);
    if (isTensionConfig || !is2.und(props.frequency) || !is2.und(props.damping) || !is2.und(props.mass)) {
      config22.duration = void 0;
      config22.decay = void 0;
    }
    if (isTensionConfig) {
      config22.frequency = void 0;
    }
  }
}
var emptyArray2 = [];
var Animation2 = class {
  constructor() {
    this.changed = false;
    this.values = emptyArray2;
    this.toValues = null;
    this.fromValues = emptyArray2;
    this.config = new AnimationConfig2();
    this.immediate = false;
  }
};
function scheduleProps2(callId, { key, props, defaultProps, state, actions }) {
  return new Promise((resolve, reject) => {
    let delay;
    let timeout;
    let cancel = matchProp2(props.cancel ?? (defaultProps == null ? void 0 : defaultProps.cancel), key);
    if (cancel) {
      onStart();
    } else {
      if (!is2.und(props.pause)) {
        state.paused = matchProp2(props.pause, key);
      }
      let pause = defaultProps == null ? void 0 : defaultProps.pause;
      if (pause !== true) {
        pause = state.paused || matchProp2(pause, key);
      }
      delay = callProp2(props.delay || 0, key);
      if (pause) {
        state.resumeQueue.add(onResume);
        actions.pause();
      } else {
        actions.resume();
        onResume();
      }
    }
    function onPause() {
      state.resumeQueue.add(onResume);
      state.timeouts.delete(timeout);
      timeout.cancel();
      delay = timeout.time - raf2.now();
    }
    function onResume() {
      if (delay > 0 && !globals_exports2.skipAnimation) {
        state.delayed = true;
        timeout = raf2.setTimeout(onStart, delay);
        state.pauseQueue.add(onPause);
        state.timeouts.add(timeout);
      } else {
        onStart();
      }
    }
    function onStart() {
      if (state.delayed) {
        state.delayed = false;
      }
      state.pauseQueue.delete(onPause);
      state.timeouts.delete(timeout);
      if (callId <= (state.cancelId || 0)) {
        cancel = true;
      }
      try {
        actions.start({ ...props, callId, cancel }, resolve);
      } catch (err) {
        reject(err);
      }
    }
  });
}
var getCombinedResult2 = (target, results) => results.length == 1 ? results[0] : results.some((result) => result.cancelled) ? getCancelledResult2(target.get()) : results.every((result) => result.noop) ? getNoopResult2(target.get()) : getFinishedResult2(
  target.get(),
  results.every((result) => result.finished)
);
var getNoopResult2 = (value) => ({
  value,
  noop: true,
  finished: true,
  cancelled: false
});
var getFinishedResult2 = (value, finished, cancelled = false) => ({
  value,
  finished,
  cancelled
});
var getCancelledResult2 = (value) => ({
  value,
  cancelled: true,
  finished: false
});
function runAsync2(to22, props, state, target) {
  const { callId, parentId, onRest } = props;
  const { asyncTo: prevTo, promise: prevPromise } = state;
  if (!parentId && to22 === prevTo && !props.reset) {
    return prevPromise;
  }
  return state.promise = (async () => {
    state.asyncId = callId;
    state.asyncTo = to22;
    const defaultProps = getDefaultProps2(
      props,
      (value, key) => (
        // The `onRest` prop is only called when the `runAsync` promise is resolved.
        key === "onRest" ? void 0 : value
      )
    );
    let preventBail;
    let bail;
    const bailPromise = new Promise(
      (resolve, reject) => (preventBail = resolve, bail = reject)
    );
    const bailIfEnded = (bailSignal) => {
      const bailResult = (
        // The `cancel` prop or `stop` method was used.
        callId <= (state.cancelId || 0) && getCancelledResult2(target) || // The async `to` prop was replaced.
        callId !== state.asyncId && getFinishedResult2(target, false)
      );
      if (bailResult) {
        bailSignal.result = bailResult;
        bail(bailSignal);
        throw bailSignal;
      }
    };
    const animate = (arg1, arg2) => {
      const bailSignal = new BailSignal2();
      const skipAnimationSignal = new SkipAnimationSignal2();
      return (async () => {
        if (globals_exports2.skipAnimation) {
          stopAsync2(state);
          skipAnimationSignal.result = getFinishedResult2(target, false);
          bail(skipAnimationSignal);
          throw skipAnimationSignal;
        }
        bailIfEnded(bailSignal);
        const props2 = is2.obj(arg1) ? { ...arg1 } : { ...arg2, to: arg1 };
        props2.parentId = callId;
        eachProp2(defaultProps, (value, key) => {
          if (is2.und(props2[key])) {
            props2[key] = value;
          }
        });
        const result2 = await target.start(props2);
        bailIfEnded(bailSignal);
        if (state.paused) {
          await new Promise((resume) => {
            state.resumeQueue.add(resume);
          });
        }
        return result2;
      })();
    };
    let result;
    if (globals_exports2.skipAnimation) {
      stopAsync2(state);
      return getFinishedResult2(target, false);
    }
    try {
      let animating;
      if (is2.arr(to22)) {
        animating = (async (queue) => {
          for (const props2 of queue) {
            await animate(props2);
          }
        })(to22);
      } else {
        animating = Promise.resolve(to22(animate, target.stop.bind(target)));
      }
      await Promise.all([animating.then(preventBail), bailPromise]);
      result = getFinishedResult2(target.get(), true, false);
    } catch (err) {
      if (err instanceof BailSignal2) {
        result = err.result;
      } else if (err instanceof SkipAnimationSignal2) {
        result = err.result;
      } else {
        throw err;
      }
    } finally {
      if (callId == state.asyncId) {
        state.asyncId = parentId;
        state.asyncTo = parentId ? prevTo : void 0;
        state.promise = parentId ? prevPromise : void 0;
      }
    }
    if (is2.fun(onRest)) {
      raf2.batchedUpdates(() => {
        onRest(result, target, target.item);
      });
    }
    return result;
  })();
}
function stopAsync2(state, cancelId) {
  flush2(state.timeouts, (t7) => t7.cancel());
  state.pauseQueue.clear();
  state.resumeQueue.clear();
  state.asyncId = state.asyncTo = state.promise = void 0;
  if (cancelId)
    state.cancelId = cancelId;
}
var BailSignal2 = class extends Error {
  constructor() {
    super(
      "An async animation has been interrupted. You see this error because you forgot to use `await` or `.catch(...)` on its returned promise."
    );
  }
};
var SkipAnimationSignal2 = class extends Error {
  constructor() {
    super("SkipAnimationSignal");
  }
};
var isFrameValue2 = (value) => value instanceof FrameValue2;
var nextId3 = 1;
var FrameValue2 = class extends FluidValue2 {
  constructor() {
    super(...arguments);
    this.id = nextId3++;
    this._priority = 0;
  }
  get priority() {
    return this._priority;
  }
  set priority(priority3) {
    if (this._priority != priority3) {
      this._priority = priority3;
      this._onPriorityChange(priority3);
    }
  }
  /** Get the current value */
  get() {
    const node = getAnimated2(this);
    return node && node.getValue();
  }
  /** Create a spring that maps our value to another value */
  to(...args) {
    return globals_exports2.to(this, args);
  }
  /** @deprecated Use the `to` method instead. */
  interpolate(...args) {
    deprecateInterpolate2();
    return globals_exports2.to(this, args);
  }
  toJSON() {
    return this.get();
  }
  observerAdded(count5) {
    if (count5 == 1)
      this._attach();
  }
  observerRemoved(count5) {
    if (count5 == 0)
      this._detach();
  }
  /** Called when the first child is added. */
  _attach() {
  }
  /** Called when the last child is removed. */
  _detach() {
  }
  /** Tell our children about our new value */
  _onChange(value, idle = false) {
    callFluidObservers2(this, {
      type: "change",
      parent: this,
      value,
      idle
    });
  }
  /** Tell our children about our new priority */
  _onPriorityChange(priority3) {
    if (!this.idle) {
      frameLoop2.sort(this);
    }
    callFluidObservers2(this, {
      type: "priority",
      parent: this,
      priority: priority3
    });
  }
};
var $P2 = Symbol.for("SpringPhase");
var HAS_ANIMATED2 = 1;
var IS_ANIMATING2 = 2;
var IS_PAUSED2 = 4;
var hasAnimated2 = (target) => (target[$P2] & HAS_ANIMATED2) > 0;
var isAnimating2 = (target) => (target[$P2] & IS_ANIMATING2) > 0;
var isPaused2 = (target) => (target[$P2] & IS_PAUSED2) > 0;
var setActiveBit2 = (target, active) => active ? target[$P2] |= IS_ANIMATING2 | HAS_ANIMATED2 : target[$P2] &= ~IS_ANIMATING2;
var setPausedBit2 = (target, paused) => paused ? target[$P2] |= IS_PAUSED2 : target[$P2] &= ~IS_PAUSED2;
var SpringValue2 = class extends FrameValue2 {
  constructor(arg1, arg2) {
    super();
    this.animation = new Animation2();
    this.defaultProps = {};
    this._state = {
      paused: false,
      delayed: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._pendingCalls = /* @__PURE__ */ new Set();
    this._lastCallId = 0;
    this._lastToId = 0;
    this._memoizedDuration = 0;
    if (!is2.und(arg1) || !is2.und(arg2)) {
      const props = is2.obj(arg1) ? { ...arg1 } : { ...arg2, from: arg1 };
      if (is2.und(props.default)) {
        props.default = true;
      }
      this.start(props);
    }
  }
  /** Equals true when not advancing on each frame. */
  get idle() {
    return !(isAnimating2(this) || this._state.asyncTo) || isPaused2(this);
  }
  get goal() {
    return getFluidValue2(this.animation.to);
  }
  get velocity() {
    const node = getAnimated2(this);
    return node instanceof AnimatedValue2 ? node.lastVelocity || 0 : node.getPayload().map((node2) => node2.lastVelocity || 0);
  }
  /**
   * When true, this value has been animated at least once.
   */
  get hasAnimated() {
    return hasAnimated2(this);
  }
  /**
   * When true, this value has an unfinished animation,
   * which is either active or paused.
   */
  get isAnimating() {
    return isAnimating2(this);
  }
  /**
   * When true, all current and future animations are paused.
   */
  get isPaused() {
    return isPaused2(this);
  }
  /**
   *
   *
   */
  get isDelayed() {
    return this._state.delayed;
  }
  /** Advance the current animation by a number of milliseconds */
  advance(dt2) {
    let idle = true;
    let changed = false;
    const anim = this.animation;
    let { toValues } = anim;
    const { config: config22 } = anim;
    const payload = getPayload2(anim.to);
    if (!payload && hasFluidValue2(anim.to)) {
      toValues = toArray2(getFluidValue2(anim.to));
    }
    anim.values.forEach((node2, i5) => {
      if (node2.done)
        return;
      const to22 = (
        // Animated strings always go from 0 to 1.
        node2.constructor == AnimatedString2 ? 1 : payload ? payload[i5].lastPosition : toValues[i5]
      );
      let finished = anim.immediate;
      let position = to22;
      if (!finished) {
        position = node2.lastPosition;
        if (config22.tension <= 0) {
          node2.done = true;
          return;
        }
        let elapsed = node2.elapsedTime += dt2;
        const from = anim.fromValues[i5];
        const v0 = node2.v0 != null ? node2.v0 : node2.v0 = is2.arr(config22.velocity) ? config22.velocity[i5] : config22.velocity;
        let velocity;
        const precision = config22.precision || (from == to22 ? 5e-3 : Math.min(1, Math.abs(to22 - from) * 1e-3));
        if (!is2.und(config22.duration)) {
          let p3 = 1;
          if (config22.duration > 0) {
            if (this._memoizedDuration !== config22.duration) {
              this._memoizedDuration = config22.duration;
              if (node2.durationProgress > 0) {
                node2.elapsedTime = config22.duration * node2.durationProgress;
                elapsed = node2.elapsedTime += dt2;
              }
            }
            p3 = (config22.progress || 0) + elapsed / this._memoizedDuration;
            p3 = p3 > 1 ? 1 : p3 < 0 ? 0 : p3;
            node2.durationProgress = p3;
          }
          position = from + config22.easing(p3) * (to22 - from);
          velocity = (position - node2.lastPosition) / dt2;
          finished = p3 == 1;
        } else if (config22.decay) {
          const decay = config22.decay === true ? 0.998 : config22.decay;
          const e7 = Math.exp(-(1 - decay) * elapsed);
          position = from + v0 / (1 - decay) * (1 - e7);
          finished = Math.abs(node2.lastPosition - position) <= precision;
          velocity = v0 * e7;
        } else {
          velocity = node2.lastVelocity == null ? v0 : node2.lastVelocity;
          const restVelocity = config22.restVelocity || precision / 10;
          const bounceFactor = config22.clamp ? 0 : config22.bounce;
          const canBounce = !is2.und(bounceFactor);
          const isGrowing = from == to22 ? node2.v0 > 0 : from < to22;
          let isMoving;
          let isBouncing = false;
          const step = 1;
          const numSteps = Math.ceil(dt2 / step);
          for (let n5 = 0; n5 < numSteps; ++n5) {
            isMoving = Math.abs(velocity) > restVelocity;
            if (!isMoving) {
              finished = Math.abs(to22 - position) <= precision;
              if (finished) {
                break;
              }
            }
            if (canBounce) {
              isBouncing = position == to22 || position > to22 == isGrowing;
              if (isBouncing) {
                velocity = -velocity * bounceFactor;
                position = to22;
              }
            }
            const springForce = -config22.tension * 1e-6 * (position - to22);
            const dampingForce = -config22.friction * 1e-3 * velocity;
            const acceleration = (springForce + dampingForce) / config22.mass;
            velocity = velocity + acceleration * step;
            position = position + velocity * step;
          }
        }
        node2.lastVelocity = velocity;
        if (Number.isNaN(position)) {
          console.warn(`Got NaN while animating:`, this);
          finished = true;
        }
      }
      if (payload && !payload[i5].done) {
        finished = false;
      }
      if (finished) {
        node2.done = true;
      } else {
        idle = false;
      }
      if (node2.setValue(position, config22.round)) {
        changed = true;
      }
    });
    const node = getAnimated2(this);
    const currVal = node.getValue();
    if (idle) {
      const finalVal = getFluidValue2(anim.to);
      if ((currVal !== finalVal || changed) && !config22.decay) {
        node.setValue(finalVal);
        this._onChange(finalVal);
      } else if (changed && config22.decay) {
        this._onChange(currVal);
      }
      this._stop();
    } else if (changed) {
      this._onChange(currVal);
    }
  }
  /** Set the current value, while stopping the current animation */
  set(value) {
    raf2.batchedUpdates(() => {
      this._stop();
      this._focus(value);
      this._set(value);
    });
    return this;
  }
  /**
   * Freeze the active animation in time, as well as any updates merged
   * before `resume` is called.
   */
  pause() {
    this._update({ pause: true });
  }
  /** Resume the animation if paused. */
  resume() {
    this._update({ pause: false });
  }
  /** Skip to the end of the current animation. */
  finish() {
    if (isAnimating2(this)) {
      const { to: to22, config: config22 } = this.animation;
      raf2.batchedUpdates(() => {
        this._onStart();
        if (!config22.decay) {
          this._set(to22, false);
        }
        this._stop();
      });
    }
    return this;
  }
  /** Push props into the pending queue. */
  update(props) {
    const queue = this.queue || (this.queue = []);
    queue.push(props);
    return this;
  }
  start(to22, arg2) {
    let queue;
    if (!is2.und(to22)) {
      queue = [is2.obj(to22) ? to22 : { ...arg2, to: to22 }];
    } else {
      queue = this.queue || [];
      this.queue = [];
    }
    return Promise.all(
      queue.map((props) => {
        const up = this._update(props);
        return up;
      })
    ).then((results) => getCombinedResult2(this, results));
  }
  /**
   * Stop the current animation, and cancel any delayed updates.
   *
   * Pass `true` to call `onRest` with `cancelled: true`.
   */
  stop(cancel) {
    const { to: to22 } = this.animation;
    this._focus(this.get());
    stopAsync2(this._state, cancel && this._lastCallId);
    raf2.batchedUpdates(() => this._stop(to22, cancel));
    return this;
  }
  /** Restart the animation. */
  reset() {
    this._update({ reset: true });
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._start();
    } else if (event.type == "priority") {
      this.priority = event.priority + 1;
    }
  }
  /**
   * Parse the `to` and `from` range from the given `props` object.
   *
   * This also ensures the initial value is available to animated components
   * during the render phase.
   */
  _prepareNode(props) {
    const key = this.key || "";
    let { to: to22, from } = props;
    to22 = is2.obj(to22) ? to22[key] : to22;
    if (to22 == null || isAsyncTo2(to22)) {
      to22 = void 0;
    }
    from = is2.obj(from) ? from[key] : from;
    if (from == null) {
      from = void 0;
    }
    const range3 = { to: to22, from };
    if (!hasAnimated2(this)) {
      if (props.reverse)
        [to22, from] = [from, to22];
      from = getFluidValue2(from);
      if (!is2.und(from)) {
        this._set(from);
      } else if (!getAnimated2(this)) {
        this._set(to22);
      }
    }
    return range3;
  }
  /** Every update is processed by this method before merging. */
  _update({ ...props }, isLoop) {
    const { key, defaultProps } = this;
    if (props.default)
      Object.assign(
        defaultProps,
        getDefaultProps2(
          props,
          (value, prop) => /^on/.test(prop) ? resolveProp2(value, key) : value
        )
      );
    mergeActiveFn2(this, props, "onProps");
    sendEvent2(this, "onProps", props, this);
    const range3 = this._prepareNode(props);
    if (Object.isFrozen(this)) {
      throw Error(
        "Cannot animate a `SpringValue` object that is frozen. Did you forget to pass your component to `animated(...)` before animating its props?"
      );
    }
    const state = this._state;
    return scheduleProps2(++this._lastCallId, {
      key,
      props,
      defaultProps,
      state,
      actions: {
        pause: () => {
          if (!isPaused2(this)) {
            setPausedBit2(this, true);
            flushCalls2(state.pauseQueue);
            sendEvent2(
              this,
              "onPause",
              getFinishedResult2(this, checkFinished2(this, this.animation.to)),
              this
            );
          }
        },
        resume: () => {
          if (isPaused2(this)) {
            setPausedBit2(this, false);
            if (isAnimating2(this)) {
              this._resume();
            }
            flushCalls2(state.resumeQueue);
            sendEvent2(
              this,
              "onResume",
              getFinishedResult2(this, checkFinished2(this, this.animation.to)),
              this
            );
          }
        },
        start: this._merge.bind(this, range3)
      }
    }).then((result) => {
      if (props.loop && result.finished && !(isLoop && result.noop)) {
        const nextProps = createLoopUpdate2(props);
        if (nextProps) {
          return this._update(nextProps, true);
        }
      }
      return result;
    });
  }
  /** Merge props into the current animation */
  _merge(range3, props, resolve) {
    if (props.cancel) {
      this.stop(true);
      return resolve(getCancelledResult2(this));
    }
    const hasToProp = !is2.und(range3.to);
    const hasFromProp = !is2.und(range3.from);
    if (hasToProp || hasFromProp) {
      if (props.callId > this._lastToId) {
        this._lastToId = props.callId;
      } else {
        return resolve(getCancelledResult2(this));
      }
    }
    const { key, defaultProps, animation: anim } = this;
    const { to: prevTo, from: prevFrom } = anim;
    let { to: to22 = prevTo, from = prevFrom } = range3;
    if (hasFromProp && !hasToProp && (!props.default || is2.und(to22))) {
      to22 = from;
    }
    if (props.reverse)
      [to22, from] = [from, to22];
    const hasFromChanged = !isEqual2(from, prevFrom);
    if (hasFromChanged) {
      anim.from = from;
    }
    from = getFluidValue2(from);
    const hasToChanged = !isEqual2(to22, prevTo);
    if (hasToChanged) {
      this._focus(to22);
    }
    const hasAsyncTo = isAsyncTo2(props.to);
    const { config: config22 } = anim;
    const { decay, velocity } = config22;
    if (hasToProp || hasFromProp) {
      config22.velocity = 0;
    }
    if (props.config && !hasAsyncTo) {
      mergeConfig2(
        config22,
        callProp2(props.config, key),
        // Avoid calling the same "config" prop twice.
        props.config !== defaultProps.config ? callProp2(defaultProps.config, key) : void 0
      );
    }
    let node = getAnimated2(this);
    if (!node || is2.und(to22)) {
      return resolve(getFinishedResult2(this, true));
    }
    const reset = (
      // When `reset` is undefined, the `from` prop implies `reset: true`,
      // except for declarative updates. When `reset` is defined, there
      // must exist a value to animate from.
      is2.und(props.reset) ? hasFromProp && !props.default : !is2.und(from) && matchProp2(props.reset, key)
    );
    const value = reset ? from : this.get();
    const goal = computeGoal2(to22);
    const isAnimatable = is2.num(goal) || is2.arr(goal) || isAnimatedString2(goal);
    const immediate = !hasAsyncTo && (!isAnimatable || matchProp2(defaultProps.immediate || props.immediate, key));
    if (hasToChanged) {
      const nodeType = getAnimatedType2(to22);
      if (nodeType !== node.constructor) {
        if (immediate) {
          node = this._set(goal);
        } else
          throw Error(
            `Cannot animate between ${node.constructor.name} and ${nodeType.name}, as the "to" prop suggests`
          );
      }
    }
    const goalType = node.constructor;
    let started = hasFluidValue2(to22);
    let finished = false;
    if (!started) {
      const hasValueChanged = reset || !hasAnimated2(this) && hasFromChanged;
      if (hasToChanged || hasValueChanged) {
        finished = isEqual2(computeGoal2(value), goal);
        started = !finished;
      }
      if (!isEqual2(anim.immediate, immediate) && !immediate || !isEqual2(config22.decay, decay) || !isEqual2(config22.velocity, velocity)) {
        started = true;
      }
    }
    if (finished && isAnimating2(this)) {
      if (anim.changed && !reset) {
        started = true;
      } else if (!started) {
        this._stop(prevTo);
      }
    }
    if (!hasAsyncTo) {
      if (started || hasFluidValue2(prevTo)) {
        anim.values = node.getPayload();
        anim.toValues = hasFluidValue2(to22) ? null : goalType == AnimatedString2 ? [1] : toArray2(goal);
      }
      if (anim.immediate != immediate) {
        anim.immediate = immediate;
        if (!immediate && !reset) {
          this._set(prevTo);
        }
      }
      if (started) {
        const { onRest } = anim;
        each2(ACTIVE_EVENTS2, (type) => mergeActiveFn2(this, props, type));
        const result = getFinishedResult2(this, checkFinished2(this, prevTo));
        flushCalls2(this._pendingCalls, result);
        this._pendingCalls.add(resolve);
        if (anim.changed)
          raf2.batchedUpdates(() => {
            var _a;
            anim.changed = !reset;
            onRest == null ? void 0 : onRest(result, this);
            if (reset) {
              callProp2(defaultProps.onRest, result);
            } else {
              (_a = anim.onStart) == null ? void 0 : _a.call(anim, result, this);
            }
          });
      }
    }
    if (reset) {
      this._set(value);
    }
    if (hasAsyncTo) {
      resolve(runAsync2(props.to, props, this._state, this));
    } else if (started) {
      this._start();
    } else if (isAnimating2(this) && !hasToChanged) {
      this._pendingCalls.add(resolve);
    } else {
      resolve(getNoopResult2(value));
    }
  }
  /** Update the `animation.to` value, which might be a `FluidValue` */
  _focus(value) {
    const anim = this.animation;
    if (value !== anim.to) {
      if (getFluidObservers2(this)) {
        this._detach();
      }
      anim.to = value;
      if (getFluidObservers2(this)) {
        this._attach();
      }
    }
  }
  _attach() {
    let priority3 = 0;
    const { to: to22 } = this.animation;
    if (hasFluidValue2(to22)) {
      addFluidObserver2(to22, this);
      if (isFrameValue2(to22)) {
        priority3 = to22.priority + 1;
      }
    }
    this.priority = priority3;
  }
  _detach() {
    const { to: to22 } = this.animation;
    if (hasFluidValue2(to22)) {
      removeFluidObserver2(to22, this);
    }
  }
  /**
   * Update the current value from outside the frameloop,
   * and return the `Animated` node.
   */
  _set(arg, idle = true) {
    const value = getFluidValue2(arg);
    if (!is2.und(value)) {
      const oldNode = getAnimated2(this);
      if (!oldNode || !isEqual2(value, oldNode.getValue())) {
        const nodeType = getAnimatedType2(value);
        if (!oldNode || oldNode.constructor != nodeType) {
          setAnimated2(this, nodeType.create(value));
        } else {
          oldNode.setValue(value);
        }
        if (oldNode) {
          raf2.batchedUpdates(() => {
            this._onChange(value, idle);
          });
        }
      }
    }
    return getAnimated2(this);
  }
  _onStart() {
    const anim = this.animation;
    if (!anim.changed) {
      anim.changed = true;
      sendEvent2(
        this,
        "onStart",
        getFinishedResult2(this, checkFinished2(this, anim.to)),
        this
      );
    }
  }
  _onChange(value, idle) {
    if (!idle) {
      this._onStart();
      callProp2(this.animation.onChange, value, this);
    }
    callProp2(this.defaultProps.onChange, value, this);
    super._onChange(value, idle);
  }
  // This method resets the animation state (even if already animating) to
  // ensure the latest from/to range is used, and it also ensures this spring
  // is added to the frameloop.
  _start() {
    const anim = this.animation;
    getAnimated2(this).reset(getFluidValue2(anim.to));
    if (!anim.immediate) {
      anim.fromValues = anim.values.map((node) => node.lastPosition);
    }
    if (!isAnimating2(this)) {
      setActiveBit2(this, true);
      if (!isPaused2(this)) {
        this._resume();
      }
    }
  }
  _resume() {
    if (globals_exports2.skipAnimation) {
      this.finish();
    } else {
      frameLoop2.start(this);
    }
  }
  /**
   * Exit the frameloop and notify `onRest` listeners.
   *
   * Always wrap `_stop` calls with `batchedUpdates`.
   */
  _stop(goal, cancel) {
    if (isAnimating2(this)) {
      setActiveBit2(this, false);
      const anim = this.animation;
      each2(anim.values, (node) => {
        node.done = true;
      });
      if (anim.toValues) {
        anim.onChange = anim.onPause = anim.onResume = void 0;
      }
      callFluidObservers2(this, {
        type: "idle",
        parent: this
      });
      const result = cancel ? getCancelledResult2(this.get()) : getFinishedResult2(this.get(), checkFinished2(this, goal ?? anim.to));
      flushCalls2(this._pendingCalls, result);
      if (anim.changed) {
        anim.changed = false;
        sendEvent2(this, "onRest", result, this);
      }
    }
  }
};
function checkFinished2(target, to22) {
  const goal = computeGoal2(to22);
  const value = computeGoal2(target.get());
  return isEqual2(value, goal);
}
function createLoopUpdate2(props, loop3 = props.loop, to22 = props.to) {
  const loopRet = callProp2(loop3);
  if (loopRet) {
    const overrides = loopRet !== true && inferTo2(loopRet);
    const reverse4 = (overrides || props).reverse;
    const reset = !overrides || overrides.reset;
    return createUpdate2({
      ...props,
      loop: loop3,
      // Avoid updating default props when looping.
      default: false,
      // Never loop the `pause` prop.
      pause: void 0,
      // For the "reverse" prop to loop as expected, the "to" prop
      // must be undefined. The "reverse" prop is ignored when the
      // "to" prop is an array or function.
      to: !reverse4 || isAsyncTo2(to22) ? to22 : void 0,
      // Ignore the "from" prop except on reset.
      from: reset ? props.from : void 0,
      reset,
      // The "loop" prop can return a "useSpring" props object to
      // override any of the original props.
      ...overrides
    });
  }
}
function createUpdate2(props) {
  const { to: to22, from } = props = inferTo2(props);
  const keys = /* @__PURE__ */ new Set();
  if (is2.obj(to22))
    findDefined2(to22, keys);
  if (is2.obj(from))
    findDefined2(from, keys);
  props.keys = keys.size ? Array.from(keys) : null;
  return props;
}
function declareUpdate2(props) {
  const update22 = createUpdate2(props);
  if (is2.und(update22.default)) {
    update22.default = getDefaultProps2(update22);
  }
  return update22;
}
function findDefined2(values, keys) {
  eachProp2(values, (value, key) => value != null && keys.add(key));
}
var ACTIVE_EVENTS2 = [
  "onStart",
  "onRest",
  "onChange",
  "onPause",
  "onResume"
];
function mergeActiveFn2(target, props, type) {
  target.animation[type] = props[type] !== getDefaultProp2(props, type) ? resolveProp2(props[type], target.key) : void 0;
}
function sendEvent2(target, type, ...args) {
  var _a, _b, _c, _d;
  (_b = (_a = target.animation)[type]) == null ? void 0 : _b.call(_a, ...args);
  (_d = (_c = target.defaultProps)[type]) == null ? void 0 : _d.call(_c, ...args);
}
var BATCHED_EVENTS2 = ["onStart", "onChange", "onRest"];
var nextId22 = 1;
var Controller2 = class {
  constructor(props, flush3) {
    this.id = nextId22++;
    this.springs = {};
    this.queue = [];
    this._lastAsyncId = 0;
    this._active = /* @__PURE__ */ new Set();
    this._changed = /* @__PURE__ */ new Set();
    this._started = false;
    this._state = {
      paused: false,
      pauseQueue: /* @__PURE__ */ new Set(),
      resumeQueue: /* @__PURE__ */ new Set(),
      timeouts: /* @__PURE__ */ new Set()
    };
    this._events = {
      onStart: /* @__PURE__ */ new Map(),
      onChange: /* @__PURE__ */ new Map(),
      onRest: /* @__PURE__ */ new Map()
    };
    this._onFrame = this._onFrame.bind(this);
    if (flush3) {
      this._flush = flush3;
    }
    if (props) {
      this.start({ default: true, ...props });
    }
  }
  /**
   * Equals `true` when no spring values are in the frameloop, and
   * no async animation is currently active.
   */
  get idle() {
    return !this._state.asyncTo && Object.values(this.springs).every((spring) => {
      return spring.idle && !spring.isDelayed && !spring.isPaused;
    });
  }
  get item() {
    return this._item;
  }
  set item(item) {
    this._item = item;
  }
  /** Get the current values of our springs */
  get() {
    const values = {};
    this.each((spring, key) => values[key] = spring.get());
    return values;
  }
  /** Set the current values without animating. */
  set(values) {
    for (const key in values) {
      const value = values[key];
      if (!is2.und(value)) {
        this.springs[key].set(value);
      }
    }
  }
  /** Push an update onto the queue of each value. */
  update(props) {
    if (props) {
      this.queue.push(createUpdate2(props));
    }
    return this;
  }
  /**
   * Start the queued animations for every spring, and resolve the returned
   * promise once all queued animations have finished or been cancelled.
   *
   * When you pass a queue (instead of nothing), that queue is used instead of
   * the queued animations added with the `update` method, which are left alone.
   */
  start(props) {
    let { queue } = this;
    if (props) {
      queue = toArray2(props).map(createUpdate2);
    } else {
      this.queue = [];
    }
    if (this._flush) {
      return this._flush(this, queue);
    }
    prepareKeys2(this, queue);
    return flushUpdateQueue2(this, queue);
  }
  /** @internal */
  stop(arg, keys) {
    if (arg !== !!arg) {
      keys = arg;
    }
    if (keys) {
      const springs = this.springs;
      each2(toArray2(keys), (key) => springs[key].stop(!!arg));
    } else {
      stopAsync2(this._state, this._lastAsyncId);
      this.each((spring) => spring.stop(!!arg));
    }
    return this;
  }
  /** Freeze the active animation in time */
  pause(keys) {
    if (is2.und(keys)) {
      this.start({ pause: true });
    } else {
      const springs = this.springs;
      each2(toArray2(keys), (key) => springs[key].pause());
    }
    return this;
  }
  /** Resume the animation if paused. */
  resume(keys) {
    if (is2.und(keys)) {
      this.start({ pause: false });
    } else {
      const springs = this.springs;
      each2(toArray2(keys), (key) => springs[key].resume());
    }
    return this;
  }
  /** Call a function once per spring value */
  each(iterator) {
    eachProp2(this.springs, iterator);
  }
  /** @internal Called at the end of every animation frame */
  _onFrame() {
    const { onStart, onChange, onRest } = this._events;
    const active = this._active.size > 0;
    const changed = this._changed.size > 0;
    if (active && !this._started || changed && !this._started) {
      this._started = true;
      flush2(onStart, ([onStart2, result]) => {
        result.value = this.get();
        onStart2(result, this, this._item);
      });
    }
    const idle = !active && this._started;
    const values = changed || idle && onRest.size ? this.get() : null;
    if (changed && onChange.size) {
      flush2(onChange, ([onChange2, result]) => {
        result.value = values;
        onChange2(result, this, this._item);
      });
    }
    if (idle) {
      this._started = false;
      flush2(onRest, ([onRest2, result]) => {
        result.value = values;
        onRest2(result, this, this._item);
      });
    }
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      this._changed.add(event.parent);
      if (!event.idle) {
        this._active.add(event.parent);
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else
      return;
    raf2.onFrame(this._onFrame);
  }
};
function flushUpdateQueue2(ctrl, queue) {
  return Promise.all(queue.map((props) => flushUpdate2(ctrl, props))).then(
    (results) => getCombinedResult2(ctrl, results)
  );
}
async function flushUpdate2(ctrl, props, isLoop) {
  const { keys, to: to22, from, loop: loop3, onRest, onResolve } = props;
  const defaults22 = is2.obj(props.default) && props.default;
  if (loop3) {
    props.loop = false;
  }
  if (to22 === false)
    props.to = null;
  if (from === false)
    props.from = null;
  const asyncTo = is2.arr(to22) || is2.fun(to22) ? to22 : void 0;
  if (asyncTo) {
    props.to = void 0;
    props.onRest = void 0;
    if (defaults22) {
      defaults22.onRest = void 0;
    }
  } else {
    each2(BATCHED_EVENTS2, (key) => {
      const handler = props[key];
      if (is2.fun(handler)) {
        const queue = ctrl["_events"][key];
        props[key] = ({ finished, cancelled }) => {
          const result2 = queue.get(handler);
          if (result2) {
            if (!finished)
              result2.finished = false;
            if (cancelled)
              result2.cancelled = true;
          } else {
            queue.set(handler, {
              value: null,
              finished: finished || false,
              cancelled: cancelled || false
            });
          }
        };
        if (defaults22) {
          defaults22[key] = props[key];
        }
      }
    });
  }
  const state = ctrl["_state"];
  if (props.pause === !state.paused) {
    state.paused = props.pause;
    flushCalls2(props.pause ? state.pauseQueue : state.resumeQueue);
  } else if (state.paused) {
    props.pause = true;
  }
  const promises = (keys || Object.keys(ctrl.springs)).map(
    (key) => ctrl.springs[key].start(props)
  );
  const cancel = props.cancel === true || getDefaultProp2(props, "cancel") === true;
  if (asyncTo || cancel && state.asyncId) {
    promises.push(
      scheduleProps2(++ctrl["_lastAsyncId"], {
        props,
        state,
        actions: {
          pause: noop2,
          resume: noop2,
          start(props2, resolve) {
            if (cancel) {
              stopAsync2(state, ctrl["_lastAsyncId"]);
              resolve(getCancelledResult2(ctrl));
            } else {
              props2.onRest = onRest;
              resolve(
                runAsync2(
                  asyncTo,
                  props2,
                  state,
                  ctrl
                )
              );
            }
          }
        }
      })
    );
  }
  if (state.paused) {
    await new Promise((resume) => {
      state.resumeQueue.add(resume);
    });
  }
  const result = getCombinedResult2(ctrl, await Promise.all(promises));
  if (loop3 && result.finished && !(isLoop && result.noop)) {
    const nextProps = createLoopUpdate2(props, loop3, to22);
    if (nextProps) {
      prepareKeys2(ctrl, [nextProps]);
      return flushUpdate2(ctrl, nextProps, true);
    }
  }
  if (onResolve) {
    raf2.batchedUpdates(() => onResolve(result, ctrl, ctrl.item));
  }
  return result;
}
function getSprings2(ctrl, props) {
  const springs = { ...ctrl.springs };
  if (props) {
    each2(toArray2(props), (props2) => {
      if (is2.und(props2.keys)) {
        props2 = createUpdate2(props2);
      }
      if (!is2.obj(props2.to)) {
        props2 = { ...props2, to: void 0 };
      }
      prepareSprings2(springs, props2, (key) => {
        return createSpring2(key);
      });
    });
  }
  setSprings2(ctrl, springs);
  return springs;
}
function setSprings2(ctrl, springs) {
  eachProp2(springs, (spring, key) => {
    if (!ctrl.springs[key]) {
      ctrl.springs[key] = spring;
      addFluidObserver2(spring, ctrl);
    }
  });
}
function createSpring2(key, observer) {
  const spring = new SpringValue2();
  spring.key = key;
  if (observer) {
    addFluidObserver2(spring, observer);
  }
  return spring;
}
function prepareSprings2(springs, props, create) {
  if (props.keys) {
    each2(props.keys, (key) => {
      const spring = springs[key] || (springs[key] = create(key));
      spring["_prepareNode"](props);
    });
  }
}
function prepareKeys2(ctrl, queue) {
  each2(queue, (props) => {
    prepareSprings2(ctrl.springs, props, (key) => {
      return createSpring2(key, ctrl);
    });
  });
}
var SpringContext2 = ({
  children,
  ...props
}) => {
  const inherited = (0, import_react28.useContext)(ctx2);
  const pause = props.pause || !!inherited.pause, immediate = props.immediate || !!inherited.immediate;
  props = useMemoOne2(() => ({ pause, immediate }), [pause, immediate]);
  const { Provider } = ctx2;
  return React4.createElement(Provider, { value: props }, children);
};
var ctx2 = makeContext2(SpringContext2, {});
SpringContext2.Provider = ctx2.Provider;
SpringContext2.Consumer = ctx2.Consumer;
function makeContext2(target, init) {
  Object.assign(target, React4.createContext(init));
  target.Provider._context = target;
  target.Consumer._context = target;
  return target;
}
var SpringRef2 = () => {
  const current = [];
  const SpringRef22 = function(props) {
    deprecateDirectCall2();
    const results = [];
    each2(current, (ctrl, i5) => {
      if (is2.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = _getProps(props, ctrl, i5);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.current = current;
  SpringRef22.add = function(ctrl) {
    if (!current.includes(ctrl)) {
      current.push(ctrl);
    }
  };
  SpringRef22.delete = function(ctrl) {
    const i5 = current.indexOf(ctrl);
    if (~i5)
      current.splice(i5, 1);
  };
  SpringRef22.pause = function() {
    each2(current, (ctrl) => ctrl.pause(...arguments));
    return this;
  };
  SpringRef22.resume = function() {
    each2(current, (ctrl) => ctrl.resume(...arguments));
    return this;
  };
  SpringRef22.set = function(values) {
    each2(current, (ctrl, i5) => {
      const update22 = is2.fun(values) ? values(i5, ctrl) : values;
      if (update22) {
        ctrl.set(update22);
      }
    });
  };
  SpringRef22.start = function(props) {
    const results = [];
    each2(current, (ctrl, i5) => {
      if (is2.und(props)) {
        results.push(ctrl.start());
      } else {
        const update22 = this._getProps(props, ctrl, i5);
        if (update22) {
          results.push(ctrl.start(update22));
        }
      }
    });
    return results;
  };
  SpringRef22.stop = function() {
    each2(current, (ctrl) => ctrl.stop(...arguments));
    return this;
  };
  SpringRef22.update = function(props) {
    each2(current, (ctrl, i5) => ctrl.update(this._getProps(props, ctrl, i5)));
    return this;
  };
  const _getProps = function(arg, ctrl, index4) {
    return is2.fun(arg) ? arg(index4, ctrl) : arg;
  };
  SpringRef22._getProps = _getProps;
  return SpringRef22;
};
function useSprings2(length, props, deps) {
  const propsFn = is2.fun(props) && props;
  if (propsFn && !deps)
    deps = [];
  const ref = (0, import_react27.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef2() : void 0,
    []
  );
  const layoutId = (0, import_react27.useRef)(0);
  const forceUpdate = useForceUpdate2();
  const state = (0, import_react27.useMemo)(
    () => ({
      ctrls: [],
      queue: [],
      flush(ctrl, updates2) {
        const springs2 = getSprings2(ctrl, updates2);
        const canFlushSync = layoutId.current > 0 && !state.queue.length && !Object.keys(springs2).some((key) => !ctrl.springs[key]);
        return canFlushSync ? flushUpdateQueue2(ctrl, updates2) : new Promise((resolve) => {
          setSprings2(ctrl, springs2);
          state.queue.push(() => {
            resolve(flushUpdateQueue2(ctrl, updates2));
          });
          forceUpdate();
        });
      }
    }),
    []
  );
  const ctrls = (0, import_react27.useRef)([...state.ctrls]);
  const updates = [];
  const prevLength = usePrev2(length) || 0;
  (0, import_react27.useMemo)(() => {
    each2(ctrls.current.slice(length, prevLength), (ctrl) => {
      detachRefs2(ctrl, ref);
      ctrl.stop(true);
    });
    ctrls.current.length = length;
    declareUpdates(prevLength, length);
  }, [length]);
  (0, import_react27.useMemo)(() => {
    declareUpdates(0, Math.min(prevLength, length));
  }, deps);
  function declareUpdates(startIndex, endIndex) {
    for (let i5 = startIndex; i5 < endIndex; i5++) {
      const ctrl = ctrls.current[i5] || (ctrls.current[i5] = new Controller2(null, state.flush));
      const update22 = propsFn ? propsFn(i5, ctrl) : props[i5];
      if (update22) {
        updates[i5] = declareUpdate2(update22);
      }
    }
  }
  const springs = ctrls.current.map((ctrl, i5) => getSprings2(ctrl, updates[i5]));
  const context = (0, import_react27.useContext)(SpringContext2);
  const prevContext = usePrev2(context);
  const hasContext = context !== prevContext && hasProps2(context);
  useIsomorphicLayoutEffect2(() => {
    layoutId.current++;
    state.ctrls = ctrls.current;
    const { queue } = state;
    if (queue.length) {
      state.queue = [];
      each2(queue, (cb) => cb());
    }
    each2(ctrls.current, (ctrl, i5) => {
      ref == null ? void 0 : ref.add(ctrl);
      if (hasContext) {
        ctrl.start({ default: context });
      }
      const update22 = updates[i5];
      if (update22) {
        replaceRef2(ctrl, update22.ref);
        if (ctrl.ref) {
          ctrl.queue.push(update22);
        } else {
          ctrl.start(update22);
        }
      }
    });
  });
  useOnce2(() => () => {
    each2(state.ctrls, (ctrl) => ctrl.stop(true));
  });
  const values = springs.map((x4) => ({ ...x4 }));
  return ref ? [values, ref] : values;
}
function useSpring2(props, deps) {
  const isFn = is2.fun(props);
  const [[values], ref] = useSprings2(
    1,
    isFn ? props : [props],
    isFn ? deps || [] : deps
  );
  return isFn || arguments.length == 2 ? [values, ref] : values;
}
function useTransition(data, props, deps) {
  const propsFn = is2.fun(props) && props;
  const {
    reset,
    sort: sort4,
    trail = 0,
    expires = true,
    exitBeforeEnter = false,
    onDestroyed,
    ref: propsRef,
    config: propsConfig
  } = propsFn ? propsFn() : props;
  const ref = (0, import_react30.useMemo)(
    () => propsFn || arguments.length == 3 ? SpringRef2() : void 0,
    []
  );
  const items = toArray2(data);
  const transitions = [];
  const usedTransitions = (0, import_react30.useRef)(null);
  const prevTransitions = reset ? null : usedTransitions.current;
  useIsomorphicLayoutEffect2(() => {
    usedTransitions.current = transitions;
  });
  useOnce2(() => {
    each2(transitions, (t7) => {
      ref == null ? void 0 : ref.add(t7.ctrl);
      t7.ctrl.ref = ref;
    });
    return () => {
      each2(usedTransitions.current, (t7) => {
        if (t7.expired) {
          clearTimeout(t7.expirationId);
        }
        detachRefs2(t7.ctrl, ref);
        t7.ctrl.stop(true);
      });
    };
  });
  const keys = getKeys(items, propsFn ? propsFn() : props, prevTransitions);
  const expired = reset && usedTransitions.current || [];
  useIsomorphicLayoutEffect2(
    () => each2(expired, ({ ctrl, item, key }) => {
      detachRefs2(ctrl, ref);
      callProp2(onDestroyed, item, key);
    })
  );
  const reused = [];
  if (prevTransitions)
    each2(prevTransitions, (t7, i5) => {
      if (t7.expired) {
        clearTimeout(t7.expirationId);
        expired.push(t7);
      } else {
        i5 = reused[i5] = keys.indexOf(t7.key);
        if (~i5)
          transitions[i5] = t7;
      }
    });
  each2(items, (item, i5) => {
    if (!transitions[i5]) {
      transitions[i5] = {
        key: keys[i5],
        item,
        phase: "mount",
        ctrl: new Controller2()
      };
      transitions[i5].ctrl.item = item;
    }
  });
  if (reused.length) {
    let i5 = -1;
    const { leave } = propsFn ? propsFn() : props;
    each2(reused, (keyIndex, prevIndex) => {
      const t7 = prevTransitions[prevIndex];
      if (~keyIndex) {
        i5 = transitions.indexOf(t7);
        transitions[i5] = { ...t7, item: items[keyIndex] };
      } else if (leave) {
        transitions.splice(++i5, 0, t7);
      }
    });
  }
  if (is2.fun(sort4)) {
    transitions.sort((a4, b3) => sort4(a4.item, b3.item));
  }
  let delay = -trail;
  const forceUpdate = useForceUpdate2();
  const defaultProps = getDefaultProps2(props);
  const changes = /* @__PURE__ */ new Map();
  const exitingTransitions = (0, import_react30.useRef)(/* @__PURE__ */ new Map());
  const forceChange = (0, import_react30.useRef)(false);
  each2(transitions, (t7, i5) => {
    const key = t7.key;
    const prevPhase = t7.phase;
    const p3 = propsFn ? propsFn() : props;
    let to22;
    let phase;
    const propsDelay = callProp2(p3.delay || 0, key);
    if (prevPhase == "mount") {
      to22 = p3.enter;
      phase = "enter";
    } else {
      const isLeave = keys.indexOf(key) < 0;
      if (prevPhase != "leave") {
        if (isLeave) {
          to22 = p3.leave;
          phase = "leave";
        } else if (to22 = p3.update) {
          phase = "update";
        } else
          return;
      } else if (!isLeave) {
        to22 = p3.enter;
        phase = "enter";
      } else
        return;
    }
    to22 = callProp2(to22, t7.item, i5);
    to22 = is2.obj(to22) ? inferTo2(to22) : { to: to22 };
    if (!to22.config) {
      const config22 = propsConfig || defaultProps.config;
      to22.config = callProp2(config22, t7.item, i5, phase);
    }
    delay += trail;
    const payload = {
      ...defaultProps,
      // we need to add our props.delay value you here.
      delay: propsDelay + delay,
      ref: propsRef,
      immediate: p3.immediate,
      // This prevents implied resets.
      reset: false,
      // Merge any phase-specific props.
      ...to22
    };
    if (phase == "enter" && is2.und(payload.from)) {
      const p22 = propsFn ? propsFn() : props;
      const from = is2.und(p22.initial) || prevTransitions ? p22.from : p22.initial;
      payload.from = callProp2(from, t7.item, i5);
    }
    const { onResolve } = payload;
    payload.onResolve = (result) => {
      callProp2(onResolve, result);
      const transitions2 = usedTransitions.current;
      const t23 = transitions2.find((t33) => t33.key === key);
      if (!t23)
        return;
      if (result.cancelled && t23.phase != "update") {
        return;
      }
      if (t23.ctrl.idle) {
        const idle = transitions2.every((t33) => t33.ctrl.idle);
        if (t23.phase == "leave") {
          const expiry = callProp2(expires, t23.item);
          if (expiry !== false) {
            const expiryMs = expiry === true ? 0 : expiry;
            t23.expired = true;
            if (!idle && expiryMs > 0) {
              if (expiryMs <= 2147483647)
                t23.expirationId = setTimeout(forceUpdate, expiryMs);
              return;
            }
          }
        }
        if (idle && transitions2.some((t33) => t33.expired)) {
          exitingTransitions.current.delete(t23);
          if (exitBeforeEnter) {
            forceChange.current = true;
          }
          forceUpdate();
        }
      }
    };
    const springs = getSprings2(t7.ctrl, payload);
    if (phase === "leave" && exitBeforeEnter) {
      exitingTransitions.current.set(t7, { phase, springs, payload });
    } else {
      changes.set(t7, { phase, springs, payload });
    }
  });
  const context = (0, import_react30.useContext)(SpringContext2);
  const prevContext = usePrev2(context);
  const hasContext = context !== prevContext && hasProps2(context);
  useIsomorphicLayoutEffect2(() => {
    if (hasContext) {
      each2(transitions, (t7) => {
        t7.ctrl.start({ default: context });
      });
    }
  }, [context]);
  each2(changes, (_2, t7) => {
    if (exitingTransitions.current.size) {
      const ind = transitions.findIndex((state) => state.key === t7.key);
      transitions.splice(ind, 1);
    }
  });
  useIsomorphicLayoutEffect2(
    () => {
      each2(
        exitingTransitions.current.size ? exitingTransitions.current : changes,
        ({ phase, payload }, t7) => {
          const { ctrl } = t7;
          t7.phase = phase;
          ref == null ? void 0 : ref.add(ctrl);
          if (hasContext && phase == "enter") {
            ctrl.start({ default: context });
          }
          if (payload) {
            replaceRef2(ctrl, payload.ref);
            if ((ctrl.ref || ref) && !forceChange.current) {
              ctrl.update(payload);
            } else {
              ctrl.start(payload);
              if (forceChange.current) {
                forceChange.current = false;
              }
            }
          }
        }
      );
    },
    reset ? void 0 : deps
  );
  const renderTransitions = (render) => React23.createElement(React23.Fragment, null, transitions.map((t7, i5) => {
    const { springs } = changes.get(t7) || t7.ctrl;
    const elem = render({ ...springs }, t7.item, t7, i5);
    return elem && elem.type ? React23.createElement(
      elem.type,
      {
        ...elem.props,
        key: is2.str(t7.key) || is2.num(t7.key) ? t7.key : t7.ctrl.id,
        ref: elem.ref
      }
    ) : elem;
  }));
  return ref ? [renderTransitions, ref] : renderTransitions;
}
var nextKey = 1;
function getKeys(items, { key, keys = key }, prevTransitions) {
  if (keys === null) {
    const reused = /* @__PURE__ */ new Set();
    return items.map((item) => {
      const t7 = prevTransitions && prevTransitions.find(
        (t23) => t23.item === item && t23.phase !== "leave" && !reused.has(t23)
      );
      if (t7) {
        reused.add(t7);
        return t7.key;
      }
      return nextKey++;
    });
  }
  return is2.und(keys) ? items : is2.fun(keys) ? items.map(keys) : toArray2(keys);
}
var Interpolation2 = class extends FrameValue2 {
  constructor(source, args) {
    super();
    this.source = source;
    this.idle = true;
    this._active = /* @__PURE__ */ new Set();
    this.calc = createInterpolator2(...args);
    const value = this._get();
    const nodeType = getAnimatedType2(value);
    setAnimated2(this, nodeType.create(value));
  }
  advance(_dt) {
    const value = this._get();
    const oldValue = this.get();
    if (!isEqual2(value, oldValue)) {
      getAnimated2(this).setValue(value);
      this._onChange(value, this.idle);
    }
    if (!this.idle && checkIdle2(this._active)) {
      becomeIdle2(this);
    }
  }
  _get() {
    const inputs = is2.arr(this.source) ? this.source.map(getFluidValue2) : toArray2(getFluidValue2(this.source));
    return this.calc(...inputs);
  }
  _start() {
    if (this.idle && !checkIdle2(this._active)) {
      this.idle = false;
      each2(getPayload2(this), (node) => {
        node.done = false;
      });
      if (globals_exports2.skipAnimation) {
        raf2.batchedUpdates(() => this.advance());
        becomeIdle2(this);
      } else {
        frameLoop2.start(this);
      }
    }
  }
  // Observe our sources only when we're observed.
  _attach() {
    let priority3 = 1;
    each2(toArray2(this.source), (source) => {
      if (hasFluidValue2(source)) {
        addFluidObserver2(source, this);
      }
      if (isFrameValue2(source)) {
        if (!source.idle) {
          this._active.add(source);
        }
        priority3 = Math.max(priority3, source.priority + 1);
      }
    });
    this.priority = priority3;
    this._start();
  }
  // Stop observing our sources once we have no observers.
  _detach() {
    each2(toArray2(this.source), (source) => {
      if (hasFluidValue2(source)) {
        removeFluidObserver2(source, this);
      }
    });
    this._active.clear();
    becomeIdle2(this);
  }
  /** @internal */
  eventObserved(event) {
    if (event.type == "change") {
      if (event.idle) {
        this.advance();
      } else {
        this._active.add(event.parent);
        this._start();
      }
    } else if (event.type == "idle") {
      this._active.delete(event.parent);
    } else if (event.type == "priority") {
      this.priority = toArray2(this.source).reduce(
        (highest, parent) => Math.max(highest, (isFrameValue2(parent) ? parent.priority : 0) + 1),
        0
      );
    }
  }
};
function isIdle2(source) {
  return source.idle !== false;
}
function checkIdle2(active) {
  return !active.size || Array.from(active).every(isIdle2);
}
function becomeIdle2(self2) {
  if (!self2.idle) {
    self2.idle = true;
    each2(getPayload2(self2), (node) => {
      node.done = true;
    });
    callFluidObservers2(self2, {
      type: "idle",
      parent: self2
    });
  }
}
var to4 = (source, ...args) => new Interpolation2(source, args);
globals_exports2.assign({
  createStringInterpolator: createStringInterpolator22,
  to: (source, args) => new Interpolation2(source, args)
});
var update4 = frameLoop2.advance;

// node_modules/@nivo/treemap/node_modules/@react-spring/web/dist/react-spring_web.modern.mjs
var import_react_dom2 = __toESM(require_react_dom(), 1);
var isCustomPropRE2 = /^--/;
function dangerousStyleValue2(name, value) {
  if (value == null || typeof value === "boolean" || value === "")
    return "";
  if (typeof value === "number" && value !== 0 && !isCustomPropRE2.test(name) && !(isUnitlessNumber2.hasOwnProperty(name) && isUnitlessNumber2[name]))
    return value + "px";
  return ("" + value).trim();
}
var attributeCache2 = {};
function applyAnimatedValues2(instance, props) {
  if (!instance.nodeType || !instance.setAttribute) {
    return false;
  }
  const isFilterElement = instance.nodeName === "filter" || instance.parentNode && instance.parentNode.nodeName === "filter";
  const {
    className,
    style,
    children,
    scrollTop,
    scrollLeft,
    viewBox,
    ...attributes
  } = props;
  const values = Object.values(attributes);
  const names = Object.keys(attributes).map(
    (name) => isFilterElement || instance.hasAttribute(name) ? name : attributeCache2[name] || (attributeCache2[name] = name.replace(
      /([A-Z])/g,
      // Attributes are written in dash case
      (n5) => "-" + n5.toLowerCase()
    ))
  );
  if (children !== void 0) {
    instance.textContent = children;
  }
  for (const name in style) {
    if (style.hasOwnProperty(name)) {
      const value = dangerousStyleValue2(name, style[name]);
      if (isCustomPropRE2.test(name)) {
        instance.style.setProperty(name, value);
      } else {
        instance.style[name] = value;
      }
    }
  }
  names.forEach((name, i5) => {
    instance.setAttribute(name, values[i5]);
  });
  if (className !== void 0) {
    instance.className = className;
  }
  if (scrollTop !== void 0) {
    instance.scrollTop = scrollTop;
  }
  if (scrollLeft !== void 0) {
    instance.scrollLeft = scrollLeft;
  }
  if (viewBox !== void 0) {
    instance.setAttribute("viewBox", viewBox);
  }
}
var isUnitlessNumber2 = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
var prefixKey2 = (prefix3, key) => prefix3 + key.charAt(0).toUpperCase() + key.substring(1);
var prefixes4 = ["Webkit", "Ms", "Moz", "O"];
isUnitlessNumber2 = Object.keys(isUnitlessNumber2).reduce((acc, prop) => {
  prefixes4.forEach((prefix3) => acc[prefixKey2(prefix3, prop)] = acc[prop]);
  return acc;
}, isUnitlessNumber2);
var domTransforms2 = /^(matrix|translate|scale|rotate|skew)/;
var pxTransforms2 = /^(translate)/;
var degTransforms2 = /^(rotate|skew)/;
var addUnit2 = (value, unit) => is2.num(value) && value !== 0 ? value + unit : value;
var isValueIdentity2 = (value, id) => is2.arr(value) ? value.every((v4) => isValueIdentity2(v4, id)) : is2.num(value) ? value === id : parseFloat(value) === id;
var AnimatedStyle2 = class extends AnimatedObject2 {
  constructor({ x: x4, y: y6, z: z3, ...style }) {
    const inputs = [];
    const transforms = [];
    if (x4 || y6 || z3) {
      inputs.push([x4 || 0, y6 || 0, z3 || 0]);
      transforms.push((xyz) => [
        `translate3d(${xyz.map((v4) => addUnit2(v4, "px")).join(",")})`,
        // prettier-ignore
        isValueIdentity2(xyz, 0)
      ]);
    }
    eachProp2(style, (value, key) => {
      if (key === "transform") {
        inputs.push([value || ""]);
        transforms.push((transform) => [transform, transform === ""]);
      } else if (domTransforms2.test(key)) {
        delete style[key];
        if (is2.und(value))
          return;
        const unit = pxTransforms2.test(key) ? "px" : degTransforms2.test(key) ? "deg" : "";
        inputs.push(toArray2(value));
        transforms.push(
          key === "rotate3d" ? ([x22, y22, z22, deg]) => [
            `rotate3d(${x22},${y22},${z22},${addUnit2(deg, unit)})`,
            isValueIdentity2(deg, 0)
          ] : (input) => [
            `${key}(${input.map((v4) => addUnit2(v4, unit)).join(",")})`,
            isValueIdentity2(input, key.startsWith("scale") ? 1 : 0)
          ]
        );
      }
    });
    if (inputs.length) {
      style.transform = new FluidTransform2(inputs, transforms);
    }
    super(style);
  }
};
var FluidTransform2 = class extends FluidValue2 {
  constructor(inputs, transforms) {
    super();
    this.inputs = inputs;
    this.transforms = transforms;
    this._value = null;
  }
  get() {
    return this._value || (this._value = this._get());
  }
  _get() {
    let transform = "";
    let identity10 = true;
    each2(this.inputs, (input, i5) => {
      const arg1 = getFluidValue2(input[0]);
      const [t7, id] = this.transforms[i5](
        is2.arr(arg1) ? arg1 : input.map(getFluidValue2)
      );
      transform += " " + t7;
      identity10 = identity10 && id;
    });
    return identity10 ? "none" : transform;
  }
  // Start observing our inputs once we have an observer.
  observerAdded(count5) {
    if (count5 == 1)
      each2(
        this.inputs,
        (input) => each2(
          input,
          (value) => hasFluidValue2(value) && addFluidObserver2(value, this)
        )
      );
  }
  // Stop observing our inputs once we have no observers.
  observerRemoved(count5) {
    if (count5 == 0)
      each2(
        this.inputs,
        (input) => each2(
          input,
          (value) => hasFluidValue2(value) && removeFluidObserver2(value, this)
        )
      );
  }
  eventObserved(event) {
    if (event.type == "change") {
      this._value = null;
    }
    callFluidObservers2(this, event);
  }
};
var primitives2 = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "big",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "keygen",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "menu",
  "menuitem",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  // SVG
  "circle",
  "clipPath",
  "defs",
  "ellipse",
  "foreignObject",
  "g",
  "image",
  "line",
  "linearGradient",
  "mask",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "radialGradient",
  "rect",
  "stop",
  "svg",
  "text",
  "tspan"
];
globals_exports2.assign({
  batchedUpdates: import_react_dom2.unstable_batchedUpdates,
  createStringInterpolator: createStringInterpolator22,
  colors: colors22
});
var host2 = createHost2(primitives2, {
  applyAnimatedValues: applyAnimatedValues2,
  createAnimatedStyle: (style) => new AnimatedStyle2(style),
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  getComponentProps: ({ scrollTop, scrollLeft, ...props }) => props
});
var animated2 = host2.animated;

// node_modules/@nivo/treemap/node_modules/@nivo/tooltip/dist/nivo-tooltip.es.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime());
function v3() {
  return v3 = Object.assign ? Object.assign.bind() : function(t7) {
    for (var i5 = 1; i5 < arguments.length; i5++) {
      var o5 = arguments[i5];
      for (var n5 in o5) Object.prototype.hasOwnProperty.call(o5, n5) && (t7[n5] = o5[n5]);
    }
    return t7;
  }, v3.apply(this, arguments);
}
var x3 = { pointerEvents: "none", position: "absolute", zIndex: 10, top: 0, left: 0 };
var m3 = function(t7, i5) {
  return "translate(" + t7 + "px, " + i5 + "px)";
};
var b2 = (0, import_react32.memo)(function(t7) {
  var o5, n5 = t7.position, r5 = t7.anchor, e7 = t7.children, l4 = zt(), d2 = Ur(), y6 = d2.animate, f3 = d2.config, b3 = kt(), g3 = b3[0], w3 = b3[1], T3 = (0, import_react32.useRef)(false), C7 = void 0, E5 = false, P3 = w3.width > 0 && w3.height > 0, j4 = Math.round(n5[0]), N4 = Math.round(n5[1]);
  P3 && ("top" === r5 ? (j4 -= w3.width / 2, N4 -= w3.height + 14) : "right" === r5 ? (j4 += 14, N4 -= w3.height / 2) : "bottom" === r5 ? (j4 -= w3.width / 2, N4 += 14) : "left" === r5 ? (j4 -= w3.width + 14, N4 -= w3.height / 2) : "center" === r5 && (j4 -= w3.width / 2, N4 -= w3.height / 2), C7 = { transform: m3(j4, N4) }, T3.current || (E5 = true), T3.current = [j4, N4]);
  var O4 = useSpring2({ to: C7, config: f3, immediate: !y6 || E5 }), V3 = v3({}, x3, l4.tooltip.wrapper, { transform: null != (o5 = O4.transform) ? o5 : m3(j4, N4), opacity: O4.transform ? 1 : 0 });
  return (0, import_jsx_runtime3.jsx)(animated2.div, { ref: g3, style: V3, children: e7 });
});
b2.displayName = "TooltipWrapper";
var g2 = (0, import_react32.memo)(function(t7) {
  var i5 = t7.size, o5 = void 0 === i5 ? 12 : i5, n5 = t7.color, r5 = t7.style;
  return (0, import_jsx_runtime3.jsx)("span", { style: v3({ display: "block", width: o5, height: o5, background: n5 }, void 0 === r5 ? {} : r5) });
});
var w2 = (0, import_react32.memo)(function(t7) {
  var i5, o5 = t7.id, n5 = t7.value, r5 = t7.format, e7 = t7.enableChip, l4 = void 0 !== e7 && e7, a4 = t7.color, c10 = t7.renderContent, h = zt(), u2 = Ot(r5);
  if ("function" == typeof c10) i5 = c10();
  else {
    var f3 = n5;
    void 0 !== u2 && void 0 !== f3 && (f3 = u2(f3)), i5 = (0, import_jsx_runtime3.jsxs)("div", { style: h.tooltip.basic, children: [l4 && (0, import_jsx_runtime3.jsx)(g2, { color: a4, style: h.tooltip.chip }), void 0 !== f3 ? (0, import_jsx_runtime3.jsxs)("span", { children: [o5, ": ", (0, import_jsx_runtime3.jsx)("strong", { children: "" + f3 })] }) : o5] });
  }
  return (0, import_jsx_runtime3.jsx)("div", { style: h.tooltip.container, children: i5 });
});
var T2 = { width: "100%", borderCollapse: "collapse" };
var C5 = (0, import_react32.memo)(function(t7) {
  var i5, o5 = t7.title, n5 = t7.rows, r5 = void 0 === n5 ? [] : n5, e7 = t7.renderContent, l4 = zt();
  return r5.length ? (i5 = "function" == typeof e7 ? e7() : (0, import_jsx_runtime3.jsxs)("div", { children: [o5 && o5, (0, import_jsx_runtime3.jsx)("table", { style: v3({}, T2, l4.tooltip.table), children: (0, import_jsx_runtime3.jsx)("tbody", { children: r5.map(function(t8, i6) {
    return (0, import_jsx_runtime3.jsx)("tr", { children: t8.map(function(t9, i7) {
      return (0, import_jsx_runtime3.jsx)("td", { style: l4.tooltip.tableCell, children: t9 }, i7);
    }) }, i6);
  }) }) })] }), (0, import_jsx_runtime3.jsx)("div", { style: l4.tooltip.container, children: i5 })) : null;
});
C5.displayName = "TableTooltip";
var E4 = (0, import_react32.memo)(function(t7) {
  var i5 = t7.x0, n5 = t7.x1, r5 = t7.y0, e7 = t7.y1, l4 = zt(), u2 = Ur(), d2 = u2.animate, y6 = u2.config, f3 = (0, import_react32.useMemo)(function() {
    return v3({}, l4.crosshair.line, { pointerEvents: "none" });
  }, [l4.crosshair.line]), x4 = useSpring2({ x1: i5, x2: n5, y1: r5, y2: e7, config: y6, immediate: !d2 });
  return (0, import_jsx_runtime3.jsx)(animated2.line, v3({}, x4, { fill: "none", style: f3 }));
});
E4.displayName = "CrosshairLine";
var P2 = (0, import_react32.memo)(function(t7) {
  var i5, o5, n5 = t7.width, r5 = t7.height, e7 = t7.type, l4 = t7.x, a4 = t7.y;
  return "cross" === e7 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: r5 }, o5 = { x0: 0, x1: n5, y0: a4, y1: a4 }) : "top-left" === e7 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: a4 }, o5 = { x0: 0, x1: l4, y0: a4, y1: a4 }) : "top" === e7 ? i5 = { x0: l4, x1: l4, y0: 0, y1: a4 } : "top-right" === e7 ? (i5 = { x0: l4, x1: l4, y0: 0, y1: a4 }, o5 = { x0: l4, x1: n5, y0: a4, y1: a4 }) : "right" === e7 ? o5 = { x0: l4, x1: n5, y0: a4, y1: a4 } : "bottom-right" === e7 ? (i5 = { x0: l4, x1: l4, y0: a4, y1: r5 }, o5 = { x0: l4, x1: n5, y0: a4, y1: a4 }) : "bottom" === e7 ? i5 = { x0: l4, x1: l4, y0: a4, y1: r5 } : "bottom-left" === e7 ? (i5 = { x0: l4, x1: l4, y0: a4, y1: r5 }, o5 = { x0: 0, x1: l4, y0: a4, y1: a4 }) : "left" === e7 ? o5 = { x0: 0, x1: l4, y0: a4, y1: a4 } : "x" === e7 ? i5 = { x0: l4, x1: l4, y0: 0, y1: r5 } : "y" === e7 && (o5 = { x0: 0, x1: n5, y0: a4, y1: a4 }), (0, import_jsx_runtime3.jsxs)(import_jsx_runtime3.Fragment, { children: [i5 && (0, import_jsx_runtime3.jsx)(E4, { x0: i5.x0, x1: i5.x1, y0: i5.y0, y1: i5.y1 }), o5 && (0, import_jsx_runtime3.jsx)(E4, { x0: o5.x0, x1: o5.x1, y0: o5.y0, y1: o5.y1 })] });
});
P2.displayName = "Crosshair";
var j2 = (0, import_react32.createContext)({ showTooltipAt: function() {
}, showTooltipFromEvent: function() {
}, hideTooltip: function() {
} });
var N2 = { isVisible: false, position: [null, null], content: null, anchor: null };
var O3 = (0, import_react32.createContext)(N2);
var k2 = function() {
  var t7 = (0, import_react32.useContext)(j2);
  if (void 0 === t7) throw new Error("useTooltip must be used within a TooltipProvider");
  return t7;
};

// node_modules/@nivo/treemap/dist/nivo-treemap.es.js
var import_jsx_runtime4 = __toESM(require_jsx_runtime());
function j3() {
  return j3 = Object.assign ? Object.assign.bind() : function(e7) {
    for (var t7 = 1; t7 < arguments.length; t7++) {
      var o5 = arguments[t7];
      for (var n5 in o5) Object.prototype.hasOwnProperty.call(o5, n5) && (e7[n5] = o5[n5]);
    }
    return e7;
  }, j3.apply(this, arguments);
}
function D3(e7, t7) {
  if (null == e7) return {};
  var o5, n5, i5 = {}, a4 = Object.keys(e7);
  for (n5 = 0; n5 < a4.length; n5++) o5 = a4[n5], t7.indexOf(o5) >= 0 || (i5[o5] = e7[o5]);
  return i5;
}
var H = function(e7, t7) {
  return to4([e7, t7], function(e8, t8) {
    return "translate(" + e8 + "," + t8 + ")";
  });
};
var V2 = function(e7, t7) {
  return to4([e7, t7], function(e8, t8) {
    return "translate(" + e8 + "px, " + t8 + "px)";
  });
};
var q2 = function(e7, t7, o5) {
  return to4([e7, t7, o5], function(e8, t8, o6) {
    return "translate(" + e8 + "," + t8 + ") rotate(" + o6 + ")";
  });
};
var A4 = function(e7, t7, o5) {
  return to4([e7, t7, o5], function(e8, t8, o6) {
    return "translate(" + e8 + "px," + t8 + "px) rotate(" + o6 + "deg)";
  });
};
var G = function(e7, t7, o5) {
  return to4([e7, t7, o5], function(e8, t8, o6) {
    return "translate(" + (e8 - (0 === o6 ? 0 : 5)) + "px," + (t8 - (0 === o6 ? 5 : 0)) + "px) rotate(" + o6 + "deg)";
  });
};
var J = (0, import_react33.memo)(function(e7) {
  var t7 = e7.node, o5 = e7.animatedProps, n5 = e7.borderWidth, i5 = e7.enableLabel, a4 = e7.enableParentLabel, r5 = e7.labelSkipSize, d2 = zt(), b3 = i5 && t7.isLeaf && (0 === r5 || Math.min(t7.width, t7.height) > r5), u2 = a4 && t7.isParent;
  return (0, import_jsx_runtime4.jsxs)(animated2.g, { transform: H(o5.x, o5.y), children: [(0, import_jsx_runtime4.jsx)(animated2.rect, { "data-testid": "node." + t7.id, width: to4(o5.width, function(e8) {
    return Math.max(e8, 0);
  }), height: to4(o5.height, function(e8) {
    return Math.max(e8, 0);
  }), fill: t7.fill ? t7.fill : o5.color, strokeWidth: n5, stroke: t7.borderColor, fillOpacity: t7.opacity, onMouseEnter: t7.onMouseEnter, onMouseMove: t7.onMouseMove, onMouseLeave: t7.onMouseLeave, onClick: t7.onClick }), b3 && (0, import_jsx_runtime4.jsx)(animated2.text, { "data-testid": "label." + t7.id, textAnchor: "middle", dominantBaseline: "central", style: j3({}, d2.labels.text, { fill: t7.labelTextColor, pointerEvents: "none" }), fillOpacity: o5.labelOpacity, transform: q2(o5.labelX, o5.labelY, o5.labelRotation), children: t7.label }), u2 && (0, import_jsx_runtime4.jsx)(animated2.text, { "data-testid": "parentLabel." + t7.id, dominantBaseline: "central", style: j3({}, d2.labels.text, { fill: t7.parentLabelTextColor, pointerEvents: "none" }), fillOpacity: o5.parentLabelOpacity, transform: q2(o5.parentLabelX, o5.parentLabelY, o5.parentLabelRotation), children: t7.parentLabel })] });
});
var K3 = (0, import_react33.memo)(function(e7) {
  var t7 = e7.node;
  return (0, import_jsx_runtime4.jsx)(w2, { id: t7.id, value: t7.formattedValue, enableChip: true, color: t7.color });
});
var N3 = (0, import_react33.memo)(function(e7) {
  var t7 = e7.node, o5 = e7.animatedProps, n5 = e7.borderWidth, i5 = e7.enableLabel, a4 = e7.enableParentLabel, r5 = e7.labelSkipSize, d2 = zt(), b3 = i5 && t7.isLeaf && (0 === r5 || Math.min(t7.width, t7.height) > r5), u2 = a4 && t7.isParent;
  return (0, import_jsx_runtime4.jsxs)(animated2.div, { "data-testid": "node." + t7.id, id: t7.path.replace(/[^\w]/gi, "-"), style: { boxSizing: "border-box", position: "absolute", top: 0, left: 0, transform: V2(o5.x, o5.y), width: o5.width, height: o5.height, borderWidth: n5, borderStyle: "solid", borderColor: t7.borderColor, overflow: "hidden" }, children: [(0, import_jsx_runtime4.jsx)(animated2.div, { style: { boxSizing: "border-box", position: "absolute", top: 0, left: 0, opacity: t7.opacity, width: o5.width, height: o5.height, background: o5.color }, onMouseEnter: t7.onMouseEnter, onMouseMove: t7.onMouseMove, onMouseLeave: t7.onMouseLeave, onClick: t7.onClick }), b3 && (0, import_jsx_runtime4.jsx)(animated2.span, { "data-testid": "label." + t7.id, style: j3({}, d2.labels.text, { position: "absolute", display: "flex", top: -5, left: -5, width: 10, height: 10, justifyContent: "center", alignItems: "center", whiteSpace: "nowrap", color: t7.labelTextColor, transformOrigin: "center center", transform: A4(o5.labelX, o5.labelY, o5.labelRotation), opacity: o5.labelOpacity, pointerEvents: "none" }), children: t7.label }), u2 && (0, import_jsx_runtime4.jsx)(animated2.span, { "data-testid": "parentLabel." + t7.id, style: j3({}, d2.labels.text, { position: "absolute", display: "flex", justifyContent: "flex-start", alignItems: "center", whiteSpace: "nowrap", width: 10, height: 10, color: t7.parentLabelTextColor, transformOrigin: "top left", transform: G(o5.parentLabelX, o5.parentLabelY, o5.parentLabelRotation), opacity: o5.parentLabelOpacity, pointerEvents: "none" }), children: t7.parentLabel })] });
});
var Q = { layers: ["nodes"], identity: "id", value: "value", tile: "squarify", leavesOnly: false, innerPadding: 0, outerPadding: 0, colors: { scheme: "nivo" }, colorBy: "pathComponents.1", nodeOpacity: 0.33, enableLabel: true, label: "formattedValue", labelSkipSize: 0, labelTextColor: { from: "color", modifiers: [["darker", 1]] }, orientLabel: true, enableParentLabel: true, parentLabel: "id", parentLabelSize: 20, parentLabelPosition: "top", parentLabelPadding: 6, parentLabelTextColor: { from: "color", modifiers: [["darker", 1]] }, borderWidth: 1, borderColor: { from: "color", modifiers: [["darker", 1]] }, isInteractive: true, tooltip: K3, role: "img", animate: true, motionConfig: "gentle" };
var U = j3({}, Q, { nodeComponent: J, defs: [], fill: [] });
var Z = j3({}, Q, { nodeComponent: N3 });
var $ = j3({}, Q, { pixelRatio: "undefined" != typeof window && window.devicePixelRatio || 1 });
var _ = { binary: binary_default, dice: dice_default, slice: slice_default, sliceDice: sliceDice_default, squarify: squarify_default };
var ee = function(e7) {
  var t7 = e7.root, o5 = e7.getValue;
  return (0, import_react33.useMemo)(function() {
    return hierarchy(t7).sum(o5);
  }, [t7, o5]);
};
var te = function(e7) {
  var t7 = e7.data, o5 = e7.width, i5 = e7.height, a4 = e7.identity, r5 = void 0 === a4 ? Q.identity : a4, u2 = e7.value, p3 = void 0 === u2 ? Q.value : u2, c10 = e7.valueFormat, s3 = e7.leavesOnly, v4 = void 0 === s3 ? Q.leavesOnly : s3, h = e7.tile, f3 = void 0 === h ? Q.tile : h, L = e7.innerPadding, g3 = void 0 === L ? Q.innerPadding : L, x4 = e7.outerPadding, P3 = void 0 === x4 ? Q.outerPadding : x4, M2 = e7.label, w3 = void 0 === M2 ? Q.label : M2, S = e7.orientLabel, k3 = void 0 === S ? Q.orientLabel : S, I = e7.enableParentLabel, z3 = void 0 === I ? Q.enableParentLabel : I, B3 = e7.parentLabel, R = void 0 === B3 ? Q.parentLabel : B3, E5 = e7.parentLabelSize, X2 = void 0 === E5 ? Q.parentLabelSize : E5, Y2 = e7.parentLabelPosition, F3 = void 0 === Y2 ? Q.parentLabelPosition : Y2, D4 = e7.parentLabelPadding, H2 = void 0 === D4 ? Q.parentLabelPadding : D4, V3 = e7.colors, q3 = void 0 === V3 ? Q.colors : V3, A5 = e7.colorBy, G2 = void 0 === A5 ? Q.colorBy : A5, J2 = e7.nodeOpacity, K4 = void 0 === J2 ? Q.nodeOpacity : J2, N4 = e7.borderColor, U2 = void 0 === N4 ? Q.borderColor : N4, Z2 = e7.labelTextColor, $2 = void 0 === Z2 ? Q.labelTextColor : Z2, te2 = e7.parentLabelTextColor, oe2 = void 0 === te2 ? Q.parentLabelTextColor : te2, ne2 = Wn(r5), ie2 = Wn(p3), ae2 = Ot(c10), re4 = Wn(w3), le2 = Wn(R), de2 = function(e8) {
    var t8 = e8.width, o6 = e8.height, i6 = e8.tile, a5 = e8.innerPadding, r6 = e8.outerPadding, l4 = e8.enableParentLabel, d2 = e8.parentLabelSize, b3 = e8.parentLabelPosition, u3 = e8.leavesOnly;
    return (0, import_react33.useMemo)(function() {
      var e9 = treemap_default().size([t8, o6]).tile(_[i6]).round(true).paddingInner(a5).paddingOuter(r6);
      if (l4 && !u3) {
        var n5 = d2 + 2 * r6;
        e9["padding" + (0, import_startCase.default)(b3)](n5);
      }
      return e9;
    }, [t8, o6, i6, a5, r6, l4, d2, b3, u3]);
  }({ width: o5, height: i5, tile: f3, innerPadding: g3, outerPadding: P3, enableParentLabel: z3, parentLabelSize: X2, parentLabelPosition: F3, leavesOnly: v4 }), be2 = ee({ root: t7, getValue: ie2 }), ue2 = (0, import_react33.useMemo)(function() {
    var e8 = (0, import_cloneDeep.default)(be2);
    return de2(e8), v4 ? e8.leaves() : e8.descendants();
  }, [be2, de2, v4]), pe2 = (0, import_react33.useMemo)(function() {
    return ue2.map(function(e8) {
      var t8 = function(e9, t9) {
        var o7 = e9.ancestors().map(function(e11) {
          return t9(e11.data);
        }).reverse();
        return { path: o7.join("."), pathComponents: o7 };
      }(e8, ne2), o6 = t8.path, n5 = t8.pathComponents, i6 = { id: ne2(e8.data), path: o6, pathComponents: n5, data: (0, import_omit.default)(e8.data, "children"), x: e8.x0, y: e8.y0, width: e8.x1 - e8.x0, height: e8.y1 - e8.y0, value: e8.value, formattedValue: ae2(e8.value), treeDepth: e8.depth, treeHeight: e8.height, isParent: e8.height > 0, isLeaf: 0 === e8.height, parentLabelX: 0, parentLabelY: 0, parentLabelRotation: 0 };
      return i6.labelRotation = k3 && i6.height > i6.width ? -90 : 0, "top" === F3 && (i6.parentLabelX = P3 + H2, i6.parentLabelY = P3 + X2 / 2), "right" === F3 && (i6.parentLabelX = i6.width - P3 - X2 / 2, i6.parentLabelY = i6.height - P3 - H2, i6.parentLabelRotation = -90), "bottom" === F3 && (i6.parentLabelX = P3 + H2, i6.parentLabelY = i6.height - P3 - X2 / 2), "left" === F3 && (i6.parentLabelX = P3 + X2 / 2, i6.parentLabelY = i6.height - P3 - H2, i6.parentLabelRotation = -90), i6.label = re4(i6), i6.parentLabel = le2(i6), i6;
    });
  }, [ue2, ne2, ae2, re4, k3, le2, X2, F3, H2, P3]), ce2 = zt(), se2 = pr(q3, G2), ve2 = Xe(U2, ce2), he2 = Xe($2, ce2), fe2 = Xe(oe2, ce2), Le3 = (0, import_react33.useMemo)(function() {
    return pe2.map(function(e8) {
      var t8 = j3({}, e8, { color: se2(e8), opacity: K4 });
      return t8.borderColor = ve2(t8), t8.labelTextColor = he2(t8), t8.parentLabelTextColor = fe2(t8), t8;
    });
  }, [pe2, se2, K4, ve2, he2, fe2]);
  return { hierarchy: be2, nodes: Le3, layout: de2 };
};
var oe = function(e7) {
  var t7 = e7.nodes;
  return (0, import_react33.useMemo)(function() {
    return { nodes: t7 };
  }, [t7]);
};
var ne = function(e7) {
  return { x: e7.x, y: e7.y, width: e7.width, height: e7.height, color: e7.color, labelX: e7.width / 2, labelY: e7.height / 2, labelRotation: e7.labelRotation, labelOpacity: 1, parentLabelX: e7.parentLabelX, parentLabelY: e7.parentLabelY, parentLabelRotation: e7.parentLabelRotation, parentLabelOpacity: 1 };
};
var ie = function(e7) {
  return { x: e7.x + e7.width / 2, y: e7.y + e7.height / 2, width: 0, height: 0, color: e7.color, labelX: 0, labelY: 0, labelRotation: e7.labelRotation, labelOpacity: 0, parentLabelX: 0, parentLabelY: 0, parentLabelRotation: e7.parentLabelRotation, parentLabelOpacity: 0 };
};
var ae = (0, import_react33.memo)(function(e7) {
  var i5 = e7.nodes, a4 = e7.nodeComponent, r5 = e7.borderWidth, l4 = e7.enableLabel, d2 = e7.labelSkipSize, b3 = e7.enableParentLabel, p3 = function(e8, i6) {
    var a5 = i6.isInteractive, r6 = i6.onMouseEnter, l5 = i6.onMouseMove, d3 = i6.onMouseLeave, b4 = i6.onClick, u2 = i6.tooltip, p4 = k2(), c11 = p4.showTooltipFromEvent, s4 = p4.hideTooltip, v5 = (0, import_react33.useCallback)(function(e9, t7) {
      c11((0, import_react33.createElement)(u2, { node: e9 }), t7, "left");
    }, [c11, u2]), h2 = (0, import_react33.useCallback)(function(e9, t7) {
      v5(e9, t7), null == r6 || r6(e9, t7);
    }, [r6, v5]), f3 = (0, import_react33.useCallback)(function(e9, t7) {
      v5(e9, t7), null == l5 || l5(e9, t7);
    }, [l5, v5]), L = (0, import_react33.useCallback)(function(e9, t7) {
      s4(), null == d3 || d3(e9, t7);
    }, [d3, s4]), g3 = (0, import_react33.useCallback)(function(e9, t7) {
      null == b4 || b4(e9, t7);
    }, [b4]);
    return (0, import_react33.useMemo)(function() {
      return e8.map(function(e9) {
        return a5 ? j3({}, e9, { onMouseEnter: function(t7) {
          return h2(e9, t7);
        }, onMouseMove: function(t7) {
          return f3(e9, t7);
        }, onMouseLeave: function(t7) {
          return L(e9, t7);
        }, onClick: function(t7) {
          return g3(e9, t7);
        } }) : e9;
      });
    }, [a5, e8, h2, f3, L, g3]);
  }(i5, { isInteractive: e7.isInteractive, onMouseEnter: e7.onMouseEnter, onMouseMove: e7.onMouseMove, onMouseLeave: e7.onMouseLeave, onClick: e7.onClick, tooltip: e7.tooltip }), c10 = Ur(), s3 = c10.animate, v4 = c10.config, h = useTransition(p3, { keys: function(e8) {
    return e8.path;
  }, initial: ne, from: ie, enter: ne, update: ne, leave: ie, config: v4, immediate: !s3 });
  return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: h(function(e8, t7) {
    return (0, import_react33.createElement)(a4, { key: t7.path, node: t7, animatedProps: e8, borderWidth: r5, enableLabel: l4, labelSkipSize: d2, enableParentLabel: b3 });
  }) });
});
var re3 = ["isInteractive", "animate", "motionConfig", "theme", "renderWrapper"];
var le = function(e7) {
  var t7 = e7.data, n5 = e7.identity, a4 = void 0 === n5 ? U.identity : n5, r5 = e7.value, l4 = void 0 === r5 ? U.value : r5, d2 = e7.valueFormat, b3 = e7.tile, u2 = void 0 === b3 ? U.tile : b3, p3 = e7.nodeComponent, h = void 0 === p3 ? U.nodeComponent : p3, f3 = e7.innerPadding, L = void 0 === f3 ? U.innerPadding : f3, g3 = e7.outerPadding, m5 = void 0 === g3 ? U.outerPadding : g3, y6 = e7.leavesOnly, C7 = void 0 === y6 ? U.leavesOnly : y6, x4 = e7.width, P3 = e7.height, M2 = e7.margin, w3 = e7.layers, S = void 0 === w3 ? U.layers : w3, O4 = e7.colors, k3 = void 0 === O4 ? U.colors : O4, T3 = e7.colorBy, W2 = void 0 === T3 ? U.colorBy : T3, I = e7.nodeOpacity, z3 = void 0 === I ? U.nodeOpacity : I, B3 = e7.borderWidth, R = void 0 === B3 ? U.borderWidth : B3, E5 = e7.borderColor, X2 = void 0 === E5 ? U.borderColor : E5, F3 = e7.defs, j4 = void 0 === F3 ? U.defs : F3, D4 = e7.fill, H2 = void 0 === D4 ? U.fill : D4, V3 = e7.enableLabel, q3 = void 0 === V3 ? U.enableLabel : V3, A5 = e7.label, G2 = void 0 === A5 ? U.label : A5, J2 = e7.labelTextColor, K4 = void 0 === J2 ? U.labelTextColor : J2, N4 = e7.orientLabel, Q2 = void 0 === N4 ? U.orientLabel : N4, Z2 = e7.labelSkipSize, $2 = void 0 === Z2 ? U.labelSkipSize : Z2, _2 = e7.enableParentLabel, ee2 = void 0 === _2 ? U.enableParentLabel : _2, ne2 = e7.parentLabel, ie2 = void 0 === ne2 ? U.parentLabel : ne2, re4 = e7.parentLabelSize, le2 = void 0 === re4 ? U.parentLabelSize : re4, de2 = e7.parentLabelPosition, be2 = void 0 === de2 ? U.parentLabelPosition : de2, ue2 = e7.parentLabelPadding, pe2 = void 0 === ue2 ? U.parentLabelPadding : ue2, ce2 = e7.parentLabelTextColor, se2 = void 0 === ce2 ? U.parentLabelTextColor : ce2, ve2 = e7.isInteractive, he2 = void 0 === ve2 ? U.isInteractive : ve2, fe2 = e7.onMouseEnter, Le3 = e7.onMouseMove, ge2 = e7.onMouseLeave, me = e7.onClick, ye = e7.tooltip, Ce2 = void 0 === ye ? U.tooltip : ye, xe = e7.role, Pe2 = e7.ariaLabel, Me2 = e7.ariaLabelledBy, we2 = e7.ariaDescribedBy, Se = wt(x4, P3, M2), Oe2 = Se.margin, ke2 = Se.innerWidth, Te2 = Se.innerHeight, We2 = Se.outerWidth, Ie = Se.outerHeight, ze = te({ data: t7, identity: a4, value: l4, valueFormat: d2, leavesOnly: C7, width: ke2, height: Te2, tile: u2, innerPadding: L, outerPadding: m5, colors: k3, colorBy: W2, nodeOpacity: z3, borderColor: X2, label: G2, labelTextColor: K4, orientLabel: Q2, enableParentLabel: ee2, parentLabel: ie2, parentLabelSize: le2, parentLabelPosition: be2, parentLabelPadding: pe2, parentLabelTextColor: se2 }).nodes, Be2 = { nodes: null };
  S.includes("nodes") && (Be2.nodes = (0, import_jsx_runtime4.jsx)(ae, { nodes: ze, nodeComponent: h, borderWidth: R, enableLabel: q3, labelSkipSize: $2, enableParentLabel: ee2, isInteractive: he2, onMouseEnter: fe2, onMouseMove: Le3, onMouseLeave: ge2, onClick: me, tooltip: Ce2 }, "nodes"));
  var Re2 = oe({ nodes: ze }), Ee = In(j4, ze, H2);
  return (0, import_jsx_runtime4.jsx)(gn, { width: We2, height: Ie, margin: Oe2, defs: Ee, role: xe, ariaLabel: Pe2, ariaLabelledBy: Me2, ariaDescribedBy: we2, children: S.map(function(e8, t8) {
    var n6;
    return "function" == typeof e8 ? (0, import_jsx_runtime4.jsx)(import_react33.Fragment, { children: (0, import_react33.createElement)(e8, Re2) }, t8) : null != (n6 = null == Be2 ? void 0 : Be2[e8]) ? n6 : null;
  }) });
};
var de = function(e7) {
  var t7 = e7.isInteractive, o5 = void 0 === t7 ? U.isInteractive : t7, n5 = e7.animate, i5 = void 0 === n5 ? U.animate : n5, a4 = e7.motionConfig, r5 = void 0 === a4 ? U.motionConfig : a4, l4 = e7.theme, d2 = e7.renderWrapper, b3 = D3(e7, re3);
  return (0, import_jsx_runtime4.jsx)(St, { animate: i5, isInteractive: o5, motionConfig: r5, renderWrapper: d2, theme: l4, children: (0, import_jsx_runtime4.jsx)(le, j3({ isInteractive: o5 }, b3)) });
};
var be = function(e7) {
  return (0, import_jsx_runtime4.jsx)(It, { children: function(t7) {
    var o5 = t7.width, n5 = t7.height;
    return (0, import_jsx_runtime4.jsx)(de, j3({ width: o5, height: n5 }, e7));
  } });
};
var ue = ["isInteractive", "animate", "motionConfig", "theme", "renderWrapper"];
var pe = function(e7) {
  var t7 = e7.data, n5 = e7.identity, a4 = void 0 === n5 ? Z.identity : n5, r5 = e7.value, l4 = void 0 === r5 ? Z.value : r5, d2 = e7.tile, b3 = void 0 === d2 ? Z.tile : d2, u2 = e7.nodeComponent, p3 = void 0 === u2 ? Z.nodeComponent : u2, s3 = e7.valueFormat, v4 = e7.innerPadding, h = void 0 === v4 ? Z.innerPadding : v4, f3 = e7.outerPadding, L = void 0 === f3 ? Z.outerPadding : f3, g3 = e7.leavesOnly, m5 = void 0 === g3 ? Z.leavesOnly : g3, y6 = e7.width, C7 = e7.height, x4 = e7.margin, P3 = e7.layers, M2 = void 0 === P3 ? U.layers : P3, w3 = e7.colors, S = void 0 === w3 ? Z.colors : w3, O4 = e7.colorBy, k3 = void 0 === O4 ? Z.colorBy : O4, T3 = e7.nodeOpacity, W2 = void 0 === T3 ? Z.nodeOpacity : T3, I = e7.borderWidth, z3 = void 0 === I ? Z.borderWidth : I, B3 = e7.borderColor, R = void 0 === B3 ? Z.borderColor : B3, E5 = e7.enableLabel, X2 = void 0 === E5 ? Z.enableLabel : E5, F3 = e7.label, j4 = void 0 === F3 ? Z.label : F3, D4 = e7.labelTextColor, H2 = void 0 === D4 ? Z.labelTextColor : D4, V3 = e7.orientLabel, q3 = void 0 === V3 ? Z.orientLabel : V3, A5 = e7.labelSkipSize, G2 = void 0 === A5 ? Z.labelSkipSize : A5, J2 = e7.enableParentLabel, K4 = void 0 === J2 ? Z.enableParentLabel : J2, N4 = e7.parentLabel, Q2 = void 0 === N4 ? Z.parentLabel : N4, $2 = e7.parentLabelSize, _2 = void 0 === $2 ? Z.parentLabelSize : $2, ee2 = e7.parentLabelPosition, ne2 = void 0 === ee2 ? Z.parentLabelPosition : ee2, ie2 = e7.parentLabelPadding, re4 = void 0 === ie2 ? Z.parentLabelPadding : ie2, le2 = e7.parentLabelTextColor, de2 = void 0 === le2 ? Z.parentLabelTextColor : le2, be2 = e7.isInteractive, ue2 = void 0 === be2 ? Z.isInteractive : be2, pe2 = e7.onMouseEnter, ce2 = e7.onMouseMove, se2 = e7.onMouseLeave, ve2 = e7.onClick, he2 = e7.tooltip, fe2 = void 0 === he2 ? Z.tooltip : he2, Le3 = e7.role, ge2 = e7.ariaLabel, me = e7.ariaLabelledBy, ye = e7.ariaDescribedBy, Ce2 = wt(y6, C7, x4), xe = Ce2.margin, Pe2 = Ce2.innerWidth, Me2 = Ce2.innerHeight, we2 = Ce2.outerWidth, Se = Ce2.outerHeight, Oe2 = te({ data: t7, identity: a4, value: l4, valueFormat: s3, leavesOnly: m5, width: Pe2, height: Me2, tile: b3, innerPadding: h, outerPadding: L, colors: S, colorBy: k3, nodeOpacity: W2, borderColor: R, label: j4, labelTextColor: H2, orientLabel: q3, enableParentLabel: K4, parentLabel: Q2, parentLabelSize: _2, parentLabelPosition: ne2, parentLabelPadding: re4, parentLabelTextColor: de2 }).nodes, ke2 = { nodes: null };
  M2.includes("nodes") && (ke2.nodes = (0, import_jsx_runtime4.jsx)(ae, { nodes: Oe2, nodeComponent: p3, borderWidth: z3, enableLabel: X2, labelSkipSize: G2, enableParentLabel: K4, isInteractive: ue2, onMouseEnter: pe2, onMouseMove: ce2, onMouseLeave: se2, onClick: ve2, tooltip: fe2 }, "nodes"));
  var Te2 = oe({ nodes: Oe2 });
  return (0, import_jsx_runtime4.jsx)("div", { role: Le3, "aria-label": ge2, "aria-labelledby": me, "aria-describedby": ye, style: { position: "relative", width: we2, height: Se }, children: (0, import_jsx_runtime4.jsx)("div", { style: { position: "absolute", top: xe.top, left: xe.left }, children: M2.map(function(e8, t8) {
    var n6;
    return "function" == typeof e8 ? (0, import_jsx_runtime4.jsx)(import_react33.Fragment, { children: (0, import_react33.createElement)(e8, Te2) }, t8) : null != (n6 = null == ke2 ? void 0 : ke2[e8]) ? n6 : null;
  }) }) });
};
var ce = function(e7) {
  var t7 = e7.isInteractive, o5 = void 0 === t7 ? Z.isInteractive : t7, n5 = e7.animate, i5 = void 0 === n5 ? Z.animate : n5, a4 = e7.motionConfig, r5 = void 0 === a4 ? Z.motionConfig : a4, l4 = e7.theme, d2 = e7.renderWrapper, b3 = D3(e7, ue);
  return (0, import_jsx_runtime4.jsx)(St, { animate: i5, isInteractive: o5, motionConfig: r5, renderWrapper: d2, theme: l4, children: (0, import_jsx_runtime4.jsx)(pe, j3({ isInteractive: o5 }, b3)) });
};
var se = function(e7) {
  return (0, import_jsx_runtime4.jsx)(It, { children: function(t7) {
    var o5 = t7.width, n5 = t7.height;
    return (0, import_jsx_runtime4.jsx)(ce, j3({ width: o5, height: n5 }, e7));
  } });
};
var ve = ["theme", "isInteractive", "animate", "motionConfig", "renderWrapper"];
var he = function(e7, t7, o5, n5) {
  return e7.find(function(e8) {
    return jn(e8.x + t7.left, e8.y + t7.top, e8.width, e8.height, o5, n5);
  });
};
var fe = function(e7) {
  var n5 = e7.data, i5 = e7.identity, d2 = void 0 === i5 ? $.identity : i5, b3 = e7.value, u2 = void 0 === b3 ? $.identity : b3, p3 = e7.tile, s3 = void 0 === p3 ? $.tile : p3, v4 = e7.valueFormat, h = e7.innerPadding, g3 = void 0 === h ? $.innerPadding : h, m5 = e7.outerPadding, y6 = void 0 === m5 ? $.outerPadding : m5, C7 = e7.leavesOnly, x4 = void 0 === C7 ? $.leavesOnly : C7, P3 = e7.width, M2 = e7.height, w3 = e7.margin, S = e7.colors, O4 = void 0 === S ? $.colors : S, k3 = e7.colorBy, T3 = void 0 === k3 ? $.colorBy : k3, W2 = e7.nodeOpacity, I = void 0 === W2 ? $.nodeOpacity : W2, B3 = e7.borderWidth, R = void 0 === B3 ? $.borderWidth : B3, E5 = e7.borderColor, X2 = void 0 === E5 ? $.borderColor : E5, F3 = e7.enableLabel, j4 = void 0 === F3 ? $.enableLabel : F3, D4 = e7.label, H2 = void 0 === D4 ? $.label : D4, V3 = e7.labelTextColor, q3 = void 0 === V3 ? $.labelTextColor : V3, A5 = e7.orientLabel, G2 = void 0 === A5 ? $.orientLabel : A5, J2 = e7.labelSkipSize, K4 = void 0 === J2 ? $.labelSkipSize : J2, N4 = e7.isInteractive, Q2 = void 0 === N4 ? $.isInteractive : N4, U2 = e7.onMouseMove, Z2 = e7.onClick, _2 = e7.tooltip, ee2 = void 0 === _2 ? $.tooltip : _2, oe2 = e7.pixelRatio, ne2 = void 0 === oe2 ? $.pixelRatio : oe2, ie2 = e7.role, ae2 = e7.ariaLabel, re4 = e7.ariaLabelledBy, le2 = e7.ariaDescribedBy, de2 = (0, import_react33.useRef)(null), be2 = wt(P3, M2, w3), ue2 = be2.margin, pe2 = be2.innerWidth, ce2 = be2.innerHeight, se2 = be2.outerWidth, ve2 = be2.outerHeight, fe2 = te({ data: n5, identity: d2, value: u2, valueFormat: v4, leavesOnly: x4, width: pe2, height: ce2, tile: s3, innerPadding: g3, outerPadding: y6, colors: O4, colorBy: T3, nodeOpacity: I, borderColor: X2, label: H2, labelTextColor: q3, orientLabel: G2, enableParentLabel: false }).nodes, Le3 = zt();
  (0, import_react33.useEffect)(function() {
    if (null !== de2.current) {
      var e8 = de2.current.getContext("2d");
      null !== e8 && (de2.current.width = se2 * ne2, de2.current.height = ve2 * ne2, e8.scale(ne2, ne2), e8.fillStyle = Le3.background, e8.fillRect(0, 0, se2, ve2), e8.translate(ue2.left, ue2.top), fe2.forEach(function(t7) {
        e8.fillStyle = t7.color, e8.fillRect(t7.x, t7.y, t7.width, t7.height), R > 0 && (e8.strokeStyle = t7.borderColor, e8.lineWidth = R, e8.strokeRect(t7.x, t7.y, t7.width, t7.height));
      }), j4 && (e8.textAlign = "center", e8.textBaseline = "middle", e8.font = Le3.labels.text.fontSize + "px " + Le3.labels.text.fontFamily, fe2.forEach(function(t7) {
        if (t7.isLeaf && (0 === K4 || Math.min(t7.width, t7.height) > K4)) {
          var o5 = G2 && t7.height > t7.width;
          e8.save(), e8.translate(t7.x + t7.width / 2, t7.y + t7.height / 2), e8.rotate(Kt(o5 ? -90 : 0)), e8.fillStyle = t7.labelTextColor, e8.fillText("" + t7.label, 0, 0), e8.restore();
        }
      })));
    }
  }, [de2, fe2, se2, ve2, pe2, ce2, ue2, R, j4, G2, K4, Le3, ne2]);
  var ge2 = k2(), me = ge2.showTooltipFromEvent, ye = ge2.hideTooltip, Ce2 = (0, import_react33.useCallback)(function(e8) {
    if (null !== de2.current) {
      var t7 = Sn(de2.current, e8), n6 = t7[0], i6 = t7[1], a4 = he(fe2, ue2, n6, i6);
      void 0 !== a4 ? (me((0, import_react33.createElement)(ee2, { node: a4 }), e8, "left"), null == U2 || U2(a4, e8)) : ye();
    }
  }, [de2, fe2, ue2, me, ye, ee2, U2]), xe = (0, import_react33.useCallback)(function() {
    ye();
  }, [ye]), Pe2 = (0, import_react33.useCallback)(function(e8) {
    if (null !== de2.current) {
      var t7 = Sn(de2.current, e8), o5 = t7[0], n6 = t7[1], i6 = he(fe2, ue2, o5, n6);
      void 0 !== i6 && (null == Z2 || Z2(i6, e8));
    }
  }, [de2, fe2, ue2, Z2]);
  return (0, import_jsx_runtime4.jsx)("canvas", { ref: de2, width: se2 * ne2, height: ve2 * ne2, style: { width: se2, height: ve2 }, onMouseEnter: Q2 ? Ce2 : void 0, onMouseMove: Q2 ? Ce2 : void 0, onMouseLeave: Q2 ? xe : void 0, onClick: Q2 ? Pe2 : void 0, role: ie2, "aria-label": ae2, "aria-labelledby": re4, "aria-describedby": le2 });
};
var Le2 = function(e7) {
  var t7 = e7.theme, o5 = e7.isInteractive, n5 = void 0 === o5 ? $.isInteractive : o5, i5 = e7.animate, a4 = void 0 === i5 ? $.animate : i5, r5 = e7.motionConfig, l4 = void 0 === r5 ? $.motionConfig : r5, d2 = e7.renderWrapper, b3 = D3(e7, ve);
  return (0, import_jsx_runtime4.jsx)(St, { isInteractive: n5, animate: a4, motionConfig: l4, theme: t7, renderWrapper: d2, children: (0, import_jsx_runtime4.jsx)(fe, j3({ isInteractive: n5 }, b3)) });
};
var ge = function(e7) {
  return (0, import_jsx_runtime4.jsx)(It, { children: function(t7) {
    var o5 = t7.width, n5 = t7.height;
    return (0, import_jsx_runtime4.jsx)(Le2, j3({ width: o5, height: n5 }, e7));
  } });
};
export {
  be as ResponsiveTreeMap,
  ge as ResponsiveTreeMapCanvas,
  se as ResponsiveTreeMapHtml,
  de as TreeMap,
  Le2 as TreeMapCanvas,
  ce as TreeMapHtml,
  $ as canvasDefaultProps,
  Q as commonDefaultProps,
  Z as htmlDefaultProps,
  A4 as htmlLabelTransform,
  V2 as htmlNodeTransform,
  G as htmlParentLabelTransform,
  U as svgDefaultProps,
  q2 as svgLabelTransform,
  H as svgNodeTransform,
  _ as tileByType
};
/*! Bundled license information:

react-is/cjs/react-is.development.js:
  (** @license React v16.13.1
   * react-is.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

object-assign/index.js:
  (*
  object-assign
  (c) Sindre Sorhus
  @license MIT
  *)

react-is/cjs/react-is.development.js:
  (** @license React v16.13.1
   * react-is.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

object-assign/index.js:
  (*
  object-assign
  (c) Sindre Sorhus
  @license MIT
  *)
*/
//# sourceMappingURL=@nivo_treemap.js.map
